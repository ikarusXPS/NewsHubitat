---
phase: 16-pwa-service-worker
plan: 05
type: execute
wave: 3
depends_on: [16-01, 16-02]
files_modified:
  - src/hooks/useInstallPrompt.ts
  - src/components/InstallPromptBanner.tsx
  - src/components/Layout.tsx
autonomous: true
requirements: [PERF-07]

must_haves:
  truths:
    - Install banner shows after 3+ visits OR 5+ articles read
    - Banner does not show if dismissed within last 7 days
    - Clicking "Install" triggers browser's native install prompt
    - Clicking "Dismiss" hides banner for 7 days
    - Banner matches NewsHub cyber aesthetic
  artifacts:
    - path: src/hooks/useInstallPrompt.ts
      provides: beforeinstallprompt event handling
      min_lines: 70
      exports: ["useInstallPrompt"]
    - path: src/components/InstallPromptBanner.tsx
      provides: Install prompt UI
      min_lines: 80
      exports: ["InstallPromptBanner"]
    - path: src/components/Layout.tsx
      provides: Banner integration
      contains: "InstallPromptBanner"
  key_links:
    - from: src/components/InstallPromptBanner.tsx
      to: src/hooks/useInstallPrompt.ts
      via: hook consumption
      pattern: "useInstallPrompt"
---

<objective>
Create custom install prompt banner with engagement-based triggering and localStorage persistence.

Purpose: Users see contextual install prompt after demonstrating engagement, improving PWA install conversion rates.
Output: useInstallPrompt hook, InstallPromptBanner component, Layout integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-pwa-service-worker/16-CONTEXT.md
@.planning/phases/16-pwa-service-worker/16-RESEARCH.md
@.planning/phases/16-pwa-service-worker/16-PATTERNS.md

# Hook patterns
@src/hooks/useKeyboardShortcuts.ts
@src/hooks/useBackendStatus.ts

# Banner patterns
@src/components/VerificationBanner.tsx
@src/components/OfflineBanner.tsx

# Layout integration
@src/components/Layout.tsx
</context>

<interfaces>
<!-- Key patterns from existing code -->

From src/hooks/useKeyboardShortcuts.ts (event listener hook):
```typescript
import { useEffect, useCallback } from 'react';

const handleKeyDown = useCallback(
  (event: KeyboardEvent) => {
    // Event logic
  },
  [dependencies]
);

useEffect(() => {
  document.addEventListener('keydown', handleKeyDown);
  return () => document.removeEventListener('keydown', handleKeyDown);
}, [handleKeyDown]);
```

From src/components/VerificationBanner.tsx (banner component):
```typescript
import { useState } from 'react';
import { X, Loader2 } from 'lucide-react';
import { motion, AnimatePresence } from 'framer-motion';
import { cn } from '../lib/utils';

export function VerificationBanner() {
  const [isDismissed, setIsDismissed] = useState(false);
  const [isResending, setIsResending] = useState(false);

  if (isDismissed) return null;

  return (
    <AnimatePresence>
      <motion.div
        initial={{ height: 0, opacity: 0 }}
        animate={{ height: 'auto', opacity: 1 }}
        exit={{ height: 0, opacity: 0 }}
        className="border-b border-cyan-500/30"
      >
        <div className="max-w-7xl mx-auto px-4 sm:px-6 py-2.5">
          <div className="flex items-center justify-between gap-4">
            {/* Left: Icon + Message */}
            <div className="flex items-center gap-3">
              {/* Icon and text */}
            </div>

            {/* Right: Action + Dismiss */}
            <div className="flex items-center gap-3">
              <button onClick={handleAction}>Action</button>
              <button onClick={() => setIsDismissed(true)}>
                <X className="h-4 w-4" />
              </button>
            </div>
          </div>
        </div>
      </motion.div>
    </AnimatePresence>
  );
}
```

BeforeInstallPromptEvent type (from RESEARCH.md lines 240-244):
```typescript
interface BeforeInstallPromptEvent extends Event {
  prompt: () => Promise<void>;
  userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>;
}
```

From src/components/Layout.tsx (banner placement):
```typescript
<Header onMenuClick={() => setSidebarOpen(true)} />
<BreakingNewsTicker />
<OfflineBanner />
{/* Add InstallPromptBanner here */}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create useInstallPrompt hook</name>
  <files>src/hooks/useInstallPrompt.ts</files>
  <read_first>
    - src/hooks/useKeyboardShortcuts.ts (event listener pattern)
    - .planning/phases/16-pwa-service-worker/16-CONTEXT.md (D-07, D-08, D-09, D-10 decisions)
    - .planning/phases/16-pwa-service-worker/16-RESEARCH.md (useInstallPrompt pattern, lines 233-290)
    - .planning/phases/16-pwa-service-worker/16-PATTERNS.md (useKeyboardShortcuts analog, lines 311-382)
  </read_first>
  <action>
Create src/hooks/useInstallPrompt.ts following useKeyboardShortcuts event listener pattern.

**Imports:**
```typescript
import { useEffect, useState } from 'react';
```

**BeforeInstallPromptEvent type (per RESEARCH.md lines 240-244):**
```typescript
interface BeforeInstallPromptEvent extends Event {
  prompt: () => Promise<void>;
  userChoice: Promise<{ outcome: 'accepted' | 'dismissed' }>;
}
```

**Hook implementation (per D-07, D-08, D-09, D-10):**

```typescript
export function useInstallPrompt() {
  const [deferredPrompt, setDeferredPrompt] = useState<BeforeInstallPromptEvent | null>(null);
  const [showBanner, setShowBanner] = useState(false);

  useEffect(() => {
    const handleBeforeInstall = (e: Event) => {
      // Prevent browser's default mini-infobar (D-07)
      e.preventDefault();
      const promptEvent = e as BeforeInstallPromptEvent;
      setDeferredPrompt(promptEvent);

      // Check engagement threshold (D-08)
      const visitCount = Number(localStorage.getItem('visitCount') || 0) + 1;
      const articlesRead = Number(localStorage.getItem('articlesRead') || 0);
      const lastDismissed = localStorage.getItem('installPromptDismissed');

      localStorage.setItem('visitCount', String(visitCount));

      // Show banner if: 3+ visits OR 5+ articles, AND not dismissed in last 7 days (D-08, D-10)
      const sevenDaysAgo = Date.now() - 7 * 24 * 60 * 60 * 1000;
      const dismissedRecently = lastDismissed && Number(lastDismissed) > sevenDaysAgo;

      if ((visitCount >= 3 || articlesRead >= 5) && !dismissedRecently) {
        setShowBanner(true);
      }
    };

    window.addEventListener('beforeinstallprompt', handleBeforeInstall);
    return () => window.removeEventListener('beforeinstallprompt', handleBeforeInstall);
  }, []);

  const install = async () => {
    if (!deferredPrompt) return;

    deferredPrompt.prompt();
    const { outcome } = await deferredPrompt.userChoice;

    if (outcome === 'accepted') {
      setShowBanner(false);
    }

    setDeferredPrompt(null);
  };

  const dismiss = () => {
    setShowBanner(false);
    localStorage.setItem('installPromptDismissed', String(Date.now()));
  };

  return { showBanner, install, dismiss };
}
```

Pattern combines:
- useKeyboardShortcuts event listener pattern (lines 19-156)
- RESEARCH.md useInstallPrompt pattern (lines 233-290)
- Engagement threshold from D-08 (3+ visits or 5+ articles)
- 7-day cooldown from D-10
  </action>
  <acceptance_criteria>
    - src/hooks/useInstallPrompt.ts exists
    - File contains "export function useInstallPrompt()"
    - File contains "interface BeforeInstallPromptEvent"
    - File contains "window.addEventListener('beforeinstallprompt'"
    - File contains "localStorage.getItem('visitCount')"
    - File contains "localStorage.getItem('articlesRead')"
    - File contains "localStorage.getItem('installPromptDismissed')"
    - File contains "visitCount >= 3 || articlesRead >= 5" threshold check
    - File contains "7 * 24 * 60 * 60 * 1000" (7 days in milliseconds)
    - File exports install and dismiss functions
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>
grep -q "export function useInstallPrompt" src/hooks/useInstallPrompt.ts &&
grep -q "interface BeforeInstallPromptEvent" src/hooks/useInstallPrompt.ts &&
grep -q "addEventListener.*beforeinstallprompt" src/hooks/useInstallPrompt.ts &&
grep -q "localStorage.getItem.*visitCount" src/hooks/useInstallPrompt.ts &&
grep -q "localStorage.getItem.*articlesRead" src/hooks/useInstallPrompt.ts &&
grep -q "visitCount >= 3.*articlesRead >= 5" src/hooks/useInstallPrompt.ts &&
grep -q "7 \* 24 \* 60 \* 60 \* 1000" src/hooks/useInstallPrompt.ts &&
npm run typecheck 2>&1 | grep -q "0 errors" &&
echo "PASS: useInstallPrompt hook created" || echo "FAIL"
    </automated>
  </verify>
  <done>useInstallPrompt hook captures beforeinstallprompt event, tracks engagement, enforces 7-day dismissal cooldown</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create InstallPromptBanner component</name>
  <files>src/components/InstallPromptBanner.tsx</files>
  <read_first>
    - src/components/VerificationBanner.tsx (banner layout pattern)
    - src/components/OfflineBanner.tsx (design aesthetic)
    - src/hooks/useInstallPrompt.ts (just created, hook interface)
    - .planning/phases/16-pwa-service-worker/16-CONTEXT.md (D-09, D-10 decisions)
    - .planning/phases/16-pwa-service-worker/16-PATTERNS.md (VerificationBanner analog, lines 81-209)
  </read_first>
  <action>
Create src/components/InstallPromptBanner.tsx following VerificationBanner.tsx pattern with NewsHub cyber aesthetic.

**Imports:**
```typescript
import { Download, X } from 'lucide-react';
import { motion, AnimatePresence } from 'framer-motion';
import { useInstallPrompt } from '../hooks/useInstallPrompt';
import { cn } from '../lib/utils';
```

**Component (per D-09 bottom-fixed positioning):**

```typescript
export function InstallPromptBanner() {
  const { showBanner, install, dismiss } = useInstallPrompt();

  if (!showBanner) {
    return null;
  }

  return (
    <AnimatePresence>
      <motion.div
        initial={{ y: 100, opacity: 0 }}
        animate={{ y: 0, opacity: 1 }}
        exit={{ y: 100, opacity: 0 }}
        transition={{ duration: 0.3 }}
        className={cn(
          'fixed bottom-0 left-0 right-0 z-50',
          'border-t border-cyan-500/30',
          'bg-gradient-to-r from-[#00f0ff]/20 via-[#00f0ff]/10 to-[#00f0ff]/20',
          'backdrop-blur-sm'
        )}
      >
        <div className="max-w-7xl mx-auto px-4 sm:px-6 py-3">
          <div className="flex items-center justify-between gap-4 flex-wrap sm:flex-nowrap">
            {/* Left: Icon + Message */}
            <div className="flex items-center gap-3">
              <div className="flex items-center justify-center h-8 w-8 rounded-full bg-[#00f0ff]/20 border border-[#00f0ff]/40">
                <Download className="h-4 w-4 text-[#00f0ff]" />
              </div>
              <div className="text-sm">
                <span className="text-white font-mono font-semibold">
                  Install NewsHub for offline access and faster loading
                </span>
              </div>
            </div>

            {/* Right: Install Button + Dismiss */}
            <div className="flex items-center gap-3">
              <button
                onClick={install}
                className={cn(
                  'px-4 py-1.5 rounded text-sm font-mono font-semibold transition-colors',
                  'bg-[#00f0ff]/20 border border-[#00f0ff]/40 text-white hover:bg-[#00f0ff]/30'
                )}
              >
                Install App
              </button>

              <button
                onClick={dismiss}
                className="text-gray-500 hover:text-gray-300 transition-colors"
                aria-label="Dismiss install prompt"
              >
                <X className="h-4 w-4" />
              </button>
            </div>
          </div>
        </div>
      </motion.div>
    </AnimatePresence>
  );
}
```

Pattern combines:
- VerificationBanner.tsx layout (lines 74-150)
- OfflineBanner.tsx cyber aesthetic (cyan #00f0ff accent)
- D-09 bottom-fixed positioning (fixed bottom-0 left-0 right-0)
- Framer Motion slide-up animation (y: 100 → 0)
  </action>
  <acceptance_criteria>
    - src/components/InstallPromptBanner.tsx exists
    - File contains "export function InstallPromptBanner"
    - File imports useInstallPrompt from '../hooks/useInstallPrompt'
    - File imports Download and X icons from 'lucide-react'
    - File imports motion and AnimatePresence from 'framer-motion'
    - File contains "fixed bottom-0 left-0 right-0" for positioning
    - File contains "#00f0ff" accent color (NewsHub cyan)
    - File contains onClick={install} button
    - File contains onClick={dismiss} button
    - npm run typecheck exits 0
  </acceptance_criteria>
  <verify>
    <automated>
grep -q "export function InstallPromptBanner" src/components/InstallPromptBanner.tsx &&
grep -q "useInstallPrompt" src/components/InstallPromptBanner.tsx &&
grep -q "Download.*X.*lucide-react" src/components/InstallPromptBanner.tsx &&
grep -q "motion.*AnimatePresence.*framer-motion" src/components/InstallPromptBanner.tsx &&
grep -q "fixed bottom-0" src/components/InstallPromptBanner.tsx &&
grep -q "#00f0ff" src/components/InstallPromptBanner.tsx &&
grep -q "onClick={install}" src/components/InstallPromptBanner.tsx &&
grep -q "onClick={dismiss}" src/components/InstallPromptBanner.tsx &&
npm run typecheck 2>&1 | grep -q "0 errors" &&
echo "PASS: InstallPromptBanner component created" || echo "FAIL"
    </automated>
  </verify>
  <done>InstallPromptBanner renders bottom-fixed with NewsHub aesthetic, triggers install on click, dismisses for 7 days</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Integrate banner in Layout.tsx</name>
  <files>src/components/Layout.tsx</files>
  <read_first>
    - src/components/Layout.tsx (existing banner placement)
    - src/components/InstallPromptBanner.tsx (just created)
    - .planning/phases/16-pwa-service-worker/16-PATTERNS.md (Layout integration, lines 710-748)
  </read_first>
  <action>
Add InstallPromptBanner to Layout.tsx AFTER existing banners.

**Find imports section** (around line 1-10):
Add import:
```typescript
import { InstallPromptBanner } from './InstallPromptBanner';
```

**Find banner placement section** (around line 47-50):
Current structure:
```typescript
<Header onMenuClick={() => setSidebarOpen(true)} />
<BreakingNewsTicker />
<OfflineBanner />
```

Add InstallPromptBanner AFTER OfflineBanner:
```typescript
<Header onMenuClick={() => setSidebarOpen(true)} />
<BreakingNewsTicker />
<OfflineBanner />
<InstallPromptBanner />
```

**Do NOT modify** any other parts of Layout.tsx. This is a single-line addition in the banner stack.
  </action>
  <acceptance_criteria>
    - src/components/Layout.tsx contains "import { InstallPromptBanner } from './InstallPromptBanner'"
    - src/components/Layout.tsx contains "<InstallPromptBanner />"
    - InstallPromptBanner appears after OfflineBanner in JSX
    - npm run typecheck exits 0
    - npm run dev:frontend starts successfully
  </acceptance_criteria>
  <verify>
    <automated>
grep -q "import.*InstallPromptBanner.*./InstallPromptBanner" src/components/Layout.tsx &&
grep -q "<InstallPromptBanner />" src/components/Layout.tsx &&
npm run typecheck 2>&1 | grep -q "0 errors" &&
echo "PASS: InstallPromptBanner integrated in Layout" || echo "FAIL"
    </automated>
  </verify>
  <done>InstallPromptBanner integrated in Layout, renders below OfflineBanner</done>
</task>

</tasks>

<verification>
## Overall Phase Checks

After completing all tasks:

1. **TypeScript compiles**: `npm run typecheck` passes
2. **Dev server starts**: `npm run dev:frontend` starts without errors
3. **Banner visibility**: Open app in Chrome → Manually set visitCount=3 in localStorage → Reload → Banner appears
4. **Install flow**: Click "Install App" → Browser's install dialog appears → Accept → App installs
5. **Dismiss flow**: Click X → Banner disappears → Check localStorage → installPromptDismissed timestamp set
6. **7-day cooldown**: Manually set installPromptDismissed to 6 days ago → Reload → Banner does not appear
</verification>

<success_criteria>
1. useInstallPrompt hook tracks visitCount and articlesRead engagement
2. Hook shows banner after 3+ visits OR 5+ articles read
3. Hook enforces 7-day dismissal cooldown
4. InstallPromptBanner renders with NewsHub cyber aesthetic
5. Banner positioned at bottom of screen (fixed)
6. Banner integrated in Layout after OfflineBanner
</success_criteria>

<output>
After completion, create `.planning/phases/16-pwa-service-worker/16-05-SUMMARY.md`
</output>
