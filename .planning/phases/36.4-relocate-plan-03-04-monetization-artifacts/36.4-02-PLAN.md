---
phase: 36.4-relocate-plan-03-04-monetization-artifacts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/subscription/TierCard.tsx
  - apps/web/src/components/subscription/SubscriptionBadge.tsx
  - apps/web/src/components/subscription/UpgradePrompt.tsx
  - apps/web/src/components/subscription/AIUsageCounter.tsx
autonomous: true
requirements:
  - PAY-01
  - PAY-04
  - PAY-05
must_haves:
  truths:
    - "TierCard renders 3 columns (Free/Premium/Enterprise) with feature checkmarks"
    - "TierCard with isPopular=true displays Most Popular badge AND cyan #00f0ff border AND scale-105 transform"
    - "SubscriptionBadge returns null for FREE tier (no clutter for free users per CONTEXT.md)"
    - "SubscriptionBadge renders cyan badge for PREMIUM and purple for ENTERPRISE"
    - "UpgradePrompt overlay variant shows Lock icon over blurred children; click navigates to /pricing"
    - "AIUsageCounter returns null for PREMIUM/ENTERPRISE; renders progress bar with used/10 for FREE"
  artifacts:
    - path: "apps/web/src/components/subscription/TierCard.tsx"
      provides: "TierCard component with isPopular/isCurrent/isEnterprise props, billing-cycle aware"
      min_lines: 130
      contains: "#00f0ff"
    - path: "apps/web/src/components/subscription/SubscriptionBadge.tsx"
      provides: "SubscriptionBadge component returning null for FREE"
      min_lines: 50
    - path: "apps/web/src/components/subscription/UpgradePrompt.tsx"
      provides: "UpgradePrompt with overlay (blur+lock) + inline (button) variants"
      min_lines: 60
    - path: "apps/web/src/components/subscription/AIUsageCounter.tsx"
      provides: "FREE-tier-only progress bar; fetches /api/ai/usage"
      min_lines: 90
  key_links:
    - from: "apps/web/src/components/subscription/TierCard.tsx"
      to: "apps/web/src/lib/utils"
      via: "import { cn } from '../../lib/utils'"
      pattern: "from '\\.\\./\\.\\./lib/utils'"
    - from: "apps/web/src/components/subscription/AIUsageCounter.tsx"
      to: "apps/web/src/contexts/AuthContext"
      via: "import { useAuth } from '../../contexts/AuthContext'"
      pattern: "useAuth"
    - from: "apps/web/src/components/subscription/UpgradePrompt.tsx"
      to: "react-router-dom"
      via: "useNavigate to push '/pricing'"
      pattern: "/pricing"
---

<objective>
Recover Phase 36 Plan 04 client-side subscription components to canonical `apps/web/src/components/subscription/` paths.

Purpose: Phase 36-04 originally wrote these to root `src/components/subscription/`. Phase 36.3-03's orphan sweep deleted root `src/`. The four components are required by Plan 36.4-03's Pricing page recovery (depends_on this plan).

Output: 4 reusable subscription UI components in canonical monorepo path, each consumable by Pricing.tsx, SubscriptionSuccess.tsx, header/profile views, and feature-gating overlays.
</objective>

<execution_context>
@C:/Users/krsat/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/krsat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/.continue-here.md
@.planning/phases/36-monetization-core/36-04-PLAN.md

@CLAUDE.md

<interfaces>
<!-- Existing modules these components import. Executor should NOT explore the codebase. -->

From apps/web/src/lib/utils:
```typescript
export function cn(...inputs: ClassValue[]): string;  // tailwind-merge wrapper
```

From apps/web/src/contexts/AuthContext (existing):
```typescript
export function useAuth(): {
  user: { id: string; email: string; name: string; subscriptionTier?: string; ... } | null;
  isAuthenticated: boolean;
  // ... other auth methods
};
```

From `react-i18next` (existing dependency):
```typescript
export function useTranslation(ns: string | string[]): { t: (key: string) => string; ... };
```

From `lucide-react` (existing dependency):
```typescript
export const Check, X, Lock, Sparkles, ...: LucideIcon;
```

Tailwind utility classes used (already configured in apps/web/tailwind.config.cjs):
- `text-[#00f0ff]`, `border-[#00f0ff]`, `text-[#ff0044]`, `text-[#bf00ff]` — arbitrary cyber palette colors per CLAUDE.md UI Design System
- `scale-105` — Tailwind built-in transform utility
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recover TierCard, SubscriptionBadge, UpgradePrompt</name>
  <files>apps/web/src/components/subscription/TierCard.tsx, apps/web/src/components/subscription/SubscriptionBadge.tsx, apps/web/src/components/subscription/UpgradePrompt.tsx</files>
  <read_first>
    1. Source content for the 3 files (commit `17066dd`, +286 lines total):
       - `git show 17066dd:src/components/subscription/TierCard.tsx` (150 lines)
       - `git show 17066dd:src/components/subscription/SubscriptionBadge.tsx` (62 lines)
       - `git show 17066dd:src/components/subscription/UpgradePrompt.tsx` (74 lines)
    2. Existing closest analogs in apps/web/src/components/ for monorepo import conventions:
       `apps/web/src/components/Header.tsx` (uses lucide-react + cn from `'../lib/utils'`) — note: from a top-level component the path is `'../lib/utils'`, but from `subscription/` subdir the path is `'../../lib/utils'`.
    3. apps/web/tailwind.config.cjs — verify cyber palette colors are arbitrary-value Tailwind (no theme.extend.colors required).
    4. CLAUDE.md "UI Design System" section: confirm cyan #00f0ff is the Premium accent.
  </read_first>
  <action>
    Create the subscription/ subdirectory and write the 3 components from commit `17066dd`. All paths in the original files were `'../../lib/utils'` and `'../../contexts/AuthContext'` — these resolve correctly from `apps/web/src/components/subscription/` (depth identical to root `src/components/subscription/`). NO import-path rewrites needed for client code.

    Steps (run from D:/NewsHub):
    1. `mkdir -p apps/web/src/components/subscription`
    2. `git show 17066dd:src/components/subscription/TierCard.tsx > apps/web/src/components/subscription/TierCard.tsx`
    3. `git show 17066dd:src/components/subscription/SubscriptionBadge.tsx > apps/web/src/components/subscription/SubscriptionBadge.tsx`
    4. `git show 17066dd:src/components/subscription/UpgradePrompt.tsx > apps/web/src/components/subscription/UpgradePrompt.tsx`
    5. Verify each file resolves all imports (open each, scan import statements):
       - TierCard.tsx imports: `lucide-react`, `react-i18next`, `'../../lib/utils'` — all exist
       - SubscriptionBadge.tsx imports: similar set
       - UpgradePrompt.tsx imports: includes `react-router-dom` (useNavigate)
    6. Run `pnpm typecheck` from repo root — exit 0 expected (these are pure presentation components; no DB/server types).
    7. NO test creation in this task — Plan 04 (verification gate) handles UI test additions if needed; the existing E2E `apps/web/e2e/subscription.spec.ts` will exercise these components transitively via Pricing.tsx (recovered in Plan 03).
  </action>
  <verify>
    <automated>
test -d apps/web/src/components/subscription && \
test -f apps/web/src/components/subscription/TierCard.tsx && \
test -f apps/web/src/components/subscription/SubscriptionBadge.tsx && \
test -f apps/web/src/components/subscription/UpgradePrompt.tsx && \
grep -c "#00f0ff" apps/web/src/components/subscription/TierCard.tsx | { read n; [ "$n" -ge 2 ] || exit 1; } && \
grep -c "scale-105" apps/web/src/components/subscription/TierCard.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
grep -c "isPopular" apps/web/src/components/subscription/TierCard.tsx | { read n; [ "$n" -ge 2 ] || exit 1; } && \
grep -c "Most Popular\|mostPopular\|isPopular" apps/web/src/components/subscription/TierCard.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
grep -c "export function TierCard\|export const TierCard" apps/web/src/components/subscription/TierCard.tsx | { read n; [ "$n" -eq 1 ] || exit 1; } && \
grep -c "FREE" apps/web/src/components/subscription/SubscriptionBadge.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
grep -c "return null" apps/web/src/components/subscription/SubscriptionBadge.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
grep -c "/pricing" apps/web/src/components/subscription/UpgradePrompt.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
grep -c "Lock" apps/web/src/components/subscription/UpgradePrompt.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - Directory `apps/web/src/components/subscription/` exists
    - All 3 files present (TierCard.tsx >= 130 lines, SubscriptionBadge.tsx >= 50, UpgradePrompt.tsx >= 60)
    - TierCard.tsx contains `#00f0ff` (cyan Premium accent — at least 2 hits per source: border + ring/text), `scale-105` (transform), `isPopular` prop (at least 2 hits: prop declaration + conditional)
    - SubscriptionBadge.tsx contains `FREE` and `return null` (FREE tier shows no badge per CONTEXT.md)
    - UpgradePrompt.tsx contains `/pricing` (navigation target) and `Lock` (lucide icon import)
    - `pnpm typecheck` exits 0
    - Zero new files written under root `src/`
  </acceptance_criteria>
  <done>
    Three subscription UI components recovered to apps/web/src/components/subscription/; typecheck clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Recover AIUsageCounter</name>
  <files>apps/web/src/components/subscription/AIUsageCounter.tsx</files>
  <read_first>
    1. Source content (from commit `6956f02`, 109 lines):
       `git show 6956f02:src/components/subscription/AIUsageCounter.tsx`
    2. apps/web/src/contexts/AuthContext.tsx — confirm `useAuth` exposes a `user` object with `subscriptionTier` field accessible (the source casts: `(user as { subscriptionTier?: string } | null)?.subscriptionTier || 'FREE'`).
    3. The component will fetch `/api/ai/usage` — confirm whether this endpoint exists in `apps/web/server/routes/ai.ts` or whether the component handles a 404 gracefully.
  </read_first>
  <action>
    Steps (run from D:/NewsHub):
    1. `git show 6956f02:src/components/subscription/AIUsageCounter.tsx > apps/web/src/components/subscription/AIUsageCounter.tsx`
    2. Imports in the source:
       - `react` (useEffect, useState)
       - `lucide-react` (Sparkles)
       - `react-i18next` (useTranslation)
       - `'../../contexts/AuthContext'` — RESOLVES from apps/web/src/components/subscription/
       - `'../../lib/utils'` — RESOLVES
       All paths preserved byte-stable.
    3. The component fetches `/api/ai/usage` to populate `usage.used`. If that endpoint doesn't yet exist server-side, the existing component must already handle the failure gracefully (it returns a default of `{ used: 0, limit: 10 }` from useState init). Verify graceful degradation by reading the source: it wraps fetch in try/catch.
    4. NO server-side endpoint creation in this task — `/api/ai/usage` adoption is a future-phase concern (the component will simply show "0 of 10" until that endpoint is wired). Adding the endpoint is OUT OF SCOPE for 36.4 per the ROADMAP success criteria (which list only requireTier middleware + UI components + routes + i18n; no `/api/ai/usage` handler).
    5. Run `pnpm typecheck` — exit 0 expected.
  </action>
  <verify>
    <automated>
test -f apps/web/src/components/subscription/AIUsageCounter.tsx && \
grep -c "subscriptionTier" apps/web/src/components/subscription/AIUsageCounter.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
grep -c "FREE" apps/web/src/components/subscription/AIUsageCounter.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
grep -c "useAuth" apps/web/src/components/subscription/AIUsageCounter.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
grep -c "Sparkles" apps/web/src/components/subscription/AIUsageCounter.tsx | { read n; [ "$n" -ge 1 ] || exit 1; } && \
grep -c "export function AIUsageCounter\|export const AIUsageCounter" apps/web/src/components/subscription/AIUsageCounter.tsx | { read n; [ "$n" -eq 1 ] || exit 1; } && \
pnpm typecheck
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at `apps/web/src/components/subscription/AIUsageCounter.tsx`, line count >= 90
    - Imports useAuth from `'../../contexts/AuthContext'`
    - References `subscriptionTier`, `'FREE'`, `Sparkles` (lucide icon)
    - Default-export OR named-export `AIUsageCounter`
    - `pnpm typecheck` exits 0
    - Zero new files under root `src/`
  </acceptance_criteria>
  <done>
    AIUsageCounter recovered; typecheck clean. Plan 03 can now import it from Pricing.tsx / future Settings views.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/* | These are pure presentation components; no privileged capabilities are exposed client-side |
| AIUsageCounter → /api/ai/usage | Component reads its own user's usage; server-side authorization gates access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.4-10 | Information Disclosure | AIUsageCounter fetches /api/ai/usage | mitigate | Component is FREE-tier-only (returns null for PREMIUM/ENTERPRISE — per source); endpoint will be authMiddleware-gated when added; no PII rendered. |
| T-36.4-11 | Tampering | UpgradePrompt navigation target | accept | Hardcoded `/pricing` path; no user-controllable nav input. Open-redirect not applicable. |
| T-36.4-12 | Cross-Site Scripting | t() translation values rendered as text | mitigate | i18next interpolates via React's text node escaping by default; values are translation keys (server-trusted JSON files), not user input. Plan 03 will populate pricing.json — mitigation belongs to that plan's i18n injection check (T-36.4-15). |
| T-36.4-13 | UI Spoofing | TierCard "Most Popular" badge | accept | Marketing-only flag; isPopular is a literal prop set by the consumer (Pricing.tsx in Plan 03), not user-controllable. |
</threat_model>

<verification>
After all 2 tasks complete:

1. `ls apps/web/src/components/subscription/` shows 4 .tsx files
2. `pnpm typecheck` exits 0
3. `pnpm test:run` passes 1289/1289 (these are presentation-only files; no test should regress)
4. Anti-pattern guardrail: `git diff --stat HEAD~2..HEAD | grep -E "^\\s+(server|prisma|src)/" | grep -v "apps/web/" | wc -l` returns 0
</verification>

<success_criteria>
- 4 .tsx files exist under `apps/web/src/components/subscription/`
- TierCard.tsx renders cyan #00f0ff Premium border, scale-105, "Most Popular" badge logic
- SubscriptionBadge.tsx returns null for FREE
- UpgradePrompt.tsx has `/pricing` navigation
- AIUsageCounter.tsx has FREE-tier-only conditional render
- `pnpm typecheck` exits 0
- Zero new files under root `src/`
</success_criteria>

<output>
After completion, create `.planning/phases/36.4-relocate-plan-03-04-monetization-artifacts/36.4-02-SUMMARY.md` with:
- 4 files created (with source-commit traceability: 17066dd × 3 + 6956f02 × 1)
- Confirmed: NO import-path rewrites needed (client-side imports preserved byte-stable)
- One-line note: `/api/ai/usage` endpoint NOT created in this phase; AIUsageCounter degrades gracefully (try/catch + default state)
- Test result: typecheck clean
- Anti-pattern audit: 0 root-level files
</output>
