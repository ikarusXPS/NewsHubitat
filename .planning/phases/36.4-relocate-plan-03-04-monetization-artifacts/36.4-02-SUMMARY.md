---
phase: 36.4-relocate-plan-03-04-monetization-artifacts
plan: 02
status: complete
subsystem: monetization-ui
tags: [subscription, recovery, ui-components, monetization]
requires:
  - apps/web/src/lib/utils (cn)
  - apps/web/src/contexts/AuthContext (useAuth)
  - lucide-react, react-i18next, react-router-dom
provides:
  - apps/web/src/components/subscription/TierCard
  - apps/web/src/components/subscription/SubscriptionBadge
  - apps/web/src/components/subscription/UpgradePrompt
  - apps/web/src/components/subscription/AIUsageCounter
affects:
  - Plan 36.4-03 (Pricing.tsx will import these)
tech-stack:
  added: []
  patterns:
    - Verbatim git-history recovery via `git show <sha>:<path>`
    - Byte-stable import paths (no rewrites needed when target depth matches source)
key-files:
  created:
    - path: apps/web/src/components/subscription/TierCard.tsx
      lines: 150
      source-commit: 17066dd
      summary: 3-column tier display with isPopular cyan #00f0ff border + scale-105 + Most Popular badge
    - path: apps/web/src/components/subscription/SubscriptionBadge.tsx
      lines: 62
      source-commit: 17066dd
      summary: Tier indicator returning null for FREE; cyan for Premium / purple for Enterprise
    - path: apps/web/src/components/subscription/UpgradePrompt.tsx
      lines: 74
      source-commit: 17066dd
      summary: Overlay (Lock icon over blurred children) + inline button variant; navigates to /pricing
    - path: apps/web/src/components/subscription/AIUsageCounter.tsx
      lines: 109
      source-commit: 6956f02
      summary: FREE-tier-only progress bar fetching /api/ai/usage with try/catch graceful default
  modified: []
decisions:
  - "Recovered components verbatim from history rather than rewriting — source content already met all spec requirements (cyan accents, isPopular logic, FREE-tier null returns, Lock icon, /pricing navigation)"
  - "No import-path rewrites needed: original paths `'../../lib/utils'` and `'../../contexts/AuthContext'` resolve correctly from apps/web/src/components/subscription/ (depth identical to source root src/components/subscription/)"
  - "/api/ai/usage server endpoint NOT created — out of scope per ROADMAP success criteria; AIUsageCounter degrades gracefully via try/catch + useState default `{used: 0, limit: 10}`"
metrics:
  duration: ~3 minutes
  tasks_completed: 2
  files_created: 4
  files_modified: 0
  total_lines: 395
  completed: 2026-04-28
---

# Phase 36.4 Plan 02: Recover Subscription UI Components Summary

Recovered 4 client-side subscription components verbatim from git history (commits 17066dd × 3 + 6956f02 × 1) into the canonical monorepo path `apps/web/src/components/subscription/`, fixing the anti-pattern where Phase 36-04 originally wrote them to root `src/components/subscription/` (later swept by Phase 36.3-03).

## One-liner

Recovery via `git show <sha>:<path>` of TierCard, SubscriptionBadge, UpgradePrompt, AIUsageCounter — paths preserved byte-stable, zero rewrites, all original Premium-tier UX semantics intact.

## Tasks Executed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Recover TierCard, SubscriptionBadge, UpgradePrompt | bcbfe00 | TierCard.tsx (150), SubscriptionBadge.tsx (62), UpgradePrompt.tsx (74) |
| 2 | Recover AIUsageCounter | 382f3b3 | AIUsageCounter.tsx (109) |

## Source-Commit Traceability

| File | Source Commit | Source Path | Lines |
|------|---------------|-------------|-------|
| TierCard.tsx | 17066dd | src/components/subscription/TierCard.tsx | 150 |
| SubscriptionBadge.tsx | 17066dd | src/components/subscription/SubscriptionBadge.tsx | 62 |
| UpgradePrompt.tsx | 17066dd | src/components/subscription/UpgradePrompt.tsx | 74 |
| AIUsageCounter.tsx | 6956f02 | src/components/subscription/AIUsageCounter.tsx | 109 |

All 4 files extracted as exact byte copies of the source-commit content. Line counts match commit `--stat` figures from 17066dd (62+150+74=286 insertions) and 6956f02 (109 lines for AIUsageCounter).

## Import Paths (No Rewrites)

The original commits used relative paths from `src/components/subscription/`:
- `import { cn } from '../../lib/utils'`
- `import { useAuth } from '../../contexts/AuthContext'`

Since the new location `apps/web/src/components/subscription/` has identical relative depth to `apps/web/src/lib/utils` and `apps/web/src/contexts/AuthContext`, no path edits were required. All imports resolve correctly as verified by `pnpm typecheck`.

## Verification Results

| Check | Result |
|-------|--------|
| 4 files exist at apps/web/src/components/subscription/ | PASS (TierCard, SubscriptionBadge, UpgradePrompt, AIUsageCounter) |
| Task 1 grep verify (cyan #00f0ff x2, scale-105, isPopular x2, Most Popular, FREE return null, /pricing, Lock) | PASS |
| Task 2 grep verify (subscriptionTier, FREE, useAuth, Sparkles, named export) | PASS |
| `pnpm typecheck` | PASS (exit 0) |
| `pnpm test:run` | PASS (1289/1289 tests, 44 files) |
| Anti-pattern audit: `git diff --stat HEAD~2..HEAD \| grep -E "^\s+(server\|prisma\|src)/" \| grep -v "apps/web/" \| wc -l` | 0 (no root-level pollution) |

## Out-of-Scope (Documented)

- **`/api/ai/usage` server endpoint** — NOT created. AIUsageCounter handles missing endpoint gracefully via try/catch with default `{used: 0, limit: 10}` state. Per ROADMAP success criteria, only middleware + UI components + routes + i18n are in 36.4 scope.
- **Tests** — Plan 04 handles UI test additions if needed; existing E2E `apps/web/e2e/subscription.spec.ts` will exercise these components transitively via Pricing.tsx (recovered in Plan 03).

## Deviations from Plan

None — plan executed exactly as written. Both source commits existed at expected SHAs, all 4 files extracted with the exact line counts the plan predicted (150/62/74/109).

## Threat Model Compliance

| Threat ID | Mitigation Status |
|-----------|------------------|
| T-36.4-10 (AIUsageCounter info disclosure) | Mitigated — component is FREE-tier-only (`tier !== 'FREE'` returns null); endpoint creation deferred to future phase with authMiddleware gating |
| T-36.4-11 (UpgradePrompt nav tampering) | Accepted — hardcoded `/pricing` literal, no user-controllable nav input |
| T-36.4-12 (i18n XSS) | Mitigated transitively — t() values rendered as React text nodes; Plan 03 owns pricing.json injection check |
| T-36.4-13 (Most Popular spoofing) | Accepted — isPopular is a literal prop set by Pricing.tsx in Plan 03 |

No new security-relevant surface introduced; no new network endpoints or auth paths.

## Anti-Pattern Audit

The .planning/.continue-here.md severity:blocking pattern (writing to root server/, prisma/, src/) was actively prevented:
- Source commits 17066dd and 6956f02 wrote to root src/ — that was the bug
- Recovery deliberately wrote to `apps/web/src/components/subscription/` only
- Audit command `git diff --stat HEAD~2..HEAD | grep -E "^\s+(server|prisma|src)/" | grep -v "apps/web/" | wc -l` returns **0**

## Self-Check: PASSED

Verified post-write:
- FOUND: apps/web/src/components/subscription/TierCard.tsx (150 lines)
- FOUND: apps/web/src/components/subscription/SubscriptionBadge.tsx (62 lines)
- FOUND: apps/web/src/components/subscription/UpgradePrompt.tsx (74 lines)
- FOUND: apps/web/src/components/subscription/AIUsageCounter.tsx (109 lines)
- FOUND: commit bcbfe00 (Task 1 — 3 files)
- FOUND: commit 382f3b3 (Task 2 — AIUsageCounter)
- VERIFIED: pnpm typecheck exit 0
- VERIFIED: pnpm test:run 1289/1289 passing
- VERIFIED: anti-pattern audit returns 0
