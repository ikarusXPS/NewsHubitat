---
phase: 36.4-relocate-plan-03-04-monetization-artifacts
plan: 01
status: complete
subsystem: monetization-tier-middleware
tags: [tier-gating, rate-limiting, premium-features, recovery, monorepo-relocation]
requirements:
  - PAY-04
  - PAY-05
  - PAY-06
  - PAY-07
dependency-graph:
  requires:
    - apps/web/server/db/prisma (PrismaClient singleton)
    - apps/web/server/services/cacheService (CacheService + CACHE_TTL.MEDIUM)
    - apps/web/server/config/stripe (TIER_LIMITS, SubscriptionTier, SubscriptionStatus re-exports)
    - apps/web/src/generated/prisma (Prisma.ReadingHistoryWhereInput type)
  provides:
    - apps/web/server/middleware/requireTier (requireTier, attachUserTier, hasTier, invalidateTierCache)
    - apps/web/server/middleware/rateLimiter (aiTierLimiter — additive)
  affects:
    - /api/ai/* (now FREE 10/day, PREMIUM/ENTERPRISE unlimited)
    - /api/analysis/* (now FREE 10/day, PREMIUM/ENTERPRISE unlimited)
    - /api/account/export (FREE 403; PREMIUM json/csv; ENTERPRISE json/csv/pdf)
    - /api/history GET (FREE 7-day/100 entries; PREMIUM unbounded/1000 cap)
key-files:
  created:
    - path: apps/web/server/middleware/requireTier.ts
      lines: 171
      source: 3630fab:server/middleware/requireTier.ts (verbatim)
  modified:
    - path: apps/web/server/middleware/rateLimiter.ts
      lines: 216
      delta: "+69 lines (aiTierLimiter IIFE), +1 import (CACHE_TTL), +1 import (prisma)"
      source: ce2f742
    - path: apps/web/server/index.ts
      lines: 518
      delta: "import: -aiLimiter +aiTierLimiter; mounts: aiLimiter→aiTierLimiter on /api/ai and /api/analysis"
      source: ce2f742
    - path: apps/web/server/routes/account.ts
      lines: 170
      delta: "+35 lines: +2 imports, attachUserTier in /export chain, 403/400 tier guards"
      source: e07545b
    - path: apps/web/server/routes/history.ts
      lines: 83
      delta: "+49 lines: +3 imports, GET / handler with isPremium logic, AuthRequest→TierRequest"
      source: e07545b
decisions:
  - "Path adjustment: history.ts Prisma import rewritten from `'../generated/prisma'` to `'../../src/generated/prisma'` (monorepo client at apps/web/src/generated/prisma)"
  - "aiLimiter import REMOVED from apps/web/server/index.ts (ESLint no-unused-vars) — aiLimiter export remains in rateLimiter.ts for any future caller; no runtime call sites left in index.ts after aiTierLimiter swap"
  - "Type aliases SubscriptionTier/SubscriptionStatus kept as inline string-literal unions in requireTier.ts (not re-imported from config/stripe) for git log --follow continuity per Phase 36.3 state"
  - "Request import REMOVED from history.ts (TierRequest covers POST + GET handlers; ESLint no-unused-vars)"
metrics:
  duration: ~4 min wall (3 task commits + verify gates)
  completed: 2026-04-28
  tasks-completed: 3
  files-created: 1
  files-modified: 4
  lines-added: 322
  lines-deleted: 12
---

# Phase 36.4 Plan 01: Recover Server-Side Tier Middleware Summary

Recovered `requireTier` / `attachUserTier` / `hasTier` / `invalidateTierCache` + `aiTierLimiter` + tier-aware route gating from git history into canonical `apps/web/server/` paths, closing the documentation/reality gap that Phase 36.3's orphan sweep left when it deleted root `server/` wholesale (the original Plan 03 had written tier middleware to root `server/` rather than the monorepo path).

## What Was Done

### Task 1 — Recover requireTier.ts (commit `bc9e34c`)
Extracted `apps/web/server/middleware/requireTier.ts` byte-stable from source commit `3630fab:server/middleware/requireTier.ts` (171 lines). Imports `from '../db/prisma'` and `from '../services/cacheService'` resolve correctly at the new monorepo depth without modification. Exports four named symbols: `requireTier(minTier)`, `attachUserTier`, `hasTier`, `invalidateTierCache`. Implements 5-min Redis cache (`user:tier:<userId>`) with DB fallback; 7-day grace for `PAST_DUE`; CANCELED/PAUSED return 403 with `upgradeUrl: '/pricing'`.

### Task 2 — Add aiTierLimiter (commit `e99c1df`)
Two coupled edits, single atomic commit:
- `apps/web/server/middleware/rateLimiter.ts`: Added `CACHE_TTL` to existing `cacheService` import; added new `prisma` import; appended 69-line `aiTierLimiter` IIFE block (24h window, max 10 for FREE, async `skip` callback that bypasses for PREMIUM/ENTERPRISE via 5-min cached tier lookup). 429 handler returns `{ upgradeUrl: '/pricing', limit: 10 }`. All five existing exports preserved byte-stable.
- `apps/web/server/index.ts`: Swapped `aiLimiter`→`aiTierLimiter` on `/api/ai` and `/api/analysis` mounts. Removed now-unused `aiLimiter` from import line per ESLint `no-unused-vars` enforcement (deviation Rule 3).

### Task 3 — Apply tier gates to routes (commit `fe94f02`)
Two coupled edits, single atomic commit:
- `apps/web/server/routes/account.ts`: Added `attachUserTier`/`TierRequest` and `TIER_LIMITS`/`SubscriptionTier` imports; added `attachUserTier` to `/export` chain with `TierRequest` typedef; prepended 403 (export not allowed for tier) and 400 (format not in tier's allowed list) guards. Body of `/export` handler unchanged.
- `apps/web/server/routes/history.ts`: Replaced top-of-file imports adding `Prisma` (with monorepo path adjustment), `attachUserTier`/`TierRequest`/`hasTier`, `prisma`. Removed local `interface AuthRequest` and unused `Request` import. Added new `GET /` handler before existing `POST /`: `isPremium = hasTier(req.userTier || 'FREE', 'PREMIUM')`; FREE applies `readAt >= now - 7d` filter and `take: 100`; PREMIUM/ENTERPRISE skips the date filter and uses `take: 1000`; response carries `meta.tier`, `meta.isPremium`, `meta.limit`. POST handler signature updated to `TierRequest`.

## Source-Commit Traceability

| Source Commit | What It Contributed |
|---------------|---------------------|
| `3630fab` | Verbatim source of `requireTier.ts` (Phase 36-03 original) |
| `ce2f742` | aiTierLimiter IIFE + index.ts mount swap (Phase 36-03 original) |
| `e07545b` | account.ts /export tier gate + history.ts GET / handler (Phase 36-03 original) |

## Path Adjustments

| File | Original Path | Monorepo Path | Reason |
|------|---------------|---------------|--------|
| `apps/web/server/routes/history.ts` | `from '../generated/prisma'` | `from '../../src/generated/prisma'` | Generated Prisma client lives at `apps/web/src/generated/prisma/` per Phase 36.2-03 db push, not `server/generated/prisma` |

## Verification Results

| Gate | Result |
|------|--------|
| `pnpm typecheck` (after each task + final) | exit 0 |
| `pnpm test:run` (after Task 2, Task 3, final) | 1289/1289 pass (matches Phase 36.3 baseline) |
| ESLint on touched files (`history.ts`, `account.ts`, `index.ts`) | 0 errors after unused-import cleanup |
| Anti-pattern audit (files outside `apps/web/`, `packages/`, `.planning/`, `.github/`, top-level config) | 0 |
| Deletion guard (`git diff --diff-filter=D HEAD~3 HEAD`) | 0 deletions |

## Anti-Pattern Audit

```
$ git diff --name-only HEAD~3..HEAD
apps/web/server/index.ts
apps/web/server/middleware/rateLimiter.ts
apps/web/server/middleware/requireTier.ts
apps/web/server/routes/account.ts
apps/web/server/routes/history.ts
```

**Zero files written under root `server/`, `prisma/`, `src/`, `public/`.** All five touched files begin with `apps/web/`, satisfying the .planning/.continue-here.md blocking anti-pattern allow-list.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] ESLint no-unused-vars on `aiLimiter` import in index.ts (Task 2)**
- **Found during:** Task 2 verification (`pnpm exec eslint server/index.ts`)
- **Issue:** After swapping `aiLimiter` → `aiTierLimiter` on the two AI/analysis mounts, `aiLimiter` had zero remaining call sites in `index.ts` but was still in the import line. `tsc --noEmit` did not flag it (server folder isn't in the active tsc include path), but ESLint's `@typescript-eslint/no-unused-vars` correctly errored.
- **Fix:** Removed `aiLimiter` from the import line in `apps/web/server/index.ts` (the export remains in `rateLimiter.ts` for any future consumer).
- **Plan-anticipated:** Yes — the plan's Task 2 step 4 explicitly says "if pnpm typecheck flags `aiLimiter` as unused-import, remove it from the import." The lint surface caught it, not typecheck, but the action taken matches the plan's contingency exactly.
- **Files modified:** `apps/web/server/index.ts`
- **Commit:** `e99c1df`

**2. [Rule 3 - Blocking] ESLint no-unused-vars on `Request` import in history.ts (Task 3)**
- **Found during:** Task 3 verification (`pnpm exec eslint server/routes/history.ts`)
- **Issue:** After replacing the `interface AuthRequest extends Request` block with the imported `TierRequest`, the bare `Request` from express was no longer referenced anywhere in `history.ts`. ESLint flagged it as unused.
- **Fix:** Changed `import { Router, Request, Response } from 'express';` to `import { Router, Response } from 'express';`.
- **Files modified:** `apps/web/server/routes/history.ts`
- **Commit:** `fe94f02`

Neither deviation alters the plan's behavior contract. Both are surface-level ESLint hygiene tied to imports the plan itself instructed me to swap out, with explicit plan-anticipated contingency in the case of `aiLimiter`.

## Decision Log (One-Liners for Plan 04 Awareness)

- **aiLimiter import status:** REMOVED from `apps/web/server/index.ts` (no remaining call sites after the aiTierLimiter swap). The `aiLimiter` *export* in `rateLimiter.ts` is preserved for any future caller — no breaking change to the module API.
- **Prisma path:** `apps/web/server/routes/history.ts` imports `Prisma` types from `'../../src/generated/prisma'`, not from a server-local re-export. Plan 04 should use the same path for any new generated-Prisma type imports under `apps/web/server/routes/`.

## Self-Check: PASSED

Verified at SUMMARY-write time:
- `apps/web/server/middleware/requireTier.ts` exists (171 lines) — FOUND
- `apps/web/server/middleware/rateLimiter.ts` modified (216 lines, contains `aiTierLimiter` x1) — FOUND
- `apps/web/server/index.ts` modified (518 lines, contains `aiTierLimiter` x3) — FOUND
- `apps/web/server/routes/account.ts` modified (170 lines, contains `attachUserTier`) — FOUND
- `apps/web/server/routes/history.ts` modified (83 lines, contains `isPremium` x5) — FOUND
- Commit `bc9e34c` (Task 1) — FOUND in `git log`
- Commit `e99c1df` (Task 2) — FOUND in `git log`
- Commit `fe94f02` (Task 3) — FOUND in `git log`
- `pnpm typecheck` final run — exit 0
- `pnpm test:run` final run — 1289/1289 pass

All success criteria met. Ready for the orchestrator's verification gate (Plan 04).
