---
phase: 24-mobile-responsive
plan: 04
type: execute
wave: 2
depends_on:
  - 24-01
  - 24-02
files_modified:
  - src/components/mobile/SwipeableCard.tsx
  - src/components/mobile/PullToRefresh.tsx
  - src/components/mobile/ScrollToTopFAB.tsx
  - src/components/NewsFeed.tsx
  - src/components/NewsCard.tsx
autonomous: true
requirements:
  - MOBILE-02
must_haves:
  truths:
    - "News cards support swipe-right-to-bookmark gesture on mobile"
    - "News feed supports pull-to-refresh with custom cyber indicator"
    - "Scroll-to-top FAB appears after 500px scroll"
    - "All touch interactions have haptic feedback"
    - "Gestures use spring animations for natural feel"
  artifacts:
    - path: "src/components/mobile/SwipeableCard.tsx"
      provides: "Swipe gesture wrapper for bookmarking"
      min_lines: 80
      exports: ["SwipeableCard"]
    - path: "src/components/mobile/PullToRefresh.tsx"
      provides: "Pull-to-refresh component with cyber indicator"
      min_lines: 60
      exports: ["PullToRefresh"]
    - path: "src/components/mobile/ScrollToTopFAB.tsx"
      provides: "Floating action button for scroll-to-top"
      min_lines: 50
      exports: ["ScrollToTopFAB"]
  key_links:
    - from: "SwipeableCard.tsx"
      to: "Framer Motion drag"
      via: "motion.div with drag='x'"
      pattern: "drag=\"x\""
    - from: "NewsCard.tsx"
      to: "SwipeableCard wrapper"
      via: "conditional mobile wrapper"
      pattern: "<SwipeableCard"
    - from: "NewsFeed.tsx"
      to: "PullToRefresh wrapper"
      via: "wraps article list"
      pattern: "<PullToRefresh"
---

<objective>
Implement touch interaction components for mobile: swipe-to-bookmark, pull-to-refresh, and scroll-to-top FAB with haptic feedback and spring animations.

Purpose: Mobile-first interaction patterns that feel native and responsive — enhance user engagement with touch-optimized gestures.

Output: SwipeableCard, PullToRefresh, ScrollToTopFAB components integrated into NewsFeed and Layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\NewsHub\.planning\PROJECT.md
@D:\NewsHub\.planning\ROADMAP.md
@D:\NewsHub\.planning\STATE.md
@D:\NewsHub\.planning\phases\24-mobile-responsive\24-CONTEXT.md
@D:\NewsHub\.planning\phases\24-mobile-responsive\24-PATTERNS.md
@D:\NewsHub\.planning\phases\24-mobile-responsive\24-RESEARCH.md
@D:\NewsHub\.planning\phases\24-mobile-responsive\24-UI-SPEC.md
@D:\NewsHub\src\components\ForYouCarousel.tsx
@D:\NewsHub\src\components\InfiniteScroll.tsx
@D:\NewsHub\src\components\OfflineBanner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SwipeableCard component</name>
  <read_first>
    - D:\NewsHub\src\components\ForYouCarousel.tsx (horizontal scroll pattern, lines 89-99 from PATTERNS.md)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-PATTERNS.md (SwipeableCard section, lines 199-243)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-RESEARCH.md (Pattern 3: Swipe-to-Bookmark, lines 448-499)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-CONTEXT.md (D-61 for swipe-to-bookmark spec)
  </read_first>
  <files>
    - D:\NewsHub\src\components\mobile\SwipeableCard.tsx
  </files>
  <action>
Create src/components/mobile/SwipeableCard.tsx with Framer Motion drag gestures:

**Imports:**
```typescript
import { motion, useMotionValue, useTransform, PanInfo } from 'framer-motion';
import { Bookmark } from 'lucide-react';
import { useHapticFeedback } from '../../hooks/useHapticFeedback';
```

**Interface:**
```typescript
interface SwipeableCardProps {
  children: React.ReactNode;
  onBookmark: () => void;
  isBookmarked: boolean;
}
```

**Implementation (per RESEARCH.md Pattern 3, lines 456-499):**

```tsx
export function SwipeableCard({ children, onBookmark, isBookmarked }: SwipeableCardProps) {
  const x = useMotionValue(0);
  const opacity = useTransform(x, [0, 100], [0, 1]);
  const { lightTap } = useHapticFeedback();

  const handleDragEnd = (event: MouseEvent | TouchEvent | PointerEvent, info: PanInfo) => {
    // Threshold: 80px swipe right (Claude's discretion from RESEARCH.md)
    if (info.offset.x > 80 && !isBookmarked) {
      onBookmark();
      lightTap(); // Haptic feedback per D-61
    }
    // Snap back to original position
    x.set(0);
  };

  return (
    <div className="relative">
      {/* Bookmark reveal background */}
      <motion.div
        className="absolute inset-y-0 right-0 flex items-center justify-end px-6 bg-[#00f0ff]/10 rounded-lg"
        style={{ opacity }}
      >
        <Bookmark className="h-6 w-6 text-[#00f0ff]" fill="currentColor" />
      </motion.div>

      {/* Draggable card */}
      <motion.div
        drag="x"
        dragConstraints={{ left: 0, right: 150 }}
        dragElastic={0.2}
        style={{ x }}
        onDragEnd={handleDragEnd}
        transition={{
          type: 'spring',
          damping: 20,
          stiffness: 180,
          reducedMotion: 'user' // Accessibility per RESEARCH.md line 731
        }}
      >
        {children}
      </motion.div>
    </div>
  );
}
```

**Key specs (per D-61, RESEARCH.md):**
- Swipe right only (no swipe-left action per D-62)
- 80px threshold (Claude's discretion from RESEARCH.md line 962)
- Spring animation with damping: 20, stiffness: 180 (per RESEARCH.md line 800)
- Cyan bookmark icon reveal (per CONTEXT.md Specific Ideas)
- Haptic feedback on successful bookmark
- Snap back to position after drag (x.set(0))
- No bookmark action if already bookmarked (prevents duplicate)
  </action>
  <acceptance_criteria>
    - src/components/mobile/SwipeableCard.tsx exists
    - File exports SwipeableCard function component
    - Component accepts { children, onBookmark, isBookmarked } props
    - Uses motion.div with drag="x" from Framer Motion
    - dragConstraints set to { left: 0, right: 150 }
    - onDragEnd checks info.offset.x > 80 before calling onBookmark
    - Haptic feedback triggered on successful bookmark
    - Bookmark icon visible in reveal background with cyan color (#00f0ff)
    - Spring transition with damping: 20, stiffness: 180
    - reducedMotion: 'user' for accessibility
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export.*function SwipeableCard" D:\NewsHub\src\components\mobile\SwipeableCard.tsx && grep -q "drag=\"x\"" D:\NewsHub\src\components\mobile\SwipeableCard.tsx && grep -q "useHapticFeedback" D:\NewsHub\src\components\mobile\SwipeableCard.tsx</automated>
  </verify>
  <done>SwipeableCard component created with drag gesture, 80px threshold, haptic feedback</done>
</task>

<task type="auto">
  <name>Task 2: Create PullToRefresh component</name>
  <read_first>
    - D:\NewsHub\src\components\InfiniteScroll.tsx (loading state UI, lines 36-59 from PATTERNS.md)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-PATTERNS.md (PullToRefresh section, lines 245-300)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-RESEARCH.md (Pull-to-Refresh example, lines 860-900)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-CONTEXT.md (D-60 for pull-to-refresh spec)
  </read_first>
  <files>
    - D:\NewsHub\src\components\mobile\PullToRefresh.tsx
  </files>
  <action>
Install react-simple-pull-to-refresh:

```bash
npm install react-simple-pull-to-refresh
```

Create src/components/mobile/PullToRefresh.tsx:

**Imports:**
```typescript
import PullToRefreshLib from 'react-simple-pull-to-refresh';
import { Radio } from 'lucide-react';
```

**Interface:**
```typescript
interface PullToRefreshProps {
  onRefresh: () => Promise<void>;
  children: React.ReactNode;
}
```

**Implementation (per RESEARCH.md lines 866-900):**

```tsx
export function PullToRefresh({ onRefresh, children }: PullToRefreshProps) {
  return (
    <PullToRefreshLib
      onRefresh={onRefresh}
      pullingContent={
        <div className="flex justify-center py-4">
          <Radio className="h-6 w-6 text-[#00f0ff] animate-pulse" />
        </div>
      }
      refreshingContent={
        <div className="flex justify-center py-4">
          <Radio className="h-6 w-6 text-[#00f0ff] animate-spin" />
        </div>
      }
      pullDownThreshold={80}
      maxPullDownDistance={120}
      resistance={2}
    >
      {children}
    </PullToRefreshLib>
  );
}
```

**Custom cyber indicator (per CONTEXT.md Specific Ideas):**
- Pulling: Radio icon with pulse animation
- Refreshing: Radio icon with spin animation
- Cyan color (#00f0ff) matches theme
- pullDownThreshold: 80px (reasonable pull distance)
- resistance: 2 (prevents excessive pull)
  </action>
  <acceptance_criteria>
    - react-simple-pull-to-refresh package installed in package.json
    - src/components/mobile/PullToRefresh.tsx exists
    - File exports PullToRefresh function component
    - Component accepts { onRefresh, children } props
    - Uses PullToRefreshLib from react-simple-pull-to-refresh
    - pullingContent shows Radio icon with animate-pulse
    - refreshingContent shows Radio icon with animate-spin
    - Radio icon color is #00f0ff (cyan)
    - pullDownThreshold is 80
    - resistance is 2
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export.*function PullToRefresh" D:\NewsHub\src\components\mobile\PullToRefresh.tsx && grep -q "react-simple-pull-to-refresh" D:\NewsHub\package.json && grep -q "Radio.*animate-spin" D:\NewsHub\src\components\mobile\PullToRefresh.tsx</automated>
  </verify>
  <done>PullToRefresh component created with custom Radio icon indicator and cyber styling</done>
</task>

<task type="auto">
  <name>Task 3: Create ScrollToTopFAB component</name>
  <read_first>
    - D:\NewsHub\src\components\OfflineBanner.tsx (conditional rendering pattern)
    - D:\NewsHub\src\components\Sidebar.tsx (AnimatePresence usage, lines 100-111 from PATTERNS.md)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-PATTERNS.md (ScrollToTopFAB section, lines 303-343)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-CONTEXT.md (D-31, D-32 for FAB positioning)
  </read_first>
  <files>
    - D:\NewsHub\src\components\mobile\ScrollToTopFAB.tsx
  </files>
  <action>
Create src/components/mobile/ScrollToTopFAB.tsx with scroll detection:

**Imports:**
```typescript
import { useState, useEffect } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { ArrowUp } from 'lucide-react';
import { useHapticFeedback } from '../../hooks/useHapticFeedback';
```

**Implementation (per PATTERNS.md lines 308-343):**

```tsx
export function ScrollToTopFAB() {
  const [isVisible, setIsVisible] = useState(false);
  const { lightTap } = useHapticFeedback();

  useEffect(() => {
    const handleScroll = () => {
      // D-32: 500px threshold
      setIsVisible(window.scrollY > 500);
    };

    window.addEventListener('scroll', handleScroll, { passive: true });
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const scrollToTop = () => {
    window.scrollTo({ top: 0, behavior: 'smooth' });
    lightTap();
  };

  return (
    <AnimatePresence>
      {isVisible && (
        <motion.button
          initial={{ opacity: 0, scale: 0.8 }}
          animate={{ opacity: 1, scale: 1 }}
          exit={{ opacity: 0, scale: 0.8 }}
          transition={{ duration: 0.2 }}
          onClick={scrollToTop}
          className="fixed z-40 md:hidden glass-panel rounded-full p-3 border border-[#00f0ff]/20 shadow-lg shadow-[#00f0ff]/10"
          style={{
            bottom: 'calc(var(--bottom-nav-height) + 16px)',
            right: '16px',
          }}
          aria-label="Scroll to top"
        >
          <ArrowUp className="h-5 w-5 text-[#00f0ff]" />
        </motion.button>
      )}
    </AnimatePresence>
  );
}
```

**Key specs (per D-31, D-32, PATTERNS.md):**
- Appears after 500px scroll (D-32)
- Positioned above bottom nav: bottom-nav-height + 16px gap (D-31)
- Right: 16px from edge
- Glass panel styling with cyan border (per PATTERNS.md line 340)
- ArrowUp icon (Claude's discretion from RESEARCH.md line 950)
- Smooth scroll behavior
- Haptic feedback on tap
- md:hidden (mobile only)
- AnimatePresence for mount/unmount animation
  </action>
  <acceptance_criteria>
    - src/components/mobile/ScrollToTopFAB.tsx exists
    - File exports ScrollToTopFAB function component
    - Component uses useState for isVisible
    - Scroll listener sets isVisible when scrollY > 500
    - Event listener is passive
    - Cleanup function removes event listener
    - Uses AnimatePresence for conditional render
    - Button positioned using var(--bottom-nav-height) + 16px
    - Button has md:hidden class
    - onClick calls window.scrollTo with behavior: 'smooth'
    - Haptic feedback triggered on click
    - ArrowUp icon from lucide-react
  </acceptance_criteria>
  <verify>
    <automated>grep -q "export.*function ScrollToTopFAB" D:\NewsHub\src\components\mobile\ScrollToTopFAB.tsx && grep -q "scrollY > 500" D:\NewsHub\src\components\mobile\ScrollToTopFAB.tsx && grep -q "AnimatePresence" D:\NewsHub\src\components\mobile\ScrollToTopFAB.tsx</automated>
  </verify>
  <done>ScrollToTopFAB component created with 500px threshold, glass styling, haptic feedback</done>
</task>

<task type="auto">
  <name>Task 4: Wrap NewsCard with SwipeableCard on mobile</name>
  <read_first>
    - D:\NewsHub\src\components\NewsCard.tsx (current structure)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-PATTERNS.md (NewsCard modification, lines 486-512)
  </read_first>
  <files>
    - D:\NewsHub\src\components\NewsCard.tsx
  </files>
  <action>
Update NewsCard.tsx to conditionally wrap with SwipeableCard on mobile:

**1. Add import:**
```typescript
import { SwipeableCard } from './mobile/SwipeableCard';
```

**2. Add media query hook (or use window.innerWidth check):**
```typescript
const isMobile = window.innerWidth < 768;
```

**3. Wrap card content with SwipeableCard conditionally:**

```tsx
const cardContent = (
  <motion.article
    // ... existing NewsCard motion.article content
  >
    {/* All existing card content */}
  </motion.article>
);

// Conditional wrapper
if (isMobile) {
  return (
    <SwipeableCard
      onBookmark={handleBookmark}
      isBookmarked={localArticle.isBookmarked || false}
    >
      {cardContent}
    </SwipeableCard>
  );
}

return cardContent;
```

**Note:** NewsCard already has handleBookmark function (from Sidebar patterns). Reuse existing bookmark logic.

**Alternative (if handleBookmark doesn't exist):** NewsCard might use a mutation or store action for bookmarking. Check the existing bookmark button onClick handler and extract that logic.
  </action>
  <acceptance_criteria>
    - NewsCard.tsx imports SwipeableCard from './mobile/SwipeableCard'
    - NewsCard checks viewport width or uses media query
    - On mobile (<768px), NewsCard wrapped in SwipeableCard
    - SwipeableCard receives onBookmark and isBookmarked props
    - onBookmark prop uses existing bookmark logic from NewsCard
    - Desktop (>=768px) renders NewsCard without SwipeableCard wrapper
  </acceptance_criteria>
  <verify>
    <automated>grep -q "import.*SwipeableCard" D:\NewsHub\src\components\NewsCard.tsx && grep -q "SwipeableCard" D:\NewsHub\src\components\NewsCard.tsx</automated>
  </verify>
  <done>NewsCard conditionally wrapped with SwipeableCard on mobile for swipe-to-bookmark gesture</done>
</task>

<task type="auto">
  <name>Task 5: Wrap NewsFeed with PullToRefresh and add ScrollToTopFAB</name>
  <read_first>
    - D:\NewsHub\src\components\NewsFeed.tsx (current structure)
    - D:\NewsHub\.planning\phases\24-mobile-responsive\24-PATTERNS.md (PullToRefresh usage)
  </read_first>
  <files>
    - D:\NewsHub\src\components\NewsFeed.tsx
  </files>
  <action>
Update NewsFeed.tsx to wrap article list with PullToRefresh:

**1. Add imports:**
```typescript
import { PullToRefresh } from './mobile/PullToRefresh';
import { ScrollToTopFAB } from './mobile/ScrollToTopFAB';
```

**2. Wrap article list (likely inside a div or fragment):**

**BEFORE:**
```tsx
<div className="space-y-4">
  {articles.map((article, index) => (
    <NewsCard
      key={article.id}
      article={article}
      priority={index < 3}
    />
  ))}
</div>
```

**AFTER:**
```tsx
<PullToRefresh onRefresh={handleRefresh}>
  <div className="space-y-4">
    {articles.map((article, index) => (
      <NewsCard
        key={article.id}
        article={article}
        priority={index < 3}
      />
    ))}
  </div>
</PullToRefresh>

<ScrollToTopFAB />
```

**3. Create handleRefresh function:**
```typescript
const handleRefresh = async () => {
  await refetch(); // TanStack Query refetch
};
```

NewsFeed likely uses useQuery for fetching articles. Extract the refetch function from query result.

**4. Add ScrollToTopFAB after article list** (outside PullToRefresh wrapper).
  </action>
  <acceptance_criteria>
    - NewsFeed.tsx imports PullToRefresh from './mobile/PullToRefresh'
    - NewsFeed.tsx imports ScrollToTopFAB from './mobile/ScrollToTopFAB'
    - Article list wrapped in <PullToRefresh onRefresh={handleRefresh}>
    - handleRefresh function calls refetch from useQuery result
    - ScrollToTopFAB rendered after article list
  </acceptance_criteria>
  <verify>
    <automated>grep -q "import.*PullToRefresh" D:\NewsHub\src\components\NewsFeed.tsx && grep -q "import.*ScrollToTopFAB" D:\NewsHub\src\components\NewsFeed.tsx && grep -q "<PullToRefresh" D:\NewsHub\src\components\NewsFeed.tsx</automated>
  </verify>
  <done>NewsFeed wrapped with PullToRefresh, ScrollToTopFAB added for scroll-to-top interaction</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure UI interaction components, no data processing or external communication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-09 | Denial of Service | Rapid swipe spam | accept | Framer Motion debounces drag events, onDragEnd fires once per gesture. Browser rate-limits haptic vibrations. |
| T-24-10 | Denial of Service | Pull-to-refresh spam | accept | react-simple-pull-to-refresh library prevents concurrent refresh calls (onRefresh awaits Promise). |
</threat_model>

<verification>
After task completion:
1. Run `npm run typecheck` — must pass
2. Test swipe-to-bookmark: swipe right on card → bookmark added
3. Verify swipe threshold: small swipe (<80px) snaps back, large swipe (>80px) bookmarks
4. Test pull-to-refresh: pull down → Radio icon spins → feed refreshes
5. Check FAB: scroll down 500px → FAB appears, click → smooth scroll to top
6. Verify haptic feedback: tap FAB, swipe to bookmark → device vibrates (on Android)
7. Test spring animations: smooth, natural feel on all gestures
</verification>

<success_criteria>
- [ ] SwipeableCard component created with 80px threshold
- [ ] PullToRefresh component created with Radio icon indicator
- [ ] ScrollToTopFAB component created with 500px threshold
- [ ] NewsCard wrapped with SwipeableCard on mobile
- [ ] NewsFeed wrapped with PullToRefresh
- [ ] ScrollToTopFAB added to NewsFeed
- [ ] TypeScript compilation passes
- [ ] All gestures use haptic feedback
</success_criteria>

<output>
After completion, create `.planning/phases/24-mobile-responsive/24-04-SUMMARY.md` with:
- SwipeableCard behavior (threshold, animation feel)
- PullToRefresh integration (Radio icon indicator, refresh logic)
- ScrollToTopFAB positioning (above bottom nav)
- Haptic feedback verification (which devices support)
- Spring animation quality assessment
- Any issues with gesture detection or animation performance
</output>
