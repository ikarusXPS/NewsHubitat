---
phase: 11-e2e-tests
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - e2e/analysis.spec.ts
autonomous: true
requirements: [E2E-02]
must_haves:
  truths:
    - "Analysis page loads with PERSPEKTIVEN-ANALYSE header"
    - "Cluster summaries section is visible"
    - "Compare mode modal can be opened"
    - "Sentiment chart and framing comparison are displayed"
  artifacts:
    - path: "e2e/analysis.spec.ts"
      provides: "Analysis page E2E tests"
      min_lines: 70
  key_links:
    - from: "e2e/analysis.spec.ts"
      to: "src/pages/Analysis.tsx"
      via: "page.goto('/analysis')"
      pattern: "await page.goto\\('/analysis'\\)"
---

<objective>
Create E2E tests for the Analysis page verifying cluster summaries, compare mode, and visualization components.

Purpose: Verify Analysis page interactions per E2E-02 requirement. Per D-06/D-07, test UI flow without asserting AI response quality.
Output: e2e/analysis.spec.ts with 8-10 tests covering clusters, compare mode, charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e2e-tests/11-RESEARCH.md
@.planning/phases/11-e2e-tests/11-PATTERNS.md
</context>

<interfaces>
<!-- Analysis page structure -->
From src/pages/Analysis.tsx (lines 54-89):
```typescript
export function Analysis() {
  const [showCompare, setShowCompare] = useState(false);
  // ...
  return (
    <div className="space-y-6">
      <motion.div>
        <h1 className="text-2xl font-bold text-white font-mono flex items-center gap-3">
          <BarChart3 className="h-6 w-6 text-[#00f0ff]" />
          <span className="gradient-text-cyber">PERSPEKTIVEN-ANALYSE</span>
        </h1>
        // ...
        <button onClick={() => setShowCompare(true)}>
          <span className="font-mono uppercase tracking-wider">Artikel vergleichen</span>
        </button>
      </motion.div>
      // Components: ClusterSummary, SentimentChart, FramingComparison, BiasRadarChart
      {showCompare && <CompareMode onClose={() => setShowCompare(false)} articles={articles} />}
    </div>
  );
}
```

<!-- Test patterns -->
From e2e/monitor.spec.ts:
```typescript
test('should display panel containers', async ({ page }) => {
  await page.waitForTimeout(1000);
  const glassPanels = page.locator('.glass-panel');
  const panelCount = await glassPanels.count();
  expect(panelCount).toBeGreaterThan(0);
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create analysis.spec.ts with core tests</name>
  <files>e2e/analysis.spec.ts</files>
  <read_first>
    - D:\NewsHub\e2e\monitor.spec.ts (test structure pattern)
    - D:\NewsHub\src\pages\Analysis.tsx (lines 1-100, component structure)
    - D:\NewsHub\.planning\phases\11-e2e-tests\11-PATTERNS.md (Analysis tests pattern)
  </read_first>
  <action>
Create e2e/analysis.spec.ts with the following tests:

```typescript
import { test, expect } from '@playwright/test';

test.describe('Analysis Page', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/analysis');
    await page.waitForLoadState('networkidle');
  });

  test('should load the Analysis page with header', async ({ page }) => {
    await expect(page).toHaveURL('/analysis');

    // Check for PERSPEKTIVEN-ANALYSE header
    const heading = page.locator('h1');
    await expect(heading).toContainText('PERSPEKTIVEN-ANALYSE');
  });

  test('should display gradient-text-cyber styling', async ({ page }) => {
    // Check for cyber-styled text
    const cyberText = page.locator('.gradient-text-cyber');
    await expect(cyberText).toBeVisible();
  });

  test('should have compare articles button', async ({ page }) => {
    // "Artikel vergleichen" button
    const compareBtn = page.locator('button:has-text("Artikel vergleichen")');
    await expect(compareBtn).toBeVisible();
  });

  test('should open compare mode modal', async ({ page }) => {
    // Click compare button
    const compareBtn = page.locator('button:has-text("Artikel vergleichen")');
    await compareBtn.click();

    // Modal should appear (CompareMode uses fixed positioning)
    const modal = page.locator('.fixed.inset-0, [role="dialog"]');
    await expect(modal.first()).toBeVisible();
  });

  test('should close compare mode modal', async ({ page }) => {
    // Open modal
    const compareBtn = page.locator('button:has-text("Artikel vergleichen")');
    await compareBtn.click();

    // Wait for modal
    await page.waitForTimeout(300);

    // Find and click close button (X icon)
    const closeBtn = page.locator('.fixed button').filter({ has: page.locator('[class*="lucide-x"]') }).first();
    if (await closeBtn.isVisible()) {
      await closeBtn.click();
      await page.waitForTimeout(300);

      // Modal should be closed
      const modalContent = page.locator('.fixed.inset-0 .bg-\\[#0a0e1a\\]');
      await expect(modalContent).not.toBeVisible();
    }
  });

  test('should display cluster summaries section', async ({ page }) => {
    // Wait for content to load
    await page.waitForTimeout(1000);

    // ClusterSummary section - look for "Themen-Cluster" or cluster-related content
    const clusterSection = page.locator('text=/Themen|Cluster|Topics/i').first();
    if (await clusterSection.isVisible()) {
      await expect(clusterSection).toBeVisible();
    }
  });

  test('should display glass panel containers', async ({ page }) => {
    // Wait for page to load
    await page.waitForTimeout(1000);

    // Check for glass-panel class (Analysis uses this for sections)
    const glassPanels = page.locator('.glass-panel');
    const panelCount = await glassPanels.count();

    // Should have at least one glass panel
    expect(panelCount).toBeGreaterThan(0);
  });

  test('should display sentiment chart section', async ({ page }) => {
    // SentimentChart shows sentiment distribution
    const sentimentSection = page.locator('text=/Sentiment|Stimmung/i').first();
    if (await sentimentSection.isVisible()) {
      await expect(sentimentSection).toBeVisible();
    }
  });

  test('should display framing comparison section', async ({ page }) => {
    // FramingComparison shows how different sources frame topics
    const framingSection = page.locator('text=/Framing|Berichterstattung/i').first();
    if (await framingSection.isVisible()) {
      await expect(framingSection).toBeVisible();
    }
  });

  test('should navigate from sidebar', async ({ page }) => {
    // Navigate away first
    await page.goto('/');
    await page.waitForLoadState('networkidle');

    // Click Analysis link in sidebar
    const analysisLink = page.locator('a[href="/analysis"]');
    await analysisLink.click();

    await expect(page).toHaveURL('/analysis');
  });

  test('should show loading state while fetching data', async ({ page }) => {
    // Reload to see loading state
    await page.reload();

    // Loading spinner (Loader2 icon with animate-spin)
    const loader = page.locator('[class*="animate-spin"]').first();

    // Loader may be visible briefly
    // After networkidle, content should be visible
    await page.waitForLoadState('networkidle');

    // Verify page loaded by checking header
    const heading = page.locator('h1');
    await expect(heading).toContainText('PERSPEKTIVEN-ANALYSE');
  });
});
```

NOTE: Per D-06/D-07, these tests focus on UI flow without asserting AI response content quality. The tests verify that components render and modals open/close correctly.
  </action>
  <verify>
    <automated>npx playwright test e2e/analysis.spec.ts --project=chromium 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at e2e/analysis.spec.ts
    - grep -q "test.describe('Analysis Page'" e2e/analysis.spec.ts returns 0
    - grep -q "PERSPEKTIVEN-ANALYSE" e2e/analysis.spec.ts returns 0
    - grep -q "should open compare mode modal" e2e/analysis.spec.ts returns 0
    - grep -q "should display cluster summaries" e2e/analysis.spec.ts returns 0
    - grep -c "test\\(" e2e/analysis.spec.ts shows at least 8 tests
  </acceptance_criteria>
  <done>analysis.spec.ts exists with 8-10 tests covering Analysis page interactions</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Analysis is public, no auth required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | No new security surfaces - testing existing UI |
</threat_model>

<verification>
1. Run `npx playwright test e2e/analysis.spec.ts --project=chromium`
2. All tests should pass or skip gracefully for conditional elements
3. Test output should show 8+ tests executed
</verification>

<success_criteria>
- e2e/analysis.spec.ts exists with 8+ tests
- Tests cover: page load, header, compare mode modal, cluster section, charts
- Tests follow D-06/D-07: test UI flow, not AI response quality
- All tests pass against dev server
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e-tests/11-03-SUMMARY.md`
</output>
