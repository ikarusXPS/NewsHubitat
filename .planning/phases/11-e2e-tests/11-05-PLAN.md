---
phase: 11-e2e-tests
plan: 05
type: execute
wave: 3
depends_on: [11-01]
files_modified:
  - e2e/profile.spec.ts
autonomous: true
requirements: [E2E-07]
must_haves:
  truths:
    - "Profile page loads for authenticated users"
    - "User stats (bookmarks count, articles read) are displayed"
    - "Quick actions navigate to correct pages"
    - "Password change form is accessible"
  artifacts:
    - path: "e2e/profile.spec.ts"
      provides: "Profile page E2E tests (auth required)"
      min_lines: 70
  key_links:
    - from: "e2e/profile.spec.ts"
      to: "src/pages/Profile.tsx"
      via: "page.goto('/profile')"
      pattern: "await page.goto\\('/profile'\\)"
    - from: "chromium-auth project"
      to: "e2e/profile.spec.ts"
      via: "testMatch pattern"
      pattern: "**/profile.spec.ts"
---

<objective>
Create E2E tests for the Profile page verifying user stats, quick actions, and password change form.

Purpose: Verify authenticated Profile page interactions per E2E-07 requirement.
Output: e2e/profile.spec.ts with 8-10 tests. Runs under chromium-auth project with storageState.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-e2e-tests/11-RESEARCH.md
@.planning/phases/11-e2e-tests/11-PATTERNS.md
@.planning/phases/11-e2e-tests/11-01-SUMMARY.md
</context>

<interfaces>
<!-- Profile page structure -->
From src/pages/Profile.tsx (lines 27-72):
```typescript
export function Profile() {
  const { user, isAuthenticated, logout, isLoading } = useAuth();
  const { bookmarkedArticles, readingHistory, language } = useAppStore();
  // ...
  const [isChangingPassword, setIsChangingPassword] = useState(false);
  // Stats: Bookmarks count, Articles Read count
  // Quick actions: Settings, Bookmarks, History, Logout
}
```

<!-- Test runs with storageState -->
This spec file runs under chromium-auth project which:
- Depends on setup project (auth.setup.ts)
- Uses storageState: 'playwright/.auth/user.json'
- User is already authenticated when tests run
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create profile.spec.ts with auth-required tests</name>
  <files>e2e/profile.spec.ts</files>
  <read_first>
    - D:\NewsHub\e2e\monitor.spec.ts (test structure pattern)
    - D:\NewsHub\src\pages\Profile.tsx (lines 1-100, component structure)
    - D:\NewsHub\.planning\phases\11-e2e-tests\11-PATTERNS.md (Profile tests pattern)
  </read_first>
  <action>
Create e2e/profile.spec.ts with the following tests. NOTE: This file runs under chromium-auth project which already has storageState loaded - user is authenticated.

```typescript
import { test, expect } from '@playwright/test';

// These tests run with storageState from auth.setup.ts
// User is already authenticated when tests start

test.describe('Profile Page (Authenticated)', () => {
  test.beforeEach(async ({ page }) => {
    await page.goto('/profile');
    await page.waitForLoadState('networkidle');
  });

  test('should load the Profile page', async ({ page }) => {
    await expect(page).toHaveURL('/profile');

    // Check for PROFILE header
    const heading = page.locator('h1:has-text("PROFILE")');
    await expect(heading).toBeVisible();
  });

  test('should display user info section', async ({ page }) => {
    // User icon or avatar area
    const userSection = page.locator('[class*="lucide-user"], [class*="avatar"]').first();
    if (await userSection.isVisible()) {
      await expect(userSection).toBeVisible();
    }
  });

  test('should display bookmarks stat', async ({ page }) => {
    // Stats grid shows Bookmarks count
    const bookmarksLabel = page.locator('text=Bookmarks').first();
    await expect(bookmarksLabel).toBeVisible();
  });

  test('should display articles read stat', async ({ page }) => {
    // Stats grid shows Articles Read count
    const articlesLabel = page.locator('text=/Articles Read|Artikel gelesen/i').first();
    await expect(articlesLabel).toBeVisible();
  });

  test('should have quick action buttons', async ({ page }) => {
    // Quick actions section
    const settingsBtn = page.locator('button:has-text("Settings")');
    const bookmarksBtn = page.locator('button:has-text("Bookmarks")');
    const historyBtn = page.locator('button:has-text("History")');

    await expect(settingsBtn).toBeVisible();
    await expect(bookmarksBtn).toBeVisible();
    await expect(historyBtn).toBeVisible();
  });

  test('should navigate to Settings via quick action', async ({ page }) => {
    const settingsBtn = page.locator('button:has-text("Settings")');
    await settingsBtn.click();

    await expect(page).toHaveURL('/settings');
  });

  test('should navigate to Bookmarks via quick action', async ({ page }) => {
    const bookmarksBtn = page.locator('button:has-text("Bookmarks")');
    await bookmarksBtn.click();

    await expect(page).toHaveURL('/bookmarks');
  });

  test('should navigate to History via quick action', async ({ page }) => {
    const historyBtn = page.locator('button:has-text("History")');
    await historyBtn.click();

    await expect(page).toHaveURL('/history');
  });

  test('should have password change section', async ({ page }) => {
    // Password change area (may be a button to reveal form)
    const passwordSection = page.locator('text=/Password|Passwort|Change Password/i').first();
    if (await passwordSection.isVisible()) {
      await expect(passwordSection).toBeVisible();
    }
  });

  test('should have logout button', async ({ page }) => {
    // Logout button
    const logoutBtn = page.locator('button:has-text("Logout"), button:has-text("Abmelden")');
    await expect(logoutBtn.first()).toBeVisible();
  });

  test('should display glass panel styling', async ({ page }) => {
    // Profile uses glass-panel for sections
    const glassPanels = page.locator('.glass-panel');
    const panelCount = await glassPanels.count();
    expect(panelCount).toBeGreaterThan(0);
  });
});
```
  </action>
  <verify>
    <automated>npx playwright test e2e/profile.spec.ts --project=chromium-auth 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at e2e/profile.spec.ts
    - grep -q "test.describe('Profile Page" e2e/profile.spec.ts returns 0
    - grep -q "should display bookmarks stat" e2e/profile.spec.ts returns 0
    - grep -q "should navigate to Settings via quick action" e2e/profile.spec.ts returns 0
    - grep -q "should have logout button" e2e/profile.spec.ts returns 0
    - grep -c "test\\(" e2e/profile.spec.ts shows at least 8 tests
  </acceptance_criteria>
  <done>profile.spec.ts exists with 8-10 tests for authenticated Profile page</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Auth-required page | Profile requires valid session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | Tests run with valid storageState; no new attack surfaces |
</threat_model>

<verification>
1. Run `npx playwright test e2e/profile.spec.ts --project=chromium-auth`
2. Tests should run with pre-existing auth state
3. All tests should pass
</verification>

<success_criteria>
- e2e/profile.spec.ts exists with 8+ tests
- Tests run under chromium-auth project (not chromium)
- Tests verify: stats display, quick actions, navigation, logout button
- All tests pass with authenticated session
</success_criteria>

<output>
After completion, create `.planning/phases/11-e2e-tests/11-05-SUMMARY.md`
</output>
