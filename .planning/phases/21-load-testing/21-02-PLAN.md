---
phase: 21-load-testing
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - .github/workflows/load-test.yml
  - k6/README.md
  - package.json
autonomous: true
requirements: [LOAD-03, LOAD-04]
user_setup:
  - service: GitHub Actions
    why: "Manual load test execution via workflow_dispatch"
    env_vars:
      - name: STAGING_URL
        source: "GitHub repo Settings → Secrets and variables → Actions → Repository secrets"
        example: "https://staging.newshub.example.com"
    dashboard_config:
      - task: "Add STAGING_URL secret with staging server base URL"
        location: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret"

must_haves:
  truths:
    - "Load tests can be triggered manually via GitHub Actions workflow_dispatch"
    - "CI workflow runs full 10k VU load test on demand"
    - "Workflow fails if performance thresholds are breached"
    - "Test results (JSON + HTML) are uploaded as GitHub Actions artifacts"
    - "Performance baselines are documented with p95, p99, error rate thresholds"
  artifacts:
    - path: ".github/workflows/load-test.yml"
      provides: "GitHub Actions workflow for on-demand load testing"
      min_lines: 80
    - path: "k6/README.md"
      provides: "Usage instructions and performance baseline documentation"
      min_lines: 100
      contains: "## Performance Baselines"
  key_links:
    - from: ".github/workflows/load-test.yml"
      to: "grafana/setup-k6-action"
      via: "uses: grafana/setup-k6-action@v1"
      pattern: "uses: grafana/setup-k6-action"
    - from: ".github/workflows/load-test.yml"
      to: "k6/load-test.js"
      via: "k6 run command"
      pattern: "k6 run.*load-test.js"
---

<objective>
Integrate k6 load testing into CI pipeline with manual dispatch workflow, artifact uploads, and comprehensive baseline documentation.

Purpose: Enable on-demand load testing execution from GitHub Actions, supporting LOAD-03 (CI integration) and LOAD-04 (documented baselines).

Output:
- GitHub Actions workflow with manual trigger for load tests
- Performance baseline documentation with thresholds and rationale
- npm scripts for local test execution
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-load-testing/21-CONTEXT.md
@.planning/phases/21-load-testing/21-RESEARCH.md
@.planning/phases/21-load-testing/21-01-PLAN.md
@CLAUDE.md
@.github/workflows/ci.yml
</context>

<interfaces>
<!-- GitHub Actions context from existing CI workflow -->

From .github/workflows/ci.yml (pattern reference):
```yaml
name: CI
on:
  pull_request:
  push:
    branches: [master]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '25'
```

k6 Actions (from RESEARCH.md):
```yaml
- uses: grafana/setup-k6-action@v1
  with:
    k6-version: '0.53.0'

- run: k6 run k6/load-test.js --out json=summary.json
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create GitHub Actions workflow for load testing</name>
  <files>.github/workflows/load-test.yml</files>

  <read_first>
    @.planning/phases/21-load-testing/21-CONTEXT.md (D-29 through D-35: CI integration decisions)
    @.planning/phases/21-load-testing/21-RESEARCH.md (Pattern 7: GitHub Actions Workflow, Example 2: GitHub Actions Workflow with Manual Dispatch)
    @.github/workflows/ci.yml (existing workflow pattern)
    @k6/load-test.js (scenarios: smoke, load)
  </read_first>

  <action>
Per D-29 through D-35: Create GitHub Actions workflow with workflow_dispatch trigger (manual only), grafana/setup-k6-action for k6 installation, artifact uploads, and GitHub Actions summary generation.

**.github/workflows/load-test.yml**:
```yaml
name: Load Test

on:
  workflow_dispatch:  # D-29: Manual dispatch only (expensive test)
    inputs:
      scenario:
        description: 'Test scenario to run'
        required: true
        default: 'load'
        type: choice
        options:
          - smoke
          - load

jobs:
  load-test:
    name: k6 Load Test
    runs-on: ubuntu-latest
    timeout-minutes: 30  # D-11: 10min test + buffer

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup k6
        uses: grafana/setup-k6-action@v1  # D-33: Use grafana/k6-action
        with:
          k6-version: '0.53.0'

      - name: Run k6 load test
        env:
          K6_BASE_URL: ${{ secrets.STAGING_URL }}  # D-34: Hit staging environment
          K6_SCENARIO: ${{ github.event.inputs.scenario }}
        run: |
          # D-30: CI runs full load test (10k VUs, 10 minutes)
          k6 run k6/load-test.js --out json=summary.json
        continue-on-error: true  # D-31: Allow workflow to complete even if thresholds fail

      - name: Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          # D-32: Artifacts uploaded with 30-day retention
          name: k6-results-${{ github.event.inputs.scenario }}-${{ github.run_number }}
          path: |
            summary.json
            summary.html
          retention-days: 30

      - name: Generate GitHub Actions Summary
        if: always()
        run: |
          # D-35: Create GitHub Actions summary with p95, p99, pass/fail status
          echo "## 📊 Load Test Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Scenario:** \`${{ github.event.inputs.scenario }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Target URL:** ${{ secrets.STAGING_URL }}" >> $GITHUB_STEP_SUMMARY
          echo "**Run ID:** ${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by:** @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          # Extract metrics from summary.json
          if [ -f summary.json ]; then
            P95=$(jq -r '.metrics.http_req_duration.values["p(95)"] // 0' summary.json)
            P99=$(jq -r '.metrics.http_req_duration.values["p(99)"] // 0' summary.json)
            ERROR_RATE=$(jq -r '(.metrics.http_req_failed.values.rate // 0) * 100' summary.json)
            CHECKS_PASSED=$(jq -r '.metrics.checks.values.passes // 0' summary.json)
            CHECKS_FAILED=$(jq -r '.metrics.checks.values.fails // 0' summary.json)
            ITERATIONS=$(jq -r '.metrics.iterations.values.count // 0' summary.json)

            # D-36, D-37, D-38, D-39: Status determination based on thresholds
            if (( $(echo "$P95 < 500" | bc -l) )) && (( $(echo "$ERROR_RATE < 1" | bc -l) )); then
              STATUS="✅ PASSED"
            else
              STATUS="❌ FAILED"
            fi

            echo "**Status:** $STATUS" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 📈 Performance Metrics" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Metric | Value | Threshold | Status |" >> $GITHUB_STEP_SUMMARY
            echo "|--------|-------|-----------|--------|" >> $GITHUB_STEP_SUMMARY
            echo "| p95 latency | ${P95}ms | <500ms | $(if (( $(echo "$P95 < 500" | bc -l) )); then echo "✅"; else echo "❌"; fi) |" >> $GITHUB_STEP_SUMMARY
            echo "| p99 latency | ${P99}ms | <1500ms | $(if (( $(echo "$P99 < 1500" | bc -l) )); then echo "✅"; else echo "❌"; fi) |" >> $GITHUB_STEP_SUMMARY
            echo "| Error rate | ${ERROR_RATE}% | <1% | $(if (( $(echo "$ERROR_RATE < 1" | bc -l) )); then echo "✅"; else echo "❌"; fi) |" >> $GITHUB_STEP_SUMMARY
            echo "| Checks passed | ${CHECKS_PASSED} | — | ✅ |" >> $GITHUB_STEP_SUMMARY
            echo "| Checks failed | ${CHECKS_FAILED} | 0 | $(if [ "$CHECKS_FAILED" -eq 0 ]; then echo "✅"; else echo "❌"; fi) |" >> $GITHUB_STEP_SUMMARY
            echo "| Total iterations | ${ITERATIONS} | — | — |" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "📄 [View full HTML report in artifacts](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **Status:** Test execution failed - summary.json not generated" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Fail if thresholds breached
        if: always()
        run: |
          # D-31: Workflow fails on threshold breach (blocks release)
          if [ -f summary.json ]; then
            # Check if any threshold was breached
            THRESHOLD_FAILURES=$(jq -r '.metrics | to_entries | map(select(.value.thresholds != null)) | map(.value.thresholds | to_entries | map(select(.value.ok == false))) | flatten | length' summary.json)

            if [ "$THRESHOLD_FAILURES" -gt 0 ]; then
              echo "❌ $THRESHOLD_FAILURES threshold(s) breached - failing workflow"
              exit 1
            else
              echo "✅ All thresholds passed"
            fi
          else
            echo "❌ Test results not found"
            exit 1
          fi
```
  </action>

  <verify>
    <automated>
grep -q "workflow_dispatch" .github/workflows/load-test.yml && grep -q "grafana/setup-k6-action" .github/workflows/load-test.yml && echo "VALID" || echo "INVALID"
    </automated>
  </verify>

  <acceptance_criteria>
- .github/workflows/load-test.yml exists with workflow_dispatch trigger (D-29)
- Input parameter "scenario" with choices: smoke, load
- Uses grafana/setup-k6-action@v1 with k6-version: '0.53.0' (D-33)
- K6_BASE_URL environment variable set to secrets.STAGING_URL (D-34)
- k6 run command outputs to summary.json
- upload-artifact@v4 uploads summary.json and summary.html with 30-day retention (D-32)
- GitHub Actions summary generation step extracts p95, p99, error rate from summary.json (D-35)
- Final step fails workflow if thresholds breached (D-31)
  </acceptance_criteria>

  <done>GitHub Actions workflow exists with manual trigger, k6 execution, artifact uploads, and threshold validation</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Document performance baselines and usage instructions</name>
  <files>k6/README.md</files>

  <read_first>
    @.planning/phases/21-load-testing/21-CONTEXT.md (D-01 through D-18: test scope, D-36 through D-39: performance baselines)
    @.planning/phases/21-load-testing/21-RESEARCH.md (Architectural Responsibility Map, Standard Stack)
    @k6/load-test.js (scenarios, thresholds)
    @scripts/seed-load-test-users.js (seeding instructions)
  </read_first>

  <action>
Per LOAD-04, D-36 through D-39: Create comprehensive README documenting performance baselines (p95, p99 latency), rationale, usage instructions, and troubleshooting.

**k6/README.md**:
```markdown
# NewsHub Load Testing

Load testing suite using [Grafana k6](https://k6.io/) to validate system stability under high concurrent user load.

## Overview

This directory contains k6 test scripts that simulate realistic user journeys with gradual ramp-up to **10,000 concurrent virtual users** (D-18). Tests validate critical API endpoints (/api/news, /api/auth/login, /api/ai/ask, /api/bookmarks) with performance thresholds based on production requirements.

**Test Scenarios:**
- **Smoke Test** (D-13): 1 minute, 10 VUs - Quick validation before full load test
- **Load Test** (D-04, D-11): 10 minutes, 0→10k VUs - Full production capacity validation

**User Journey Pattern** (D-02):
1. Login (100% of requests)
2. Browse news (90% read-heavy, D-06)
3. Ask AI (~5% of requests, D-05)
4. Bookmark article (~10% writes, D-06)

## Performance Baselines (D-36, D-37, D-38, D-39)

**LOAD-04 Requirement:** Performance baselines documented with p95 and p99 latency thresholds.

| Endpoint | p95 Latency Threshold | p99 Latency Threshold | Rationale |
|----------|------------------------|------------------------|-----------|
| `/api/news` | < 500ms (D-36) | < 1500ms | News browsing is primary user flow; fast response critical for UX |
| `/api/ai/ask` | < 5000ms (D-37) | < 15000ms | AI analysis involves external API calls; 5s acceptable for complex queries |
| `/api/auth/login` | < 300ms (D-38) | < 1000ms | Login is first impression; must be instant (<300ms) |
| **Overall Error Rate** | **< 1%** (D-39) | — | Excludes expected 429 rate limit responses (D-07) |

**Why these thresholds?**
- **p95**: 95% of requests must meet this threshold (reasonable expectation for most users)
- **p99**: 99% of requests must stay under this limit (catches edge cases, prevents worst-case experiences)
- **<1% error rate**: High availability target (99%+ success rate for production)

**Rate Limiting (D-07):** 429 responses are expected and NOT counted as errors. Rate limits per server/config/rateLimits.ts:
- Auth endpoints: 5 req/min per IP
- AI endpoints: 10 req/min per user
- News endpoints: 100 req/min per IP

## Prerequisites

### Local Execution

Install k6 (load tests can also run in CI without local installation):

```bash
# macOS
brew install k6

# Windows
choco install k6

# Linux
sudo gpg --no-default-keyring --keyring /usr/share/keyrings/k6-archive-keyring.gpg --keyserver hkp://keyserver.ubuntu.com:80 --recv-keys C5AD17C747E3415A3642D57D77C6C491D6AC1D69
echo "deb [signed-by=/usr/share/keyrings/k6-archive-keyring.gpg] https://dl.k6.io/deb stable main" | sudo tee /etc/apt/sources.list.d/k6.list
sudo apt-get update
sudo apt-get install k6
```

### Test User Setup (D-03)

Load tests require 100 pre-seeded test users. Create them once before first test run:

```bash
# 1. Ensure database is running
docker compose up -d postgres

# 2. Seed test users (creates loadtest1@example.com through loadtest100@example.com)
npm run seed:load-test

# 3. Verify users.json was created (gitignored per D-24)
ls k6/data/users.json
```

**Security Note:** `k6/data/users.json` contains plain passwords and is gitignored. Never commit this file.

## Usage

### Local Execution

```bash
# Smoke test (1 minute, 10 VUs) - D-13
K6_BASE_URL=http://localhost:3001 k6 run k6/load-test.js --env K6_SCENARIO=smoke

# Full load test (10 minutes, 10k VUs) - D-04, D-11
K6_BASE_URL=http://localhost:3001 k6 run k6/load-test.js --env K6_SCENARIO=load

# Custom VU count (override default)
K6_BASE_URL=http://localhost:3001 K6_VUS=5000 k6 run k6/load-test.js
```

**Environment Variables (D-22):**
- `K6_BASE_URL`: Target server URL (default: http://localhost:3001)
- `K6_SCENARIO`: Scenario to run - smoke or load (default: load)
- `K6_VUS`: Max VU count override (default: 10000)

### CI Execution (D-29)

**LOAD-03 Requirement:** Load tests run as part of CI pipeline on demand.

Load tests run via GitHub Actions **manual dispatch** (workflow_dispatch) - not on every PR/push due to cost.

**To trigger a load test:**
1. Go to GitHub repo → Actions tab
2. Select "Load Test" workflow
3. Click "Run workflow"
4. Choose scenario: `smoke` (1 min) or `load` (10 min)
5. Click "Run workflow"

**CI Artifacts (D-32):**
- `summary.json`: Machine-readable test results
- `summary.html`: Human-readable HTML report with charts
- Retention: 30 days

**Threshold Validation (D-31):**
CI workflow fails if any performance threshold is breached. This blocks release until performance is restored.

## File Structure (D-19, D-20, D-23)

```
k6/
├── load-test.js              # Main test script with smoke/load scenarios
├── lib/
│   ├── auth.js               # Authentication helper (login, token management)
│   ├── checks.js             # Reusable check functions (status, schema validation)
│   └── config.js             # Environment variable loading
├── data/
│   ├── users.json            # Test user credentials (gitignored, D-24)
│   └── questions.json        # AI question pool (version-controlled, D-27)
└── README.md                 # This file
```

## Test Data (D-15, D-27)

- **users.json** (gitignored): 100 test users with plain passwords (loadtest1@example.com through loadtest100@example.com)
- **questions.json**: 15 varied AI questions to prevent cache hits

## Troubleshooting

### Error: "users.json not found"

Run the seeding script:
```bash
npm run seed:load-test
```

### Error: "Connection refused"

Ensure staging server is running:
```bash
# Local
npm run dev

# Docker
docker compose up -d
```

### Error: "Too many 429 responses"

Expected behavior (D-07). 429 rate limit responses are NOT counted as errors. Check GitHub Actions summary for actual error rate (<1% threshold).

If error rate exceeds threshold, rate limits may need adjustment in `server/config/rateLimits.ts`.

### Error: "Threshold breached"

Performance regression detected. Investigate:
1. Check GitHub Actions summary for which threshold failed (p95, p99, error rate)
2. Review Prometheus metrics at `/metrics` during test run (D-12)
3. Check database query performance
4. Check AI provider latency
5. Check Redis connection pool

## References

- [Grafana k6 Documentation](https://k6.io/docs/)
- [k6 Scenarios](https://k6.io/docs/using-k6/scenarios/)
- [k6 Thresholds](https://k6.io/docs/using-k6/thresholds/)
- [benc-uk/k6-reporter](https://github.com/benc-uk/k6-reporter)

---

**Phase:** 21-load-testing
**Context:** D-01 through D-39 from `.planning/phases/21-load-testing/21-CONTEXT.md`
**Requirements:** LOAD-01, LOAD-03, LOAD-04
```
  </action>

  <verify>
    <automated>
grep -q "## Performance Baselines" k6/README.md && grep -q "p95" k6/README.md && grep -q "LOAD-04" k6/README.md && echo "VALID" || echo "INVALID"
    </automated>
  </verify>

  <acceptance_criteria>
- k6/README.md exists with "## Performance Baselines" section
- Documents D-36 (news p95 < 500ms), D-37 (AI p95 < 5s), D-38 (auth p95 < 300ms), D-39 (<1% error rate)
- Includes rationale for each threshold (why these values?)
- Documents p95 vs p99 explanation
- Usage instructions for local and CI execution
- Prerequisites section covers k6 installation and test user seeding
- Troubleshooting section covers common errors
- References LOAD-03 and LOAD-04 requirements explicitly
  </acceptance_criteria>

  <done>Performance baselines documented with p95/p99 thresholds, rationale, and comprehensive usage instructions</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add npm scripts for local load test execution</name>
  <files>package.json</files>

  <read_first>
    @.planning/phases/21-load-testing/21-CONTEXT.md (D-26: npm scripts for load:smoke and load:full)
    @package.json (existing scripts pattern)
    @k6/load-test.js (scenarios: smoke, load)
  </read_first>

  <action>
Per D-26: Add npm scripts to package.json for convenient local load test execution.

**Update package.json** - add to existing scripts section:
```json
"load:smoke": "k6 run k6/load-test.js --env K6_SCENARIO=smoke",
"load:full": "k6 run k6/load-test.js --env K6_SCENARIO=load"
```

These scripts assume k6 is installed locally (brew install k6 / choco install k6). K6_BASE_URL defaults to http://localhost:3001 per k6/lib/config.js.
  </action>

  <verify>
    <automated>
grep -q "load:smoke" package.json && grep -q "load:full" package.json && echo "VALID" || echo "INVALID"
    </automated>
  </verify>

  <acceptance_criteria>
- package.json contains "load:smoke" script running k6 with K6_SCENARIO=smoke
- package.json contains "load:full" script running k6 with K6_SCENARIO=load
- Scripts match D-26 naming convention exactly
  </acceptance_criteria>

  <done>npm scripts added for local load test execution (smoke and full)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Staging Server | CI runner sends HTTP requests to staging via STAGING_URL secret |
| Public → GitHub Artifacts | Test results (JSON, HTML) uploaded to public repository artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05 | Denial of Service | Accidental production load test | mitigate | D-09: Enforce staging-only via STAGING_URL secret; document K6_BASE_URL validation in README |
| T-21-06 | Information Disclosure | Secrets in GitHub Actions logs | accept | K6_BASE_URL redacted in logs (GitHub Actions secret masking); no credentials in test scripts |
| T-21-07 | Elevation of Privilege | Unauthorized workflow dispatch | accept | GitHub Actions permissions enforce repository write access; only maintainers can trigger |
| T-21-08 | Tampering | Modified load test results | accept | Artifacts immutable after upload; no production decision based solely on artifacts |
</threat_model>

<verification>
After completing all tasks:

1. Verify GitHub Actions workflow exists:
```bash
ls .github/workflows/load-test.yml
```

2. Validate workflow syntax:
```bash
npm run validate:ci || echo "Install action-validator first: npm install -g action-validator"
```

3. Verify k6 README exists and is comprehensive:
```bash
wc -l k6/README.md | awk '{if ($1 >= 100) print "VALID (100+ lines)"; else print "INVALID"}'
```

4. Verify npm scripts exist:
```bash
grep -E "load:(smoke|full)" package.json
```

5. Test local smoke test execution (requires k6 installed):
```bash
# Optional - only if k6 installed locally and server running
npm run load:smoke
```
</verification>

<success_criteria>
- GitHub Actions workflow with workflow_dispatch trigger for manual execution (LOAD-03)
- Workflow installs k6 v0.53.0 via grafana/setup-k6-action
- Workflow runs against STAGING_URL secret (D-34)
- Workflow uploads summary.json and summary.html as artifacts (D-32)
- Workflow generates GitHub Actions summary with p95, p99, error rate (D-35)
- Workflow fails on threshold breach (D-31)
- k6/README.md documents performance baselines with p95, p99 thresholds (LOAD-04)
- npm scripts load:smoke and load:full added for local execution (D-26)
- user_setup frontmatter documents STAGING_URL secret requirement
</success_criteria>

<output>
After completion, create `.planning/phases/21-load-testing/21-02-SUMMARY.md` with:
- Workflow file created
- Performance baseline documentation completed
- Decision IDs implemented (D-26, D-29 through D-39)
- user_setup instructions for STAGING_URL secret
- Commit hashes
</output>
