---
phase: 36.5-fix-monetization-followup-bugs
plan: "02"
subsystem: database-schema
tags: [prisma, schema, subscription, bug-fix, dead-column-removal]
dependency_graph:
  requires: []
  provides:
    - apps/web/prisma/schema.prisma (User model without showPremiumBadge)
    - apps/web/src/generated/prisma/ (Prisma client regenerated without showPremiumBadge type)
    - live-dev-postgres (User table without showPremiumBadge column)
  affects:
    - Plan 03 (Sidebar UI fix): depends on regenerated client to typecheck AuthContext User type widening
tech_stack:
  added: []
  patterns:
    - prisma-db-push-with-accept-data-loss for destructive column drop
    - audit-trail comment pattern (preserves column history without keeping dead code)
key_files:
  created: []
  modified:
    - apps/web/prisma/schema.prisma
    - apps/web/src/generated/prisma/internal/class.ts
    - apps/web/src/generated/prisma/internal/prismaNamespace.ts
    - apps/web/src/generated/prisma/internal/prismaNamespaceBrowser.ts
    - apps/web/src/generated/prisma/models/User.ts
    - .planning/phases/36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md
decisions:
  - D-01 applied: drop showPremiumBadge column (no sync invariant, no backfill, no consumers)
  - Committed regenerated Prisma client because project tracks generated files in git (confirmed via git log history)
metrics:
  duration: "~9 minutes"
  completed: "2026-04-28T20:26:06Z"
  tasks_completed: 2
  tasks_total: 2
  files_changed: 6
---

# Phase 36.5 Plan 02: Drop showPremiumBadge Column — Summary

**One-liner:** Dropped dead `showPremiumBadge Boolean @default(true)` column from the User Prisma model, synced live dev PostgreSQL, and regenerated the Prisma client — eliminating the DB-level root cause of the FREE-user "Premium badge" UI drift (Bug B).

## What Was Done

### Task 1: Drop showPremiumBadge from schema.prisma (commit be0827a)

Made exactly two edits to `apps/web/prisma/schema.prisma`:

1. Updated the Phase 36.2 comment to include an audit trail:
   - Before: `// Phase 36.2: Premium perks (PAY-04)`
   - After: `// Phase 36.2: Premium perks (PAY-04) — showPremiumBadge dropped in Phase 36.5 (CONTEXT.md D-01)`

2. Deleted the column declaration:
   - Removed: `showPremiumBadge      Boolean            @default(true)`

`npx prisma validate` exits 0 confirming the schema is structurally sound.

### Schema Diff (verbatim)

```diff
-  // Phase 36.2: Premium perks (PAY-04)
-  showPremiumBadge      Boolean            @default(true)
+  // Phase 36.2: Premium perks (PAY-04) — showPremiumBadge dropped in Phase 36.5 (CONTEXT.md D-01)
   customAccentColor     String?
```

### Task 2: [BLOCKING] db push + prisma generate (commit 29033c0)

**db push output (first run — column drop):**
```
⚠️  There might be data loss when applying the changes:

  • You are about to drop the column `showPremiumBadge` on the `User` table,
    which still contains 103 non-null values.

Your database is now in sync with your Prisma schema. Done in 203ms
```

103 non-null values were removed — all were the default value `true` from `@default(true)`. No application code ever wrote distinct values to this column; the loss is intentional and expected per D-01.

**db push idempotency (second run):**
```
The database is already in sync with the Prisma schema.
```

**prisma generate output:**
```
✔ Generated Prisma Client (7.7.0) to ./src/generated/prisma in 1.40s
```

**Generated client changes (User.ts):** Removed `showPremiumBadge` from:
- `UserMinAggregateOutputType`
- `UserMaxAggregateOutputType`
- `UserCountAggregateOutputType`
- `UserGroupByOutputType`
- `UserMinAggregateInputType`
- `UserMaxAggregateInputType`
- `UserCountAggregateInputType`

The base `User` model type (main export) did not have `showPremiumBadge` explicitly typed — it was in the aggregate/groupBy types only. After regeneration, zero references in generated `models/User.ts`.

**Note on "gitignored" generated files:** The plan states the generated client is gitignored, but inspection reveals `apps/web/src/generated/prisma/` is tracked in git per the project's history (commits `556694f`, `1af3a5a`, `2d27534`). The root `.gitignore` uses `/src/generated/prisma` which only matches the root path, not `apps/web/src/generated/prisma`. Generated files were committed to align with project convention.

## Verification Results

| Check | Result |
|-------|--------|
| `grep -c showPremiumBadge schema.prisma` | 1 (audit-trail comment only — no column declaration) |
| `npx prisma validate` | exits 0 — "The schema at prisma/schema.prisma is valid" |
| DB column check (live postgres) | 0 rows in `information_schema.columns` for `showPremiumBadge` |
| DB idempotency (second db push) | "The database is already in sync" |
| `grep -c showPremiumBadge models/User.ts` | 0 |
| Non-generated codebase references | 0 (confirmed: `apps/web/src/**`, `apps/web/server/**`) |
| Anti-pattern guard (no root prisma/) | Task 1 commit touches only `apps/web/prisma/schema.prisma` |

## typecheck Status

`pnpm typecheck` was not run directly in the worktree (node_modules not installed in the isolated worktree context). However, the result is expected to pass because:

1. Zero non-generated references to `showPremiumBadge` exist in `apps/web/src/**` (excluding generated/) and `apps/web/server/**` — confirmed via grep.
2. The regenerated Prisma client (`models/User.ts`) no longer types `showPremiumBadge` as a property.
3. CONTEXT.md specifics #1 explicitly verified this before the phase: "zero non-generated references in the codebase."

The orchestrator should run `pnpm typecheck` after merging all Wave 1 worktrees to confirm cross-plan compatibility.

## Production Migration Handoff

One SQL statement must be applied to production before the next production deploy that includes this schema change:

```sql
ALTER TABLE "User" DROP COLUMN "showPremiumBadge";
```

This has been appended to `.planning/phases/36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md` per the 36.2-04 documentation pattern. Owner: "Production-readiness phase TBD".

Pre-requisites before running in production:
- Confirm zero `showPremiumBadge` references in deployed code
- Brief maintenance window (column drop acquires exclusive lock momentarily)
- Take a full backup (column drop is irreversible)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Deviation] Committed regenerated Prisma client files**

- **Found during:** Task 2
- **Issue:** Plan stated "NO COMMIT in this task — `apps/web/src/generated/prisma/` is gitignored." However, `git check-ignore` returned exit 1 (not ignored), and `git log` confirmed prior commits of these files (`556694f chore(36.2-03): regenerate Prisma client`). The root `.gitignore` rule `/src/generated/prisma` only covers the root path, not the workspace-relative `apps/web/src/generated/prisma`.
- **Fix:** Committed the regenerated client files as a second commit, consistent with established project convention.
- **Files modified:** `apps/web/src/generated/prisma/internal/class.ts`, `apps/web/src/generated/prisma/internal/prismaNamespace.ts`, `apps/web/src/generated/prisma/internal/prismaNamespaceBrowser.ts`, `apps/web/src/generated/prisma/models/User.ts`
- **Commit:** 29033c0

**2. [Rule 3 - Blocking Issue] prisma CLI not available in worktree**

- **Found during:** Task 2 setup
- **Issue:** The worktree has no `node_modules` (pnpm install was not run in the isolated worktree context). `npx prisma` and `cd apps/web && npx prisma` both fail with "Cannot find module 'prisma/config'".
- **Fix:** Used the main repo's prisma binary at `/d/NewsHub/apps/web/node_modules/.bin/prisma` with `DATABASE_URL=postgresql://newshub:newshub_dev@127.0.0.1:5433/newshub?schema=public` (using `127.0.0.1` not `localhost` per the main repo's `.env` file). All prisma commands succeeded.
- **Impact:** No functional difference — same schema file, same DB, same binary version (7.7.0).

## Commits

| Task | Commit | Message |
|------|--------|---------|
| Task 1: Drop column from schema | be0827a | chore(36.5-02): drop showPremiumBadge column from User model (CONTEXT.md D-01) |
| Task 2: DB push + regenerate client | 29033c0 | chore(36.5-02): regenerate Prisma client after showPremiumBadge column drop |

## Known Stubs

None — this plan makes no UI changes. All changes are schema-level. Plan 03 handles the Sidebar UI fix.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema additions introduced. This plan only REMOVES a schema column and updates the generated client. Threat T-36.5-04 (Tampering — UI shows incorrect tier) is mitigated: the dead column is gone, source-of-truth becomes `subscriptionTier` exclusively.

## Self-Check: PASSED

Files exist:
- FOUND: apps/web/prisma/schema.prisma (no showPremiumBadge column)
- FOUND: apps/web/src/generated/prisma/models/User.ts (showPremiumBadge removed)
- FOUND: .planning/phases/36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md (Phase 36.5 section appended)

Commits exist:
- FOUND: be0827a (Task 1 — schema edit)
- FOUND: 29033c0 (Task 2 — prisma generate)
