---
phase: 36.5-fix-monetization-followup-bugs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/prisma/schema.prisma
autonomous: true
requirements:
  - PAY-04
threat_model:
  - id: T-36.5-04
    category: T  # Tampering / state corruption (false PREMIUM badge)
    component: apps/web/prisma/schema.prisma:127 (showPremiumBadge column)
    severity: low
    disposition: mitigate
    mitigation: "Drop the dead column. With no source of truth other than subscriptionTier, tier-badge UI cannot drift from real tier (defense at the schema layer). Locked by D-01."
  - id: T-36.5-05
    category: I  # Schema drift between Prisma client and live DB
    component: prisma generate vs npx prisma db push timing
    severity: high
    disposition: mitigate
    mitigation: "[BLOCKING] task runs `cd apps/web && npx prisma db push --accept-data-loss && npx prisma generate` AFTER schema edit and BEFORE Plan 03 starts. Without prisma generate, downstream typecheck would falsely pass against stale generated client (D-07)."

must_haves:
  truths:
    - "apps/web/prisma/schema.prisma no longer declares the `showPremiumBadge` column on the User model"
    - "Live dev Postgres User table no longer has a showPremiumBadge column (npx prisma db push --accept-data-loss applied successfully)"
    - "Generated Prisma client at apps/web/src/generated/prisma/ no longer types `showPremiumBadge` as a property of User (npx prisma generate ran AFTER db push)"
    - "After this plan: pnpm typecheck still exits 0 (no consumers of showPremiumBadge exist anywhere outside generated/, per CONTEXT.md specifics #1)"
  artifacts:
    - path: "apps/web/prisma/schema.prisma"
      provides: "User model definition without showPremiumBadge"
      not_contains: "showPremiumBadge"
  key_links:
    - from: "apps/web/prisma/schema.prisma"
      to: "live dev Postgres User table"
      via: "npx prisma db push --accept-data-loss"
      pattern: "Database is now in sync"
    - from: "apps/web/prisma/schema.prisma"
      to: "apps/web/src/generated/prisma/ (regenerated client)"
      via: "npx prisma generate"
      pattern: "Generated Prisma Client"
---

<objective>
Drop the dead `showPremiumBadge` boolean column from the User model. Per D-01 (CONTEXT.md), this column has zero non-generated references in the codebase and was the proximate cause of Bug B (sidebar showed PREMIUM for FREE users because the column had `@default(true)` and no server logic ever resyncs it). Source-of-truth becomes `User.subscriptionTier` directly.

Purpose: Closes ROADMAP success criterion #3 (option a — drop column, no sync invariant, no backfill). Unblocks Plan 03 (Sidebar UI fix), which depends on the regenerated Prisma client to widen AuthContext User type.

Output: One-line schema edit + [BLOCKING] db push + prisma generate. Schema commit only — no code commits in this plan (Plan 03 handles UI).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/36.5-fix-monetization-followup-bugs/36.5-CONTEXT.md
@.planning/phases/36-monetization-core/36-05-SUMMARY.md
@.planning/phases/36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md
@.planning/.continue-here.md

<interfaces>
<!-- Current shape of the column to drop, from apps/web/prisma/schema.prisma:120-138 -->

```prisma
  stripeSubscriptionId  String?            @unique
  subscriptionTier      SubscriptionTier   @default(FREE)
  subscriptionStatus    SubscriptionStatus @default(ACTIVE)
  subscriptionEndsAt    DateTime?
  pausedUntil           DateTime?

  // Phase 36.2: Premium perks (PAY-04)
  showPremiumBadge      Boolean            @default(true)        // <-- LINE 127 — DROP THIS
  customAccentColor     String?

  // Phase 36.2: Referrals — referredByUser self-relation declared in relations block below
  referralCode          String?            @unique
  referredBy            String?
  freeMonthsEarned      Int                @default(0)

  // Phase 36.2: Student discount
  isStudent             Boolean            @default(false)
  studentVerifiedUntil  DateTime?
```

The leading comment line `// Phase 36.2: Premium perks (PAY-04)` should be UPDATED — not deleted — to read `// Phase 36.2: Premium perks (PAY-04) — showPremiumBadge dropped in Phase 36.5 (CONTEXT.md D-01)`. This preserves audit trail per the 36.2-04 history-preservation pattern (D-11 of that phase).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop showPremiumBadge column from schema.prisma</name>
  <files>apps/web/prisma/schema.prisma</files>
  <read_first>
    - apps/web/prisma/schema.prisma lines 100-145 (full User model — confirm column position and surrounding context)
    - .planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-01-SUMMARY.md (if present — confirms how showPremiumBadge was added; the audit-trail comment style)
  </read_first>
  <action>
    Open `apps/web/prisma/schema.prisma`. Make exactly two edits in the User model block:

    1. Replace the comment on line 126 from:
       ```
         // Phase 36.2: Premium perks (PAY-04)
       ```
       To:
       ```
         // Phase 36.2: Premium perks (PAY-04) — showPremiumBadge dropped in Phase 36.5 (CONTEXT.md D-01)
       ```

    2. DELETE the entire line 127 (the `showPremiumBadge      Boolean            @default(true)` declaration). The blank-line spacing around the comment block must be preserved (the comment retains its position; only the column line is removed).

    DO NOT touch any other column, relation, or enum. DO NOT modify customAccentColor (it stays — out of scope per CONTEXT.md domain section). DO NOT run `prisma db push` in this task — that is Task 2 (the [BLOCKING] gate).

    Verify the edit:
    - `grep -c "showPremiumBadge" apps/web/prisma/schema.prisma` MUST return 0
    - `grep -F "Phase 36.2: Premium perks (PAY-04) — showPremiumBadge dropped in Phase 36.5" apps/web/prisma/schema.prisma` MUST return 1 hit
    - `cd apps/web && npx prisma validate` MUST exit 0 (validates the schema is structurally sound)

    Commit with message: `chore(36.5-02): drop showPremiumBadge column from User model (CONTEXT.md D-01)`. Files in commit: ONLY `apps/web/prisma/schema.prisma`.
  </action>
  <verify>
    <automated>cd apps/web && npx prisma validate && (! grep -F "showPremiumBadge" prisma/schema.prisma)</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "showPremiumBadge" apps/web/prisma/schema.prisma` returns 0
    - `grep -F "Phase 36.2: Premium perks (PAY-04) — showPremiumBadge dropped in Phase 36.5 (CONTEXT.md D-01)" apps/web/prisma/schema.prisma` returns 1 hit
    - `cd apps/web && npx prisma validate` exits 0 with output `The schema at prisma/schema.prisma is valid`
    - `git diff --stat HEAD~1 HEAD` shows ONLY `apps/web/prisma/schema.prisma | 2 +-` (1 added comment-line, 1 removed showPremiumBadge declaration)
    - No other files modified in the commit
  </acceptance_criteria>
  <done>
    Schema edit committed; column declaration deleted; comment annotated with audit trail; `prisma validate` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] db push --accept-data-loss + prisma generate</name>
  <files>apps/web/prisma/schema.prisma (no edit — just running tooling against it)</files>
  <read_first>
    - apps/web/prisma/schema.prisma (verify Task 1's edit landed)
    - apps/web/prisma.config.ts (confirm datasource resolution per the Phase 36.3-03 fix)
    - CLAUDE.md (confirm the canonical command form: `cd apps/web && npx prisma db push`)
  </read_first>
  <action>
    This is a [BLOCKING] gate per D-07. The phase CANNOT pass verification without it. Two commands, both run from `apps/web/` cwd:

    1. `cd apps/web && npx prisma db push --accept-data-loss`
       - The `--accept-data-loss` flag is REQUIRED because dropping a column is technically destructive even though no application code reads it.
       - Expected output: `Database is now in sync with Prisma schema.` and a notice about the dropped column.
       - If the local Postgres is not running: `docker compose up -d postgres redis` first, then retry.
       - If the command fails with "datasource.url required": Phase 36.3-03's `apps/web/prisma.config.ts` is missing or has been modified; this is a regression — STOP and check.

    2. `cd apps/web && npx prisma generate`
       - Regenerates the client at the gitignored path `apps/web/src/generated/prisma/` per `.gitignore:31`. NOT committed.
       - Expected output: `Generated Prisma Client (vX.Y.Z) to ./src/generated/prisma`.

    3. Verify column actually dropped from live DB:
       ```bash
       docker compose exec -T postgres psql -U newshub -d newshub -c "\d \"User\"" | grep -c showPremiumBadge
       ```
       MUST return `0`.

    4. Verify regenerated client doesn't type the column:
       ```bash
       grep -c "showPremiumBadge" apps/web/src/generated/prisma/client.d.ts 2>/dev/null || grep -rc "showPremiumBadge" apps/web/src/generated/prisma/ 2>/dev/null
       ```
       MUST return `0` across all generated files (or 0 if grep returns no matches at all).

    5. Idempotency check: running `cd apps/web && npx prisma db push --accept-data-loss` a SECOND time MUST report `Database is already in sync` (D-07 confirmation).

    NO COMMIT in this task — `apps/web/src/generated/prisma/` is gitignored. The schema commit from Task 1 is the only artifact for this plan.

    If you are running Plan 02 in parallel with Plan 01 (both Wave 1): coordinate so that Plan 01's commit lands FIRST or SECOND atomically. Plan 02's schema-only commit + the runtime `prisma db push` do not interact with Plan 01's webhook-source-file commit. They modify disjoint files (`apps/web/prisma/schema.prisma` vs `apps/web/server/...`), so parallel git operations are safe per the merge-base assumption.
  </action>
  <verify>
    <automated>cd apps/web && npx prisma db push --accept-data-loss 2>&1 | tee /tmp/dbpush.log && grep -E "already in sync|Database is now in sync" /tmp/dbpush.log && cd apps/web && npx prisma generate 2>&1 | grep "Generated Prisma Client"</automated>
  </verify>
  <acceptance_criteria>
    - `npx prisma db push --accept-data-loss` exits 0
    - First-run output contains either `Database is now in sync` OR `The following warning was found ... showPremiumBadge` (drop notice)
    - Second-run output contains `Database is already in sync` (idempotency)
    - `npx prisma generate` exits 0 with output containing `Generated Prisma Client`
    - `docker compose exec -T postgres psql -U newshub -d newshub -c "\d \"User\""` does NOT include a `showPremiumBadge` row (`grep -c showPremiumBadge` returns 0)
    - `grep -rc "showPremiumBadge" apps/web/src/generated/prisma/` returns 0 (column no longer typed in generated client)
    - No new git commits in this task (the artifacts are runtime state + gitignored generated files)
  </acceptance_criteria>
  <done>
    Live dev Postgres User table no longer has the column. Regenerated Prisma client no longer types it. Plan 03 unblocked (it depends on the regenerated client to typecheck cleanly when AuthContext widens to include subscriptionTier).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema source-of-truth → live DB | `prisma db push` is the only legitimate write path; manual ALTER is forbidden in dev (production migration is documented separately in Phase 36.2 PRODUCTION-MIGRATION.md) |
| Schema source-of-truth → generated client | `prisma generate` writes to gitignored apps/web/src/generated/prisma/; never edited by hand |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.5-04 | T (Tampering — UI shows incorrect tier) | apps/web/prisma/schema.prisma:127 (showPremiumBadge) | mitigate | Drop the column. No source-of-truth other than subscriptionTier remains, so UI cannot drift. |
| T-36.5-05 | I (Schema drift — typecheck false-positive) | prisma generate timing | mitigate | [BLOCKING] gate runs both `db push` AND `prisma generate` before any consumer (Plan 03) typechecks against the new shape. |
| T-36.5-06 | R (Repudiation — production migration audit) | dev-only schema change without prod plan | accept | Phase 36.2 PRODUCTION-MIGRATION.md owner ("Production-readiness phase TBD") inherits this drop; documented in SUMMARY for the future production cutover. No production action this phase. |
</threat_model>

<verification>
1. `grep -c showPremiumBadge apps/web/prisma/schema.prisma` returns 0
2. `cd apps/web && npx prisma validate` exits 0
3. `cd apps/web && npx prisma db push --accept-data-loss` second run reports `Database is already in sync` (idempotency)
4. `docker compose exec -T postgres psql -U newshub -d newshub -c "\d \"User\""` does not show showPremiumBadge
5. `cd apps/web && pnpm typecheck` exits 0 (no consumers exist; CONTEXT.md specifics #1 confirmed before plan; verifying after)
6. Anti-pattern guard: `git diff --stat HEAD~1 HEAD` includes ONLY paths under `apps/web/`, NOT root `prisma/`
</verification>

<success_criteria>
- ROADMAP success criterion #3 satisfied (option a — column dropped, no sync invariant, no backfill).
- Live dev Postgres in sync; regenerated client in sync; gitignored client artifact never committed.
- Schema commit isolated (one-file diff).
- PAY-04 progresses; closure completes in Plan 03.
</success_criteria>

<output>
After completion, write `.planning/phases/36.5-fix-monetization-followup-bugs/36.5-02-SUMMARY.md` covering:
- The schema diff (verbatim)
- The `db push` output (column-drop notice line)
- Confirmation that `pnpm typecheck` still passes
- The deferred production-migration handoff: 1 SQL statement (`ALTER TABLE "User" DROP COLUMN "showPremiumBadge";`) appended to `.planning/phases/36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md` per the 36.2-04 documentation pattern, owner = "Production-readiness phase TBD"
- Commit SHA
</output>
