---
phase: 36.5-fix-monetization-followup-bugs
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - .planning/phases/36.5-fix-monetization-followup-bugs/36.5-VERIFICATION.md
autonomous: false  # Has checkpoint:human-verify task for live UI probe of PREMIUM vs FREE badge
requirements:
  - PAY-04
  - PAY-06
threat_model:
  - id: T-36.5-10
    category: R  # Repudiation — verification gate without auditable evidence
    component: phase verification gate (criterion #5/#6/#7)
    severity: low
    disposition: mitigate
    mitigation: "Capture all 7 ROADMAP success criteria as a checklist in 36.5-VERIFICATION.md with grep/test/CLI evidence per criterion. Include manual UI probe sign-off from human-verify checkpoint."
  - id: T-36.5-11
    category: T  # Tampering — anti-pattern guard regression
    component: files_modified across plans 01-03
    severity: medium
    disposition: mitigate
    mitigation: "Verification gate runs `git diff --name-only` against the phase commit range and asserts NO paths begin with root `server/`, `prisma/`, `src/`, or `e2e/` per .planning/.continue-here.md."

must_haves:
  truths:
    - "All 7 ROADMAP success criteria for Phase 36.5 are checked off with concrete evidence"
    - "pnpm test:run exits 0 with >= 1305 tests passing"
    - "Anti-pattern guard verified: no commits in this phase touch root server/, prisma/, src/, or e2e/"
    - "Three per-plan SUMMARY.md files exist (36.5-01-SUMMARY.md, 36.5-02-SUMMARY.md, 36.5-03-SUMMARY.md)"
    - "Phase-level VERIFICATION.md exists with the 7-criterion checklist + verbatim CLI evidence"
    - "Live UI probe (human-verify) confirms PREMIUM badge for ikarus.nbg@gmail.com and no badge for FREE user"
  artifacts:
    - path: ".planning/phases/36.5-fix-monetization-followup-bugs/36.5-VERIFICATION.md"
      provides: "Phase-level verification gate with 7-criterion evidence table"
      min_lines: 60
      contains: "Success Criterion #1"
    - path: ".planning/phases/36.5-fix-monetization-followup-bugs/36.5-01-SUMMARY.md"
      provides: "Plan 01 summary (Bug A fix)"
    - path: ".planning/phases/36.5-fix-monetization-followup-bugs/36.5-02-SUMMARY.md"
      provides: "Plan 02 summary (schema drop)"
    - path: ".planning/phases/36.5-fix-monetization-followup-bugs/36.5-03-SUMMARY.md"
      provides: "Plan 03 summary (UI fix)"
  key_links:
    - from: ".planning/phases/36.5-fix-monetization-followup-bugs/36.5-VERIFICATION.md"
      to: "ROADMAP.md lines 223-230"
      via: "verbatim 7-criterion checklist with evidence column"
      pattern: "Success Criterion #[1-7]"
    - from: "git log --oneline test-ci-pipeline (range covering Plans 01-03)"
      to: "anti-pattern guard"
      via: "git diff --name-only assertion that no path begins with `^(server|prisma|src|e2e)/`"
      pattern: "^apps/web/|^\\.planning/"
---

<objective>
Phase verification gate. Verify all 7 ROADMAP success criteria with concrete CLI/grep/test evidence; execute the live human-verify probe (PREMIUM vs FREE badge) that closes criterion #4; write `36.5-VERIFICATION.md` with the evidence checklist; sign off the phase.

Purpose: Closes ROADMAP success criteria #5 (full suite green), #6 (anti-pattern guard), #7 (per-plan SUMMARY + phase VERIFICATION). PAY-04 + PAY-06 closed at the requirement level.

This plan is `autonomous: false` because the badge UI probe is a checkpoint:human-verify task — Claude can boot the app and curl the API but cannot visually inspect the rendered sidebar.

Output: 1 verification document committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/36.5-fix-monetization-followup-bugs/36.5-CONTEXT.md
@.planning/phases/36.5-fix-monetization-followup-bugs/36.5-01-SUMMARY.md
@.planning/phases/36.5-fix-monetization-followup-bugs/36.5-02-SUMMARY.md
@.planning/phases/36.5-fix-monetization-followup-bugs/36.5-03-SUMMARY.md
@.planning/.continue-here.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification gate + collect evidence</name>
  <files>.planning/phases/36.5-fix-monetization-followup-bugs/36.5-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md lines 223-230 (the 7 ROADMAP success criteria — verbatim source for the checklist)
    - .planning/phases/36.5-fix-monetization-followup-bugs/36.5-01-SUMMARY.md (Plan 01 evidence)
    - .planning/phases/36.5-fix-monetization-followup-bugs/36.5-02-SUMMARY.md (Plan 02 evidence)
    - .planning/phases/36.5-fix-monetization-followup-bugs/36.5-03-SUMMARY.md (Plan 03 evidence)
    - .planning/.continue-here.md (anti-pattern guard FORBIDDEN list)
  </read_first>
  <action>
    Run all verification commands in sequence and capture output. Then write `36.5-VERIFICATION.md` with a row per criterion + evidence.

    **Run these commands and capture output verbatim:**

    1. `cd apps/web && pnpm typecheck 2>&1 | tail -5`  — exit 0
    2. `pnpm test:run 2>&1 | tail -20`  — exit 0, count test summary line (`Test Files X passed`, `Tests Y passed`); Y MUST be >= 1305.
    3. `cd apps/web && npx vitest run server/services/stripeWebhookService.test.ts 2>&1 | tail -15` — confirm 2/2 pass
    4. `grep -c "showPremiumBadge" apps/web/prisma/schema.prisma` — MUST return 0
    5. `grep -F "JSON.stringify({" apps/web/server/routes/webhooks/stripe.ts | wc -l` — MUST return >= 1
    6. `grep -F "subscriptionTier: 'FREE' | 'PREMIUM' | 'ENTERPRISE'" apps/web/src/contexts/AuthContext.tsx | wc -l` — MUST return 1
    7. `grep -F "<SubscriptionBadge tier={user?.subscriptionTier" apps/web/src/components/Sidebar.tsx | wc -l` — MUST return 1
    8. `grep -nE "^\s*Premium\s*$" apps/web/src/components/Sidebar.tsx | wc -l` — MUST return 0
    9. Anti-pattern guard — find the merge-base of this phase's commits:
       ```bash
       BASE=$(git log --oneline | grep -B999 "Phase 36.5 inserted" | tail -1 | awk '{print $1}')
       # Or simpler: use the commit BEFORE the first 36.5-01 commit
       PHASE_START=$(git log --all --oneline --grep="36.5-01\|36.5-02\|36.5-03" | tail -1 | awk '{print $1}')
       git diff --name-only "${PHASE_START}~1" HEAD | sort -u
       ```
       Assert NO output line begins with `server/`, `prisma/`, `src/`, or `e2e/` (root-level paths).
       Capture the output and grep for forbidden roots:
       ```bash
       git diff --name-only "${PHASE_START}~1" HEAD | grep -E "^(server|prisma|src|e2e)/" | wc -l
       ```
       MUST return 0.

    10. Idempotency replay test for Bug A — confirm in real backend (optional but high-value):
        - `pnpm dev:backend` (or already running)
        - Tail logs: `tail -f apps/web/server.log` (or wherever logs go) in another terminal
        - Trigger or replay a `customer.subscription.created` event via Stripe CLI: `stripe events resend evt_<existing-id>` (use one from 36.3-04 captured logs)
        - In the log tail, confirm: NO empty `error: [Webhook] Processing error:` line; instead either silent success OR a structured `error: [Webhook] Processing error: {"message":"...","name":"...","stack":"...","code":"...","eventId":"evt_...","eventType":"customer.subscription.created"}` payload.
        - This is the SUCCESS criterion #1 acceptance probe at the wire-protocol level. Document the log line verbatim in VERIFICATION.md.
        - If `pnpm dev:backend` cannot be booted in this environment (e.g., no Redis), document the limitation in VERIFICATION.md and note the unit-test coverage from Plan 01 as the alternative evidence path.

    **Write `36.5-VERIFICATION.md` using this exact structure:**

    ```markdown
    # Phase 36.5 — VERIFICATION

    **Phase:** 36.5-fix-monetization-followup-bugs
    **Verified:** {ISO date}
    **Verifier:** claude-code (gsd-execute-phase)
    **Status:** PASS / FAIL

    ## ROADMAP Success Criteria — Evidence Matrix

    | # | Criterion (verbatim from ROADMAP.md:223-230) | Status | Evidence |
    |---|----------------------------------------------|--------|----------|
    | 1 | customer.subscription.created handler no longer emits empty error log; structured payload always rendered | PASS | `grep -F "JSON.stringify({" apps/web/server/routes/webhooks/stripe.ts` returns N hits; live replay log shows `{"message":"...","eventId":"evt_..."}` (or unit-test-only path if dev backend unavailable) |
    | 2 | Integration test covers (i) post-checkout replay + (ii) isolated subscription.created; both PASS | PASS | `cd apps/web && npx vitest run server/services/stripeWebhookService.test.ts` reports `2 passed` |
    | 3 | showPremiumBadge column dropped from User model | PASS | `grep -c showPremiumBadge apps/web/prisma/schema.prisma` returns 0; `\d "User"` does not include the column |
    | 4 | Sidebar tier-badge reads from user.subscriptionTier; PREMIUM shows; FREE hides | PASS | `grep -F "<SubscriptionBadge tier={user?.subscriptionTier" apps/web/src/components/Sidebar.tsx` returns 1; manual probe (Task 2) confirms |
    | 5 | pnpm test:run exits 0 with >= 1305 tests passing | PASS | `pnpm test:run` reports `Tests N passed` where N >= 1305 |
    | 6 | No new files under root server/, prisma/, src/, or e2e/ | PASS | `git diff --name-only` for the phase range filtered against `^(server|prisma|src|e2e)/` returns 0 lines |
    | 7 | SUMMARY.md per plan + Phase-level VERIFICATION.md created | PASS | files exist: 36.5-01-SUMMARY.md, 36.5-02-SUMMARY.md, 36.5-03-SUMMARY.md, 36.5-VERIFICATION.md |

    ## Requirement Closure

    | Requirement | Status | Closing evidence |
    |-------------|--------|------------------|
    | PAY-06 (idempotent webhook with observable error) | CLOSED | criterion #1 + #2 above |
    | PAY-04 (premium feature gating consistent with real tier) | CLOSED | criterion #3 + #4 above |

    ## Verbatim CLI Output

    ### typecheck
    ```
    {paste output}
    ```

    ### test:run
    ```
    {paste tail of output showing pass count}
    ```

    ### vitest run server/services/stripeWebhookService.test.ts
    ```
    {paste output}
    ```

    ### Anti-pattern guard (git diff --name-only)
    ```
    {paste full file list — should be all under apps/web/ or .planning/}
    ```

    ## Manual UI Probe (criterion #4)

    See checkpoint sign-off in Task 2 of this plan.

    ## Deferred / Out-of-scope

    - Re-running Phase 36.4-04 D-09 probes against corrected `authMiddleware → aiTierLimiter` middleware chain — already queued as Phase 36.6 candidate per STATE.md row 76.
    - Cleanup of `(user as { subscriptionTier?: string })` casts at `Pricing.tsx:151` and `AIUsageCounter.tsx:23` — handled or deferred per Plan 03 SUMMARY discretion.
    - Production-DB execution of `ALTER TABLE "User" DROP COLUMN "showPremiumBadge"` — appended to `.planning/phases/36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md` per Plan 02; owner = production-readiness phase.

    ## Sign-off

    Phase 36.5 status: COMPLETE. Awaiting `/gsd-verify-phase 36.5` for the second-pass auditor.
    ```

    Save to `.planning/phases/36.5-fix-monetization-followup-bugs/36.5-VERIFICATION.md`. Commit with message `docs(36.5-04): write phase verification gate evidence`.
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck && pnpm test:run 2>&1 | grep -E "Tests.*passed" && test -f .planning/phases/36.5-fix-monetization-followup-bugs/36.5-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/36.5-fix-monetization-followup-bugs/36.5-VERIFICATION.md` exists
    - File contains the literal string `Success Criterion #1` (or `| 1 |` row)
    - File contains exactly 7 PASS/FAIL rows (one per ROADMAP criterion)
    - File contains the literal `PAY-06` and the literal `PAY-04` in the requirement closure section
    - `pnpm test:run` output captured in the file shows test count >= 1305
    - `git diff --name-only` output captured in the file does NOT contain any line starting with `server/`, `prisma/`, `src/`, or `e2e/`
    - All three per-plan SUMMARY.md files exist in the phase directory
  </acceptance_criteria>
  <done>
    VERIFICATION.md written with all 7 criteria checked off and CLI evidence captured. Commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live UI probe — PREMIUM vs FREE badge</name>
  <what-built>
    Plans 01-03 fixed the two follow-up bugs from Phase 36-05:
    - Bug A: structured error log + integration test for customer.subscription.created handler
    - Bug B: schema column dropped + Sidebar UI now reads from user.subscriptionTier via SubscriptionBadge component
    All test suites pass. The visual rendering of the sidebar badge cannot be confirmed by CLI alone; this checkpoint is the final gate for ROADMAP criterion #4.
  </what-built>
  <how-to-verify>
    1. Boot the dev environment:
       - Terminal A: `docker compose up -d postgres redis`
       - Terminal B: `pnpm dev` (waits until both `Local: http://localhost:5173` and `Server running on port 3001` appear)

    2. Open http://localhost:5173 in a browser (Chrome or Firefox).

    3. **PREMIUM probe (must show badge):**
       - Log in as `ikarus.nbg@gmail.com` (real PREMIUM tier from 36-05 sandbox per CONTEXT.md and STATE.md). Use the password from your local `.env` or password manager.
       - Open the sidebar (left navigation panel; should be open by default on desktop).
       - Scroll to the bottom of the sidebar to find the version block.
       - EXPECTED: To the right of `v2.0.0`, you see a small cyan-accented pill containing a Crown icon and the text `Premium` (cyan #00f0ff color, with the SubscriptionBadge styling per `apps/web/src/components/subscription/SubscriptionBadge.tsx:23-27`).
       - If you see only `v2.0.0` with nothing to its right: the user's `subscriptionTier` is not actually PREMIUM in the DB — verify with `psql -U newshub -d newshub -c "SELECT email, \"subscriptionTier\" FROM \"User\" WHERE email = 'ikarus.nbg@gmail.com';"` (expected: `PREMIUM`).
       - If you see the OLD hardcoded `Premium` (gray, no icon): Sidebar.tsx fix didn't land — re-check Plan 03 commit history.

    4. **FREE probe (must show no badge):**
       - Log out.
       - Either log in as a freshly-seeded FREE user (`pnpm seed:load-test` creates loadtest1@example.com through loadtest100@example.com — all FREE), OR create a new account via /register.
       - Scroll to the sidebar version block.
       - EXPECTED: ONLY `v2.0.0` is visible. Nothing rendered to its right (the SubscriptionBadge component returns null when tier === 'FREE' per `apps/web/src/components/subscription/SubscriptionBadge.tsx:42`).
       - If you see a `Premium` label or any badge: the conditional render is broken — re-check Plan 03 Task 2.

    5. **Persistence check:**
       - Reload the page while still logged in as the FREE user.
       - EXPECTED: still no badge after reload.

    6. **Optional Bug-A live probe (if Stripe CLI available):**
       - In a separate terminal: `stripe listen --forward-to http://localhost:3001/api/webhooks/stripe`
       - In another: `stripe events resend evt_<id>` for an existing customer.subscription.created event from 36.3-04 captured logs.
       - Tail the backend log (`tail -f apps/web/server.log` or the terminal running `pnpm dev:backend`).
       - EXPECTED: NO empty error log line. EITHER silent success OR a structured error payload (`{"message":...,"eventId":"evt_...","eventType":"customer.subscription.created"}`).
       - Document the log line verbatim by appending it to 36.5-VERIFICATION.md under "Manual UI Probe" section.

    7. Append your sign-off to `36.5-VERIFICATION.md` under "Manual UI Probe (criterion #4)":
       ```markdown
       - PREMIUM probe: PASS — ikarus.nbg@gmail.com sidebar shows cyan Crown badge
       - FREE probe: PASS — loadtest1@example.com sidebar shows no badge
       - Bug-A replay (optional): PASS — log line: <verbatim>
       - Verified by: <name> on <ISO timestamp>
       ```

       Commit with `docs(36.5-04): record live human-verify sign-off`.
  </how-to-verify>
  <resume-signal>
    Type `approved` if both PREMIUM and FREE probes match expectation. If either fails, describe the symptom and which plan/file likely needs revision; we will return to that plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase commits → master branch | Verification gate is the final reviewable boundary before /gsd-verify-phase |
| Local browser → dev backend | Visual probe trusts the wire response from /api/auth/me; the user's real tier comes from Postgres |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.5-10 | R (auditability) | phase verification doc | mitigate | 7-criterion checklist with verbatim CLI evidence; preserved in git history |
| T-36.5-11 | T (anti-pattern regression) | files_modified across phase | mitigate | git diff --name-only assertion against forbidden root paths |
| T-36.5-12 | E (badge as authorization) | UI probe assumes badge implies access | accept | Server-side requireTier middleware (Phase 36.4) is the actual authorization gate; UI badge is display-only |
</threat_model>

<verification>
1. `.planning/phases/36.5-fix-monetization-followup-bugs/36.5-VERIFICATION.md` exists and is committed to git
2. All 3 per-plan SUMMARY.md files exist
3. Manual UI probe sign-off appended to VERIFICATION.md with PREMIUM-shows + FREE-hides evidence
4. `pnpm test:run` passes with count >= 1305
5. Anti-pattern guard: git diff --name-only output in VERIFICATION.md shows zero forbidden paths
</verification>

<success_criteria>
- All 7 ROADMAP success criteria checked off with concrete evidence.
- PAY-04 + PAY-06 closed at the requirement level.
- Phase status: COMPLETE; ready for `/gsd-verify-phase 36.5`.
- ROADMAP.md updated by orchestrator post-phase to mark 36.5 complete.
</success_criteria>

<output>
After completion, write `.planning/phases/36.5-fix-monetization-followup-bugs/36.5-04-SUMMARY.md` covering:
- Confirmation that 36.5-VERIFICATION.md was written and committed
- The human-verify checkpoint outcome (approved / failed + remediation)
- Final test count
- Commit SHAs for the verification doc + sign-off
</output>
