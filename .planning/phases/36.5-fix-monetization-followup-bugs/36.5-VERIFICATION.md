---
status: passed
phase: 36.5-fix-monetization-followup-bugs
verified: 2026-04-28T22:41:15Z
verifier: claude-code (gsd-execute-phase orchestrator)
human_verification_pending: true  # checkpoint:human-verify in 36.5-04 plan task 2
---

# Phase 36.5 — VERIFICATION

**Phase:** 36.5-fix-monetization-followup-bugs
**Verified:** 2026-04-28T22:41:15Z
**Verifier:** claude-code (gsd-execute-phase)
**Status:** PASS (automated gates) — pending human-verify sign-off for criterion #4 live UI probe

## ROADMAP Success Criteria — Evidence Matrix

| # | Criterion (verbatim from ROADMAP.md:223-230) | Status | Evidence |
|---|----------------------------------------------|--------|----------|
| 1 | `customer.subscription.created` handler no longer emits empty error log lines; structured payload with diagnostic content always rendered (or silent success) | PASS | `grep -F "JSON.stringify({" apps/web/server/routes/webhooks/stripe.ts \| wc -l` returns `1`. The replacement at `routes/webhooks/stripe.ts:72-79` builds `{message, name, stack, code, eventId, eventType}`. Live `stripe events resend` replay deferred to plan 04 task 2 (human-verify); unit-test coverage from Plan 01 (commit `65dd62d` + `2896766`) supplies the alternative evidence path. |
| 2 | Integration test covers (i) post-checkout replay + (ii) isolated `subscription.created`; both PASS | PASS | `cd apps/web && npx vitest run server/services/stripeWebhookService.test.ts` reports `Test Files 1 passed (1)` / `Tests 2 passed (2)`. The test file was added in commit `65dd62d`; the fix lands in `2896766`. |
| 3 | `showPremiumBadge` column dropped from User model | PASS | The column declaration is gone — `grep -nE "showPremiumBadge\\s+Boolean" apps/web/prisma/schema.prisma` returns 0 hits. Plan 02 SUMMARY confirms `prisma db push --accept-data-loss` ran against `127.0.0.1:5433` and dropped 103 default-`true` rows; `prisma generate` regenerated the client without the field. The single remaining grep hit on the bare token (`grep -c showPremiumBadge schema.prisma` = 1) is an audit-trail comment at line 126 (`// Phase 36.2: Premium perks (PAY-04) — showPremiumBadge dropped in Phase 36.5 (CONTEXT.md D-01)`) — no field declaration. Production-DB execution of the same `ALTER TABLE` is queued in `36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md`. |
| 4 | Sidebar tier-badge reads from `user.subscriptionTier`; PREMIUM shows; FREE hides | PASS (automated) / PENDING (live UI probe) | `grep -F "<SubscriptionBadge tier={user?.subscriptionTier" apps/web/src/components/Sidebar.tsx \| wc -l` returns `1` (line 293). `grep -F "import { SubscriptionBadge }" apps/web/src/components/Sidebar.tsx \| wc -l` returns `1`. `grep -nE "^\\s*Premium\\s*$" apps/web/src/components/Sidebar.tsx \| wc -l` returns `0` (the standalone hardcoded literal is gone). `grep -F "subscriptionTier: 'FREE' \| 'PREMIUM' \| 'ENTERPRISE'" apps/web/src/contexts/AuthContext.tsx \| wc -l` returns `1`. Defense-in-depth: `SubscriptionBadge.tsx:42` returns null for FREE — even a regression at the call site fails closed. Live human-verify probe is plan 04 task 2 (see "Manual UI Probe" section below). |
| 5 | `pnpm test:run` exits 0 with `>= 1305` tests passing | PASS | `pnpm test:run` reports `Test Files 46 passed (46)` / `Tests 1306 passed (1306)`. Exit code 0. Baseline was 1304 + 2 new from Plan 01 = 1306, matching the expectation exactly. |
| 6 | No new files written under root `server/`, `prisma/`, `src/`, or `e2e/` (anti-pattern guard) | PASS | `git diff --name-only f5fab9c..HEAD \| grep -E "^(server\|prisma\|src\|e2e)/" \| wc -l` returns `0`. Every modified path is under `apps/web/` or `.planning/`. Full file list captured below. |
| 7 | SUMMARY.md per plan + Phase-level VERIFICATION.md created | PASS | All four artifacts exist in `.planning/phases/36.5-fix-monetization-followup-bugs/`: `36.5-01-SUMMARY.md`, `36.5-02-SUMMARY.md`, `36.5-03-SUMMARY.md`, and this `36.5-VERIFICATION.md` (plan 04's `36.5-04-SUMMARY.md` lands at the close of this plan). |

## Requirement Closure

| Requirement | Status | Closing evidence |
|-------------|--------|------------------|
| PAY-06 (idempotent webhook with observable error) | CLOSED | criterion #1 (structured error log) + criterion #2 (integration test passes) |
| PAY-04 (premium feature gating consistent with real tier) | CLOSED | criterion #3 (column dropped) + criterion #4 (Sidebar reads single source of truth `user.subscriptionTier`) |

## Verbatim CLI Output

### typecheck (`cd apps/web && pnpm typecheck`)
```
> @newshub/web@0.0.0 typecheck D:\NewsHub\apps\web
> tsc --noEmit
```
Exit 0. No errors.

### test:run (`pnpm test:run`)
```
 RUN  v4.1.4 D:/NewsHub/apps/web

 Test Files  46 passed (46)
      Tests  1306 passed (1306)
   Start at  00:40:20
   Duration  46.78s (transform 16.24s, setup 84.96s, import 32.74s, tests 34.94s, environment 406.85s)
```
Exit 0.

### Focused webhook test (`cd apps/web && npx vitest run server/services/stripeWebhookService.test.ts`)
```
 RUN  v4.1.4 D:/NewsHub/apps/web

 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  00:39:37
   Duration  3.28s (transform 148ms, setup 571ms, import 121ms, tests 21ms, environment 2.06s)
```

### Anti-pattern guard (`git diff --name-only f5fab9c..HEAD | sort -u`)
```
.planning/ROADMAP.md
.planning/STATE.md
.planning/phases/36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md
.planning/phases/36.5-fix-monetization-followup-bugs/36.5-01-SUMMARY.md
.planning/phases/36.5-fix-monetization-followup-bugs/36.5-02-SUMMARY.md
.planning/phases/36.5-fix-monetization-followup-bugs/36.5-03-SUMMARY.md
apps/web/prisma/schema.prisma
apps/web/server/routes/webhooks/stripe.ts
apps/web/server/services/stripeWebhookService.test.ts
apps/web/server/services/stripeWebhookService.ts
apps/web/src/components/Sidebar.tsx
apps/web/src/contexts/AuthContext.tsx
apps/web/src/generated/prisma/internal/class.ts
apps/web/src/generated/prisma/internal/prismaNamespace.ts
apps/web/src/generated/prisma/internal/prismaNamespaceBrowser.ts
apps/web/src/generated/prisma/models/User.ts
```
Forbidden-root grep: `git diff --name-only f5fab9c..HEAD | grep -E "^(server|prisma|src|e2e)/" | wc -l` returns `0`.

### Phase commits (`git log --oneline f5fab9c..HEAD`)
```
9875072 docs(phase-36.5): update tracking after wave 2
b400b79 docs(36.5-03): complete plan execution — Sidebar SubscriptionBadge + AuthContext widening
bfc4f34 feat(36.5-03): replace hardcoded Premium label in Sidebar with SubscriptionBadge (CONTEXT.md D-02)
e3e0d7f feat(36.5-03): widen AuthContext User type with subscriptionTier (CONTEXT.md D-03)
8803c26 docs(phase-36.5): update tracking after wave 1
e4f7824 chore: merge executor worktree (worktree-agent-a5ec064488504d6c8) — 36.5-02
bded8de docs(36.5-01): complete plan execution — webhook handler fix + structured logging
2896766 fix(36.5-01): structured error logging + defensive subscription item guard
65dd62d test(36.5-01): add failing integration test for stripeWebhookService webhook replay
00d6027 docs(36.5-02): complete plan execution — drop showPremiumBadge schema column
29033c0 chore(36.5-02): regenerate Prisma client after showPremiumBadge column drop
be0827a chore(36.5-02): drop showPremiumBadge column from User model (CONTEXT.md D-01)
0bdfd54 docs(state): begin phase 36.5 execution
```

## Manual UI Probe (criterion #4)

**Status:** PENDING — see plan 36.5-04 task 2 (`checkpoint:human-verify`).

The orchestrator cannot visually inspect the rendered Sidebar in a browser. Plan 04 task 2 carries the live probe instructions verbatim:

1. Boot dev (`docker compose up -d postgres redis`, then `pnpm dev`).
2. Log in as `ikarus.nbg@gmail.com` (real PREMIUM tier from 36-05 sandbox per CONTEXT.md and STATE.md). Sidebar bottom-right of the v2.0.0 line MUST show a cyan-accented Crown badge with "Premium" label.
3. Log out and log in as a FREE seeded user (`pnpm seed:load-test` → `loadtest1@example.com`). Sidebar version block MUST show ONLY `v2.0.0`, no badge to its right.
4. Reload the FREE-user page; badge MUST still be absent.
5. (Optional) Stripe CLI live replay of a `customer.subscription.created` event; backend log line MUST be empty-message-free.

Append sign-off to this section after the probe:

```markdown
- PREMIUM probe: PASS / FAIL — <observation>
- FREE probe: PASS / FAIL — <observation>
- Bug-A replay (optional): PASS / FAIL — <log line>
- Verified by: <name> on <ISO timestamp>
```

## Deferred / Out-of-scope

- **Re-running Phase 36.4-04 D-09 probes** against corrected `authMiddleware → aiTierLimiter` middleware chain — already queued as a Phase 36.6 candidate per STATE.md row 76.
- **Cleanup of redundant `(user as { subscriptionTier?: string })` casts** at `apps/web/src/pages/Pricing.tsx:151` and `apps/web/src/components/subscription/AIUsageCounter.tsx:23` — typecheck still passes (downcasts to a wider type are harmless), so cleanup deferred per Plan 03 SUMMARY discretion.
- **Production-DB execution** of `ALTER TABLE "User" DROP COLUMN "showPremiumBadge"` — appended to `.planning/phases/36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md` per Plan 02; owner = production-readiness phase.
- **Wave 2 worktree-isolated execution** — the spawned `gsd-executor` agent for plan 03 failed at its Bash-permission check (zero commits). Recovered by executing inline; same diff, same gates green. No data loss; empty worktree cleaned up.

## Sign-off

Phase 36.5 status: **AUTOMATED GATES PASS**. Pending human-verify sign-off (plan 04 task 2) for live UI probe of criterion #4. Once the operator confirms PREMIUM-shows + FREE-hides in a real browser, the phase closes; otherwise the failure routes back to Plan 03 with the symptom described.
