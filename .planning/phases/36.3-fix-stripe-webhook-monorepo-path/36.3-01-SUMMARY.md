---
phase: 36.3-fix-stripe-webhook-monorepo-path
plan: 01
subsystem: payments
tags: [stripe, webhook, monorepo, git-mv, refactor, repo-structure]

# Dependency graph
requires:
  - phase: 36-monetization-core
    provides: "Stripe webhook service, route, and subscription routes (written to wrong root server/ path on 2026-04-26)"
  - phase: 36.2-close-36-debt-schema-models-cleanup
    provides: "ProcessedWebhookEvent / ReferralReward / Campaign / StudentVerification models + non-null SubscriptionTier/Status enums in live apps/web schema (commits 1976489 / 44aa829 / 556694f / 30466e2)"
  - phase: 35-infrastructure-foundation
    provides: "pnpm monorepo with apps/web/ as the canonical backend workspace (root server/ became orphan after 35-01)"
provides:
  - "Stripe webhook service relocated from orphan root server/services/ to canonical apps/web/server/services/ with full git history preserved through the rename"
  - "Stripe webhook route relocated from orphan root server/routes/webhooks/ to canonical apps/web/server/routes/webhooks/ (destination directory created since it did not previously exist)"
  - "Subscription routes relocated from orphan root server/routes/ to canonical apps/web/server/routes/ with full git history preserved"
  - "Three atomic refactor(36.3-01) commits, each recorded as `R` (rename) with 100% similarity — auditable via `git log --follow`"
  - "Plan 02 unblocked: it can now `import stripeWebhookRouter from './routes/webhooks/stripe'` and `import subscriptionRoutes from './routes/subscriptions'` from the live apps/web/server/index.ts"
  - "Plan 03 unblocked: root server/ no longer contains any of the three target files, so wholesale `git rm -rf server/` will not destroy the moved code"
affects: [36.3-02-mount-routes, 36.3-03-orphan-sweep, 36.3-04-verify, 36.3-05-anti-pattern-doc, future-monorepo-relocations]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "git mv (not cp + rm) for cross-workspace relocations — git records `R` rename with 100% similarity, history preserved through `git log --follow`"
    - "Per-file atomic commit policy for relocations (D-01) — each move reviewable independently"
    - "No source-file edits during relocation (D-02) — directory layout mirroring keeps relative imports valid"
    - "Pre-step `mkdir -p` for destination directories that do not yet exist (mitigates L-01 — `git mv` does not create intermediate dirs)"

key-files:
  created:
    - "apps/web/server/routes/webhooks/ (directory; previously absent)"
  modified:
    - "apps/web/server/services/stripeWebhookService.ts (moved from root server/, byte-identical content)"
    - "apps/web/server/routes/webhooks/stripe.ts (moved from root server/, byte-identical content)"
    - "apps/web/server/routes/subscriptions.ts (moved from root server/, byte-identical content)"

key-decisions:
  - "git mv preferred over cp+rm so git records `R` with 100% similarity (per D-01) — auditable rename trail for security/HMAC code"
  - "Directory `apps/web/server/routes/webhooks/` was absent (L-01) — created with `mkdir -p` before the third `git mv` per RESEARCH.md L-01 mitigation"
  - "Known L-02 redundancy preserved: per-route `authMiddleware` (lines 49, 112, 153 of subscriptions.ts) NOT removed; will overlap with Plan 02 mount-level guard as deliberate defense-in-depth (D-02 forbids edits during relocation)"
  - "Verification gates run between each `git mv`+commit: pnpm typecheck after every move; pnpm test:run after final move (1289/1289 PASS, matches Phase 36.2 baseline)"

patterns-established:
  - "Three-step atomic relocation: (1) verify source+destination, (2) `mkdir -p` if dest dir missing, (3) `git mv` + typecheck + atomic commit per file"
  - "Use `git status --short | grep <filename>` to confirm rename status `R` (proves not D+A copy-then-delete) before committing"
  - "Use `git log --follow --oneline <path>` post-commit to verify history continuity through the rename (≥2 entries: rename commit + at least one pre-rename origin commit)"

requirements-completed: [PAY-02, PAY-03, PAY-06]

# Metrics
duration: 2min
completed: 2026-04-28
---

# Phase 36.3 Plan 01: Relocate orphaned Stripe webhook + subscription source files to apps/web/server/ Summary

**Three orphaned Stripe source files (webhook service, webhook route, subscription routes) relocated from dead root `server/` to canonical `apps/web/server/` via three atomic `git mv` commits — full git history preserved, byte-identical content, typecheck and 1289-test suite green throughout.**

## Performance

- **Duration:** 2 min 9 sec
- **Started:** 2026-04-28T11:45:36Z
- **Completed:** 2026-04-28T11:47:45Z
- **Tasks:** 3 of 3 complete
- **Files modified:** 3 (one rename per task; zero content edits)

## Accomplishments

- All three Stripe-related source files now live at their canonical monorepo paths under `apps/web/server/` (the path that `pnpm dev:backend` actually loads)
- Git history for each file is preserved through the rename — `git log --follow` traces each file back to its original Phase 36-02 commit (`0ec4634` for the service, `a32f285` for the webhook route, `6049d1e` for the subscription routes)
- All three commits show `rename {server => apps/web/server}/...` with `(100%)` similarity — byte-stable per D-02
- `pnpm typecheck` exits 0 after each of the three relocations (relative imports under `apps/web/server/` mirror the orphan layout, so no import edits required)
- `pnpm test:run` after the final relocation: 1289/1289 PASS — zero regression versus Phase 36.2 baseline
- Created the previously absent `apps/web/server/routes/webhooks/` directory (mitigation of L-01)
- Plan 02 now unblocked (mount imports will resolve); Plan 03's orphan sweep is now safe (no live code remains under root `server/services/stripeWebhookService.ts` etc.)

## Task Commits

Each task was committed atomically (one git mv = one commit per D-01):

1. **Task 1: Pre-flight + create webhooks/ + git mv stripeWebhookService.ts** — `71507b7` (refactor)
2. **Task 2: git mv routes/subscriptions.ts** — `6597dfa` (refactor)
3. **Task 3: git mv routes/webhooks/stripe.ts + full verification** — `eb42362` (refactor)

All three are on branch `test-ci-pipeline`. Each commit is `1 file changed, 0 insertions(+), 0 deletions(-)` with `rename ... (100%)` — proves no content edit.

**git log --follow trail per file:**

| File (canonical path) | Rename commit | Pre-rename origin |
|---|---|---|
| `apps/web/server/services/stripeWebhookService.ts` | `71507b7` | `0ec4634` (Phase 36-02) |
| `apps/web/server/routes/subscriptions.ts` | `6597dfa` | `6049d1e` (Phase 36-02) |
| `apps/web/server/routes/webhooks/stripe.ts` | `eb42362` | `a32f285` (Phase 36-02) |

## Files Created/Modified

- `apps/web/server/routes/webhooks/` — new directory; created with `mkdir -p` in Task 1 step 4 because `git mv` does not create intermediate destinations (L-01 mitigation)
- `apps/web/server/services/stripeWebhookService.ts` — Stripe webhook handler with Redis+DB idempotency (`processWebhookIdempotently`); moved from `server/services/` byte-identical
- `apps/web/server/routes/webhooks/stripe.ts` — Express route with raw-body HMAC verification (`express.raw({ type: 'application/json' })` + `stripe.webhooks.constructEvent`); moved from `server/routes/webhooks/` byte-identical
- `apps/web/server/routes/subscriptions.ts` — Checkout / Portal / Status routes (`isValidPriceId` whitelist, three per-route `authMiddleware` calls); moved from `server/routes/` byte-identical

The root orphan paths (`server/services/stripeWebhookService.ts`, `server/routes/webhooks/stripe.ts`, `server/routes/subscriptions.ts`) no longer exist on disk. Other files under root `server/` are unaffected — wholesale deletion of that tree is Plan 03's responsibility.

## Decisions Made

None — plan executed exactly as specified. All four locked decisions cited in the plan (`D-01` per-file atomic commits, `D-02` no source edits, `L-01` `mkdir -p` pre-step, `L-02` accept per-route + mount-level authMiddleware redundancy) were honored without deviation.

## Deviations from Plan

None — plan executed exactly as written. No bugs found, no missing functionality detected, no blocking issues encountered, no architectural questions raised. Rules 1–4 did not fire.

## Issues Encountered

None. Each `git mv` succeeded on the first attempt, each `pnpm typecheck` exited 0 immediately, and `pnpm test:run` reported 1289/1289 PASS on the first attempt — exactly matching the Phase 36.2 green baseline.

## User Setup Required

None — this plan does no environment / external-service configuration. Stripe CLI testing (D-09 in the phase plan) is Plan 04's responsibility.

## Next Phase Readiness

- **Plan 02 (mount routes) unblocked.** `apps/web/server/index.ts` can now successfully `import stripeWebhookRouter from './routes/webhooks/stripe'` and `import subscriptionRoutes from './routes/subscriptions'` — both paths now resolve. The two mount edits at lines 108 and 171 (per D-04 / D-05) can proceed.
- **Plan 03 (orphan sweep) safe to execute.** Root `server/` no longer contains any of the three relocated files; the wholesale `git rm -rf server/` (plus `prisma/`, `src/`, `tsup.config.ts`, `prisma.config.ts`, and the four discretion additions per RESEARCH Q-01) will not destroy live code.
- **Note from RESEARCH Q-01:** Root `server/` still contains 100+ other files (orphan duplicates of services, routes, generated client, etc.) and orphan test files — none are loaded by the live backend or test runner, but their deletion is Plan 03's job, not this plan's. This plan's `git status` correctly shows zero leftover plan-owned changes after the third commit.
- **Branch state:** `test-ci-pipeline` is at `eb42362` (HEAD); the three plan commits are at HEAD~2..HEAD; the working tree shows only pre-existing untracked items (`.planning/debug/`, multiple `*-PATTERNS.md`, `.planning/tmp/`) and pre-existing modifications to `.claude/settings.local.json` and `.planning/STATE.md` — none of which were introduced by this plan.

---

## Self-Check: PASSED

Verification of every claim in this SUMMARY against on-disk reality and git state:

| Claim | Verification command | Result |
|---|---|---|
| `apps/web/server/services/stripeWebhookService.ts` exists | `ls apps/web/server/services/stripeWebhookService.ts` | FOUND |
| `apps/web/server/routes/webhooks/stripe.ts` exists | `ls apps/web/server/routes/webhooks/stripe.ts` | FOUND |
| `apps/web/server/routes/subscriptions.ts` exists | `ls apps/web/server/routes/subscriptions.ts` | FOUND |
| Orphan `server/services/stripeWebhookService.ts` gone | `ls server/services/stripeWebhookService.ts` | NOT FOUND (exit 2) |
| Orphan `server/routes/webhooks/stripe.ts` gone | `ls server/routes/webhooks/stripe.ts` | NOT FOUND (exit 2) |
| Orphan `server/routes/subscriptions.ts` gone | `ls server/routes/subscriptions.ts` | NOT FOUND (exit 2) |
| Commit `71507b7` exists (Task 1) | `git log --oneline --all \| grep 71507b7` | FOUND |
| Commit `6597dfa` exists (Task 2) | `git log --oneline --all \| grep 6597dfa` | FOUND |
| Commit `eb42362` exists (Task 3) | `git log --oneline --all \| grep eb42362` | FOUND |
| All 3 HEAD commits prefixed `refactor(36.3-01)` | `git log -3 --format=%s` | 3/3 match |
| History preserved for stripeWebhookService.ts | `git log --follow --oneline ...` | 2 entries (71507b7 + 0ec4634) |
| History preserved for routes/subscriptions.ts | `git log --follow --oneline ...` | 2 entries (6597dfa + 6049d1e) |
| History preserved for routes/webhooks/stripe.ts | `git log --follow --oneline ...` | 2 entries (eb42362 + a32f285) |
| Each commit recorded as `R` (rename), not D+A | `git show --stat <hash>` | "rename ... (100%)" on all three |
| `pnpm typecheck` exits 0 at HEAD | run during Task 3 | exit 0 |
| `pnpm test:run` 1289/1289 PASS at HEAD | run during Task 3 | "Test Files 44 passed (44) / Tests 1289 passed (1289)" |
| `isValidPriceId` still in moved subscriptions.ts | `grep -c isValidPriceId apps/web/server/routes/subscriptions.ts` | 2 (definition + call) |
| `authMiddleware` still in moved subscriptions.ts | `grep -c authMiddleware apps/web/server/routes/subscriptions.ts` | 4 (1 import + 3 per-route, L-02 redundancy preserved) |
| `express.raw` still in moved webhooks/stripe.ts | `grep -c express.raw apps/web/server/routes/webhooks/stripe.ts` | 3 |
| `stripe.webhooks.constructEvent` still in moved webhooks/stripe.ts | `grep -c ... apps/web/server/routes/webhooks/stripe.ts` | 1 |
| `processWebhookIdempotently` still in moved stripeWebhookService.ts | `grep ... in HEAD blob` | found |

All 21 self-check items pass. No missing items.

---

*Phase: 36.3-fix-stripe-webhook-monorepo-path*
*Plan: 01*
*Completed: 2026-04-28*
