# Phase 36.3 Discussion Log

**Phase:** 36.3 — Fix Stripe Webhook Monorepo Path
**Date:** 2026-04-28
**Mode:** discuss (default)

## Context Loaded
- ROADMAP.md §"Phase 36.3 (INSERTED)" lines 167–180 (6 success criteria)
- STATE.md lines 88–93 — three milestone-36 "SUMMARY claimed but never written" incidents
- Live `apps/web/server/index.ts` (route imports, mount order, `express.json()` placement)
- Orphan `server/{index,services/stripeWebhookService,routes/{webhooks/stripe,subscriptions}}.ts` (~537 lines total across the 3 source files; index.ts is a stale Phase 35-pre snapshot)
- Discovered during scout: root `prisma/`, `src/`, `tsup.config.ts`, `prisma.config.ts` are also stale orphans
- Phase 36.2-03 SUMMARY — root `prisma.config.ts` near-data-loss incident (Rule-3 auto-fix)
- CLAUDE.md lines 47–49 — `cd apps/web && npx prisma ...` already documented as canonical path

## Gray Areas Presented (5 → consolidated to 4 due to AskUserQuestion 4-option max)

1. Move tactic (git mv vs copy) — DEFAULT to safe choice, not asked
2. Mount strategy details
3. End-to-end verification depth
4. Recurrence prevention (4th orphan)
5. Orphan deletion scope

User selected: **all 4 areas**.

## Discussion Outcomes

### Area 1 — Orphan deletion scope

**Q:** How aggressively should this phase clean up? (3 options: 3 named files only / root server only / wholesale sweep)

**Options presented:**
- Sweep root/server + root/prisma (Recommended) — delete entire root server/ AND root prisma/ trees
- Delete root/server only — minimum per criterion 5
- Delete only the 3 named files

**User selected:** Sweep root/server + root/prisma (Recommended)

**Followup discovered during scout:** root `src/`, `tsup.config.ts`, `prisma.config.ts` also orphans. Re-asked.

**Followup Q:** Should the sweep also include root src/ + tsup.config.ts + prisma.config.ts?

**Options presented:**
- Full sweep — server, prisma, src, tsup, prisma.config (Recommended)
- Keep original scope (server + prisma only)
- Server + prisma + prisma.config.ts (no src/tsup)

**User selected:** Full sweep — server, prisma, src, tsup, prisma.config (Recommended)

**Decisions captured:** D-07, D-08

### Area 2 — Mount strategy details

**Q:** Rate limiter on subscription routes + middleware order?

**Options presented:**
- authMiddleware only, no rate limiter (Recommended)
- authMiddleware + authLimiter
- Defer middleware tuning

**User selected:** authMiddleware only, no rate limiter (Recommended)

**Decisions captured:** D-03, D-04, D-05, D-06

### Area 3 — End-to-end verification depth

**Q:** How thorough should `stripe trigger` validation be?

**Options presented:**
- Full event matrix + DB idempotency check (Recommended)
- Full event matrix only
- Minimum (criterion 4 literal)

**User selected:** Full event matrix + DB idempotency check (Recommended)

**Decisions captured:** D-09, D-10

### Area 4 — Recurrence prevention

**Q:** Add structural mechanism for 4th orphan-write incident?

**Options presented:**
- Anti-pattern entry in .continue-here.md (Recommended)
- Post-execute hook scanning new files outside apps/web/
- Accept the pattern, no new mechanism

**User selected:** Anti-pattern entry in .continue-here.md (Recommended)

**Decisions captured:** D-11

## Deferred Ideas Captured

- Subscription route rate-limiting (authLimiter on /checkout) — wait for abuse signal
- Webhook event-type expansion (customer.created, .updated) — out of phase scope
- Production Stripe webhook URL registration — production-readiness phase
- `pnpm build` workbox precache fix — pre-existing, tracked elsewhere
- Milestone-level `.continue-here.md` duplication — Claude's discretion at execute time
- Root `tsconfig.json` audit — verifier checks at execute time
- `packages/types` reorganization — milestone-37

## Claude's Discretion Items (NOT asked)

- Move tactic: defaulted to `git mv` (preserves history) per the question header note
- Commit grouping: one combined orphan-sweep commit vs five small ones
- `.continue-here.md` commit-with-sweep vs separate `docs(36.3)` commit
- Webhook mount line placement: line 108 default; move to line 86 only if `stripe listen` output shows middleware-order issues during D-09

## Sign-off

User confirmed all 4 recommended options. CONTEXT.md ready for `/gsd-plan-phase 36.3`.
