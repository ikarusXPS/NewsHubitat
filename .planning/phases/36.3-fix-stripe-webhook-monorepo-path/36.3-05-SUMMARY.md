---
phase: 36.3-fix-stripe-webhook-monorepo-path
plan: 05
subsystem: documentation
tags: [anti-pattern, continue-here, governance, orchestrator-input, monorepo-discipline, blocking-marker]

# Dependency graph
requires:
  - phase: 36.3-fix-stripe-webhook-monorepo-path
    provides: "Plan 03 — orphan sweep deleted root server/, prisma/, src/, prisma.config.ts, tsup.config.ts (commits 651ce93 + a69bbff). Without that structural deletion, the anti-pattern's 'Structural prevention' column claim ('those root paths physically do not exist') would be untrue. The doc and the deletion are belt-and-suspenders — order matters."
provides:
  - "Phase-local .continue-here.md at .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md — read by 36.x phase orchestrators"
  - "Milestone-level .continue-here.md at .planning/.continue-here.md — read by ALL future phase orchestrators (Q-03 cross-phase reach)"
  - "Markdown table format with severity:blocking row, parseable by execute-phase.md's check_blocking_antipatterns step"
  - "Compliance allow-list (apps/web/, packages/, .github/, .planning/, named top-level configs) — explicit guidance for future <files_modified> path validation"
  - "Documentation belt complementing Plan 03's structural suspenders — together require BOTH undoing the deletion AND ignoring the doc to recreate the failure mode"
  - "T-36.3-17 mitigated (Repudiation — recurrence of wrong-path bug): two-tier doc (phase + milestone) ensures future agents reading either file are reminded of the anti-pattern; combined with structural deletion, recurrence requires deliberate two-step regression"
  - "T-36.3-18 accepted (stale guidance): structural-prevention claim validated by Plan 03; if a future phase recreates root server/, the anti-pattern entry remains correct as a recurrence signal"
affects: [all-future-phases, 37-horizontal-scaling, 38-advanced-ai-features, 39-mobile-apps, 40-content-expansion, 36-05-monetization-resume, future-phase-orchestrators]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Two-tier .continue-here.md anti-pattern registry (phase-local + milestone-level) — phase-local for narrow recurrence within a phase family; milestone-level for cross-phase reach. Future debt-prevention markers can follow this pattern."
    - "Belt-and-suspenders documentation — pair structural prevention (deletion / refactor / type-system constraint) with documentation prevention (anti-pattern marker). Recurrence requires undoing BOTH."
    - "Markdown table with `severity: blocking` rows — specific column convention enables programmatic parsing by orchestrator workflows (execute-phase.md / plan-phase.md `check_blocking_antipatterns` step)."

# Live code touched
key-files:
  created:
    - ".planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md (D-11 phase-local marker)"
    - ".planning/.continue-here.md (Q-03 milestone-level marker)"
    - ".planning/phases/36.3-fix-stripe-webhook-monorepo-path/36.3-05-SUMMARY.md (this file)"
  modified: []
  deleted: []

# Decisions
decisions:
  - "D-11 satisfied with phase-local file at .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md — read when 36.3 (or downstream phases inheriting this directory) is the active phase. Locked content from CONTEXT.md D-11 used verbatim including the structural-prevention claim that root paths physically do not exist post-36.3."
  - "Q-03 discretion exercised — milestone-level .planning/.continue-here.md ALSO created with the same blocking row plus an explicit allow-list of valid <files_modified> roots (apps/web/, apps/<other>/, packages/, .github/, .planning/, named top-level configs). Trade-off: low-effort duplication, high future-prevention value because phase-local is invisible from outside the active phase directory."
  - "Single atomic docs(36.3-05) commit with both markdown files (per RESEARCH Q-02 — separate commit, NOT bundled with the orphan-sweep chore commit, so the documentation artifact is reviewable independently)."

# Metrics
metrics:
  duration: "~2m (verify dir, write 2 files, grep-verify, single atomic commit)"
  started: "2026-04-28T12:15:00Z"
  completed: "2026-04-28T12:16:54Z"
  tasks_completed: 1
  files_changed: 2
  insertions: 51
  deletions: 0

requirements-completed: [PAY-02, PAY-03, PAY-06]
---

# Phase 36.3 Plan 05: Anti-Pattern Markers (D-11 + Q-03) Summary

**Two `.continue-here.md` anti-pattern registry files created (phase-local + milestone-level) at `severity: blocking`, documenting the wrong-path failure mode that caused three milestone-36 incidents — pairs documentation prevention with Plan 03's structural deletion as belt-and-suspenders.**

## Performance

- **Duration:** ~2m
- **Started:** 2026-04-28T12:15:00Z
- **Completed:** 2026-04-28T12:16:54Z
- **Tasks:** 1 / 1
- **Files modified:** 2 (both new, +51 lines, 0 deletions)

## Accomplishments

- **Phase-local marker** created at `.planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md` (D-11). Contains a single `severity: blocking` row in markdown-table format describing the wrong-path anti-pattern. Compliant with execute-phase.md's `check_blocking_antipatterns` step (table parseable; row contains the literal `blocking` token).
- **Milestone-level marker** created at `.planning/.continue-here.md` (Q-03 discretion). Same blocking row, plus an explicit `<files_modified>` allow-list (`apps/web/...`, `apps/<other>/...`, `packages/...`, `.github/...`, `.planning/...`, named top-level configs) and an explicit FORBIDDEN list (`server/`, `prisma/`, `src/` at repo root). Read by ALL future phase orchestrators for cross-phase reach.
- **Belt-and-suspenders pairing**: this documentation belt complements Plan 03's structural suspenders (root `server/`, `prisma/`, `src/`, `tsup.config.ts`, `prisma.config.ts` deleted in commit `651ce93`). Recurrence of the wrong-path bug now requires BOTH recreating the deleted paths AND ignoring the anti-pattern markers — no silent failure mode.
- **Atomic commit** per RESEARCH Q-02 — single `docs(36.3-05)` commit (`c81a941`), NOT bundled with the orphan-sweep `chore(36.3-03)` commit. Makes the documentation artifact independently reviewable / revertable.

## Task Commits

Single task, single atomic commit:

1. **Task 1: Create phase-local + milestone-level .continue-here.md + commit** — `c81a941` (docs)

**Plan metadata:** This SUMMARY.md + STATE.md/ROADMAP.md updates will land in a follow-up `docs(36.3-05): complete anti-pattern marker plan` commit.

## Files Created/Modified

- `.planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md` (new, 22 insertions) — Phase-local anti-pattern registry. Markdown table with one `severity: blocking` row about writing to root `server/`, `prisma/`, `src/`. Includes "How to comply" checklist (3 items) and provenance footer. Read when 36.3 is the active phase.
- `.planning/.continue-here.md` (new, 29 insertions) — Milestone-level anti-pattern registry. Same blocking row plus a "Compliance check (for plan-phase / execute-phase)" section enumerating valid `<files_modified>` roots and forbidden root paths. Read by ALL future phase orchestrators.

## Decisions Made

- **D-11 (phase-local) honored verbatim** — content matches the locked decision from CONTEXT.md exactly. The structural-prevention claim ("Post-36.3 those root paths physically do not exist") is true as of commit `651ce93` (Plan 03's orphan sweep) and `a69bbff` (Plan 03's Rule 3 auto-fix that recreated `apps/web/prisma.config.ts` at the workspace level — NOT root).
- **Q-03 (milestone-level) exercised at Claude's discretion** — phase-local entries are invisible from outside the active phase directory, so the same blocking row is duplicated at `.planning/.continue-here.md` for cross-phase reach. The milestone-level file adds an explicit allow-list of valid `<files_modified>` roots that the phase-local version omits, giving orchestrators a positive checklist (not just the negative anti-pattern).
- **Q-02 (atomic commit) honored** — separate `docs(36.3-05)` commit, not bundled with the `chore(36.3-03)` orphan-sweep commit. The two concerns (deletion of paths vs. documentation of the anti-pattern) are independently reviewable; revert of the doc commit does not undo the structural deletion and vice versa.

## Deviations from Plan

None — plan executed exactly as written. No Rule 1/2/3 auto-fixes triggered (the task was pure documentation with no code paths to surface bugs); no Rule 4 architectural decisions surfaced (the file format was locked by D-11 and the milestone-level addition was already approved via Q-03).

## Issues Encountered

None. The only minor friction was a Git Windows line-ending warning during `git add` (`LF will be replaced by CRLF the next time Git touches it`) — informational only, files committed correctly with their authored LF endings preserved.

## Acceptance Criteria Check

All criteria from the PLAN's `<acceptance_criteria>` block:

- [x] `.planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md` exists
- [x] `.planning/.continue-here.md` exists
- [x] Both files contain literal `blocking` (grep count: 1 each)
- [x] Both files contain `Writing files to root` (grep count: 1 each)
- [x] Both files contain `apps/web/` (phase-local: 3 occurrences, milestone-level: 2 occurrences)
- [x] Both files contain `Post-36.3` (grep count: 1 each)
- [x] HEAD commit subject starts with `docs(36.3-05)` (`c81a941 docs(36.3-05): add anti-pattern markers...`)
- [x] HEAD commit modifies ONLY the two `.continue-here.md` files (`git show --stat HEAD` confirms 2 files / 51 insertions / 0 code/schema/package files)
- [x] No accidental deletions in the commit (`git diff --diff-filter=D HEAD~1 HEAD` returned empty)

## Phase 36.3 Closure

This is the FINAL plan of Phase 36.3. All 5 plans are now complete:

- **36.3-01** (refactor) — Relocated 3 orphan stripe sources to `apps/web/server/` (commits `71507b7`, `6597dfa`, `eb42362`)
- **36.3-02** (feat) — Mounted `stripeWebhookRouter` + `subscriptionRoutes` in live `apps/web/server/index.ts` (commit `18237b9`)
- **36.3-03** (chore + fix) — Orphan sweep + Prisma 7 datasource fix (commits `651ce93`, `a69bbff`)
- **36.3-04** (verify) — Stripe CLI E2E verification (status: see plan file; Plan 04 is the actual PAY-02/PAY-03/PAY-06 closure gate)
- **36.3-05** (docs) — Anti-pattern markers (this plan, commit `c81a941`)

**Phase 36.3 is ready for `/gsd-verify-phase 36.3`** — all 5 plans land on `test-ci-pipeline`. Verification will check the 6 ROADMAP success criteria against the cumulative work.

## Next Phase Readiness

- Phase 36.3 complete (5/5 plans). Awaits `/gsd-verify-phase 36.3` to close.
- After 36.3 verifies: resume `/gsd-execute-phase 36` at plan 05 (the original Phase 36 monetization plan that was paused when 36.3 was inserted).
- The two `.continue-here.md` markers will be consulted by future plan-phase / execute-phase orchestrators starting from the next phase invocation.

---

## Self-Check: PASSED

**Files verified to exist:**
- [x] `D:\NewsHub\.planning\phases\36.3-fix-stripe-webhook-monorepo-path\.continue-here.md` — FOUND
- [x] `D:\NewsHub\.planning\.continue-here.md` — FOUND
- [x] `D:\NewsHub\.planning\phases\36.3-fix-stripe-webhook-monorepo-path\36.3-05-SUMMARY.md` — being written now

**Commits verified to exist in git log:**
- [x] `c81a941` — `docs(36.3-05): add anti-pattern markers (D-11 phase-local + Q-03 milestone-level)` — FOUND

**Acceptance criteria:** All 9 from the PLAN's `<acceptance_criteria>` block pass.

---
*Phase: 36.3-fix-stripe-webhook-monorepo-path*
*Plan: 05*
*Completed: 2026-04-28*
