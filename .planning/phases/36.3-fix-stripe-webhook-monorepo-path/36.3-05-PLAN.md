---
phase: 36.3-fix-stripe-webhook-monorepo-path
plan: 05
type: execute
wave: 4
depends_on:
  - "36.3-03"
files_modified:
  - .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md
autonomous: true
requirements:
  - PAY-02
  - PAY-03
  - PAY-06

must_haves:
  truths:
    - "A `.continue-here.md` file exists in the phase directory with a `severity: blocking` anti-pattern row about writing to root server/, prisma/, or src/"
    - "The anti-pattern row includes the structural prevention claim: post-36.3 those root paths physically do not exist (validated by Plan 03)"
    - "The file is in markdown table format that future plan-phase / execute-phase orchestrators can parse"
    - "(Discretion Q-03) A milestone-level `.planning/.continue-here.md` with the same anti-pattern is also created for cross-phase reach"
  artifacts:
    - path: ".planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md"
      provides: "Phase-local anti-pattern marker; read by 36.x phase orchestrators"
      contains: "severity: blocking"
    - path: ".planning/.continue-here.md"
      provides: "Milestone-level anti-pattern marker; read by ALL future phase orchestrators (Q-03 discretion)"
      contains: "severity: blocking"
  key_links:
    - from: "future plan-phase / execute-phase orchestrators"
      to: ".planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md"
      via: "blocking anti-pattern check at start of orchestrator workflow"
      pattern: "severity: blocking"
---

<objective>
Create the `.continue-here.md` anti-pattern markers required by D-11. The file documents the wrong-path failure mode (root `server/`, `prisma/`, `src/`) at `severity: blocking` so future executor agents who try to write code to those paths must demonstrate they have read this entry and understand the structural prevention is in place.

Per Claude's discretion (Q-03), also create a milestone-level `.planning/.continue-here.md` with the same anti-pattern row. The phase-local entry is read only when 36.3 is the active phase; the milestone-level entry surfaces for ALL future phases. Low-effort duplication, high future-prevention value.

Per RESEARCH Q-02, this is a separate `docs(36.3-05)` commit (not bundled with the orphan-sweep `chore` commit), so the documentation artifact is reviewable independently.

Purpose: Belt-and-suspenders. Plan 03 deleted the wrong paths (the structural mechanism). This plan makes the anti-pattern explicit for future reference (the documentation mechanism). Together they prevent recurrence.

Output: One or two new markdown files committed in a `docs(36.3-05)` atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/36.3-fix-stripe-webhook-monorepo-path/36.3-CONTEXT.md
@.planning/phases/36.3-fix-stripe-webhook-monorepo-path/36.3-RESEARCH.md
@.planning/phases/36.3-fix-stripe-webhook-monorepo-path/36.3-PATTERNS.md
@.planning/phases/36.3-fix-stripe-webhook-monorepo-path/36.3-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase-local .continue-here.md (D-11) + milestone-level .continue-here.md (Q-03) + commit</name>
  <read_first>
    - D:/NewsHub/.planning/phases/36.3-fix-stripe-webhook-monorepo-path/36.3-CONTEXT.md (D-11 — exact anti-pattern row content)
    - D:/NewsHub/.planning/phases/36.3-fix-stripe-webhook-monorepo-path/36.3-PATTERNS.md (Category D-01, D-02 — file format)
    - D:/NewsHub/.planning/phases/36.3-fix-stripe-webhook-monorepo-path/36.3-RESEARCH.md (Q-03 milestone-level recommendation)
  </read_first>
  <action>
1. Verify the phase directory exists:
   ```bash
   ls D:/NewsHub/.planning/phases/36.3-fix-stripe-webhook-monorepo-path/
   ```
   Expected: directory exists with the 4 prior PLAN files and the CONTEXT/RESEARCH/PATTERNS files.

2. Create `D:/NewsHub/.planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md` with the EXACT D-11 row content. Use the Write tool with this content:

```markdown
# Anti-Pattern Registry — Phase 36.3

> Future plan-phase / execute-phase orchestrators must read this file before scoping a phase.
> Entries here apply when phase 36.3 (or downstream phases reading this directory) is active.

| Anti-pattern | Severity | Manifestation | Structural prevention |
|--------------|----------|---------------|------------------------|
| Writing files to root `server/`, `prisma/`, or `src/` instead of `apps/web/...` | blocking | Phase 36-02 wrote 3 webhook source files to root `server/` instead of `apps/web/server/`, leaving the live backend with a 404 on every Stripe event for ~2 days. Three milestone-36 incidents total (36.1 schema additions claimed but never written; 36.2 schema models claimed but never written; 36.3 webhook files written to wrong path). | Post-36.3, root `server/`, `prisma/`, `src/`, `tsup.config.ts`, `prisma.config.ts` are deleted — they cannot be a write target by accident. Future executor agents reading this file MUST verify their `<files_modified>` paths begin with `apps/web/`, `packages/`, or top-level config files (`package.json`, `pnpm-workspace.yaml`, `.github/`, etc.) before commit. |

## How to comply

When planning or executing any phase that touches backend / schema / frontend code:

1. Confirm `<files_modified>` paths begin with one of: `apps/web/`, `packages/`, `.github/`, `.planning/`, `pnpm-workspace.yaml`, `package.json`, `tsconfig.base.json` (if it survived 36.3 sweep), `docker-compose.yml`.
2. NEVER write to root `server/`, `prisma/`, `src/`, `prisma.config.ts`, or `tsup.config.ts`. These were deleted in 36.3-03; recreating them is a regression.
3. Run prisma commands ONLY from `apps/web/` (per CLAUDE.md lines 47-49). The deletion of root `prisma.config.ts` enforces this — running `npx prisma db push` from repo root will fail with "no schema found" rather than silently load a stale duplicate (the failure mode that nearly dropped the ApiKey table in Phase 36.2-03).

## Provenance

- Created: 2026-04-28 (Phase 36.3 Plan 05)
- Source decision: D-11 (CONTEXT.md)
- Validated by: Phase 36.3 Plans 01-04 (relocation + mount + sweep + E2E verification)
```

3. (Per Q-03 discretion) Also create `D:/NewsHub/.planning/.continue-here.md` with the milestone-level scope header. Use the Write tool with this content:

```markdown
# Anti-Pattern Registry — Project Global

> Entries here apply to ALL future phases in this project, not just one phase.
> Phase-local entries are in `.planning/phases/<phase>/.continue-here.md`.
> Future plan-phase / execute-phase orchestrators should read BOTH this file
> and the active phase's local `.continue-here.md`.

| Anti-pattern | Severity | Manifestation | Structural prevention |
|--------------|----------|---------------|------------------------|
| Writing files to root `server/`, `prisma/`, or `src/` instead of `apps/web/...` | blocking | Phase 36-02 wrote 3 webhook source files to root `server/` instead of `apps/web/server/`, leaving the live backend with a 404 on every Stripe event for ~2 days. Three milestone-36 incidents total (36.1 schema additions claimed but never written; 36.2 schema models claimed but never written; 36.3 webhook files written to wrong path). | Post-36.3, root `server/`, `prisma/`, `src/`, `tsup.config.ts`, `prisma.config.ts` are deleted — they cannot be a write target by accident. Future executor agents reading this file MUST verify their `<files_modified>` paths begin with `apps/web/`, `packages/`, or top-level config files (`package.json`, `pnpm-workspace.yaml`, `.github/`, etc.) before commit. |

## Compliance check (for plan-phase / execute-phase)

When scoping `<files_modified>` for any new plan, the orchestrator MUST verify each path against this allow-list of valid roots:

- `apps/web/...`        (live application — backend, frontend, schema)
- `apps/<other>/...`    (future apps in monorepo)
- `packages/...`        (shared workspace packages)
- `.github/...`         (CI / workflows)
- `.planning/...`       (planning artifacts)
- `pnpm-workspace.yaml`, `package.json`, `pnpm-lock.yaml`, `docker-compose.yml`, `Dockerfile`, `.gitignore` (top-level config)
- `tsconfig.base.json`  (only if it survived the 36.3 orphan sweep — check existence first)

Any path beginning with `server/`, `prisma/`, `src/` (at repo root) is FORBIDDEN. These are the wrong-path failure modes that caused the 36.x incidents. The orphan sweep in 36.3-03 deleted them; recreating them silently is a regression.

## Provenance

- Created: 2026-04-28 (Phase 36.3 Plan 05; Q-03 discretion)
- Source decision: D-11 (CONTEXT.md) extended to milestone scope
```

4. Verify both files were created with the expected content:
   ```bash
   ls .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md
   ls .planning/.continue-here.md
   grep -F "severity: blocking" .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md  # may be 0 hits — colon-form not in row, table uses pipe
   grep -cF "blocking" .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md
   grep -cF "blocking" .planning/.continue-here.md
   ```

5. Stage both files and commit atomically:
   ```bash
   git add .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md .planning/.continue-here.md
   git status --short  # should show 2 new files staged
   git commit -m "$(cat <<'EOF'
docs(36.3-05): add anti-pattern markers (D-11 phase-local + Q-03 milestone-level)

Two .continue-here.md files document the wrong-path anti-pattern that
caused three milestone-36 incidents (36.1, 36.2, 36.3 all wrote files to
the wrong directory). Severity: blocking.

- .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md
  Phase-local marker (D-11). Read when 36.3 is active.
- .planning/.continue-here.md
  Milestone-level marker (Q-03 discretion). Read by ALL future phase
  orchestrators for cross-phase reach.

Structural prevention is already in place via Plan 03's orphan sweep
(server/, prisma/, src/, prisma.config.ts, tsup.config.ts deleted).
This commit is the documentation/process belt to the structural suspenders.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
   ```

6. Verify the commit landed:
   ```bash
   git log -1 --format="%h %s"
   git show --stat HEAD --format= | head -10
   ```
   Expected: HEAD subject starts with `docs(36.3-05)`; only the two `.continue-here.md` files are in the diff.
  </action>
  <verify>
    <automated>cd D:/NewsHub && test -f .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md && test -f .planning/.continue-here.md && grep -cF "blocking" .planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md && grep -cF "blocking" .planning/.continue-here.md && git log -1 --format=%s | grep -cF "docs(36.3-05)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/36.3-fix-stripe-webhook-monorepo-path/.continue-here.md` exists
    - `.planning/.continue-here.md` exists
    - Both files contain the literal word `blocking` in the severity column (verifiable via `grep -cF "blocking" $path` returns >=1 for each)
    - Both files contain `Writing files to root` substring (verifiable via `grep -cF "Writing files to root" $path` returns 1 for each)
    - Both files contain `apps/web/` substring (verifiable via `grep -cF "apps/web/" $path` returns >=1 for each)
    - Both files contain the word `Post-36.3` somewhere (verifiable via `grep -cF "Post-36.3" $path` returns >=1 for each) — proves the structural-prevention claim is preserved
    - HEAD commit subject starts with `docs(36.3-05)` (verifiable via `git log -1 --format=%s | grep -cF "docs(36.3-05)"` returns 1)
    - HEAD commit modifies ONLY the two `.continue-here.md` files (verifiable via `git show --stat HEAD --format= | grep -cE "\.continue-here\.md"` returns 2 and `git show --stat HEAD --format= | grep -cE "\.(ts|tsx|js|json|prisma)$"` returns 0)
  </acceptance_criteria>
  <done>
    Two .continue-here.md files exist (phase-local + milestone-level), both with severity:blocking anti-pattern row about writing to root server/prisma/src/. One atomic docs(36.3-05) commit on test-ci-pipeline. Future orchestrators that consult either file will be reminded to verify their <files_modified> paths begin with apps/web/, packages/, or other valid roots.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future executor agents -> repo file paths | Agents writing new code must verify the path is valid; the .continue-here.md is the documentation surface that informs them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.3-17 | Repudiation | Recurrence of wrong-path bug | mitigate | Two .continue-here.md files (phase + milestone) document the anti-pattern at severity:blocking. Combined with the structural deletion (Plan 03), recurrence requires both deleting the protection and recreating the path — no silent failure mode |
| T-36.3-18 | Information Disclosure | Stale anti-pattern guidance | accept | The structural prevention claim ("Post-36.3 those root paths physically do not exist") is validated by Plan 03's verification gate. If a future phase recreates root server/, the anti-pattern entry remains correct as a signal that the recreation is a regression |
</threat_model>

<verification>
After Task 1 completes:

1. Both .continue-here.md files exist (phase-local + milestone-level)
2. Both files contain severity:blocking anti-pattern row
3. HEAD commit prefixed `docs(36.3-05)` with only the 2 markdown files in the diff
</verification>

<success_criteria>
- D-11 satisfied (phase-local .continue-here.md exists with the locked-decision content)
- Q-03 satisfied (milestone-level .continue-here.md exists for cross-phase reach)
- Documentation belt complements Plan 03's structural suspenders
- All five 36.3 plans complete; Phase 36.3 ready for `/gsd-verify-phase 36.3`
</success_criteria>

<output>
After completion, create `.planning/phases/36.3-fix-stripe-webhook-monorepo-path/36.3-05-SUMMARY.md` with:
- Commit SHA
- Confirmation both files exist with severity:blocking entries
- Note Phase 36.3 ready for verification (`/gsd-verify-phase 36.3`)
</output>
