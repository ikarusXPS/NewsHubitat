---
phase: 36.3-fix-stripe-webhook-monorepo-path
plan: 02
subsystem: payments
tags: [stripe, webhook, express, mount, raw-body, hmac, monorepo, defense-in-depth]

# Dependency graph
requires:
  - phase: 36.3-fix-stripe-webhook-monorepo-path
    provides: "Plan 01 — Stripe webhook service, route, and subscription routes relocated from orphan root server/ to canonical apps/web/server/ via three atomic git mv commits (71507b7, 6597dfa, eb42362) with full history preserved"
  - phase: 36-monetization-core
    provides: "Stripe webhook + subscription handlers (stripeWebhookService, /api/webhooks/stripe POST handler with express.raw, subscription checkout/portal/status with per-route authMiddleware)"
  - phase: 36.2-close-36-debt-schema-models-cleanup
    provides: "ProcessedWebhookEvent model + non-null SubscriptionTier/Status enums in apps/web schema (commits 1976489 / 44aa829 / 556694f / 30466e2) — required for runtime persistence of webhook events once route is mounted"
  - phase: 35-infrastructure-foundation
    provides: "Live apps/web/server/index.ts wired with Phase 35 routes (apiKeyRoutes, publicApiRoutes, X-API-Key CORS) — preserved byte-stable through this plan's diff-only edit per D-03"
provides:
  - "Live mount of /api/webhooks/stripe in apps/web/server/index.ts at the Phase 22 raw-body anchor (BEFORE express.json) — webhook signature verification now functional end-to-end (T-36.3-05 mitigated)"
  - "Live mount of /api/subscriptions in apps/web/server/index.ts between /api/teams and /api/keys with authMiddleware at the mount level (T-36.3-06 mitigated; L-02 redundancy preserved as deliberate defense-in-depth)"
  - "Three new imports in apps/web/server/index.ts: stripeWebhookRouter, subscriptionRoutes, authMiddleware — symbols that Plan 01 made resolvable from the canonical apps/web/server/ paths"
  - "One atomic feat(36.3-02) commit on test-ci-pipeline (18237b9) — single file, +9 insertions, 0 deletions; reviewable in <30 seconds (T-36.3-07 mitigated by surgical diff)"
  - "Live probe proof: POST /api/webhooks/stripe returns 400 (missing signature, expected per stripe.ts:38-42), GET /api/subscriptions/status returns 401 (authMiddleware rejects unauthenticated, expected) — neither returns 404"
affects: [36.3-03-orphan-sweep, 36.3-04-stripe-cli-e2e-verify, 36.3-05-anti-pattern-doc, 36-05-monetization-resume, 37-horizontal-scaling]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Webhook mount at the Phase 22 raw-body anchor (BEFORE express.json) — preserves the rawBody Buffer for stripe.webhooks.constructEvent HMAC verification (D-04)"
    - "Mount-level authMiddleware with intentional per-route redundancy on subscription routes (D-05) — no source-file edits per D-02 leaves subscriptions.ts:49/112/153 untouched; double auth check is defense-in-depth and not a correctness bug"
    - "Diff-only surgical edit policy for live entrypoint files (D-03) — never replace with orphan copy; verify Phase 35 wiring (apiKeyRoutes, publicApiRoutes, apiKeyAuth, X-API-Key CORS) survives the diff"
    - "Live curl probe as mount-existence proof — 400/401/503 acceptable; 404 is hard fail (proves route is reachable independently of business-logic outcome)"

key-files:
  created: []
  modified:
    - "apps/web/server/index.ts (+9 lines: 3 imports + 2 mount blocks; 0 deletions)"

key-decisions:
  - "D-04 honored: webhook mount placed between configurePassport() (line 107) and express.json() (line 110) — raw body preserved for HMAC"
  - "D-05 honored: subscription mount placed between /api/teams (line 170/175) and /api/keys (line 173/178) with authMiddleware at mount level"
  - "D-03 honored: diff-only edit; Phase 35 wiring (apiKeyRoutes line 37, publicApiRoutes line 36/39, apiKeyAuth + apiKeyLimiter at /api/v1/public, X-API-Key CORS header) untouched"
  - "D-02 honored: zero edits to subscriptions.ts — L-02 redundancy with the new mount-level authMiddleware is documented as intentional defense-in-depth, not a bug to fix"
  - "Atomic single-commit policy: Task 1 (imports) and Task 2 (mounts) committed together in 18237b9 — imports without mounts would compile but be dead weight; one logical change = one commit"

patterns-established:
  - "Pattern: Stripe webhook Express mount — must precede express.json so the route's own express.raw inside the sub-router intercepts the raw Buffer for HMAC. Compression / etag / metrics / queryCounter middlewares (lines 91-103) all operate on response headers / timing only and never read req.body, so they are safe to run before the webhook mount (D-06 verified)"
  - "Pattern: Mount-level authMiddleware on a route module that already applies per-route authMiddleware = double JWT verify + Redis blacklist + DB lookup per request. Cost is acceptable for low-volume subscription routes; not a pattern to apply to high-traffic routes"
  - "Pattern: Live route reachability proof = 400/401/503 from curl, NOT 404. Business-logic-dependent codes (200, 422, 500) are NOT proof of mount existence — they require valid input. 400/401 from a route's own validators or auth middleware are the cheapest reachability signal"

requirements-completed: []  # PAY-02 / PAY-03 / PAY-06 are listed in the plan's `requirements:` field but are NOT closed by this plan alone — Plan 04 (stripe trigger 8 events end-to-end + ProcessedWebhookEvent row check + idempotency replay) is the actual closure gate. This plan structurally enables those flows; full requirement satisfaction is recorded in 36.3-04-SUMMARY.md.

# Metrics
duration: 6min
completed: 2026-04-28
---

# Phase 36.3 Plan 02: Mount Stripe Webhook + Subscription Routes Summary

**Live backend now serves `/api/webhooks/stripe` (raw-body preserved before express.json for HMAC) and `/api/subscriptions` (authMiddleware at mount + per-route) — closes the 404 introduced when Phase 36-02 wrote mount edits to the orphan root server/ path; one atomic +9-line surgical diff to apps/web/server/index.ts.**

## Performance

- **Duration:** ~6 min (file reads + 2 edits + typecheck + 1289-test run + curl probe + commit)
- **Started:** 2026-04-28T11:50:30Z (approx — first Read of PLAN.md)
- **Completed:** 2026-04-28T11:56:06Z (commit 18237b9)
- **Tasks:** 2 of 2
- **Files modified:** 1 (apps/web/server/index.ts)
- **Diff size:** +9 insertions, -0 deletions (3 imports + 2 mount blocks each comprising comment + app.use line)

## Accomplishments

- **Webhook mount live and reachable** — `app.use('/api/webhooks/stripe', stripeWebhookRouter)` inserted between `configurePassport();` and the Phase 22 raw-body comment; the route's own `express.raw({ type: 'application/json' })` middleware (declared inside `routes/webhooks/stripe.ts:35`) intercepts the request body before `express.json()` would see it, so `stripe.webhooks.constructEvent` will receive the verbatim Buffer required for HMAC.
- **Subscription mount live and gated** — `app.use('/api/subscriptions', authMiddleware, subscriptionRoutes)` inserted between `/api/teams` and `/api/keys` with authMiddleware applied at the mount level per D-05.
- **Live curl probes prove non-404** — backend booted on port 3001, `POST /api/webhooks/stripe` returned **400** (Missing signature, expected per stripe.ts:38-42), `GET /api/subscriptions/status` returned **401** (authMiddleware rejected unauthenticated request); both routes are mounted and reachable.
- **Phase 35 wiring intact** — `apiKeyRoutes`, `publicApiRoutes`, `apiKeyAuth`, `apiKeyLimiter`, and the `X-API-Key` CORS allowedHeader all preserved unchanged in the diff (D-03 surgical-edit policy enforced).
- **Test suite green** — `pnpm typecheck` exit 0, `pnpm test:run` 1289/1289 PASS — exact match to the Phase 36.2 / 36.3-01 baseline; zero regression.

## Task Commits

Per the plan's design, Tasks 1 and 2 were committed together as a single atomic change. Imports without mounts would compile but be dead weight (no functional change); imports + mounts together are the smallest reviewable unit that actually changes runtime behavior.

1. **Task 1: Add three imports (stripeWebhookRouter, subscriptionRoutes, authMiddleware)** — staged into 18237b9
2. **Task 2: Insert webhook mount + subscription mount; probe routes; commit atomically** — `18237b9` (feat)

**Plan metadata commit (this SUMMARY + STATE.md/ROADMAP.md updates):** appended in a follow-up `docs(36.3-02): complete mount-routes plan` commit.

## Files Created/Modified

- `apps/web/server/index.ts` — surgical +9 line diff:
  - **Imports (after line 35 `import teamsRoutes`):** added `import stripeWebhookRouter from './routes/webhooks/stripe';` and `import subscriptionRoutes from './routes/subscriptions';`
  - **Imports (after line 46 `import { queryCounterMiddleware }`):** added `import { authMiddleware } from './services/authService';`
  - **Webhook mount (after `configurePassport();`, before `// JSON parser ... (Phase 22)`):** added `// Stripe webhook route - MUST be before express.json() for raw body signature verification (Phase 36.3)` and `app.use('/api/webhooks/stripe', stripeWebhookRouter);`
  - **Subscription mount (after `app.use('/api/teams', teamsRoutes);`, before `// API key management routes (Phase 35-04)`):** added `// Subscription routes (Phase 36.3 - relocated from orphaned root server/)` and `app.use('/api/subscriptions', authMiddleware, subscriptionRoutes);`

## Decisions Made

- None new — followed plan as specified. All five locked decisions from CONTEXT.md (D-02, D-03, D-04, D-05, D-06) were honored verbatim. The L-02 redundancy (per-route authMiddleware in subscriptions.ts:49/112/153 + new mount-level authMiddleware) was preserved without source edits per D-02.

## Deviations from Plan

None — plan executed exactly as written.

The plan's anchor line numbers (107, 110, 170, 173) shifted by 3 in the live file after Task 1 added 3 import lines, but the Edit tool was anchored by string content (`configurePassport();` and `app.use('/api/teams', teamsRoutes);`) rather than line numbers, so the shift caused no friction. Both insertions landed at the canonical positions.

**Total deviations:** 0
**Impact on plan:** Zero scope creep; surgical diff matches the plan's "small and reviewable" intent.

## Issues Encountered

- **Boot-and-probe parallelism:** the live curl probe step requires `pnpm dev:backend` to fully boot, then probing both endpoints, then killing the backend. The bash background-job pattern with a `sleep 2` poll loop completed cleanly (`Ping ready after 1 tries` — backend was already warm from a prior dev session, but the loop is robust to cold starts up to 30s). After kill, an explicit `pkill -f "tsx watch"` and `pkill -f "server/index.ts"` was added to ensure no orphan tsx watcher children survived the bash background-process termination on Windows. Not a deviation; precautionary cleanup.

## Threat Mitigation

- **T-36.3-05 (Spoofing — webhook signature bypass):** mitigated. Webhook mount placed at the canonical raw-body anchor; `awk '/configurePassport/{p=1} p && /app.use\(express.json/{exit} p' apps/web/server/index.ts | grep webhooks` returns the mount line, confirming order.
- **T-36.3-06 (Elevation of Privilege — unauthenticated subscription access):** mitigated. Live probe of GET /api/subscriptions/status returned 401 (authMiddleware rejection); per-route authMiddleware in subscriptions.ts:49/112/153 is preserved as defense-in-depth.
- **T-36.3-07 (Tampering — replacing live index.ts with orphan copy):** mitigated by surgical diff. `git show --stat HEAD` shows only `apps/web/server/index.ts | 9 +++++++++` — single file, additive only, no Phase 35 wiring removed.
- **T-36.3-08 (Information Disclosure — bypassed compression/etag/metrics):** accepted per D-06. None of compression / etag / metrics / queryCounter / serverTiming middlewares read req.body (verified via grep in RESEARCH §D-06). No new attack surface.

## Threat Flags

None — no new security-relevant surface introduced by this plan beyond the threats already in the plan's `<threat_model>`.

## User Setup Required

None — no external service configuration required for this plan. Plan 04 will require Stripe CLI authentication (`stripe login`) and a running `stripe listen` session for end-to-end webhook trigger verification, but that is out of scope for this plan.

## Self-Check: PASSED

**Files claimed modified — verified:**
- `apps/web/server/index.ts` — FOUND (modified, +9 lines via `git show --stat HEAD` confirms: `1 file changed, 9 insertions(+)`)

**Commits claimed — verified:**
- `18237b9` (feat(36.3-02)) — FOUND (`git log --oneline -3` lists it as HEAD)

**Acceptance grep checks — verified:**
- `grep -cF "import stripeWebhookRouter from './routes/webhooks/stripe';" apps/web/server/index.ts` returns 1 ✓
- `grep -cF "import subscriptionRoutes from './routes/subscriptions';" apps/web/server/index.ts` returns 1 ✓
- `grep -cF "import { authMiddleware } from './services/authService';" apps/web/server/index.ts` returns 1 ✓
- `grep -cF "app.use('/api/webhooks/stripe', stripeWebhookRouter);" apps/web/server/index.ts` returns 1 ✓
- `grep -cF "app.use('/api/subscriptions', authMiddleware, subscriptionRoutes);" apps/web/server/index.ts` returns 1 ✓
- `awk '/configurePassport/{p=1} p && /app.use\(express.json/{exit} p' apps/web/server/index.ts | grep -cF "/api/webhooks/stripe"` returns 1 ✓ (webhook mount BEFORE express.json)
- `awk '/api\/teams/{p=1} p && /api\/keys/{exit} p' apps/web/server/index.ts | grep -cF "/api/subscriptions"` returns 1 ✓ (subscription mount BETWEEN teams and keys)

**Verification gates — verified:**
- `pnpm typecheck` exit 0 ✓
- `pnpm test:run` exit 0, 1289/1289 PASS ✓ (matches Phase 36.2 / 36.3-01 baseline)
- Live POST /api/webhooks/stripe → 400 (NOT 404) ✓
- Live GET /api/subscriptions/status → 401 (NOT 404) ✓

## Next Phase Readiness

- **Plan 03 (orphan sweep) UNBLOCKED** — the live backend now serves the relocated routes, so `git rm -rf server/` (plus the four discretion-list files) has zero functional impact. Deletion of orphans is now a pure cleanup operation.
- **Plan 04 (stripe CLI E2E verification) UNBLOCKED** — `stripe listen --forward-to http://localhost:3001/api/webhooks/stripe` will reach a mounted route. The 8 `stripe trigger` events should each show `[200]` in `stripe listen` output (or expected business-logic non-200 codes for synthetic events lacking matching userId). The `ProcessedWebhookEvent` row-count check in D-10 is now wired end-to-end (Phase 36.2 schema + Phase 36.3-01 service relocation + this plan's mount).
- **Plan 05 (anti-pattern docs) UNBLOCKED** — the runtime regression is now closed; the anti-pattern entry can document the Phase 36-02 mistake (writing to orphan root server/) as a fully-resolved historical case study.
- **No blockers, no open questions, no carry-over.** The L-02 redundancy is documented and accepted; addressing it requires editing subscriptions.ts which D-02 forbids during this phase. A future "subscription-route auth cleanup" plan (out of scope for 36.3) could remove the per-route authMiddleware once the mount-level guard is locked in by tests.

---
*Phase: 36.3-fix-stripe-webhook-monorepo-path*
*Plan: 02 (mount-routes)*
*Completed: 2026-04-28*
