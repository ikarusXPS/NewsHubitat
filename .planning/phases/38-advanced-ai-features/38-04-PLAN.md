---
id: 38-04
phase: 38-advanced-ai-features
plan: 4
type: execute
wave: 3
depends_on: []
files_modified:
  - apps/web/public/locales/en/factcheck.json
  - apps/web/public/locales/de/factcheck.json
  - apps/web/public/locales/fr/factcheck.json
  - apps/web/public/locales/en/credibility.json
  - apps/web/public/locales/de/credibility.json
  - apps/web/public/locales/fr/credibility.json
  - apps/web/src/i18n/i18n.ts
autonomous: true
requirements: [AI-01, AI-03, AI-05, AI-06]
requirements_addressed: [AI-01, AI-03, AI-05, AI-06]
tags: [phase-38, i18n, locales, factcheck, credibility, fr-locale-creation]
must_haves:
  truths:
    - "Each of the three locales (de, en, fr) has a factcheck.json + credibility.json file at apps/web/public/locales/<locale>/"
    - "All five verdict labels (true/mostly-true/mixed/unverified/false) per D-08 LOCKED are translated for each locale"
    - "All three confidence buckets (low/medium/high) per D-05 LOCKED are translated for each locale"
    - "All three bias buckets (left/center/right) per D-04 LOCKED are translated for each locale"
    - "All UI labels enumerated in RESEARCH.md §i18n String Inventory lines 950-1000 are present in their respective locale files"
    - "i18n.ts has 'factcheck' and 'credibility' added to the namespace array (line 16) so React components can resolve t('factcheck:verdicts.true') / t('credibility:bias.left')"
    - "FR locale files are net-new (the fr/ directory previously had only pricing.json); other existing FR namespaces (auth, common, dashboard, etc.) are explicitly OUT OF SCOPE this phase per CONTEXT.md scope discipline"
  artifacts:
    - path: "apps/web/public/locales/en/factcheck.json"
      provides: "English fact-check UI strings (verdict labels, drawer labels, error messages)"
      contains: "verdicts"
    - path: "apps/web/public/locales/de/factcheck.json"
      provides: "German fact-check UI strings"
      contains: "Wahr"
    - path: "apps/web/public/locales/fr/factcheck.json"
      provides: "French fact-check UI strings"
      contains: "Vrai"
    - path: "apps/web/public/locales/en/credibility.json"
      provides: "English credibility/framing/bias/confidence UI strings"
      contains: "credibility"
    - path: "apps/web/public/locales/de/credibility.json"
      provides: "German credibility/framing/bias/confidence UI strings"
      contains: "Glaubwürdigkeit"
    - path: "apps/web/public/locales/fr/credibility.json"
      provides: "French credibility/framing/bias/confidence UI strings"
      contains: "Crédibilité"
    - path: "apps/web/src/i18n/i18n.ts"
      provides: "Updated namespace registration for factcheck + credibility namespaces"
      contains: "'factcheck'"
  key_links:
    - from: "apps/web/src/i18n/i18n.ts:ns array"
      to: "apps/web/public/locales/<locale>/{factcheck,credibility}.json"
      via: "i18n loadPath '/locales/{{lng}}/{{ns}}.json' fetches the JSON files at runtime when React components call t('namespace:key')"
      pattern: "ns: \\["
---

<objective>
Ship the user-visible string layer for Phase 38. Deliver six new JSON locale files (factcheck.json + credibility.json across DE/EN/FR) plus the i18n.ts namespace registration that makes them discoverable by React components in Plan 38-05. Every key listed in RESEARCH.md §"i18n String Inventory" (lines 950-1000) is present.

Purpose: Plan 38-05's CredibilityPill, BiasBadge, VerdictPill, FactCheckButton, FactCheckDrawer, CitationCard, and the rewritten FramingComparison consume these keys via `useTranslation('factcheck')` / `useTranslation('credibility')`. Without this plan they would crash on missing keys (or silently fall back to the key string in dev — neither is acceptable user experience).

Output: 6 new JSON locale files + 1 single-line edit to i18n.ts. No code logic, no tests.

Wave placement (revised post-checker): Held in Wave 3 (parallel with 38-03) so i18n string keys land in the same review window as the routes that emit them; could technically run in Wave 1 with no prerequisites (depends_on: []), but co-scheduling with the route plan keeps reviewer cognitive context unified.

Scope discipline (per CONTEXT.md scope_reduction_prohibition): The French locale directory currently has ONLY `pricing.json`. This plan creates the two new FR namespaces (factcheck + credibility) but does NOT backfill the other existing FR namespaces (auth, common, dashboard, settings, etc.). The i18n config has `fallbackLng: 'en'` so missing FR keys in those other namespaces fall back to English at render time without crashing. Phase 38 explicitly scopes the FR work to fact-check + credibility surfaces only — this is a planner-acknowledged carve-out, NOT silent omission. A separate "FR locale completion" phase is the right place to backfill the rest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/.continue-here.md
@.planning/phases/38-advanced-ai-features/38-CONTEXT.md
@.planning/phases/38-advanced-ai-features/38-RESEARCH.md
@.planning/phases/38-advanced-ai-features/38-PATTERNS.md
@apps/web/src/i18n/i18n.ts
@apps/web/public/locales/en/common.json

<interfaces>
<!-- Existing pattern from on-disk locale files (the JSON shape every i18next namespace follows). -->

From apps/web/public/locales/en/common.json (existing structure):

```json
{
  "buttons": {
    "save": "Save",
    "cancel": "Cancel"
  },
  "time": {
    "minutesAgo": "{count, plural, one {# minute ago} other {# minutes ago}}"
  }
}
```

From apps/web/src/i18n/i18n.ts (existing namespace array — single-line edit target):

```typescript
ns: ['common', 'share', 'teams', 'pricing'],
```

Becomes (Phase 38):

```typescript
ns: ['common', 'share', 'teams', 'pricing', 'factcheck', 'credibility'],
```

From RESEARCH.md §"i18n String Inventory" lines 950-1000 (the exhaustive list — copy verbatim into the locale files; planner has carved out the precise key/value mapping for each locale):

Verdict labels (D-08 LOCKED):
- factcheck.verdicts.true → True / Wahr / Vrai
- factcheck.verdicts.mostly-true → Mostly true / Größtenteils wahr / Plutôt vrai
- factcheck.verdicts.mixed → Mixed / Gemischt / Mitigé
- factcheck.verdicts.unverified → Unverified / Nicht verifiziert / Non vérifié
- factcheck.verdicts.false → False / Falsch / Faux

Confidence buckets (D-05 LOCKED):
- credibility.confidence.low → Low confidence / Geringe Sicherheit / Confiance faible
- credibility.confidence.medium → Medium confidence / Mittlere Sicherheit / Confiance moyenne
- credibility.confidence.high → High confidence / Hohe Sicherheit / Confiance élevée

Bias buckets (D-04 LOCKED):
- credibility.bias.left → Left-leaning / Linksgerichtet / Tendance gauche
- credibility.bias.center → Centrist / Mitte / Centriste
- credibility.bias.right → Right-leaning / Rechtsgerichtet / Tendance droite

UI labels — see RESEARCH.md lines 977-1000 for the verbatim EN/DE/FR mapping for:
- factcheck.button.label, factcheck.drawer.{title,methodology,citations,close}
- factcheck.error.{tooShort,tooLong,rateLimit}, factcheck.unverified.explanation
- credibility.pill.label, credibility.drawer.{title,subDimensions,accuracy,transparency,corrections,disclosure}
- framing.{aiGenerated.badge,section.{narrative,omissions,vocabulary,evidenceQuotes}}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the three factcheck.json locale files (de/en/fr)</name>
  <files>apps/web/public/locales/en/factcheck.json, apps/web/public/locales/de/factcheck.json, apps/web/public/locales/fr/factcheck.json</files>
  <read_first>
    - apps/web/public/locales/en/common.json (existing namespace shape — JSON structure with nested objects)
    - .planning/phases/38-advanced-ai-features/38-RESEARCH.md §"i18n String Inventory" lines 950-1000 (verbatim per-locale strings)
    - .planning/phases/38-advanced-ai-features/38-PATTERNS.md §"apps/web/public/locales/{de,en,fr}/credibility.json and factcheck.json" lines 829-865 (locale path + recommended namespace split)
  </read_first>
  <action>
    Create three JSON files with the exact content below. UTF-8 encoding, LF line endings, 2-space indent.

    **File 1 — `apps/web/public/locales/en/factcheck.json`:**

    ```json
    {
      "verdicts": {
        "true": "True",
        "mostly-true": "Mostly true",
        "mixed": "Mixed",
        "unverified": "Unverified",
        "false": "False"
      },
      "button": {
        "label": "Fact-check this",
        "loading": "Checking..."
      },
      "drawer": {
        "title": "Fact-check result",
        "methodology": "Methodology",
        "citations": "Sources cited",
        "close": "Close",
        "noCitations": "No citations available."
      },
      "error": {
        "tooShort": "Highlight at least 10 characters",
        "tooLong": "Highlight no more than 500 characters",
        "rateLimit": "Daily AI limit reached. Upgrade for unlimited fact-checks.",
        "rejected": "Claim contains forbidden patterns",
        "generic": "Could not complete fact-check. Please retry."
      },
      "unverified": {
        "explanation": "The corpus did not contain enough evidence to verify this claim."
      }
    }
    ```

    **File 2 — `apps/web/public/locales/de/factcheck.json`** (German — UTF-8 with umlauts; verbatim from RESEARCH.md):

    ```json
    {
      "verdicts": {
        "true": "Wahr",
        "mostly-true": "Größtenteils wahr",
        "mixed": "Gemischt",
        "unverified": "Nicht verifiziert",
        "false": "Falsch"
      },
      "button": {
        "label": "Faktencheck",
        "loading": "Wird geprüft..."
      },
      "drawer": {
        "title": "Faktencheck-Ergebnis",
        "methodology": "Methodik",
        "citations": "Zitierte Quellen",
        "close": "Schließen",
        "noCitations": "Keine Quellen verfügbar."
      },
      "error": {
        "tooShort": "Mindestens 10 Zeichen markieren",
        "tooLong": "Höchstens 500 Zeichen markieren",
        "rateLimit": "Tägliches AI-Limit erreicht. Upgrade für unbegrenzte Faktenchecks.",
        "rejected": "Behauptung enthält verbotene Muster",
        "generic": "Faktencheck konnte nicht abgeschlossen werden. Bitte erneut versuchen."
      },
      "unverified": {
        "explanation": "Der Korpus enthielt nicht genügend Belege, um diese Behauptung zu prüfen."
      }
    }
    ```

    **File 3 — `apps/web/public/locales/fr/factcheck.json`** (French — UTF-8 with accents):

    ```json
    {
      "verdicts": {
        "true": "Vrai",
        "mostly-true": "Plutôt vrai",
        "mixed": "Mitigé",
        "unverified": "Non vérifié",
        "false": "Faux"
      },
      "button": {
        "label": "Vérifier",
        "loading": "Vérification en cours..."
      },
      "drawer": {
        "title": "Résultat de la vérification",
        "methodology": "Méthodologie",
        "citations": "Sources citées",
        "close": "Fermer",
        "noCitations": "Aucune source disponible."
      },
      "error": {
        "tooShort": "Sélectionnez au moins 10 caractères",
        "tooLong": "Sélectionnez au maximum 500 caractères",
        "rateLimit": "Limite IA quotidienne atteinte. Passez Premium pour des vérifications illimitées.",
        "rejected": "L'affirmation contient des motifs interdits",
        "generic": "Impossible de finaliser la vérification. Veuillez réessayer."
      },
      "unverified": {
        "explanation": "Le corpus ne contenait pas assez de preuves pour vérifier cette affirmation."
      }
    }
    ```

    **Critical UTF-8 rule:** When the executor's tool/shell environment may transliterate non-ASCII characters (the milestone-36 history shows this happened in Plan 36.4-03 for DE umlauts and FR accents — the source commit had ASCII transliterations and required a Rule-1 auto-fix), the executor MUST verify the saved file contains the literal UTF-8 bytes for `Größtenteils`, `Schließen`, `Plutôt`, `Mitigé`, `vérifié`, `Méthodologie`, etc. If `grep -q 'Größtenteils' apps/web/public/locales/de/factcheck.json` fails post-write, re-write the file using a tool that preserves UTF-8 (the Write tool does; avoid bash heredoc which sometimes strips encoding).

    Each file MUST be valid JSON — verify with `node -e "JSON.parse(require('fs').readFileSync('PATH','utf8'))"` after writing.
  </action>
  <verify>
    <automated>test -f apps/web/public/locales/en/factcheck.json && test -f apps/web/public/locales/de/factcheck.json && test -f apps/web/public/locales/fr/factcheck.json && node -e "JSON.parse(require('fs').readFileSync('apps/web/public/locales/en/factcheck.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('apps/web/public/locales/de/factcheck.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('apps/web/public/locales/fr/factcheck.json','utf8'))" && grep -q '"verdicts"' apps/web/public/locales/en/factcheck.json && grep -q 'Wahr' apps/web/public/locales/de/factcheck.json && grep -q 'Größtenteils' apps/web/public/locales/de/factcheck.json && grep -q 'Vrai' apps/web/public/locales/fr/factcheck.json && grep -q 'Plutôt' apps/web/public/locales/fr/factcheck.json && grep -q 'Méthodologie' apps/web/public/locales/fr/factcheck.json && echo OK</automated>
  </verify>
  <done>
    All three factcheck.json files exist; all three parse as valid JSON; DE file contains the literal UTF-8 umlaut character `ö` (Größtenteils) and `ß` (Schließen); FR file contains literal accented characters `ô` (Plutôt), `é` (vérifié), `é` (Méthodologie). All five verdict keys (true/mostly-true/mixed/unverified/false) present in each locale.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create the three credibility.json locale files (de/en/fr)</name>
  <files>apps/web/public/locales/en/credibility.json, apps/web/public/locales/de/credibility.json, apps/web/public/locales/fr/credibility.json</files>
  <read_first>
    - apps/web/public/locales/en/factcheck.json (created in Task 1; mirrors the JSON shape)
    - .planning/phases/38-advanced-ai-features/38-RESEARCH.md §"i18n String Inventory" lines 950-1000 + §"Recommended namespace split" lines 1002-1005 (credibility namespace owns bias/confidence/credibility/framing keys per the planner's locked decision)
  </read_first>
  <action>
    Create three JSON files. The `credibility` namespace owns the credibility/bias/confidence/framing keys (per RESEARCH.md namespace-split recommendation).

    **File 1 — `apps/web/public/locales/en/credibility.json`:**

    ```json
    {
      "pill": {
        "label": "Credibility"
      },
      "drawer": {
        "title": "Source credibility",
        "subDimensions": "Sub-dimensions",
        "accuracy": "Accuracy",
        "transparency": "Transparency",
        "corrections": "Corrections",
        "disclosure": "These sub-dimensions are AI-attributed estimates, not measured signals.",
        "score": "Score: {{score}}/100",
        "close": "Close"
      },
      "bias": {
        "left": "Left-leaning",
        "center": "Centrist",
        "right": "Right-leaning"
      },
      "confidence": {
        "low": "Low confidence",
        "medium": "Medium confidence",
        "high": "High confidence"
      },
      "framing": {
        "aiGenerated": "AI-generated",
        "section": {
          "narrative": "Narrative",
          "omissions": "Notable omissions",
          "vocabulary": "Charged vocabulary",
          "evidenceQuotes": "Evidence quotes"
        },
        "noData": "No framing analysis available for this topic."
      }
    }
    ```

    **File 2 — `apps/web/public/locales/de/credibility.json`** (German):

    ```json
    {
      "pill": {
        "label": "Glaubwürdigkeit"
      },
      "drawer": {
        "title": "Quellen-Glaubwürdigkeit",
        "subDimensions": "Unterdimensionen",
        "accuracy": "Genauigkeit",
        "transparency": "Transparenz",
        "corrections": "Korrekturen",
        "disclosure": "Diese Unterdimensionen sind KI-attribuierte Schätzungen, keine gemessenen Signale.",
        "score": "Bewertung: {{score}}/100",
        "close": "Schließen"
      },
      "bias": {
        "left": "Linksgerichtet",
        "center": "Mitte",
        "right": "Rechtsgerichtet"
      },
      "confidence": {
        "low": "Geringe Sicherheit",
        "medium": "Mittlere Sicherheit",
        "high": "Hohe Sicherheit"
      },
      "framing": {
        "aiGenerated": "KI-generiert",
        "section": {
          "narrative": "Narrativ",
          "omissions": "Auffällige Auslassungen",
          "vocabulary": "Wertende Begriffe",
          "evidenceQuotes": "Belegstellen"
        },
        "noData": "Für dieses Thema ist keine Framing-Analyse verfügbar."
      }
    }
    ```

    **File 3 — `apps/web/public/locales/fr/credibility.json`** (French):

    ```json
    {
      "pill": {
        "label": "Crédibilité"
      },
      "drawer": {
        "title": "Crédibilité de la source",
        "subDimensions": "Sous-dimensions",
        "accuracy": "Précision",
        "transparency": "Transparence",
        "corrections": "Corrections",
        "disclosure": "Ces sous-dimensions sont des estimations attribuées par IA, pas des signaux mesurés.",
        "score": "Note: {{score}}/100",
        "close": "Fermer"
      },
      "bias": {
        "left": "Tendance gauche",
        "center": "Centriste",
        "right": "Tendance droite"
      },
      "confidence": {
        "low": "Confiance faible",
        "medium": "Confiance moyenne",
        "high": "Confiance élevée"
      },
      "framing": {
        "aiGenerated": "Généré par IA",
        "section": {
          "narrative": "Narratif",
          "omissions": "Omissions notables",
          "vocabulary": "Vocabulaire chargé",
          "evidenceQuotes": "Citations de preuve"
        },
        "noData": "Aucune analyse de cadrage disponible pour ce sujet."
      }
    }
    ```

    Same UTF-8 verification rule as Task 1: confirm `Glaubwürdigkeit` (DE), `Crédibilité` / `élevée` / `Généré` (FR) are saved as their literal UTF-8 byte sequences.
  </action>
  <verify>
    <automated>test -f apps/web/public/locales/en/credibility.json && test -f apps/web/public/locales/de/credibility.json && test -f apps/web/public/locales/fr/credibility.json && node -e "JSON.parse(require('fs').readFileSync('apps/web/public/locales/en/credibility.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('apps/web/public/locales/de/credibility.json','utf8'))" && node -e "JSON.parse(require('fs').readFileSync('apps/web/public/locales/fr/credibility.json','utf8'))" && grep -q 'Credibility' apps/web/public/locales/en/credibility.json && grep -q 'Glaubwürdigkeit' apps/web/public/locales/de/credibility.json && grep -q 'Crédibilité' apps/web/public/locales/fr/credibility.json && grep -q '"left"' apps/web/public/locales/en/credibility.json && grep -q '"right"' apps/web/public/locales/de/credibility.json && grep -q '"center"' apps/web/public/locales/fr/credibility.json && grep -q 'aiGenerated' apps/web/public/locales/en/credibility.json && echo OK</automated>
  </verify>
  <done>
    All three credibility.json files exist and parse as valid JSON; UTF-8 characters are preserved literally (Glaubwürdigkeit, Crédibilité, élevée); all four key groups (pill/drawer/bias/confidence/framing) present in each locale.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add 'factcheck' and 'credibility' to the i18n.ts namespace array</name>
  <files>apps/web/src/i18n/i18n.ts</files>
  <read_first>
    - apps/web/src/i18n/i18n.ts (entire file — locate line 16 with the existing `ns:` array)
    - .planning/phases/38-advanced-ai-features/38-PATTERNS.md §"apps/web/src/i18n/i18n.ts (MODIFY)" lines 868-882 (single-line edit target)
  </read_first>
  <action>
    Edit `apps/web/src/i18n/i18n.ts`. Find the existing namespace array on line 16 (verify exact line via grep — it may have shifted). The current value should be (per PATTERNS.md):

    ```typescript
    ns: ['common', 'share', 'teams', 'pricing'],
    ```

    Replace with:

    ```typescript
    ns: ['common', 'share', 'teams', 'pricing', 'factcheck', 'credibility'],
    ```

    Single line edit. Do NOT modify the `defaultNS`, `fallbackLng`, `loadPath`, or any other config field.

    Run typecheck (this file is part of the React app, so it's checked under apps/web/tsconfig.app.json):
    ```bash
    cd apps/web && pnpm typecheck 2>&1 | tail -3
    ```

    Run the broader frontend test suite to confirm no test references the old namespace array length or order (unlikely; existing tests likely query specific keys, not the array shape):
    ```bash
    cd apps/web && pnpm test -- i18n --run 2>&1 | tail -5
    ```

    If a test fails because it asserts the namespace array length or specific values, update the test to match the new array.

    **Regression probe — escapeValue assumption (T-38-18 dependency):** The threat-row T-38-18 mitigation depends on i18next's `escapeValue: false` setting being preserved (the comment on i18n.ts:30 explicitly states 'React handles XSS'). Verify this still holds:

    ```bash
    grep -q "escapeValue: false" apps/web/src/i18n/i18n.ts
    ```

    If a future change ever removes this setting or flips it to `escapeValue: true` in a way that conflicts with the React-auto-escape assumption, the verify probe in this task will fail and surface the regression for review.
  </action>
  <verify>
    <automated>grep -q "ns: \['common', 'share', 'teams', 'pricing', 'factcheck', 'credibility'\]" apps/web/src/i18n/i18n.ts && grep -q "escapeValue: false" apps/web/src/i18n/i18n.ts && cd apps/web && pnpm typecheck 2>&1 | tail -3 && echo OK</automated>
  </verify>
  <done>
    i18n.ts contains the updated `ns:` array with both new namespaces; the `escapeValue: false` config is still present (XSS-handling assumption from threat-row T-38-18 holds — React auto-escapes interpolation since i18next escapeValue is disabled per the existing comment 'React handles XSS' on i18n.ts:30); pnpm typecheck exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public/locales JSON → React render | Locale strings are loaded over HTTP; an attacker who can replace public/locales/*.json could XSS via i18next interpolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-18 | Tampering | apps/web/public/locales/<locale>/factcheck.json + credibility.json | accept | These files ship as static assets via Vite; tampering requires write-access to the deployed bundle, which is the same trust boundary that protects all client-side code. The strings contain no template-literal escape hatches; React auto-escapes interpolation (e.g. `{{score}}`) when rendered as text children since i18next is configured with `escapeValue: false` (see i18n.ts:30 — 'React handles XSS'). Task 3's verify block includes a regression probe asserting the `escapeValue: false` setting remains in place. |
| T-38-19 | Information Disclosure | locale string contents are visible in `/locales/{lng}/<ns>.json` to anyone who fetches them | accept | Intentional — these are user-facing UI strings, not secrets. |
</threat_model>

<verification>
- 6 new JSON files exist under apps/web/public/locales/{en,de,fr}/{factcheck,credibility}.json
- Each parses as valid JSON
- DE files contain literal UTF-8 umlauts (Größtenteils, Schließen, Glaubwürdigkeit)
- FR files contain literal UTF-8 accents (Plutôt, Méthodologie, Crédibilité, élevée, Généré)
- All five verdict keys + all three confidence keys + all three bias keys present per locale
- i18n.ts namespace array contains both 'factcheck' and 'credibility'
- pnpm typecheck exit 0
- Manual smoke test: in apps/web/, the dev server can resolve `t('factcheck:verdicts.true')` to "True" / "Wahr" / "Vrai" depending on language (deferred to Plan 38-05 component tests)
</verification>

<success_criteria>
This plan succeeds when Plan 38-05's React components can call `t('factcheck:verdicts.true')`, `t('credibility:bias.left')`, `t('factcheck:button.label')` and receive the expected localized strings without falling through to the raw key. The Plan 38-06 verification gate's i18n probe (`curl http://localhost:5173/locales/de/factcheck.json | jq '.verdicts.true'` returns `"Wahr"`) succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/38-advanced-ai-features/38-04-SUMMARY.md` listing the six new files + i18n.ts edit + the explicit FR scope-discipline note (factcheck/credibility added; auth/common/dashboard/etc. remain English-fallback per CONTEXT.md scope reduction prohibition's "split, don't reduce" boundary).
</output>
