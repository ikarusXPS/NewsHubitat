---
phase: 10-frontend-hook-library-tests
plan: 04
type: execute
wave: 2
depends_on: []
files_modified:
  - src/hooks/useMapCenter.test.ts
autonomous: true
requirements:
  - HOOK-06

must_haves:
  truths:
    - "Returns preset mapCenter when activeFocusPreset has mapCenter"
    - "Calculates center from filter regions when no preset"
    - "Returns global fallback when no preset and no regions"
  artifacts:
    - path: "src/hooks/useMapCenter.test.ts"
      provides: "Unit tests for useMapCenter hook"
      min_lines: 80
  key_links:
    - from: "src/hooks/useMapCenter.test.ts"
      to: "src/hooks/useMapCenter.ts"
      via: "import"
      pattern: "from.*useMapCenter"
---

<objective>
Write unit tests for src/hooks/useMapCenter.ts achieving 80%+ coverage.

Purpose: HOOK-06 requires useMapCenter has unit tests covering preset priority, region calculation, and global fallback.

Output: src/hooks/useMapCenter.test.ts with comprehensive test coverage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-frontend-hook-library-tests/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write useMapCenter unit tests</name>
  <files>src/hooks/useMapCenter.test.ts</files>
  <read_first>
    - src/hooks/useMapCenter.ts (source file - 39 lines, exports useMapCenter)
    - src/utils/mapCentering.ts (calculateOptimalMapView function used by hook)
    - src/store/index.ts (useAppStore structure for activeFocusPreset, filters)
    - src/test/testUtils.tsx (renderWithProviders, createTestQueryClient)
  </read_first>
  <behavior>
    Test useMapCenter hook:
    - Priority 1: When activeFocusPreset.mapCenter exists, returns it exactly
    - Priority 2: When no preset but filters.regions has values, calls calculateOptimalMapView
    - Priority 3: When no preset and no regions, returns { lat: 30, lng: 0, zoom: 2, label: 'Global View' }
    - Memoization: Returns same reference when inputs unchanged
  </behavior>
  <action>
Create src/hooks/useMapCenter.test.ts with these test suites:

1. Import dependencies and set up mocks:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { renderHook } from '@testing-library/react';
import { useMapCenter } from './useMapCenter';

// Mock the store
vi.mock('../store', () => ({
  useAppStore: vi.fn(),
}));

// Mock the mapCentering utility
vi.mock('../utils/mapCentering', () => ({
  calculateOptimalMapView: vi.fn(),
}));

import { useAppStore } from '../store';
import { calculateOptimalMapView } from '../utils/mapCentering';
```

2. Test suite structure:
- describe('useMapCenter')
  - beforeEach: reset mocks, set default store state
  - afterEach: vi.restoreAllMocks()

  - describe('Priority 1: preset mapCenter')
    - it('returns preset mapCenter when activeFocusPreset has mapCenter')
      - Mock useAppStore to return { activeFocusPreset: { mapCenter: { lat: 50, lng: 10, zoom: 5, label: 'Custom' } }, filters: { regions: [] } }
      - renderHook(() => useMapCenter())
      - expect(result.current).toEqual({ lat: 50, lng: 10, zoom: 5, label: 'Custom' })
      - expect(calculateOptimalMapView).not.toHaveBeenCalled()

  - describe('Priority 2: calculated from regions')
    - it('calculates center from filter regions when no preset')
      - Mock useAppStore to return { activeFocusPreset: null, filters: { regions: ['usa', 'europa'] } }
      - Mock calculateOptimalMapView to return { lat: 45, lng: -50, zoom: 3, label: '2 regions' }
      - renderHook(() => useMapCenter())
      - expect(calculateOptimalMapView).toHaveBeenCalledWith(['usa', 'europa'])
      - expect(result.current).toEqual({ lat: 45, lng: -50, zoom: 3, label: '2 regions' })

  - describe('Priority 3: global fallback')
    - it('returns global view when no preset and no regions')
      - Mock useAppStore to return { activeFocusPreset: null, filters: { regions: [] } }
      - renderHook(() => useMapCenter())
      - expect(result.current).toEqual({ lat: 30, lng: 0, zoom: 2, label: 'Global View' })
      - expect(calculateOptimalMapView).not.toHaveBeenCalled()

    - it('returns global view when preset exists but has no mapCenter')
      - Mock useAppStore to return { activeFocusPreset: { name: 'Test', regions: ['usa'] }, filters: { regions: [] } }
      - Result should fall through to priority 2 or 3

Per D-10: Mock useAppStore via vi.mock('../store') with controlled state per test.
Use renderHook from @testing-library/react (not react-hooks library).
  </action>
  <verify>
    <automated>npm run test -- src/hooks/useMapCenter.test.ts --reporter=verbose</automated>
    <automated>npm run test:coverage -- --reporter=json --coverage.include=src/hooks/useMapCenter.ts 2>&1 | grep -E "(useMapCenter|Statements|Branches|Functions|Lines)"</automated>
  </verify>
  <acceptance_criteria>
    - File src/hooks/useMapCenter.test.ts exists
    - File contains `vi.mock('../store'`
    - File contains `vi.mock('../utils/mapCentering'`
    - File contains `describe('useMapCenter'`
    - File tests all three priority levels
    - npm run test -- src/hooks/useMapCenter.test.ts exits 0
    - Coverage for src/hooks/useMapCenter.ts shows >= 80% lines
  </acceptance_criteria>
  <done>All tests pass, coverage >= 80% for useMapCenter.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-code | Test file only runs in test environment, no production impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | I | test data | accept | Mock store state only |
</threat_model>

<verification>
```bash
npm run test -- src/hooks/useMapCenter.test.ts --reporter=verbose
npm run test:coverage -- --coverage.include=src/hooks/useMapCenter.ts
```
</verification>

<success_criteria>
- [ ] Test file created at src/hooks/useMapCenter.test.ts
- [ ] All tests pass
- [ ] Coverage >= 80% for src/hooks/useMapCenter.ts
- [ ] Tests cover preset priority, region calculation, global fallback
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-hook-library-tests/10-04-SUMMARY.md`
</output>
