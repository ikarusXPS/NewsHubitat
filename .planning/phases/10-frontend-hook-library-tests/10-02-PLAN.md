---
phase: 10-frontend-hook-library-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/historySummarizer.test.ts
autonomous: true
requirements:
  - LIB-02

must_haves:
  truths:
    - "summarizeHistory returns empty string for empty array"
    - "extractTopics identifies conflict/political/humanitarian keywords"
    - "prepareOptimizedHistory keeps recent messages full, summarizes older"
    - "estimateHistoryTokens calculates reasonable token count"
  artifacts:
    - path: "src/lib/historySummarizer.test.ts"
      provides: "Unit tests for historySummarizer library"
      min_lines: 120
  key_links:
    - from: "src/lib/historySummarizer.test.ts"
      to: "src/lib/historySummarizer.ts"
      via: "import"
      pattern: "from.*historySummarizer"
---

<objective>
Write unit tests for src/lib/historySummarizer.ts achieving 80%+ coverage.

Purpose: LIB-02 requires historySummarizer has unit tests covering topic extraction, message compression, and empty history edge cases.

Output: src/lib/historySummarizer.test.ts with comprehensive test coverage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-frontend-hook-library-tests/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write historySummarizer unit tests</name>
  <files>src/lib/historySummarizer.test.ts</files>
  <read_first>
    - src/lib/historySummarizer.ts (source file - 170 lines, exports summarizeHistory, prepareOptimizedHistory, estimateHistoryTokens)
    - src/lib/utils.test.ts (testing patterns - describe/it structure)
  </read_first>
  <behavior>
    Test summarizeHistory:
    - Empty messages array returns ''
    - Single user message extracts topics and question
    - Multiple messages collects all topics, shows last 2-3 questions
    - Output contains "Bisherige Themen:" when topics found
    - Output contains message count "[X Fragen, Y Antworten bisher]"

    Test extractTopics (internal, tested via summarizeHistory):
    - "What about Gaza conflict?" extracts ["gaza", "conflict"]
    - "humanitarian aid refugee" extracts ["humanitarian", "aid", "refugee"]
    - "Der Krieg in Ukraine" extracts ["krieg", "ukraine"]
    - Limits to 5 topics max

    Test extractCoreQuestion (internal, tested via summarizeHistory):
    - Removes filler "hi", "hello", "kannst du", "please"
    - Truncates to 80 chars with "..."

    Test prepareOptimizedHistory:
    - Returns empty array for empty input
    - Returns messages as-is when <= keepRecentCount + 2
    - Filters out welcome messages containing "Signal Analysis AI online"
    - Older messages summarized, recent messages kept in full
    - Summary appears as assistant message with "[Konversationskontext: ...]"

    Test estimateHistoryTokens:
    - Empty array returns 0
    - Single message: (content.length + 10) / 4 rounded up
    - Multiple messages sum correctly
  </behavior>
  <action>
Create src/lib/historySummarizer.test.ts with these test suites:

1. Import dependencies:
```typescript
import { describe, it, expect } from 'vitest';
import { summarizeHistory, prepareOptimizedHistory, estimateHistoryTokens } from './historySummarizer';
```

2. Define Message interface locally for test data:
```typescript
interface Message {
  role: 'user' | 'assistant';
  content: string;
}
```

3. Test suite for summarizeHistory:
- describe('summarizeHistory')
  - describe('empty history per D-07')
    - it('returns empty string for empty array')
  - describe('topic extraction')
    - it('extracts conflict-related keywords')
    - it('extracts humanitarian keywords')
    - it('limits to 5 topics maximum')
  - describe('question extraction')
    - it('extracts core question removing filler words')
    - it('truncates long questions to 80 chars')
  - describe('output format')
    - it('includes "Bisherige Themen:" section')
    - it('includes message count in brackets')

4. Test suite for prepareOptimizedHistory:
- describe('prepareOptimizedHistory')
  - it('returns empty array for empty input')
  - it('returns messages as-is when few messages')
  - it('filters welcome messages containing "Signal Analysis AI online"')
  - it('summarizes older messages, keeps recent in full')
  - it('adds context summary as assistant message')

5. Test suite for estimateHistoryTokens:
- describe('estimateHistoryTokens')
  - it('returns 0 for empty array')
  - it('estimates ~13 tokens for 40-char message') // (40+10)/4 = 12.5 -> 13
  - it('sums multiple messages correctly')

Test data: Create message arrays inline with user/assistant roles.
Per D-08: Use 5-10 representative bilingual keywords, not exhaustive.
  </action>
  <verify>
    <automated>npm run test -- src/lib/historySummarizer.test.ts --reporter=verbose</automated>
    <automated>npm run test:coverage -- --reporter=json --coverage.include=src/lib/historySummarizer.ts 2>&1 | grep -E "(historySummarizer|Statements|Branches|Functions|Lines)"</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/historySummarizer.test.ts exists
    - File contains `describe('summarizeHistory'`
    - File contains `describe('prepareOptimizedHistory'`
    - File contains `describe('estimateHistoryTokens'`
    - npm run test -- src/lib/historySummarizer.test.ts exits 0
    - Coverage for src/lib/historySummarizer.ts shows >= 80% lines
  </acceptance_criteria>
  <done>All tests pass, coverage >= 80% for historySummarizer.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-code | Test file only runs in test environment, no production impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | I | test data | accept | Mock data only, no real conversation content |
</threat_model>

<verification>
```bash
npm run test -- src/lib/historySummarizer.test.ts --reporter=verbose
npm run test:coverage -- --coverage.include=src/lib/historySummarizer.ts
```
</verification>

<success_criteria>
- [ ] Test file created at src/lib/historySummarizer.test.ts
- [ ] All tests pass
- [ ] Coverage >= 80% for src/lib/historySummarizer.ts
- [ ] Tests cover empty history, topic extraction, message optimization
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-hook-library-tests/10-02-SUMMARY.md`
</output>
