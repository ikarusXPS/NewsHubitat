---
phase: 10-frontend-hook-library-tests
plan: 06
type: execute
wave: 3
depends_on: []
files_modified:
  - src/hooks/useKeyboardShortcuts.test.ts
autonomous: true
requirements:
  - HOOK-05

must_haves:
  truths:
    - "Number keys 1-6 navigate to respective routes"
    - "/ focuses search input"
    - "r triggers onRefresh callback"
    - "Escape triggers onCloseModal callback"
    - "Shortcuts ignored when typing in input/textarea"
    - "Shortcuts ignored when hasModifier (ctrl/meta/alt)"
    - "enabled=false disables all shortcuts"
    - "getShortcutGroups returns navigation/actions/feed groups"
  artifacts:
    - path: "src/hooks/useKeyboardShortcuts.test.ts"
      provides: "Unit tests for useKeyboardShortcuts hook"
      min_lines: 180
  key_links:
    - from: "src/hooks/useKeyboardShortcuts.test.ts"
      to: "src/hooks/useKeyboardShortcuts.ts"
      via: "import"
      pattern: "from.*useKeyboardShortcuts"
---

<objective>
Write unit tests for src/hooks/useKeyboardShortcuts.ts achieving 80%+ coverage.

Purpose: HOOK-05 requires useKeyboardShortcuts has unit tests covering navigation keys, action keys, and input field bypass.

Output: src/hooks/useKeyboardShortcuts.test.ts with comprehensive test coverage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-frontend-hook-library-tests/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write useKeyboardShortcuts unit tests</name>
  <files>src/hooks/useKeyboardShortcuts.test.ts</files>
  <read_first>
    - src/hooks/useKeyboardShortcuts.ts (source file - 217 lines, exports useKeyboardShortcuts, KEYBOARD_SHORTCUTS, getShortcutGroups)
    - src/test/testUtils.tsx (renderWithProviders for router context)
    - src/lib/utils.test.ts (testing patterns)
  </read_first>
  <behavior>
    Test useKeyboardShortcuts hook:
    - Navigation keys (1-6) call navigate() with correct routes:
      - '1' -> '/'
      - '2' -> '/analysis'
      - '3' -> '/monitor'
      - '4' -> '/timeline'
      - '5' -> '/event-map'
      - '6' -> '/community'
    - Action keys:
      - '/' calls onFocusSearch callback
      - 'r' calls onRefresh callback
      - 'Escape' calls onCloseModal callback
      - 'Shift+?' calls onShowHelp callback
    - Feed navigation keys:
      - 'j' calls onNextArticle
      - 'k' calls onPrevArticle
      - 'o' calls onOpenArticle
      - 'b' calls onBookmark
      - 'm' calls onMarkRead
    - Input field bypass: shortcuts ignored when target is INPUT/TEXTAREA/contentEditable
      - Exception: Escape still works in input fields
    - Modifier key bypass: shortcuts ignored when ctrlKey/metaKey/altKey is true
    - enabled=false: all shortcuts disabled
    - Cleanup: removeEventListener on unmount

    Test getShortcutGroups:
    - Returns object with navigation, actions, feed arrays
    - Each array contains { key, description } objects

    Test KEYBOARD_SHORTCUTS constant:
    - Exports array of shortcut definitions
  </behavior>
  <action>
Create src/hooks/useKeyboardShortcuts.test.ts with these test suites:

1. Import dependencies and setup mocks:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { renderHook } from '@testing-library/react';
import { fireEvent } from '@testing-library/react';
import { MemoryRouter } from 'react-router-dom';
import { useKeyboardShortcuts, KEYBOARD_SHORTCUTS, getShortcutGroups } from './useKeyboardShortcuts';

// Mock useNavigate
const mockNavigate = vi.fn();
vi.mock('react-router-dom', async () => {
  const actual = await vi.importActual('react-router-dom');
  return {
    ...actual,
    useNavigate: () => mockNavigate,
  };
});
```

2. Wrapper for renderHook:
```typescript
const wrapper = ({ children }: { children: React.ReactNode }) => (
  <MemoryRouter>{children}</MemoryRouter>
);
```

3. Test suite structure:
- describe('useKeyboardShortcuts')
  - beforeEach: mockNavigate.mockClear()
  - afterEach: vi.restoreAllMocks()

  - describe('navigation keys (1-6) per D-06')
    - it('navigates to / on key 1')
      - renderHook with wrapper
      - fireEvent.keyDown(document, { key: '1' })
      - expect(mockNavigate).toHaveBeenCalledWith('/')
    - it('navigates to /analysis on key 2')
    - it('navigates to /monitor on key 3')
    - it('navigates to /timeline on key 4')
    - it('navigates to /event-map on key 5')
    - it('navigates to /community on key 6')

  - describe('action keys')
    - it('calls onFocusSearch on /')
    - it('calls onRefresh on r')
    - it('calls onCloseModal on Escape')
    - it('calls onShowHelp on Shift+?')

  - describe('feed navigation keys')
    - it('calls onNextArticle on j')
    - it('calls onPrevArticle on k')
    - it('calls onOpenArticle on o')
    - it('calls onBookmark on b')
    - it('calls onMarkRead on m')

  - describe('input field bypass')
    - it('ignores shortcuts when target is INPUT')
      - Create input element, fireEvent.keyDown on it
      - expect(mockNavigate).not.toHaveBeenCalled()
    - it('ignores shortcuts when target is TEXTAREA')
    - it('ignores shortcuts when target is contentEditable')
    - it('allows Escape even in input fields')

  - describe('modifier key bypass')
    - it('ignores shortcuts when ctrlKey is true')
    - it('ignores shortcuts when metaKey is true')
    - it('ignores shortcuts when altKey is true')

  - describe('enabled option')
    - it('disables all shortcuts when enabled=false')

  - describe('cleanup')
    - it('removes event listener on unmount')
      - Spy on document.removeEventListener
      - unmount hook
      - expect removeEventListener called

- describe('getShortcutGroups')
  - it('returns navigation shortcuts')
  - it('returns actions shortcuts')
  - it('returns feed shortcuts')

- describe('KEYBOARD_SHORTCUTS')
  - it('exports array of shortcut definitions')
  - it('each shortcut has key, handler, description')

Per D-03: Mock useNavigate via vi.mock('react-router-dom').
Per D-06: Use fireEvent.keyDown(document, { key: '1' }) for keyboard tests.
  </action>
  <verify>
    <automated>npm run test -- src/hooks/useKeyboardShortcuts.test.ts --reporter=verbose</automated>
    <automated>npm run test:coverage -- --reporter=json --coverage.include=src/hooks/useKeyboardShortcuts.ts 2>&1 | grep -E "(useKeyboardShortcuts|Statements|Branches|Functions|Lines)"</automated>
  </verify>
  <acceptance_criteria>
    - File src/hooks/useKeyboardShortcuts.test.ts exists
    - File contains `vi.mock('react-router-dom'`
    - File contains `fireEvent.keyDown(document`
    - File contains `describe('useKeyboardShortcuts'`
    - File contains `describe('getShortcutGroups'`
    - File tests navigation, actions, input bypass, modifier bypass
    - npm run test -- src/hooks/useKeyboardShortcuts.test.ts exits 0
    - Coverage for src/hooks/useKeyboardShortcuts.ts shows >= 80% lines
  </acceptance_criteria>
  <done>All tests pass, coverage >= 80% for useKeyboardShortcuts.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-code | Test file only runs in test environment, no production impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | I | test data | accept | Mock navigation only |
</threat_model>

<verification>
```bash
npm run test -- src/hooks/useKeyboardShortcuts.test.ts --reporter=verbose
npm run test:coverage -- --coverage.include=src/hooks/useKeyboardShortcuts.ts
```
</verification>

<success_criteria>
- [ ] Test file created at src/hooks/useKeyboardShortcuts.test.ts
- [ ] All tests pass
- [ ] Coverage >= 80% for src/hooks/useKeyboardShortcuts.ts
- [ ] Tests cover navigation, actions, input bypass, modifier bypass
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-hook-library-tests/10-06-SUMMARY.md`
</output>
