---
phase: 10-frontend-hook-library-tests
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - src/hooks/useBackendStatus.test.ts
autonomous: true
requirements:
  - HOOK-02

must_haves:
  truths:
    - "Initial state isOnline=true, isChecking=false"
    - "Successful health check sets isOnline=true, lastCheck to Date"
    - "Failed health check sets isOnline=false, error message"
    - "Timeout (5s) aborts request and sets error"
    - "Polling interval runs checkHealth every 30s"
    - "retry() triggers immediate health check"
  artifacts:
    - path: "src/hooks/useBackendStatus.test.ts"
      provides: "Unit tests for useBackendStatus hook"
      min_lines: 120
  key_links:
    - from: "src/hooks/useBackendStatus.test.ts"
      to: "src/hooks/useBackendStatus.ts"
      via: "import"
      pattern: "from.*useBackendStatus"
---

<objective>
Write unit tests for src/hooks/useBackendStatus.ts achieving 80%+ coverage.

Purpose: HOOK-02 requires useBackendStatus has unit tests covering health check success/failure, timeout handling, and retry function.

Output: src/hooks/useBackendStatus.test.ts with comprehensive test coverage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-frontend-hook-library-tests/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write useBackendStatus unit tests</name>
  <files>src/hooks/useBackendStatus.test.ts</files>
  <read_first>
    - src/hooks/useBackendStatus.ts (source file - 78 lines, exports useBackendStatus)
    - src/test/testUtils.tsx (mockFetch, mockFetchError utilities)
    - src/lib/utils.test.ts (vi.useFakeTimers pattern)
  </read_first>
  <behavior>
    Test useBackendStatus hook:
    - Initial state: { isOnline: true, isChecking: false, lastCheck: null, error: null }
    - After mount: checkHealth called automatically, isChecking becomes true then false
    - Successful fetch (response.ok): isOnline=true, lastCheck set, error=null
    - Failed fetch (response.ok=false, status=500): isOnline=false, error="Backend returned 500"
    - Network error (fetch throws): isOnline=false, error contains error message
    - Timeout (5s): AbortController aborts, error set
    - Polling: setInterval calls checkHealth every 30000ms
    - retry(): Triggers checkHealth manually
    - Cleanup: clearInterval on unmount
  </behavior>
  <action>
Create src/hooks/useBackendStatus.test.ts with these test suites:

1. Import dependencies and setup:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { renderHook, act, waitFor } from '@testing-library/react';
import { useBackendStatus } from './useBackendStatus';
```

2. Global setup for fetch mock:
```typescript
beforeEach(() => {
  vi.useFakeTimers();
  // Mock successful fetch by default
  vi.stubGlobal('fetch', vi.fn().mockResolvedValue({
    ok: true,
    status: 200,
    json: async () => ({ status: 'ok' }),
  }));
});

afterEach(() => {
  vi.useRealTimers();
  vi.unstubAllGlobals();
  vi.restoreAllMocks();
});
```

3. Test suite structure:
- describe('useBackendStatus')
  - describe('initial state')
    - it('starts with isOnline=true, isChecking=false')

  - describe('health check on mount')
    - it('calls /api/health endpoint')
    - it('sets isChecking=true during check')
    - it('sets lastCheck after successful check')

  - describe('successful health check')
    - it('sets isOnline=true and clears error')
    - it('updates lastCheck to current time')

  - describe('failed health check - HTTP error')
    - it('sets isOnline=false when response.ok is false')
    - it('sets error message with status code')
      - Mock fetch to return { ok: false, status: 500 }
      - expect error to be "Backend returned 500"

  - describe('failed health check - network error')
    - it('sets isOnline=false on fetch rejection')
    - it('captures error message')
      - Mock fetch to reject with new Error('Network error')

  - describe('timeout handling')
    - it('aborts request after 5 seconds')
      - Mock fetch to never resolve (hang)
      - Advance timers by 5000ms
      - expect AbortError to be caught

  - describe('polling interval per D-12')
    - it('calls checkHealth every 30 seconds')
      - renderHook, wait for initial check
      - vi.advanceTimersByTime(30000)
      - expect(fetch).toHaveBeenCalledTimes(2)

  - describe('retry function')
    - it('triggers immediate health check')
      - const { result } = renderHook
      - await initial check
      - act(() => result.current.retry())
      - expect(fetch).toHaveBeenCalled again

  - describe('cleanup')
    - it('clears interval on unmount')
      - const { unmount } = renderHook
      - unmount()
      - vi.advanceTimersByTime(60000)
      - expect(fetch).not.toHaveBeenCalled after unmount

Per D-05: Mock global fetch via vi.stubGlobal('fetch', vi.fn()).
Per D-12: Use vi.useFakeTimers() with vi.advanceTimersByTime() for polling/timeout tests.
Use act() for state updates, waitFor() for async assertions.
  </action>
  <verify>
    <automated>npm run test -- src/hooks/useBackendStatus.test.ts --reporter=verbose</automated>
    <automated>npm run test:coverage -- --reporter=json --coverage.include=src/hooks/useBackendStatus.ts 2>&1 | grep -E "(useBackendStatus|Statements|Branches|Functions|Lines)"</automated>
  </verify>
  <acceptance_criteria>
    - File src/hooks/useBackendStatus.test.ts exists
    - File contains `vi.stubGlobal('fetch'`
    - File contains `vi.useFakeTimers()`
    - File contains `describe('useBackendStatus'`
    - File tests success, HTTP error, network error, timeout, polling, retry
    - npm run test -- src/hooks/useBackendStatus.test.ts exits 0
    - Coverage for src/hooks/useBackendStatus.ts shows >= 80% lines
  </acceptance_criteria>
  <done>All tests pass, coverage >= 80% for useBackendStatus.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-code | Test file only runs in test environment, no production impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | I | test data | accept | Mock fetch responses only |
</threat_model>

<verification>
```bash
npm run test -- src/hooks/useBackendStatus.test.ts --reporter=verbose
npm run test:coverage -- --coverage.include=src/hooks/useBackendStatus.ts
```
</verification>

<success_criteria>
- [ ] Test file created at src/hooks/useBackendStatus.test.ts
- [ ] All tests pass
- [ ] Coverage >= 80% for src/hooks/useBackendStatus.ts
- [ ] Tests cover success, failure, timeout, polling, retry
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-hook-library-tests/10-05-SUMMARY.md`
</output>
