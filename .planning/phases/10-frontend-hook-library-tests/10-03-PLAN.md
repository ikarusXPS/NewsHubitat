---
phase: 10-frontend-hook-library-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/personalization.test.ts
autonomous: true
requirements:
  - LIB-03

must_haves:
  truths:
    - "extractKeywords removes stop words (DE/EN) and short words"
    - "extractUserInterests weights recent articles 2x per D-15"
    - "scoreArticleForUser calculates topic + region + recency score"
    - "getRecommendations excludes already-read articles per D-21"
    - "formatTopicBadge capitalizes first letter"
  artifacts:
    - path: "src/lib/personalization.test.ts"
      provides: "Unit tests for personalization library"
      min_lines: 180
  key_links:
    - from: "src/lib/personalization.test.ts"
      to: "src/lib/personalization.ts"
      via: "import"
      pattern: "from.*personalization"
---

<objective>
Write unit tests for src/lib/personalization.ts achieving 80%+ coverage.

Purpose: LIB-03 requires personalization has unit tests covering interest extraction, article scoring, and recommendation generation with cold start scenarios.

Output: src/lib/personalization.test.ts with comprehensive test coverage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-frontend-hook-library-tests/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write personalization unit tests</name>
  <files>src/lib/personalization.test.ts</files>
  <read_first>
    - src/lib/personalization.ts (source file - 203 lines, exports extractKeywords, extractUserInterests, scoreArticleForUser, getRecommendations, formatTopicBadge)
    - src/test/factories.ts (getMockNewsArticle, getMockArticleFromRegion)
    - src/lib/utils.test.ts (testing patterns)
  </read_first>
  <behavior>
    Test extractKeywords:
    - "the quick brown fox" returns ["quick", "brown"] (removes "the", and "fox" if <= 3 chars)
    - "der schnelle Hund" returns ["schnelle", "hund"] (German stop words removed)
    - Words <= 3 chars filtered out (different from articleRelevance which uses > 2)

    Test extractUserInterests:
    - Empty history returns empty maps and empty topTopics
    - Single article extracts title keywords and perspective region
    - Recent articles (< 7 days) weighted 2x per D-15
    - Article topics array extracted and weighted 1.5x
    - topTopics contains top 3 topics sorted by weight

    Test scoreArticleForUser:
    - Returns { score: 0, matchedTopic: null } for no interest overlap
    - Matches title keywords against user topics
    - Recent topic matches weighted 1.5x higher
    - Regional preference adds 0.5x bonus
    - Recency boost: < 6h = 1.3x, < 24h = 1.1x

    Test getRecommendations:
    - Excludes articles in history (already read) per D-21
    - Returns articles sorted by score descending
    - Limits to specified count (default 12)
    - Only includes articles with score > 0

    Test formatTopicBadge:
    - "climate" returns "Climate"
    - "POLITICS" returns "POLITICS" (only first char capitalized, rest unchanged)
  </behavior>
  <action>
Create src/lib/personalization.test.ts with these test suites:

1. Import dependencies:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import {
  extractKeywords,
  extractUserInterests,
  scoreArticleForUser,
  getRecommendations,
  formatTopicBadge,
} from './personalization';
import { getMockNewsArticle, getMockArticleFromRegion, resetIdCounter } from '../test/factories';
import type { PerspectiveRegion } from '../types';
```

2. Define HistoryEntry interface for test data:
```typescript
interface HistoryEntry {
  articleId: string;
  timestamp: number;
  readCount?: number;
}
```

3. Test suite for extractKeywords:
- describe('extractKeywords')
  - it('removes English stop words')
  - it('removes German stop words')
  - it('filters words with 3 or fewer characters')
  - it('lowercases all words')
  - it('removes punctuation')

4. Test suite for extractUserInterests:
- describe('extractUserInterests')
  - describe('cold start per D-07')
    - it('returns empty interests for empty history')
    - it('returns empty topTopics array')
  - describe('interest extraction per D-09, D-10')
    - it('extracts keywords from article titles')
    - it('tracks region preferences')
  - describe('recency weighting per D-15 (7-day window)')
    - it('weights recent articles 2x')
    - it('tracks recent topics separately')
  - describe('topTopics per D-12')
    - it('returns top 3 topics sorted by weight')

5. Test suite for scoreArticleForUser:
- describe('scoreArticleForUser')
  - it('returns zero score for no interest overlap')
  - it('scores higher for topic matches')
  - it('applies 1.5x weight for recent topic matches')
  - it('adds regional preference bonus')
  - it('applies recency boost for fresh articles')

6. Test suite for getRecommendations:
- describe('getRecommendations')
  - describe('read article exclusion per D-21')
    - it('excludes already-read articles')
  - it('returns articles sorted by score')
  - it('respects limit parameter')
  - it('only includes articles with positive score')

7. Test suite for formatTopicBadge:
- describe('formatTopicBadge')
  - it('capitalizes first letter')
  - it('preserves rest of string')

Use vi.useFakeTimers() + vi.setSystemTime() for recency tests.
Create article map for extractUserInterests tests.
Per D-08: Use 5-10 representative DE/EN stop words.
Per D-09: Test exact 7-day threshold for recency.
  </action>
  <verify>
    <automated>npm run test -- src/lib/personalization.test.ts --reporter=verbose</automated>
    <automated>npm run test:coverage -- --reporter=json --coverage.include=src/lib/personalization.ts 2>&1 | grep -E "(personalization|Statements|Branches|Functions|Lines)"</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/personalization.test.ts exists
    - File contains `describe('extractKeywords'`
    - File contains `describe('extractUserInterests'`
    - File contains `describe('scoreArticleForUser'`
    - File contains `describe('getRecommendations'`
    - File contains `describe('formatTopicBadge'`
    - npm run test -- src/lib/personalization.test.ts exits 0
    - Coverage for src/lib/personalization.ts shows >= 80% lines
  </acceptance_criteria>
  <done>All tests pass, coverage >= 80% for personalization.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-code | Test file only runs in test environment, no production impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | I | test data | accept | Mock data only, no real user preferences |
</threat_model>

<verification>
```bash
npm run test -- src/lib/personalization.test.ts --reporter=verbose
npm run test:coverage -- --coverage.include=src/lib/personalization.ts
```
</verification>

<success_criteria>
- [ ] Test file created at src/lib/personalization.test.ts
- [ ] All tests pass
- [ ] Coverage >= 80% for src/lib/personalization.ts
- [ ] Tests cover cold start, recency weighting, read exclusion
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-hook-library-tests/10-03-SUMMARY.md`
</output>
