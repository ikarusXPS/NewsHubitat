---
phase: 10-frontend-hook-library-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/articleRelevance.test.ts
autonomous: true
requirements:
  - LIB-01

must_haves:
  truths:
    - "extractKeywords removes stop words and normalizes text"
    - "calculateKeywordScore returns 0-50 based on keyword matches"
    - "calculateRecencyScore returns 0-30 based on article age"
    - "getTopRelevantArticles returns most relevant articles with diversity"
    - "estimateContextTokens returns reasonable token estimate"
  artifacts:
    - path: "src/lib/articleRelevance.test.ts"
      provides: "Unit tests for articleRelevance library"
      min_lines: 150
  key_links:
    - from: "src/lib/articleRelevance.test.ts"
      to: "src/lib/articleRelevance.ts"
      via: "import"
      pattern: "from.*articleRelevance"
---

<objective>
Write unit tests for src/lib/articleRelevance.ts achieving 80%+ coverage.

Purpose: LIB-01 requires articleRelevance has unit tests covering keyword extraction, scoring algorithms, and edge cases.

Output: src/lib/articleRelevance.test.ts with comprehensive test coverage
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-frontend-hook-library-tests/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write articleRelevance unit tests</name>
  <files>src/lib/articleRelevance.test.ts</files>
  <read_first>
    - src/lib/articleRelevance.ts (source file - 190 lines, exports getTopRelevantArticles, estimateContextTokens)
    - src/lib/utils.test.ts (testing patterns - vi.useFakeTimers, describe/it structure)
    - src/test/factories.ts (getMockNewsArticle, getMockArticleFromRegion factory functions)
  </read_first>
  <behavior>
    Test extractKeywords (internal function tested via getTopRelevantArticles):
    - Empty string returns empty array
    - "What is happening in Gaza?" returns ["happening", "gaza"] (removes stop words "what", "is", "in")
    - "der die das Konflikt" returns ["konflikt"] (German stop words removed)
    - Words <= 2 chars filtered out

    Test calculateKeywordScore (via getTopRelevantArticles):
    - No keywords returns neutral score (25 points)
    - Title match gets extra 0.5 bonus per keyword
    - Score normalized to 0-50 range

    Test calculateRecencyScore (via getTopRelevantArticles):
    - Article <= 6 hours old: 30 points
    - Article 6-12 hours old: 25 points
    - Article 12-24 hours old: 20 points
    - Article 24-48 hours old: 15 points
    - Article 48-72 hours old: 10 points
    - Article 72-168 hours old: 5 points
    - Article > 168 hours old: 0 points

    Test getTopRelevantArticles:
    - Returns all articles if articles.length <= limit
    - Returns exactly limit articles when more available
    - Higher scored articles appear first
    - Diversity bonus: underrepresented regions get +10 (< 2 articles) or +5 (< 3 articles)

    Test estimateContextTokens:
    - Empty array returns 0
    - Single article with 100-char title + 150-char summary returns ~70 tokens ((100+30+150)/4)
    - Multiple articles sum correctly
  </behavior>
  <action>
Create src/lib/articleRelevance.test.ts with these test suites:

1. Import dependencies:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
import { getTopRelevantArticles, estimateContextTokens } from './articleRelevance';
import { getMockNewsArticle, getMockArticleFromRegion, resetIdCounter } from '../test/factories';
import type { PerspectiveRegion } from '../types';
```

2. Test suite for getTopRelevantArticles:
- describe('getTopRelevantArticles')
  - describe('basic behavior')
    - it('returns all articles when count <= limit')
    - it('returns exactly limit articles when more available')
  - describe('keyword scoring')
    - it('scores articles higher when question keywords match title')
    - it('filters stop words from question')
    - it('gives neutral score when no specific keywords')
  - describe('recency scoring') - use vi.useFakeTimers() + vi.setSystemTime()
    - it('scores very recent articles (< 6h) with 30 points')
    - it('scores older articles (> 7 days) with 0 points')
  - describe('diversity bonus')
    - it('includes articles from underrepresented regions')
    - it('limits articles per region to maintain diversity')

3. Test suite for estimateContextTokens:
- describe('estimateContextTokens')
  - it('returns 0 for empty array')
  - it('estimates tokens based on title and summary length')
  - it('truncates summary to 150 chars for estimation')

Use factories: getMockNewsArticle({ title, summary, publishedAt, perspective }) for test data.
Use vi.useFakeTimers()/vi.setSystemTime() for recency tests per D-12.
Reset factories in beforeEach: resetIdCounter().
  </action>
  <verify>
    <automated>npm run test -- src/lib/articleRelevance.test.ts --reporter=verbose</automated>
    <automated>npm run test:coverage -- --reporter=json --coverage.include=src/lib/articleRelevance.ts 2>&1 | grep -E "(articleRelevance|Statements|Branches|Functions|Lines)"</automated>
  </verify>
  <acceptance_criteria>
    - File src/lib/articleRelevance.test.ts exists
    - File contains `describe('getTopRelevantArticles'`
    - File contains `describe('estimateContextTokens'`
    - File imports from '../test/factories'
    - npm run test -- src/lib/articleRelevance.test.ts exits 0
    - Coverage for src/lib/articleRelevance.ts shows >= 80% lines
  </acceptance_criteria>
  <done>All tests pass, coverage >= 80% for articleRelevance.ts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-code | Test file only runs in test environment, no production impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I | test data | accept | Mock data only, no real user content |
</threat_model>

<verification>
```bash
npm run test -- src/lib/articleRelevance.test.ts --reporter=verbose
npm run test:coverage -- --coverage.include=src/lib/articleRelevance.ts
```
</verification>

<success_criteria>
- [ ] Test file created at src/lib/articleRelevance.test.ts
- [ ] All tests pass
- [ ] Coverage >= 80% for src/lib/articleRelevance.ts
- [ ] Tests cover keyword extraction, scoring, recency, diversity
</success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-hook-library-tests/10-01-SUMMARY.md`
</output>
