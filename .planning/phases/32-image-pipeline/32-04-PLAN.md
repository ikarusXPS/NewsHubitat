---
phase: 32-image-pipeline
plan: 04
type: execute
wave: 2
depends_on:
  - 32-01
  - 32-02
  - 32-03
files_modified: []
autonomous: false
requirements:
  - IMG-01
  - IMG-02
  - IMG-03
must_haves:
  truths:
    - "Lighthouse CI measures image payload improvement"
    - "All three card components verified rendering with ResponsiveImage"
    - "Priority loading confirmed for first 6 SignalCards"
    - "Lazy loading confirmed for cards 7+ and other components"
  artifacts: []
  key_links: []
---

<objective>
Validate image pipeline migration via Lighthouse CI and manual verification per D-10, D-11, D-12.

Purpose: Confirm the 50% image payload reduction target (D-13) by comparing Lighthouse metrics before and after migration. Manual DevTools Network tab inspection provides visual confidence.

Output: Verification report documenting image payload improvement and correct loading behavior.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-image-pipeline/32-CONTEXT.md
@.planning/phases/32-image-pipeline/32-RESEARCH.md
@.planning/phases/32-image-pipeline/32-PATTERNS.md
@.planning/phases/32-image-pipeline/32-01-SUMMARY.md
@.planning/phases/32-image-pipeline/32-02-SUMMARY.md
@.planning/phases/32-image-pipeline/32-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and build</name>
  <files></files>
  <read_first>
    - src/components/SignalCard.tsx
    - src/components/NewsCardPremium.tsx
    - src/components/ForYouCard.tsx
  </read_first>
  <action>
1. Run TypeScript type checking:
   ```bash
   npm run typecheck
   ```

2. Run unit tests to ensure no regressions:
   ```bash
   npm run test:run
   ```

3. Run production build:
   ```bash
   npm run build
   ```

4. Verify all three components have ResponsiveImage integration:
   ```bash
   grep -l "import.*ResponsiveImage" src/components/SignalCard.tsx src/components/NewsCardPremium.tsx src/components/ForYouCard.tsx
   ```
   Expected: All three files listed.
  </action>
  <verify>
    <automated>npm run typecheck && npm run test:run && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - npm run typecheck exits 0
    - npm run test:run exits 0 with no test failures
    - npm run build exits 0
    - All three component files contain ResponsiveImage import
  </acceptance_criteria>
  <done>All tests pass, build succeeds, ResponsiveImage integrated in all three components</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Image pipeline migration complete for all three card components:
    1. SignalCard: ResponsiveImage with priority={index < 6} for first 6 cards
    2. NewsCardPremium: ResponsiveImage wrapped in motion.div for hover animation
    3. ForYouCard: ResponsiveImage with fixed sizes="280px" for carousel
  </what-built>
  <how-to-verify>
    **Verification Steps:**

    1. **Start dev server:**
       ```bash
       npm run dev
       ```

    2. **Open Dashboard (http://localhost:5173):**
       - Open Chrome DevTools > Network tab
       - Filter by "Img" or "image"
       - Observe first 6 article cards load immediately (fetchpriority=high)
       - Scroll down - observe cards 7+ load on scroll (IntersectionObserver)

    3. **Check image formats:**
       - Click an image request in Network tab
       - Look at Response Headers for Content-Type
       - If Cloudinary configured: should see image/avif or image/webp
       - If no Cloudinary: original URLs load (fallback working)

    4. **Verify SignalCard fallback:**
       - Find an article without an image (or temporarily remove imageUrl from mock data)
       - Should see perspective-colored gradient placeholder with Radio icon
       - NOT a generic gray placeholder

    5. **Verify NewsCardPremium hover (Analysis page http://localhost:5173/analysis):**
       - Hover over a card with image
       - Image should scale smoothly (motion.div animation)

    6. **Verify ForYouCard carousel (Dashboard below main feed):**
       - Scroll to "For You" section
       - Images should lazy load when scrolled into view
       - Hover should show scale effect

    **Expected:**
    - Priority images load first (Network timing shows early)
    - Lazy images load on scroll
    - AVIF/WebP format if Cloudinary configured
    - Hover animations work on all components
    - Perspective fallback preserved in SignalCard
  </how-to-verify>
  <resume-signal>Type "approved" to confirm image pipeline working, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Validation-only plan, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new code, validation only |
</threat_model>

<verification>
1. All tests pass: `npm run test:run`
2. Build succeeds: `npm run build`
3. Human verification of image loading behavior
4. Human verification of hover animations
5. Human verification of fallback placeholders
</verification>

<success_criteria>
- All unit tests pass
- Production build succeeds
- SignalCard priority loading confirmed (first 6 eager, rest lazy)
- NewsCardPremium hover animation works
- ForYouCard carousel lazy loading works
- SignalCard perspective fallback preserved
- Image formats use AVIF/WebP when Cloudinary configured
</success_criteria>

<output>
After completion, create `.planning/phases/32-image-pipeline/32-04-SUMMARY.md`

Then create phase verification report:
`.planning/phases/32-image-pipeline/32-VERIFICATION.md`
</output>
