---
phase: 36.2-close-36-debt-schema-models-cleanup
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/server/config/stripe.ts
  - apps/web/src/generated/prisma/internal/class.ts
  - apps/web/src/generated/prisma/internal/prismaNamespace.ts
  - apps/web/src/generated/prisma/internal/prismaNamespaceBrowser.ts
  - apps/web/src/generated/prisma/models/User.ts
  - apps/web/src/generated/prisma/models/ProcessedWebhookEvent.ts
  - apps/web/src/generated/prisma/models/ReferralReward.ts
  - apps/web/src/generated/prisma/models/Campaign.ts
  - apps/web/src/generated/prisma/models/StudentVerification.ts
autonomous: true
requirements: [PAY-02, PAY-03, PAY-05, PAY-07]
tags: [prisma, db-push, generate, enum-migration, stripe-config, blocking]

must_haves:
  truths:
    - "Live Postgres dev DB has 4 new tables (ProcessedWebhookEvent, ReferralReward, Campaign, StudentVerification)"
    - "Live Postgres dev DB has SubscriptionTier and SubscriptionStatus enum types"
    - "Live Postgres dev DB has 8 new columns on the User table"
    - "Generated Prisma client at apps/web/src/generated/prisma/ exports the new models and enums"
    - "apps/web/server/config/stripe.ts re-exports SubscriptionTier and SubscriptionStatus from the generated Prisma client (single source of truth per D-03)"
    - "pnpm typecheck exits 0 with the Prisma-generated enum types in place"
    - "Full unit test suite passes (subscriptionService.test.ts and 90+ sibling services with no regressions)"
  artifacts:
    - path: "apps/web/server/config/stripe.ts"
      provides: "SubscriptionTier and SubscriptionStatus re-exported from Prisma client"
      contains:
        - "export type { SubscriptionTier, SubscriptionStatus }"
        - "from '../../src/generated/prisma'"
    - path: "apps/web/src/generated/prisma/models/ProcessedWebhookEvent.ts"
      provides: "Generated TypeScript types for ProcessedWebhookEvent (consumed by Phase 36.3 webhook handler)"
    - path: "apps/web/src/generated/prisma/models/ReferralReward.ts"
      provides: "Generated TypeScript types for ReferralReward"
    - path: "apps/web/src/generated/prisma/models/Campaign.ts"
      provides: "Generated TypeScript types for Campaign"
    - path: "apps/web/src/generated/prisma/models/StudentVerification.ts"
      provides: "Generated TypeScript types for StudentVerification"
  key_links:
    - from: "apps/web/server/config/stripe.ts"
      to: "apps/web/src/generated/prisma"
      via: "type re-export of SubscriptionTier and SubscriptionStatus"
      pattern: "export type \\{ SubscriptionTier, SubscriptionStatus \\} from '\\.\\./\\.\\./src/generated/prisma'"
    - from: "apps/web/server/services/subscriptionService.ts:9"
      to: "apps/web/server/config/stripe.ts"
      via: "import { ..., type SubscriptionTier } from '../config/stripe'"
      pattern: "import \\{[^}]*SubscriptionTier[^}]*\\} from '\\.\\./config/stripe'"
---

<objective>
Apply the Plan-01 schema changes to the live Postgres dev database via `prisma db push`, regenerate the Prisma client so the new models/enums are visible to TypeScript, replace the inline TS unions in `apps/web/server/config/stripe.ts` with re-exports of the Prisma-generated enums (D-03), and verify the whole tree typechecks and tests green.

Purpose: Closes ROADMAP success criteria 3 (enums replace String?), 4 (typecheck + tests pass). This plan is the **BLOCKING db push gate** — without it the schema work in Plan 01 is invisible to the running app, creating a false-positive verification state.
Output: Updated dev DB, regenerated client (~9 files), refactored `stripe.ts` (~3 lines changed). Two atomic commits: one for the generated-client + DB sync (chore), one for the stripe.ts refactor (refactor).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-CONTEXT.md
@.planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-PATTERNS.md
@.planning/phases/36.1-add-subscription-schema-fields/36.1-01-SUMMARY.md
@apps/web/server/config/stripe.ts
@apps/web/server/services/subscriptionService.ts
@apps/web/prisma/schema.prisma
@CLAUDE.md

<interfaces>
<!-- Current stripe.ts (line 6) — TS union to be replaced with Prisma re-export -->

```typescript
export type SubscriptionTier = 'FREE' | 'PREMIUM' | 'ENTERPRISE';
```

<!-- subscriptionService.ts:9 — consumer import that MUST keep working unchanged -->

```typescript
import { STRIPE_CONFIG, PRICE_TO_TIER, type SubscriptionTier } from '../config/stripe';
```

<!-- subscriptionService.ts:151-152 — read-path fallback that MUST keep typechecking -->

```typescript
tier: (user.subscriptionTier ?? 'FREE') as SubscriptionTier,
status: user.subscriptionStatus ?? 'ACTIVE',
```

<!-- subscriptionService.ts:186 — inline status union that BECOMES a typed re-export -->

```typescript
const statusMap: Record<string, 'ACTIVE' | 'PAST_DUE' | 'CANCELED' | 'PAUSED'> = { ... };
```

<!-- 36.1 operational gotcha — copy verbatim into Task 1 -->
<!-- From 36.1-01-SUMMARY.md lines 116-117: -->
<!-- "Prisma config resolution in worktree: The worktree's apps/web/node_modules is empty -->
<!--  (pnpm workspace; packages installed only in the main repo). Running npx prisma db push -->
<!--  from the worktree failed with 'Cannot find module prisma/config'. Resolution: ran -->
<!--  npx prisma db push from the main repo at /d/NewsHub/apps/web pointing the --schema -->
<!--  flag at the worktree's updated schema file and providing --url directly." -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>[BLOCKING] Task 1: Run prisma db push --accept-data-loss + prisma generate against live dev DB</name>
  <files>apps/web/src/generated/prisma/internal/class.ts, apps/web/src/generated/prisma/internal/prismaNamespace.ts, apps/web/src/generated/prisma/internal/prismaNamespaceBrowser.ts, apps/web/src/generated/prisma/models/User.ts, apps/web/src/generated/prisma/models/ProcessedWebhookEvent.ts, apps/web/src/generated/prisma/models/ReferralReward.ts, apps/web/src/generated/prisma/models/Campaign.ts, apps/web/src/generated/prisma/models/StudentVerification.ts</files>
  <read_first>
    - apps/web/prisma/schema.prisma (verify Plan 01 commit landed: must contain `model ProcessedWebhookEvent`, `enum SubscriptionTier`, etc. — if not, STOP; Plan 01 is a hard prerequisite)
    - .planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-CONTEXT.md (D-02: db push --accept-data-loss is the chosen migration tool; production is out of scope)
    - .planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-PATTERNS.md (lines 425-446 — regenerate-client pattern + worktree gotcha)
    - .planning/phases/36.1-add-subscription-schema-fields/36.1-01-SUMMARY.md (lines 116-118 — operational gotcha for db push in a worktree)
    - CLAUDE.md ("Database (Prisma + PostgreSQL)" section: `cd apps/web && npx prisma db push` is the documented command)
  </read_first>
  <action>
This is the **mandatory BLOCKING db-push gate** per the schema_push_requirement. The phase CANNOT pass verification without this task running successfully — type checks alone create a false-positive because the generated client is read from disk independently of the live DB.

**Step 1: Verify dev Postgres is running.**

```bash
docker compose ps postgres
```

Should show the `postgres` service running on port 5433. If not, run `docker compose up -d postgres` and wait for the health check to pass.

**Step 2: Run `prisma db push` against the live dev DB.**

Working directory: `apps/web` in the **main repo** (per the 36.1 worktree gotcha — if running from a worktree, `apps/web/node_modules` is empty and the command fails with "Cannot find module 'prisma/config'"; in that case fall back to running from the main repo path with explicit `--schema` flag).

```bash
cd /d/NewsHub/apps/web && npx prisma db push --accept-data-loss
```

Expected output:
- "🚀  Your database is now in sync with your Prisma schema."
- 4 new tables created: `ProcessedWebhookEvent`, `ReferralReward`, `Campaign`, `StudentVerification`
- 2 new enum types: `SubscriptionTier`, `SubscriptionStatus`
- 8 new columns on `User` table
- 5 new indexes (referrer/referred FK indexes, status indexes, isActive composite)
- Conversion of `User.subscriptionTier` and `User.subscriptionStatus` from `text` (`String?`) to enum types — Prisma may report this as a "potentially destructive change" which is why `--accept-data-loss` is required. Per D-02, this is safe in dev because the columns currently hold `NULL` for all users (no real Stripe data).

**Why `--accept-data-loss`:** Prisma 7 flags any column type change (text → enum) and any unique-index addition over a nullable column as potentially destructive. `db push --accept-data-loss` accepts these warnings. Same flag was used by 36.1 (commit `a0df872`) for the original 5 fields. Per CLAUDE.md and 36.1 SUMMARY, this is the established convention for this repo.

If the command fails:
- "Cannot find module 'prisma/config'" → you are in a worktree. Re-run from the main repo at `/d/NewsHub/apps/web` with `--schema=<absolute-path-to-worktree-schema>` and `--url=$DATABASE_URL`.
- "value too long for type ..." or "violates not-null constraint" → an existing row has a non-null value that doesn't match the new enum or default. Run `psql -c 'SELECT DISTINCT "subscriptionTier" FROM "User"'` from inside the postgres container to inspect; if the values are not in {`FREE`, `PREMIUM`, `ENTERPRISE`} you must STOP and surface the rows for the user to decide.
- Any other error → surface it; do NOT retry blindly.

**Step 3: Regenerate the Prisma client.**

```bash
cd /d/NewsHub/apps/web && npx prisma generate
```

Expected output: "✔ Generated Prisma Client" with the path `apps/web/src/generated/prisma`. This regenerates ~9 files (4 new model files + 4 internal namespace files + the User model file). Files listed in this plan's `files_modified` cover the expected diff.

**Step 4: Verify the live DB matches the schema.**

```bash
cd /d/NewsHub/apps/web && npx prisma db push --accept-data-loss
```

Re-running the same command must report "The database is already in sync with the Prisma schema." (idempotent — same verification 36.1 used).

**Step 5: Spot-check the generated client.**

```bash
grep -l "ProcessedWebhookEvent" apps/web/src/generated/prisma/internal/class.ts apps/web/src/generated/prisma/internal/prismaNamespace.ts
grep -l "SubscriptionTier" apps/web/src/generated/prisma/internal/prismaNamespace.ts
ls apps/web/src/generated/prisma/models/ | grep -E "ProcessedWebhookEvent|ReferralReward|Campaign|StudentVerification"
```

All four model files must exist. The two internal files must contain `ProcessedWebhookEvent` and `SubscriptionTier` references.

**Step 6 (commit):** Stage and commit the regenerated client files together. The schema-only file (`schema.prisma`) is already committed in Plan 01 — DO NOT re-stage it.

```bash
git add apps/web/src/generated/prisma/
git commit -m "chore(36.2-03): regenerate Prisma client + sync dev DB with 36.2 schema additions"
```

Commit body must include the bullet list of changes (4 new tables, 2 enums, 8 User columns, etc.) and the `db push` output snippet for audit traceability.
  </action>
  <verify>
    <automated>cd /d/NewsHub/apps/web && npx prisma db push --accept-data-loss 2>&1 | grep -E "already in sync|now in sync"</automated>
  </verify>
  <acceptance_criteria>
    - `cd /d/NewsHub/apps/web && npx prisma db push --accept-data-loss` exits 0 and reports "already in sync" on the second run (idempotent)
    - `cd /d/NewsHub/apps/web && npx prisma generate` exits 0
    - File `apps/web/src/generated/prisma/models/ProcessedWebhookEvent.ts` exists
    - File `apps/web/src/generated/prisma/models/ReferralReward.ts` exists
    - File `apps/web/src/generated/prisma/models/Campaign.ts` exists
    - File `apps/web/src/generated/prisma/models/StudentVerification.ts` exists
    - `apps/web/src/generated/prisma/internal/prismaNamespace.ts` contains the literal string "ProcessedWebhookEvent" (verify via `grep -q "ProcessedWebhookEvent" apps/web/src/generated/prisma/internal/prismaNamespace.ts` exits 0)
    - `apps/web/src/generated/prisma/internal/prismaNamespace.ts` contains the literal strings "SubscriptionTier" and "SubscriptionStatus"
    - `apps/web/src/generated/prisma/models/User.ts` contains the literal string "referralCode" (verify via `grep -q "referralCode" apps/web/src/generated/prisma/models/User.ts` exits 0)
    - Live DB inspection: `docker exec -i $(docker compose ps -q postgres) psql -U newshub -d newshub -c "\dt"` lists `ProcessedWebhookEvent`, `ReferralReward`, `Campaign`, `StudentVerification` tables
    - Live DB inspection: `docker exec -i $(docker compose ps -q postgres) psql -U newshub -d newshub -c "\dT"` lists user-defined types `SubscriptionTier` and `SubscriptionStatus`
  </acceptance_criteria>
  <done>Dev DB synced with Plan-01 schema, generated client regenerated to expose all new types, `db push` is idempotent on re-run.</done>
</task>

<task type="auto">
  <name>Task 2: Replace TS unions in stripe.ts with Prisma enum re-exports + verify zero call-site churn</name>
  <files>apps/web/server/config/stripe.ts</files>
  <read_first>
    - apps/web/server/config/stripe.ts (full file, 107 lines — confirm current line 6 is `export type SubscriptionTier = 'FREE' | 'PREMIUM' | 'ENTERPRISE';`)
    - apps/web/server/services/subscriptionService.ts (line 9 import; lines 151-152 read-path fallbacks; line 186 inline statusMap union)
    - .planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-PATTERNS.md (lines 363-398 — verbatim refactor target + path-resolution check)
    - .planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-CONTEXT.md (D-03: zero-churn re-export pattern; UPPERCASE enum values match existing literals)
  </read_first>
  <action>
**Step 1: Edit `apps/web/server/config/stripe.ts` line 6.**

Current (line 6):
```typescript
export type SubscriptionTier = 'FREE' | 'PREMIUM' | 'ENTERPRISE';
```

Replace with:
```typescript
// D-03 (Phase 36.2): Re-export Prisma enums as the single source of truth.
// UPPERCASE values match existing string literals (D-01), so all call-sites
// continue to typecheck unchanged. SubscriptionStatus is also re-exported
// here so subscriptionService.ts can tighten its inline union if desired
// (currently it still spells 'ACTIVE' | 'PAST_DUE' | 'CANCELED' | 'PAUSED' inline).
export type { SubscriptionTier, SubscriptionStatus } from '../../src/generated/prisma';
```

Path verification: from `apps/web/server/config/stripe.ts`, the relative path `../../src/generated/prisma` resolves to `apps/web/src/generated/prisma/index.ts`. This is the same path used by `apps/web/server/db/prisma.ts` (the Prisma client itself). Verified by 36.1.

**Do NOT touch:**
- Line 9 (`STRIPE_CONFIG` constant) — unchanged
- Line 20 (`PRICE_TO_TIER`) — type signature `Record<string, SubscriptionTier>` continues to work because the symbol name is preserved (the union is now an enum, but UPPERCASE members align with the keys `'PREMIUM'`)
- Line 27 (`TIER_LIMITS`) — keys `FREE`, `PREMIUM`, `ENTERPRISE` continue to typecheck against the new enum
- Lines 92, 99 (`getTierLimits`, `hasFeature`) — function signatures unchanged

**Step 2 (subscriptionService.ts read-path — micro-decision left to executor per PATTERNS.md):**

The line `tier: (user.subscriptionTier ?? 'FREE') as SubscriptionTier,` (subscriptionService.ts:151) currently works because `'FREE'` is a member of the TS union. After the refactor it still works because `'FREE'` is now a member of the Prisma enum (UPPERCASE). NO change needed.

The line `status: user.subscriptionStatus ?? 'ACTIVE',` (line 152) is currently typed loosely as `string`. The executor MAY tighten it to `status: (user.subscriptionStatus ?? 'ACTIVE') as SubscriptionStatus,` for symmetry — recommended but NOT required. PATTERNS.md line 392-393 explicitly leaves this as the executor's call.

The `statusMap` literal at line 186 (`Record<string, 'ACTIVE' | 'PAST_DUE' | 'CANCELED' | 'PAUSED'>`) continues to work as-is. The executor MAY tighten it to `Record<string, SubscriptionStatus>` for symmetry — recommended but NOT required.

If the executor chooses to tighten lines 152 and/or 186, those edits go in the same commit as the stripe.ts refactor (small, semantically identical, single-file scope is fine). If the executor leaves them as-is, that is also fine — the contract is "zero call-site changes required" not "zero call-site changes permitted".

**Step 3: Verify the entire tree typechecks.**

```bash
cd /d/NewsHub && pnpm typecheck
```

Must exit 0. Expected: zero new errors. The pre-plan baseline already had zero errors per 36.1. If a new error surfaces, the most likely cause is a typo in the import path — verify line 6 of stripe.ts reads `from '../../src/generated/prisma'` (two dots, two dots, src/...).

**Step 4: Run unit tests.**

```bash
cd /d/NewsHub && pnpm test:run
```

Must exit 0 with no regressions. Expected: 26 subscriptionService tests + 90+ sibling service tests (cleanup, auth, team) all green per 36.1 baseline. If a test fails, surface the test name and the failure — do NOT modify the test to make it pass.

**Step 5: Commit.**

```bash
git add apps/web/server/config/stripe.ts
# If lines 152 / 186 were tightened, also:
# git add apps/web/server/services/subscriptionService.ts
git commit -m "refactor(36.2-03): replace SubscriptionTier TS union with Prisma enum re-export"
```

Commit body documents D-03 + zero-churn rationale.
  </action>
  <verify>
    <automated>cd /d/NewsHub && grep -q "export type { SubscriptionTier, SubscriptionStatus } from '../../src/generated/prisma'" apps/web/server/config/stripe.ts && pnpm typecheck && pnpm test:run</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/server/config/stripe.ts` no longer contains the literal string `'FREE' | 'PREMIUM' | 'ENTERPRISE'` — verify via `grep -c "'FREE' | 'PREMIUM' | 'ENTERPRISE'" apps/web/server/config/stripe.ts` returns 0
    - `apps/web/server/config/stripe.ts` contains the literal string `export type { SubscriptionTier, SubscriptionStatus }` — verify via `grep -q "export type { SubscriptionTier, SubscriptionStatus }" apps/web/server/config/stripe.ts` exits 0
    - `apps/web/server/config/stripe.ts` contains the literal string `from '../../src/generated/prisma'` — verify via `grep -q "from '../../src/generated/prisma'" apps/web/server/config/stripe.ts` exits 0
    - `cd /d/NewsHub && pnpm typecheck` exits 0
    - `cd /d/NewsHub && pnpm test:run` exits 0 with 26 subscriptionService tests passing (`pnpm test:run -- --reporter=verbose 2>&1 | grep -c "subscriptionService"` should be ≥ 26 if running verbose)
    - The import on subscriptionService.ts:9 (`import { STRIPE_CONFIG, PRICE_TO_TIER, type SubscriptionTier } from '../config/stripe';`) is unchanged — verify via `grep -q "type SubscriptionTier" apps/web/server/services/subscriptionService.ts` exits 0
    - Git diff for this commit shows changes ONLY in `apps/web/server/config/stripe.ts` (and optionally `apps/web/server/services/subscriptionService.ts` if executor chose the optional tightening) — no schema, no generated-client, no test files
  </acceptance_criteria>
  <done>stripe.ts re-exports the Prisma enums; full tree typechecks and tests green; consumer code unchanged or minimally tightened.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan touches the live dev database (via `prisma db push`) and a runtime config file. The trust boundary is the Postgres connection (controlled by DATABASE_URL — server-only secret per CLAUDE.md).

| Boundary | Description |
|----------|-------------|
| Prisma CLI → Postgres | DATABASE_URL is read from `prisma.config.ts` (Prisma 7+); not user-controlled at runtime. |
| stripe.ts re-export → consumers | Type-only re-export; no runtime data crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-08 | Tampering | `db push --accept-data-loss` may drop columns or data | accept | Per D-02 the dev DB has no real Stripe data (subscriptionTier/Status are NULL for all users). Plan 01 schema additions are purely additive (new tables, new columns); the only "risky" change is the `text` → `enum` cast on the two Stripe columns, which Prisma performs as a `USING text::"SubscriptionTier"` cast on existing values. Since values are NULL the cast is a no-op. Severity: LOW (dev only). Production migration is deferred to PRODUCTION-MIGRATION.md (Plan 04). |
| T-36.2-09 | Information Disclosure | Generated Prisma client is checked into git; could leak DATABASE_URL if committed by mistake | accept | The generator output at `apps/web/src/generated/prisma/` does NOT contain DATABASE_URL — it only contains type declarations. The URL is read at runtime from `prisma.config.ts` which is git-tracked but reads from `process.env.DATABASE_URL`. No secret material in the regenerated files. Verified by 36.1 (commit `1af3a5a`). Severity: LOW. |
| T-36.2-10 | Denial of Service | Type re-export breaks downstream consumers, causing build failure in production | mitigate | Verification gates: `pnpm typecheck` must exit 0 (Task 2 step 3) and `pnpm test:run` must exit 0 (step 4) before commit. Both gates exercise the full consumer tree. The 26 subscriptionService unit tests directly cover the read/write paths that depend on these types. |

No HIGH-severity threats. ASVS L1 baseline satisfied for a schema-cutover plan.
</threat_model>

<verification>
- `npx prisma db push --accept-data-loss` reports "already in sync" on second run (idempotency proven)
- All 4 new model files generated under `apps/web/src/generated/prisma/models/`
- `apps/web/src/generated/prisma/models/User.ts` contains all 8 new field names
- `apps/web/server/config/stripe.ts` contains the re-export and no longer contains the inline TS union
- `pnpm typecheck` exits 0
- `pnpm test:run` exits 0 (26+ subscriptionService tests + 90+ sibling tests pass with no regressions)
- Two distinct commits land: one `chore(36.2-03):` for client regeneration, one `refactor(36.2-03):` for stripe.ts
</verification>

<success_criteria>
- [ ] Live dev DB has all 4 new tables + 2 new enum types + 8 new User columns (verified via psql)
- [ ] Generated Prisma client exposes ProcessedWebhookEvent, ReferralReward, Campaign, StudentVerification, SubscriptionTier, SubscriptionStatus
- [ ] `apps/web/server/config/stripe.ts` re-exports both enums from the generated client
- [ ] `pnpm typecheck` exits 0
- [ ] `pnpm test:run` exits 0 with no regressions
- [ ] subscriptionService.ts:9 import unchanged (zero call-site churn for the SubscriptionTier symbol)
</success_criteria>

<output>
After completion:
- Two commits land:
  1. `chore(36.2-03): regenerate Prisma client + sync dev DB with 36.2 schema additions`
  2. `refactor(36.2-03): replace SubscriptionTier TS union with Prisma enum re-export`
- Then create `.planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-03-SUMMARY.md` per the standard template, recording both commit SHAs and the `psql \dt` output for audit-trail provenance (Plan 04 reads this).
</output>
