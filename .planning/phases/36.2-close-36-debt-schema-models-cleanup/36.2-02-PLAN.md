---
phase: 36.2-close-36-debt-schema-models-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [PAY-05, PAY-07]
tags: [depcheck, cleanup, dependencies, package-json, lockfile]

must_haves:
  truths:
    - "apps/web/package.json no longer lists @radix-ui/react-dialog, class-variance-authority, intl-messageformat, pg, or @types/pg"
    - "pnpm install --no-frozen-lockfile produces a clean lockfile with the 5 deps removed"
    - "pnpm typecheck exits 0 after the removals (no transitive consumers broken)"
    - "pnpm test:run exits 0 after the removals (no test imports broken)"
  artifacts:
    - path: "apps/web/package.json"
      provides: "Cleaned dependency manifest (5 unused deps removed)"
    - path: "pnpm-lock.yaml"
      provides: "Regenerated lockfile reflecting the 5 removed deps and any transitive cleanup"
  key_links:
    - from: "apps/web/package.json"
      to: "depcheck audit (commit 87ba5e4)"
      via: "audit identified the 5 deps as unused"
      pattern: "audit-driven removal — see commit 87ba5e4"
---

<objective>
Remove the 5 unused dependencies surfaced by the depcheck audit at commit `87ba5e4` from `apps/web/package.json`, regenerate the lockfile, and verify no consumers break. Schema work is in Plan 01 — this plan stays surgically scoped to dependency manifest editing so a regression in one does not block the other (per D-10).

Purpose: Close ROADMAP success criterion #5 — depcheck cleanup. Distinct atomic commit so the cleanup can be reverted independently of schema changes if a transitive consumer surfaces.
Output: Modified `apps/web/package.json` (5 lines deleted) + regenerated `pnpm-lock.yaml`. Single atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-CONTEXT.md
@.planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-PATTERNS.md
@apps/web/package.json
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove 5 unused deps from apps/web/package.json + regenerate lockfile + verify clean tree</name>
  <files>apps/web/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/web/package.json (lines 34-165 — full dependencies + devDependencies blocks; verify the 5 target lines are present at the expected positions before deleting)
    - .planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-PATTERNS.md (lines 402-422 — exact lines to remove + the 5-step verification gate per D-09)
    - .planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-CONTEXT.md (D-09: the 5-step verification gate is mandatory; D-10: cleanup must be a separate commit from schema work)
    - CLAUDE.md (project commands: `pnpm typecheck && pnpm test:run` is the standard verification chain)
  </read_first>
  <action>
**Step 1: Edit `apps/web/package.json`** — remove these 5 dependency lines:

From the `"dependencies"` object (alphabetical block at lines ~34-122):
- `"@radix-ui/react-dialog": "^1.1.15",`  (currently at line 41)
- `"class-variance-authority": "^0.7.1",` (currently at line 59)
- `"intl-messageformat": "^11.2.1",`      (currently at line 76)
- `"pg": "^8.20.0",`                      (currently at line 89)

From the `"devDependencies"` object (lines ~123-164):
- `"@types/pg": "^8.20.0",`               (currently at line 142)

Use the Edit tool (NOT Write) to delete each line individually so the rest of the manifest stays byte-stable. Preserve trailing commas correctly: each deleted line takes its trailing newline with it; the line above and below should still join cleanly.

After editing, the JSON must remain valid (no trailing comma before `}`). Verify with `node -e "JSON.parse(require('fs').readFileSync('apps/web/package.json', 'utf8'))"` — must exit 0.

**Step 2: Regenerate the lockfile** (per D-09 step 2):

```bash
cd /d/NewsHub && pnpm install --no-frozen-lockfile
```

This must complete without errors. Expect `pnpm-lock.yaml` to change (the 5 packages and their unique transitive deps will be pruned). If pnpm reports peer-dep warnings unrelated to the 5 removed packages, that is pre-existing — note in commit body but do NOT add new flags to suppress them.

**Step 3: Confirm no new transitive offenders surface** (per D-09 step 3):

```bash
cd /d/NewsHub/apps/web && npx depcheck --skip-missing
```

Expected output: the 5 removed deps no longer appear, and no NEW unused deps surface (i.e. the depcheck output should be a subset of the pre-removal output, minus the 5). If a new offender appears, STOP — surface it in the task summary so the planner can decide whether to extend the cleanup or accept the noise.

**Step 4: Typecheck** (per D-09 step 4):

```bash
cd /d/NewsHub && pnpm typecheck
```

Must exit 0. If a transitive consumer of one of the 5 removed deps shows up as a TS2307 ("cannot find module") error, STOP — that means the dep was not truly unused. Surface the consumer file and the dep, restore the dep, and re-run the verification.

**Step 5: Run unit tests** (per D-09 step 5):

```bash
cd /d/NewsHub && pnpm test:run
```

Must exit 0 with the same pass count as the pre-plan baseline (1289/1289 per 36.1 SUMMARY line 73 — but check the latest count; the number may have grown since 36.1).

**All five steps must pass before committing.** Do NOT commit if any step fails. The commit subject for the executor is:
- `chore(36.2-02): remove 5 unused deps from apps/web/package.json (depcheck cleanup)`

The commit must include both `apps/web/package.json` and `pnpm-lock.yaml`.

**Important — D-10 commit isolation:** This commit must NOT include any change to `apps/web/prisma/schema.prisma` or any file owned by Plan 01 / Plan 03. Verify with `git diff --stat HEAD` before staging — only the two files listed in `files_modified` should appear.
  </action>
  <verify>
    <automated>cd /d/NewsHub && node -e "const p=require('./apps/web/package.json'); const banned=['@radix-ui/react-dialog','class-variance-authority','intl-messageformat','pg','@types/pg']; const found=banned.filter(b=>p.dependencies?.[b]||p.devDependencies?.[b]); if(found.length){console.error('STILL PRESENT:',found);process.exit(1)} console.log('OK — all 5 deps removed')"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "const p=require('./apps/web/package.json'); console.log(!!p.dependencies['@radix-ui/react-dialog'])"` prints `false`
    - `node -e "const p=require('./apps/web/package.json'); console.log(!!p.dependencies['class-variance-authority'])"` prints `false`
    - `node -e "const p=require('./apps/web/package.json'); console.log(!!p.dependencies['intl-messageformat'])"` prints `false`
    - `node -e "const p=require('./apps/web/package.json'); console.log(!!p.dependencies['pg'])"` prints `false`
    - `node -e "const p=require('./apps/web/package.json'); console.log(!!p.devDependencies['@types/pg'])"` prints `false`
    - `node -e "JSON.parse(require('fs').readFileSync('apps/web/package.json','utf8'))"` exits 0 (manifest is valid JSON)
    - `pnpm-lock.yaml` no longer contains a top-level `'@radix-ui/react-dialog@'` entry — verify via `grep -c "^  '@radix-ui/react-dialog@" pnpm-lock.yaml` returns 0 (or whatever the exact lockfile key syntax is for pnpm v9; a non-strict check is `grep -F "react-dialog" pnpm-lock.yaml | wc -l` returns 0)
    - `cd /d/NewsHub && pnpm typecheck` exits 0
    - `cd /d/NewsHub && pnpm test:run` exits 0
    - `git diff --stat HEAD --name-only` after the change lists exactly 2 files: `apps/web/package.json` and `pnpm-lock.yaml` (no schema.prisma, no source code, no generated client files)
  </acceptance_criteria>
  <done>5 deps removed from manifest, lockfile regenerated, typecheck + tests pass, commit isolated to manifest+lockfile only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan only edits a dependency manifest and a lockfile. No runtime code paths are added; existing trust boundaries are unchanged.

| Boundary | Description |
|----------|-------------|
| developer → package.json | Only planner-controlled subtractive edit. |
| pnpm registry → lockfile | Existing; not in scope (no new deps added). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36.2-06 | Tampering | pnpm-lock.yaml regeneration could pull a tampered transitive package version | mitigate | `pnpm install --no-frozen-lockfile` resolves against the pinned semver ranges in package.json (which are unchanged for the surviving deps). Optional: re-run `pnpm audit` after the install to surface any new advisories that appeared in transitive dep updates. |
| T-36.2-07 | Denial of Service | Removing a dep that has a hidden runtime consumer breaks production | mitigate | D-09 verification gate (steps 3-5) catches this: depcheck must not surface new offenders, typecheck must pass, tests must pass. If any step fails, restore the dep. |

No HIGH-severity threats. ASVS L1 baseline satisfied.
</threat_model>

<verification>
- The 5 targeted deps no longer appear in `apps/web/package.json` (verified via Node JSON.parse + key lookup)
- `pnpm install --no-frozen-lockfile` exited 0 and updated `pnpm-lock.yaml`
- `npx depcheck --skip-missing` against `apps/web` reports the same or fewer unused deps than before (the 5 are gone; no new ones surfaced)
- `pnpm typecheck` exits 0
- `pnpm test:run` exits 0
- The commit diff includes ONLY `apps/web/package.json` and `pnpm-lock.yaml` (no schema, no source, no generated client)
</verification>

<success_criteria>
- [ ] All 5 deps removed from package.json (4 from dependencies + 1 from devDependencies)
- [ ] pnpm-lock.yaml regenerated, no orphaned references to the removed packages
- [ ] `pnpm typecheck` exits 0
- [ ] `pnpm test:run` exits 0
- [ ] depcheck does not surface new unused deps
- [ ] Commit isolated to `package.json` + `pnpm-lock.yaml` only (per D-10)
</success_criteria>

<output>
After completion:
- Commit subject: `chore(36.2-02): remove 5 unused deps from apps/web/package.json (depcheck cleanup)`
- Commit body lists each dep removed and notes the 5-step verification gate passed.

Then create `.planning/phases/36.2-close-36-debt-schema-models-cleanup/36.2-02-SUMMARY.md` per the standard template, recording the commit SHA so Plan 04's audit trail can reference it.
</output>
