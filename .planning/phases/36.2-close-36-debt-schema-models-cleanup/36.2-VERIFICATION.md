---
phase: 36.2-close-36-debt-schema-models-cleanup
verified: 2026-04-28T12:35:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 36.2: Close 36-Debt — Schema Models + Cleanup Verification Report

**Phase Goal:** Close the broader 36-Schuld — backfill the models, fields, and Prisma enums that Phase 36-01 SUMMARY claimed but never wrote to disk; remove unused dependencies surfaced during the depcheck audit (commit `87ba5e4`).
**Verified:** 2026-04-28T12:35:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (= 6 ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Prisma schema contains 4 missing models (`ProcessedWebhookEvent`, `ReferralReward`, `Campaign`, `StudentVerification`) with appropriate fields, indexes, relations | VERIFIED | `apps/web/prisma/schema.prisma` lines 475, 485, 502, 518 — all 4 models present with correct field shapes, indexes, and relations matching D-04..D-08 |
| 2 | User model has 8 new fields: `pausedUntil`, `showPremiumBadge`, `customAccentColor`, `referralCode`, `referredBy`, `freeMonthsEarned`, `isStudent`, `studentVerifiedUntil` | VERIFIED | `apps/web/prisma/schema.prisma` lines 124–137 — all 8 fields present; `referralCode` has `@unique` (D-06); `referredBy` is paired with self-relation `referredByUser` at line 153 with `onDelete: SetNull` |
| 3 | `SubscriptionTier` and `SubscriptionStatus` Prisma enums replace `String?` typing; consumer code adjusted | VERIFIED | `schema.prisma:576-587` declares both enums with UPPERCASE values (D-01); `User.subscriptionTier` (line 121) and `User.subscriptionStatus` (line 122) use enum types with defaults `FREE`/`ACTIVE`; `apps/web/server/config/stripe.ts:11` re-exports both from generated client (D-03); generated client confirms types in `apps/web/src/generated/prisma/enums.ts:28-44` |
| 4 | `pnpm typecheck` and full unit-test suite pass (no regressions) | VERIFIED | `pnpm typecheck` exit 0 (both `packages/types` and `apps/web`); `pnpm test:run` 1289/1289 tests passing across 44 test files — matches Plan 02 / Plan 03 baseline exactly, no regression |
| 5 | 5 unused deps removed: `@radix-ui/react-dialog`, `class-variance-authority`, `intl-messageformat`, `pg`, `@types/pg` | VERIFIED | `apps/web/package.json` grep returns zero matches for all 5 names; `pnpm-lock.yaml` regenerated (commit `b9e068b`) |
| 6 | `36-01-SUMMARY.md` annotated with reference to 36.1 + 36.2 (audit trail) | VERIFIED | `.planning/phases/36-monetization-core/36-01-SUMMARY.md:158` opens `## Phase 36 Debt Closure (Audit Trail)`; cites 36.1 SHA `a0df872` (line 171) + all 36.2 SHAs (`1976489`, `44aa829`, `b9e068b`, `556694f`, `30466e2`); per D-11 the original `## Known Stubs` block at lines 138-140 is preserved byte-stable (re-verified: line 140 still reads `None - all code is fully implemented and functional.`) |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/prisma/schema.prisma` | 4 new models + 2 new enums + 8 new User fields + 5 back-relations | VERIFIED | Models at lines 475/485/502/518; enums at lines 576/582; User fields at lines 121-137; back-relations at lines 151-157 |
| `apps/web/src/generated/prisma/models/ProcessedWebhookEvent.ts` | Generated TypeScript model file | VERIFIED | 43 KB; exports type for the model |
| `apps/web/src/generated/prisma/models/ReferralReward.ts` | Generated TypeScript model file | VERIFIED | 65 KB |
| `apps/web/src/generated/prisma/models/Campaign.ts` | Generated TypeScript model file | VERIFIED | 45 KB |
| `apps/web/src/generated/prisma/models/StudentVerification.ts` | Generated TypeScript model file | VERIFIED | 58 KB |
| `apps/web/src/generated/prisma/enums.ts` | UPPERCASE enum exports for SubscriptionTier + SubscriptionStatus | VERIFIED | Lines 28-44 expose both enums with members `FREE PREMIUM ENTERPRISE` and `ACTIVE PAST_DUE CANCELED PAUSED` |
| `apps/web/server/config/stripe.ts` | Re-export of Prisma enums (D-03) | VERIFIED | Line 11: `export type { SubscriptionTier, SubscriptionStatus } from '../../src/generated/prisma/client'`; replaces inline TS union |
| `apps/web/package.json` | 5 specific deps absent | VERIFIED | grep for all 5 names returns 0 matches |
| `pnpm-lock.yaml` | Regenerated after dep removal | VERIFIED | Commit `b9e068b` modifies it (22 deletions per Plan 02 SUMMARY) |
| `.planning/phases/36-monetization-core/36-01-SUMMARY.md` | Audit-trail section appended | VERIFIED | Section starts at line 158; preserves original Known Stubs at 138-140 per D-11 |
| `.planning/phases/36.2-close-36-debt-schema-models-cleanup/PRODUCTION-MIGRATION.md` | Cutover SQL handoff (per CONTEXT "Specific Ideas") | VERIFIED | 5210-byte file with `ALTER COLUMN ... USING ::text::"SubscriptionTier"` template + pre-requisites + ownership |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `User.referredByUser` | `User.id` (self-relation) | `@relation("UserReferrer", fields: [referredBy], references: [id], onDelete: SetNull)` | WIRED | schema.prisma:153 — D-06 satisfied; matches `Comment.parentComment` analog with `SetNull` instead of `NoAction` |
| `User.referralRewardsAsReferrer` / `referralRewardsAsReferred` | `ReferralReward` | Two named relations `ReferralRewardReferrer` + `ReferralRewardReferred`, both `Cascade` | WIRED | schema.prisma:151-152 + 487, 489 — D-04 satisfied |
| `User.studentVerifications` | `StudentVerification` | `Cascade` FK from `StudentVerification.userId` | WIRED | schema.prisma:157 + 520 — D-05 satisfied; index on `userId` at line 529 |
| `ProcessedWebhookEvent.id` | Stripe event ID (caller-supplied) | `String @id` (no `@default(cuid())`) | WIRED | schema.prisma:476 — D-08 satisfied; matches webhook consumer contract `prisma.processedWebhookEvent.findUnique({ where: { id: eventId } })` and `.create({ data: { id, eventType } })` |
| `Campaign` | (no FKs by D-07) | n/a | WIRED | schema.prisma:502-514; composite index on `[isActive, startsAt, endsAt]` |
| `apps/web/server/config/stripe.ts` | `apps/web/src/generated/prisma/client` | `export type { SubscriptionTier, SubscriptionStatus } from '../../src/generated/prisma/client'` | WIRED | stripe.ts:11; TS union removed from line 6, replaced by re-export |
| `User.subscriptionTier` / `subscriptionStatus` | Prisma enum types | `SubscriptionTier @default(FREE)` / `SubscriptionStatus @default(ACTIVE)` | WIRED | schema.prisma:121-122 — D-01 (UPPERCASE) preserves zero call-site churn for `?? 'FREE' as SubscriptionTier` fallback in subscriptionService.ts:151 |

### Data-Flow Trace (Level 4)

Not applicable for this phase — schema/dependency cleanup phase produces no UI components or dynamic data renderers. The generated Prisma client types are consumed at compile-time only in this phase; runtime data flow through the new models is the concern of Phase 36.3 (webhook handler) and 36-05 (E2E subscription tests).

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Workspace typecheck succeeds | `pnpm typecheck` | exit 0 (both `packages/types` and `apps/web`) | PASS |
| Full unit-test suite passes | `pnpm test:run` | `Test Files 44 passed (44)`, `Tests 1289 passed (1289)`, duration 24.59s | PASS |
| 5 target deps absent from manifest | grep `@radix-ui/react-dialog\|class-variance-authority\|intl-messageformat\|"pg":\|@types/pg` in `apps/web/package.json` | 0 matches | PASS |
| 4 generated model files exist | `ls apps/web/src/generated/prisma/models/{ProcessedWebhookEvent,ReferralReward,Campaign,StudentVerification}.ts` | All 4 present (43 KB / 65 KB / 45 KB / 58 KB) | PASS |
| Generated enums file contains UPPERCASE values | grep on `apps/web/src/generated/prisma/enums.ts` | `SubscriptionTier { FREE PREMIUM ENTERPRISE }` + `SubscriptionStatus { ACTIVE PAST_DUE CANCELED PAUSED }` confirmed at lines 28-44 | PASS |
| Stripe.ts re-exports from generated client | grep on `apps/web/server/config/stripe.ts` | Line 11: `export type { SubscriptionTier, SubscriptionStatus } from '../../src/generated/prisma/client'` | PASS |
| Audit-trail section appended without mutating the original Known Stubs | `sed -n '138,140p'` on `36-01-SUMMARY.md` | Lines 138-140 byte-stable (`## Known Stubs` / blank / `None - all code is fully implemented and functional.`); audit section opens at line 158 | PASS |
| Production-migration handoff documented | Read `PRODUCTION-MIGRATION.md` | 5.2 KB, contains `ALTER COLUMN "subscriptionTier" TYPE "SubscriptionTier" USING "subscriptionTier"::text::"SubscriptionTier"` and explicit `Owner of execution: Production-readiness phase (TBD)` | PASS |
| All 6 phase commits resolvable | `git log --oneline` | `1976489`, `44aa829`, `b9e068b`, `556694f`, `30466e2`, `5cb75f3` all present | PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|------------|----------------|-------------|--------|----------|
| PAY-02 | 36.2-01, 36.2-03 | Stripe Checkout schema substrate (subscription enums + customer fields available to checkout flow) | SATISFIED | enums + tightened User columns landed; checkout consumer in `subscriptionService.ts` typechecks against new enums |
| PAY-03 | 36.2-01, 36.2-03 | Customer Portal schema support (subscription status enum) | SATISFIED | `SubscriptionStatus` enum present + User.subscriptionStatus typed |
| PAY-04 | 36.2-01 | Premium perk fields (`showPremiumBadge`, `customAccentColor`) + paused state (`pausedUntil`) | SATISFIED | All 3 fields present at schema.prisma:124-128 |
| PAY-05 | 36.2-02, 36.2-03 | Dependency hygiene + dev-DB cutover for tier-aware backend | SATISFIED | 5 deps removed (b9e068b); db push completed (556694f) |
| PAY-06 | 36.2-01, 36.2-04 | Idempotent webhook processing schema substrate (`ProcessedWebhookEvent` model) + audit trail | SATISFIED | Model present with caller-supplied `id String @id` matching consumer contract; audit trail section appended in 36-01-SUMMARY.md |
| PAY-07 | 36.2-02, 36.2-03 | Clean baseline — no unused deps + working test suite + verified manifest | SATISFIED | depcheck output clean for 5 target deps; typecheck + tests pass |

### Anti-Patterns Found

None. The phase is documentation + schema + dependency cleanup; no runtime code paths added or modified beyond the type re-export in `stripe.ts`. No TODO/FIXME/PLACEHOLDER markers introduced. The new schema models contain only structural Prisma syntax — no stub functions, no empty handlers, no hardcoded empty data.

### Acknowledged Pre-Existing Issues (Not Blockers)

These are documented out-of-scope items confirmed pre-existing per Plan 03 SUMMARY and the additional context provided to the verifier. They do NOT count as 36.2 regressions:

1. **`pnpm build` fails on workbox precache size limit (`dist/stats.html` 4.3 MB > 2 MiB).** Confirmed pre-existing per Plan 03 SUMMARY (stash test reproduced the failure on the unmodified file). Tracked in `.planning/phases/36.2-close-36-debt-schema-models-cleanup/deferred-items.md`. Phase 36.2's own gates (`pnpm typecheck` + `pnpm test:run`) both pass; build verification is not in the ROADMAP success criteria for 36.2.
2. **Root `prisma.config.ts` schema-path quirk.** Plan 03 used the Rule-3 auto-fix (`--schema=prisma/schema.prisma` flag) to override the stale root config that points at a duplicate schema. Long-term tidy-up tracked separately; the workaround is documented in commit `556694f`.

### Human Verification Required

None. All 6 ROADMAP success criteria were programmatically verifiable via grep, file reads, generated-client inspection, and command-line checks (typecheck + tests). This phase is schema + dependency cleanup with documentation hand-offs — no UI behavior, no runtime user flows, no real-time integration, and no external services.

### Gaps Summary

No gaps. All 6 ROADMAP success criteria are independently and observably satisfied:

1. The 4 new Prisma models exist in the canonical schema with the correct field shapes, indexes, relations, and `onDelete` policies as locked in D-04..D-08.
2. All 8 new User fields are present with appropriate types/defaults; `referralCode @unique` and the `referredBy`/`referredByUser`/`referredUsers` self-relation triplet match D-06 exactly.
3. The two Prisma enums (UPPERCASE per D-01) appear in the schema, are reflected in the generated client, and the User columns have been tightened from `String?` to non-null enums with defaults — and `stripe.ts` re-exports them from the generated client per D-03.
4. `pnpm typecheck` exits 0 across both workspaces; the unit-test suite reports 1289/1289 passing — the same baseline as Plans 02 and 03 reported.
5. All 5 target deps (`@radix-ui/react-dialog`, `class-variance-authority`, `intl-messageformat`, `pg`, `@types/pg`) are absent from `apps/web/package.json`; the lockfile was regenerated in commit `b9e068b`.
6. The audit-trail section in `36-01-SUMMARY.md` (line 158) explicitly references both Phase 36.1 (commit `a0df872`) and all five Phase 36.2 commits, while the original Known Stubs block at lines 138-140 is preserved byte-stable per D-11.

The phase delivered exactly the contract the ROADMAP described, with no scope creep and no regressions. Phase 36.3 (Stripe webhook monorepo path fix) is now unblocked — the `ProcessedWebhookEvent` type it depends on is available in the generated Prisma client.

---

*Verified: 2026-04-28T12:35:00Z*
*Verifier: Claude (gsd-verifier)*
