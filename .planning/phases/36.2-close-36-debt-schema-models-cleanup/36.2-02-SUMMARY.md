---
phase: 36.2-close-36-debt-schema-models-cleanup
plan: 02
subsystem: infra
tags: [depcheck, cleanup, dependencies, package-json, lockfile, pnpm]

# Dependency graph
requires:
  - phase: 35-infrastructure-foundation
    provides: pnpm monorepo + depcheck audit baseline (commit 87ba5e4) that flagged the 5 unused deps
provides:
  - apps/web/package.json with 5 unused deps removed (4 deps + 1 devDep)
  - regenerated pnpm-lock.yaml reflecting the pruned tree
  - clean depcheck baseline for future audits (no NEW transitive offenders surfaced)
affects: [36.2-03, 36.2-04, future-dep-audits]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "5-step depcheck verification gate (D-09): edit -> install -> depcheck -> typecheck -> test, all must pass before commit"
    - "Subtractive dep edit isolated to manifest+lockfile; D-10 commit isolation enforced via git diff --stat"

key-files:
  created: []
  modified:
    - apps/web/package.json (5 deps removed: @radix-ui/react-dialog, class-variance-authority, intl-messageformat, pg, @types/pg)
    - pnpm-lock.yaml (22 lines deleted; 1 net package pruned)

key-decisions:
  - "Followed D-09 exactly: ran all 5 verification steps in order before committing"
  - "D-10 commit isolation: only manifest + lockfile staged; .claude/settings.local.json (pre-existing dirty file) deliberately left unstaged"
  - "Pre-existing depcheck false positives left untouched: @newshub/types (workspace package), autoprefixer/tailwindcss (PostCSS pipeline), nodemon (runtime tool)"

patterns-established:
  - "5-step depcheck verification gate as the canonical cleanup pattern for future dep audits"
  - "D-10 isolation rule (manifest changes never co-mingled with schema/code changes) — supports independent revert"

requirements-completed: [PAY-05, PAY-07]

# Metrics
duration: 2min
completed: 2026-04-28
---

# Phase 36.2 Plan 02: depcheck cleanup Summary

**Removed 5 unused deps (@radix-ui/react-dialog, class-variance-authority, intl-messageformat, pg, @types/pg) from apps/web/package.json with all 5 verification gates passing (typecheck 0, 1289/1289 tests).**

## Performance

- **Duration:** 2 min (128s)
- **Started:** 2026-04-28T10:03:27Z
- **Completed:** 2026-04-28T10:05:35Z
- **Tasks:** 1
- **Files modified:** 2 (apps/web/package.json, pnpm-lock.yaml)

## Accomplishments

- 5 unused deps removed from `apps/web/package.json` (4 from `dependencies`, 1 from `devDependencies`)
- `pnpm-lock.yaml` regenerated cleanly (1 net package pruned, 22 lines deleted)
- Full 5-step D-09 verification gate passed:
  1. Manifest edit -> JSON valid (Node parse OK)
  2. `pnpm install --no-frozen-lockfile` -> exit 0, 1 package pruned
  3. `npx depcheck --skip-missing` against `apps/web` -> the 5 target deps gone, no NEW transitive offenders
  4. `pnpm typecheck` -> exit 0 across all workspaces (`packages/types` + `apps/web`)
  5. `pnpm test:run` -> 1289/1289 tests pass (44 test files; same baseline as 36.1 SUMMARY)
- D-10 commit isolation enforced: diff included only `apps/web/package.json` + `pnpm-lock.yaml`; no schema/source/generated-client touched

## Task Commits

1. **Task 1: Remove 5 unused deps + regenerate lockfile + verify clean tree** - `b9e068b` (chore)

## Files Created/Modified

- `apps/web/package.json` — removed `@radix-ui/react-dialog ^1.1.15`, `class-variance-authority ^0.7.1`, `intl-messageformat ^11.2.1`, `pg ^8.20.0` from `dependencies`; removed `@types/pg ^8.20.0` from `devDependencies`. Net: -5 lines.
- `pnpm-lock.yaml` — regenerated; 22 lines deleted (5 deps + their unique transitives).

## Decisions Made

None beyond plan — followed D-09 / D-10 exactly. Notable confirmation points:

- **Typecheck did not fail on Plan 01 schema/generated-client drift.** The sequential constraints flagged that this could happen if `stripe.ts` referenced the new enums before Plan 03's `prisma generate`. It didn't — `stripe.ts` still uses literal-string TS unions; the D-03 re-export refactor is Plan 03's job. Typecheck is clean.
- **Depcheck residual noise is pre-existing false positives.** `@newshub/types` (workspace package consumed via `import type`), `autoprefixer` and `tailwindcss` (PostCSS pipeline read from `postcss.config.js`/`vite.config.ts`), and `nodemon` (runtime tool invoked via scripts) are all known false positives that were already in the depcheck output before this plan. Not in scope for 36.2-02.
- **Peer-dep warnings during `pnpm install` are pre-existing.** `@sentry/vite-plugin -> rollup` (vite 8 ships rollup 2), `@scalar -> typescript` (project on TS 6), `vite-plugin-pwa -> vite` (vite-plugin-pwa hasn't published a v8 peer range). All three predate this plan; none surfaced as a result of the 5 dep removals.

## Deviations from Plan

None — plan executed exactly as written. All 5 verification gate steps passed on first run; no Rule 1/2/3 fixes needed; no architectural Rule 4 calls.

## Issues Encountered

None.

## Threat Surface Scan

No new security-relevant surface introduced. This plan is a subtractive manifest edit only; no runtime code paths added or modified. Threat register T-36.2-06 (lockfile-regeneration tampering) and T-36.2-07 (hidden runtime consumer breakage) both mitigated by the D-09 5-step gate, which passed.

## Self-Check: PASSED

- `git log b9e068b` — FOUND
- `apps/web/package.json` — modified, 5 deps absent (verified via `node -e "JSON.parse(...)"` + key lookup)
- `pnpm-lock.yaml` — regenerated (22 lines deleted)
- `pnpm typecheck` — exit 0
- `pnpm test:run` — 1289/1289 PASS

## Next Phase Readiness

- Plan 36.2-03 unblocked: db push + prisma generate + stripe.ts re-export refactor (Wave 2). Plan 03 owns the actual Prisma client regeneration; Plan 02's manifest cleanup does not interact with the generated client.
- Plan 36.2-04 (audit trail) can now reference commit `b9e068b` as the closing SHA for the depcheck cleanup row.

---
*Phase: 36.2-close-36-debt-schema-models-cleanup*
*Completed: 2026-04-28*
