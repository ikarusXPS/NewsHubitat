---
phase: 20-monitoring-alerting
plan: 03
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - docker-compose.yml
  - grafana/provisioning/datasources/prometheus.yml
  - grafana/provisioning/dashboards/dashboards.yml
  - grafana/provisioning/dashboards/newshub-operations.json
  - docs/monitoring/uptimerobot.md
autonomous: true
requirements: [MNTR-03, MNTR-04]
user_setup:
  - service: uptimerobot
    why: "External uptime monitoring"
    dashboard_config:
      - task: "Create UptimeRobot account"
        location: "https://uptimerobot.com/signUp"
      - task: "Add health monitor"
        location: "UptimeRobot Dashboard -> Add New Monitor"
        settings: "HTTP(s), URL: https://your-domain.com/health, Interval: 5 min"
      - task: "Add readiness monitor"
        location: "UptimeRobot Dashboard -> Add New Monitor"
        settings: "HTTP(s), URL: https://your-domain.com/readiness, Interval: 5 min"
      - task: "Enable SSL monitoring"
        location: "Each monitor -> SSL Certificate Expiration alerts"

must_haves:
  truths:
    - "Grafana dashboard visualizes request rate, error rate, latency p95, and up status"
    - "Grafana auto-provisions Prometheus datasource on startup"
    - "UptimeRobot setup documentation exists"
    - "Docker Compose starts Grafana container on port 3000"
  artifacts:
    - path: "grafana/provisioning/datasources/prometheus.yml"
      provides: "Prometheus datasource configuration"
      contains: "type: prometheus"
    - path: "grafana/provisioning/dashboards/newshub-operations.json"
      provides: "Operations dashboard with Four Golden Signals"
      contains: "NewsHub Operations"
    - path: "docs/monitoring/uptimerobot.md"
      provides: "UptimeRobot setup guide"
      contains: "UptimeRobot"
    - path: "docker-compose.yml"
      provides: "Grafana service definition"
      contains: "grafana/grafana-oss"
  key_links:
    - from: "grafana/provisioning/datasources/prometheus.yml"
      to: "prometheus:9090"
      via: "url configuration"
      pattern: "url: http://prometheus:9090"
    - from: "grafana/provisioning/dashboards/dashboards.yml"
      to: "grafana/provisioning/dashboards/*.json"
      via: "dashboard provider path"
      pattern: "path: /etc/grafana/provisioning/dashboards"
---

<objective>
Add Grafana dashboard for metrics visualization and document UptimeRobot external monitoring setup.

Purpose: Enable operations visibility through Grafana dashboards showing the Four Golden Signals (latency, traffic, errors, saturation), and provide documentation for external uptime monitoring.

Output: Grafana Docker service, provisioned Prometheus datasource, operations dashboard JSON, and UptimeRobot setup documentation.
</objective>

<execution_context>
@get-shit-done/workflows/execute-plan.md
@get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/20-monitoring-alerting/20-CONTEXT.md
@.planning/phases/20-monitoring-alerting/20-RESEARCH.md

<interfaces>
<!-- Docker Compose patterns from existing file -->

From docker-compose.yml (service with volumes and healthcheck):
```yaml
  app:
    build: .
    container_name: newshub-app
    ports:
      - "3001:3001"
    volumes:
      - ./prometheus:/etc/prometheus:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
```

From 20-RESEARCH.md Pattern 7 (Grafana datasource provisioning):
```yaml
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Grafana provisioning configuration</name>
  <files>grafana/provisioning/datasources/prometheus.yml, grafana/provisioning/dashboards/dashboards.yml</files>
  <read_first>
    - .planning/phases/20-monitoring-alerting/20-RESEARCH.md (Pattern 7)
    - docker-compose.yml (service patterns)
  </read_first>
  <action>
1. Create directory structure:
```bash
mkdir -p grafana/provisioning/datasources
mkdir -p grafana/provisioning/dashboards
```

2. Create `grafana/provisioning/datasources/prometheus.yml`:

```yaml
# Grafana datasource provisioning for Prometheus
# Auto-provisioned on container startup
# See: .planning/phases/20-monitoring-alerting/20-CONTEXT.md

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "15s"    # Match Prometheus scrape interval
```

3. Create `grafana/provisioning/dashboards/dashboards.yml`:

```yaml
# Grafana dashboard provisioning
# Automatically loads dashboards from /etc/grafana/provisioning/dashboards
# See: .planning/phases/20-monitoring-alerting/20-CONTEXT.md

apiVersion: 1

providers:
  - name: 'NewsHub'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
```
  </action>
  <verify>
    <automated>test -f grafana/provisioning/datasources/prometheus.yml && test -f grafana/provisioning/dashboards/dashboards.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grafana/provisioning/datasources/prometheus.yml exists
    - grafana/provisioning/datasources/prometheus.yml contains "apiVersion: 1"
    - grafana/provisioning/datasources/prometheus.yml contains "type: prometheus"
    - grafana/provisioning/datasources/prometheus.yml contains "url: http://prometheus:9090"
    - grafana/provisioning/datasources/prometheus.yml contains "isDefault: true"
    - grafana/provisioning/datasources/prometheus.yml contains "editable: false"
    - grafana/provisioning/dashboards/dashboards.yml exists
    - grafana/provisioning/dashboards/dashboards.yml contains "apiVersion: 1"
    - grafana/provisioning/dashboards/dashboards.yml contains "type: file"
    - grafana/provisioning/dashboards/dashboards.yml contains "path: /etc/grafana/provisioning/dashboards"
  </acceptance_criteria>
  <done>Grafana datasource and dashboard provisioning configured</done>
</task>

<task type="auto">
  <name>Task 2: Create NewsHub Operations dashboard</name>
  <files>grafana/provisioning/dashboards/newshub-operations.json</files>
  <read_first>
    - grafana/provisioning/dashboards/dashboards.yml (just created)
    - .planning/phases/20-monitoring-alerting/20-CONTEXT.md (D-44, D-45)
  </read_first>
  <action>
Create `grafana/provisioning/dashboards/newshub-operations.json` with a single operations dashboard (D-44) containing the Four Golden Signals (D-45):

```json
{
  "annotations": {
    "list": []
  },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 1,
      "panels": [],
      "title": "Overview - Four Golden Signals",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "mappings": [],
          "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] },
          "unit": "reqps"
        },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 6, "x": 0, "y": 1 },
      "id": 2,
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "textMode": "auto"
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "sum(rate(http_requests_total[5m]))",
          "refId": "A"
        }
      ],
      "title": "Request Rate",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 0.005 },
              { "color": "red", "value": 0.01 }
            ]
          },
          "unit": "percentunit"
        },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 6, "x": 6, "y": 1 },
      "id": 3,
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "textMode": "auto"
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "sum(rate(http_requests_total{status_code=~\"5..\"}[5m])) / sum(rate(http_requests_total[5m]))",
          "refId": "A"
        }
      ],
      "title": "Error Rate",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 1 },
              { "color": "red", "value": 2 }
            ]
          },
          "unit": "s"
        },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 6, "x": 12, "y": 1 },
      "id": 4,
      "options": {
        "colorMode": "value",
        "graphMode": "area",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "textMode": "auto"
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))",
          "refId": "A"
        }
      ],
      "title": "Latency p95",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "thresholds" },
          "mappings": [
            { "options": { "0": { "color": "red", "index": 1, "text": "DOWN" }, "1": { "color": "green", "index": 0, "text": "UP" } }, "type": "value" }
          ],
          "thresholds": { "mode": "absolute", "steps": [{ "color": "red", "value": null }, { "color": "green", "value": 1 }] },
          "unit": "none"
        },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 6, "x": 18, "y": 1 },
      "id": 5,
      "options": {
        "colorMode": "background",
        "graphMode": "none",
        "justifyMode": "auto",
        "orientation": "auto",
        "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false },
        "textMode": "auto"
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "up{service=\"newshub\"}",
          "refId": "A"
        }
      ],
      "title": "Service Status",
      "type": "stat"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 7 },
      "id": 6,
      "panels": [],
      "title": "API Metrics",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": {
            "axisCenteredZero": false,
            "axisColorMode": "text",
            "axisLabel": "",
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "line",
            "fillOpacity": 10,
            "gradientMode": "none",
            "hideFrom": { "legend": false, "tooltip": false, "viz": false },
            "lineInterpolation": "linear",
            "lineWidth": 1,
            "pointSize": 5,
            "scaleDistribution": { "type": "linear" },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": { "group": "A", "mode": "none" },
            "thresholdsStyle": { "mode": "off" }
          },
          "mappings": [],
          "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] },
          "unit": "reqps"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 7,
      "options": {
        "legend": { "calcs": [], "displayMode": "list", "placement": "bottom", "showLegend": true },
        "tooltip": { "mode": "multi", "sort": "none" }
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "sum(rate(http_requests_total[5m])) by (route)",
          "legendFormat": "{{ route }}",
          "refId": "A"
        }
      ],
      "title": "Request Rate by Route",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": {
            "axisCenteredZero": false,
            "axisColorMode": "text",
            "axisLabel": "",
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "line",
            "fillOpacity": 10,
            "gradientMode": "none",
            "hideFrom": { "legend": false, "tooltip": false, "viz": false },
            "lineInterpolation": "linear",
            "lineWidth": 1,
            "pointSize": 5,
            "scaleDistribution": { "type": "linear" },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": { "group": "A", "mode": "none" },
            "thresholdsStyle": { "mode": "off" }
          },
          "mappings": [],
          "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] },
          "unit": "s"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 8,
      "options": {
        "legend": { "calcs": [], "displayMode": "list", "placement": "bottom", "showLegend": true },
        "tooltip": { "mode": "multi", "sort": "none" }
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le, route))",
          "legendFormat": "{{ route }}",
          "refId": "A"
        }
      ],
      "title": "Latency p95 by Route",
      "type": "timeseries"
    },
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 16 },
      "id": 9,
      "panels": [],
      "title": "System Metrics",
      "type": "row"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": {
            "axisCenteredZero": false,
            "axisColorMode": "text",
            "axisLabel": "",
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "line",
            "fillOpacity": 10,
            "gradientMode": "none",
            "hideFrom": { "legend": false, "tooltip": false, "viz": false },
            "lineInterpolation": "linear",
            "lineWidth": 1,
            "pointSize": 5,
            "scaleDistribution": { "type": "linear" },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": { "group": "A", "mode": "none" },
            "thresholdsStyle": { "mode": "off" }
          },
          "mappings": [],
          "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] },
          "unit": "short"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 0, "y": 17 },
      "id": 10,
      "options": {
        "legend": { "calcs": [], "displayMode": "list", "placement": "bottom", "showLegend": true },
        "tooltip": { "mode": "multi", "sort": "none" }
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "websocket_connections_active",
          "legendFormat": "WebSocket",
          "refId": "A"
        }
      ],
      "title": "WebSocket Connections",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": {
            "axisCenteredZero": false,
            "axisColorMode": "text",
            "axisLabel": "",
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "line",
            "fillOpacity": 10,
            "gradientMode": "none",
            "hideFrom": { "legend": false, "tooltip": false, "viz": false },
            "lineInterpolation": "linear",
            "lineWidth": 1,
            "pointSize": 5,
            "scaleDistribution": { "type": "linear" },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": { "group": "A", "mode": "none" },
            "thresholdsStyle": { "mode": "off" }
          },
          "mappings": [],
          "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] },
          "unit": "bytes"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 17 },
      "id": 11,
      "options": {
        "legend": { "calcs": [], "displayMode": "list", "placement": "bottom", "showLegend": true },
        "tooltip": { "mode": "multi", "sort": "none" }
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "newshub_process_resident_memory_bytes",
          "legendFormat": "RSS Memory",
          "refId": "A"
        },
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "newshub_process_heap_bytes",
          "legendFormat": "Heap",
          "refId": "B"
        }
      ],
      "title": "Memory Usage",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": {
            "axisCenteredZero": false,
            "axisColorMode": "text",
            "axisLabel": "",
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "line",
            "fillOpacity": 10,
            "gradientMode": "none",
            "hideFrom": { "legend": false, "tooltip": false, "viz": false },
            "lineInterpolation": "linear",
            "lineWidth": 1,
            "pointSize": 5,
            "scaleDistribution": { "type": "linear" },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": { "group": "A", "mode": "none" },
            "thresholdsStyle": { "mode": "off" }
          },
          "mappings": [],
          "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] },
          "unit": "s"
        },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 8, "x": 16, "y": 17 },
      "id": 12,
      "options": {
        "legend": { "calcs": [], "displayMode": "list", "placement": "bottom", "showLegend": true },
        "tooltip": { "mode": "multi", "sort": "none" }
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "rate(newshub_nodejs_eventloop_lag_seconds[5m])",
          "legendFormat": "Event Loop Lag",
          "refId": "A"
        }
      ],
      "title": "Event Loop Lag",
      "type": "timeseries"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["newshub", "operations"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "",
  "title": "NewsHub Operations",
  "uid": "newshub-ops",
  "version": 1,
  "weekStart": ""
}
```
  </action>
  <verify>
    <automated>test -f grafana/provisioning/dashboards/newshub-operations.json && grep -q "NewsHub Operations" grafana/provisioning/dashboards/newshub-operations.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - grafana/provisioning/dashboards/newshub-operations.json exists
    - grafana/provisioning/dashboards/newshub-operations.json contains "title": "NewsHub Operations"
    - grafana/provisioning/dashboards/newshub-operations.json contains "Request Rate" panel
    - grafana/provisioning/dashboards/newshub-operations.json contains "Error Rate" panel
    - grafana/provisioning/dashboards/newshub-operations.json contains "Latency p95" panel
    - grafana/provisioning/dashboards/newshub-operations.json contains "Service Status" panel
    - grafana/provisioning/dashboards/newshub-operations.json contains "Request Rate by Route" panel
    - grafana/provisioning/dashboards/newshub-operations.json contains "Latency p95 by Route" panel
    - grafana/provisioning/dashboards/newshub-operations.json contains "WebSocket Connections" panel
    - grafana/provisioning/dashboards/newshub-operations.json contains "Memory Usage" panel
    - grafana/provisioning/dashboards/newshub-operations.json contains prometheus queries for http_requests_total
    - grafana/provisioning/dashboards/newshub-operations.json contains prometheus query for histogram_quantile
    - grafana/provisioning/dashboards/newshub-operations.json contains "refresh": "30s"
  </acceptance_criteria>
  <done>NewsHub Operations dashboard created with Four Golden Signals per D-44, D-45</done>
</task>

<task type="auto">
  <name>Task 3: Add Grafana to Docker Compose</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (current state after Plan 02)
    - grafana/provisioning/datasources/prometheus.yml (just created)
    - grafana/provisioning/dashboards/dashboards.yml (just created)
  </read_first>
  <action>
Update `docker-compose.yml` to add Grafana service after alertmanager (D-43):

1. Add grafana service:

```yaml
  grafana:
    image: grafana/grafana-oss:13.0.1
    container_name: newshub-grafana
    ports:
      - "3000:3000"
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - grafana_data:/var/lib/grafana
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin
      - GF_USERS_ALLOW_SIGN_UP=false
    depends_on:
      - prometheus
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped
```

2. Add grafana_data volume to volumes section:

```yaml
  grafana_data:
```

The volumes section should now have: postgres_data, redis_data, prometheus_data, alertmanager_data, grafana_data
  </action>
  <verify>
    <automated>grep -q "grafana/grafana-oss" docker-compose.yml && grep -q "grafana_data:" docker-compose.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains "image: grafana/grafana-oss:13.0.1"
    - docker-compose.yml contains "container_name: newshub-grafana"
    - docker-compose.yml contains "3000:3000"
    - docker-compose.yml contains "./grafana/provisioning:/etc/grafana/provisioning"
    - docker-compose.yml contains "grafana_data:/var/lib/grafana"
    - docker-compose.yml contains "GF_SECURITY_ADMIN_USER=admin"
    - docker-compose.yml contains "GF_SECURITY_ADMIN_PASSWORD=admin"
    - docker-compose.yml contains "GF_USERS_ALLOW_SIGN_UP=false"
    - docker-compose.yml grafana depends_on prometheus
    - docker-compose.yml contains "grafana_data:" in volumes section
    - docker compose config validates successfully
  </acceptance_criteria>
  <done>Grafana Docker Compose service configured per D-43</done>
</task>

<task type="auto">
  <name>Task 4: Create UptimeRobot setup documentation</name>
  <files>docs/monitoring/uptimerobot.md</files>
  <read_first>
    - .planning/phases/20-monitoring-alerting/20-CONTEXT.md (D-34 through D-42)
  </read_first>
  <action>
1. Create directory:
```bash
mkdir -p docs/monitoring
```

2. Create `docs/monitoring/uptimerobot.md` (D-42):

```markdown
# UptimeRobot External Monitoring Setup

This document describes how to configure external uptime monitoring for NewsHub using UptimeRobot.

## Overview

UptimeRobot provides external HTTP monitoring from multiple geographic locations. It alerts when endpoints are unreachable or return errors, independent of the internal Prometheus/Alertmanager monitoring.

**Why external monitoring?**
- Detects outages that internal monitoring cannot see (e.g., network issues, DNS failures)
- Independent from application infrastructure
- Free tier includes 50 monitors with 5-minute intervals

## Account Setup

1. Create a free account at [https://uptimerobot.com/signUp](https://uptimerobot.com/signUp)
2. Verify email address
3. Navigate to Dashboard

## Monitor Configuration

### Health Monitor (D-35)

Create a monitor for the liveness endpoint:

| Setting | Value |
|---------|-------|
| Monitor Type | HTTP(s) |
| Friendly Name | NewsHub-prod-health (D-41) |
| URL | https://your-production-domain.com/health |
| Monitoring Interval | 5 minutes (D-36) |
| Alert Contact | Your email |

**Alert Settings:**
- Alert After | 2 consecutive failures (D-37: ~10 minutes total)
- Alert Type | Email (D-38)

### Readiness Monitor (D-35)

Create a monitor for the readiness endpoint:

| Setting | Value |
|---------|-------|
| Monitor Type | HTTP(s) |
| Friendly Name | NewsHub-prod-readiness (D-41) |
| URL | https://your-production-domain.com/readiness |
| Monitoring Interval | 5 minutes (D-36) |
| Alert Contact | Your email |

**Alert Settings:**
- Alert After | 2 consecutive failures (D-37)
- Alert Type | Email (D-38)

### SSL Certificate Monitor (D-39)

For each HTTP monitor, enable SSL certificate monitoring:

1. Click on the monitor
2. Go to "SSL Certificate Expiration" section
3. Enable "SSL Certificate Expiration" alerts
4. Set warning threshold (default: 30 days before expiry)

## Alert Contacts

Configure email alerts (D-38):

1. Go to "My Settings" -> "Alert Contacts"
2. Add email addresses that should receive alerts
3. Verify each email address

## Verification

After setup, verify monitors are working:

1. All monitors should show "Up" status with green indicator
2. Response time should be displayed (typically < 500ms)
3. SSL certificate expiry date should be shown

## Naming Convention (D-41)

All monitors should follow the pattern: `NewsHub-prod-{endpoint}`

Examples:
- `NewsHub-prod-health`
- `NewsHub-prod-readiness`

## Status Page (D-40)

Public status page is **not enabled** in initial setup. To enable later:

1. Go to "Status Pages" in UptimeRobot dashboard
2. Create new status page
3. Add monitors to display
4. Configure custom domain (optional)

## Maintenance Windows

When performing planned maintenance:

1. Go to "Maintenance Windows" in UptimeRobot
2. Create a maintenance window for the expected duration
3. Select affected monitors
4. Alerts will be suppressed during the window

## Troubleshooting

| Issue | Resolution |
|-------|------------|
| Monitor shows "Down" but app is running | Check if endpoint is accessible from external networks |
| Response time is high | Check server location, consider CDN |
| SSL monitoring not working | Ensure certificate is valid and properly configured |
| Too many alerts | Increase "Alert After" threshold to reduce false positives |

---

*Document created: Phase 20 - Monitoring & Alerting*
*Last updated: See git history*
```
  </action>
  <verify>
    <automated>test -f docs/monitoring/uptimerobot.md && grep -q "UptimeRobot" docs/monitoring/uptimerobot.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/monitoring/uptimerobot.md exists
    - docs/monitoring/uptimerobot.md contains "UptimeRobot External Monitoring Setup"
    - docs/monitoring/uptimerobot.md contains "Monitor Type | HTTP(s)"
    - docs/monitoring/uptimerobot.md contains "NewsHub-prod-health"
    - docs/monitoring/uptimerobot.md contains "NewsHub-prod-readiness"
    - docs/monitoring/uptimerobot.md contains "5 minutes" interval
    - docs/monitoring/uptimerobot.md contains "2 consecutive failures"
    - docs/monitoring/uptimerobot.md contains "SSL Certificate Expiration"
    - docs/monitoring/uptimerobot.md contains naming convention "NewsHub-prod-{endpoint}"
    - docs/monitoring/uptimerobot.md contains status page note about not enabled
  </acceptance_criteria>
  <done>UptimeRobot setup documentation created per D-34 through D-42</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana UI (3000) | Internal network, requires authentication |
| Grafana admin credentials | Default credentials, should be changed in production |
| UptimeRobot | External SaaS, accesses public endpoints only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-09 | I (Information Disclosure) | Grafana UI | mitigate | Requires login; disable sign-up (GF_USERS_ALLOW_SIGN_UP=false) |
| T-20-10 | E (Elevation of Privilege) | Grafana default password | mitigate | Document password change requirement; default is admin/admin |
| T-20-11 | I (Information Disclosure) | UptimeRobot | accept | Only accesses public health endpoints with no sensitive data |
| T-20-12 | T (Tampering) | Grafana provisioning | mitigate | Mount config as read-only (:ro), changes require redeploy |
</threat_model>

<verification>
```bash
# Validate YAML syntax
docker compose config

# Verify Grafana provisioning files
test -f grafana/provisioning/datasources/prometheus.yml
test -f grafana/provisioning/dashboards/dashboards.yml
test -f grafana/provisioning/dashboards/newshub-operations.json

# Verify documentation
test -f docs/monitoring/uptimerobot.md
```
</verification>

<success_criteria>
- Grafana datasource provisioning for Prometheus
- Grafana dashboard provisioning configuration
- NewsHub Operations dashboard with Four Golden Signals (D-44, D-45)
- Dashboard includes: Request Rate, Error Rate, Latency p95, Service Status
- Dashboard includes: Request Rate by Route, Latency by Route charts
- Dashboard includes: WebSocket connections, Memory usage, Event loop lag
- docker-compose.yml includes Grafana service (D-43)
- Grafana has default admin credentials with sign-up disabled
- UptimeRobot documentation covers health and readiness monitors (D-34, D-35)
- UptimeRobot documentation covers 5-min intervals (D-36), 2 failures (D-37)
- UptimeRobot documentation covers SSL monitoring (D-39)
- UptimeRobot documentation uses naming convention (D-41)
- docker compose config validates successfully
</success_criteria>

<output>
After completion, create `.planning/phases/20-monitoring-alerting/20-03-SUMMARY.md`
</output>
