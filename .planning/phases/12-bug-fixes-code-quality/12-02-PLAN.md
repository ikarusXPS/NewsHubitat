---
phase: 12-bug-fixes-code-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Multiple files with ESLint errors
autonomous: true
requirements: [QUAL-01]
must_haves:
  truths:
    - "ESLint passes with zero errors"
    - "All unused variables removed or prefixed with underscore"
    - "All let declarations changed to const where appropriate"
  artifacts:
    - path: "eslint output"
      provides: "Zero errors"
---

<objective>
Fix all ESLint errors to achieve zero errors across the codebase.

Purpose: Meet QUAL-01 requirement for clean ESLint output.
Output: All 325+ ESLint errors resolved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bug-fixes-code-quality/12-CONTEXT.md
</context>

<interfaces>
<!-- ESLint Error Categories -->
Main error types from current lint output:
1. `@typescript-eslint/no-unused-vars` - Unused variables/imports
2. `prefer-const` - Variables that should be const
3. `@typescript-eslint/no-unsafe-function-type` - Generic Function type usage
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run ESLint auto-fix</name>
  <files>All source files</files>
  <read_first>
    - D:\NewsHub\eslint.config.js (ESLint configuration)
  </read_first>
  <action>
Run ESLint with auto-fix to resolve automatically fixable issues:
```bash
npm run lint -- --fix
```

This will fix:
- `prefer-const` errors (change let to const)
- Some formatting issues
  </action>
  <verify>
    <automated>npm run lint 2>&1 | grep -E "^\d+ problems" | head -1</automated>
  </verify>
  <acceptance_criteria>
    - Auto-fix completes without errors
    - Error count decreases from 325
  </acceptance_criteria>
  <done>Auto-fixable ESLint errors resolved</done>
</task>

<task type="auto">
  <name>Task 2: Fix unused variables in test files</name>
  <files>src/**/*.test.ts, src/**/*.test.tsx</files>
  <read_first>
    - D:\NewsHub\src\hooks\useCachedQuery.test.tsx (has unused 'result')
    - D:\NewsHub\src\hooks\useEventSocket.test.ts (has unused 'GeoEvent', 'Function' type issues)
    - D:\NewsHub\src\lib\personalization.test.ts (has unused 'PerspectiveRegion')
  </read_first>
  <action>
For each file with unused variable errors:

1. **useCachedQuery.test.tsx line 253**: Remove or use the `result` variable
2. **useEventSocket.test.ts line 10**: Remove unused `GeoEvent` import
3. **useEventSocket.test.ts lines 13, 16, 46**: Replace `Function` type with specific function signature like `() => void` or `(...args: unknown[]) => unknown`
4. **personalization.test.ts line 15**: Remove unused `PerspectiveRegion` import

Pattern for fixing:
- Unused import: Remove from import statement
- Unused variable: Prefix with `_` or remove if truly unnecessary
- `Function` type: Replace with `() => void` or appropriate signature
  </action>
  <verify>
    <automated>npm run lint -- src/hooks/*.test.ts src/hooks/*.test.tsx src/lib/*.test.ts 2>&1 | grep -c "error" || echo "0"</automated>
  </verify>
  <acceptance_criteria>
    - No unused variable errors in test files
    - No `Function` type errors
    - grep -c "@typescript-eslint/no-unused-vars" in test file lint output returns 0
  </acceptance_criteria>
  <done>Test file ESLint errors fixed</done>
</task>

<task type="auto">
  <name>Task 3: Fix remaining source file errors</name>
  <files>src/**, server/**</files>
  <read_first>
    - Run npm run lint to get current error list
  </read_first>
  <action>
Review remaining ESLint errors and fix each:

1. For each file with errors, read the file first
2. Fix errors based on type:
   - Unused imports: Remove from import statement
   - Unused variables: Remove or prefix with `_`
   - `prefer-const`: Change `let` to `const`
   - Type issues: Add proper TypeScript types

3. Run lint after each batch of fixes to verify progress
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run lint exits with 0 errors
    - Output shows "0 problems" or similar success message
  </acceptance_criteria>
  <done>All ESLint errors resolved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Code quality only, no security boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | Linting changes, no security impact |
</threat_model>

<verification>
1. Run `npm run lint` - should show 0 errors
2. Run `npm run typecheck` - should still pass
3. Run `npm run test:run` - tests should still pass
</verification>

<success_criteria>
- `npm run lint` exits with code 0
- Zero ESLint errors in output
- Build and tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/12-bug-fixes-code-quality/12-02-SUMMARY.md`
</output>
