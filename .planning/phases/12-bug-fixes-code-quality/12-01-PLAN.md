---
phase: 12-bug-fixes-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/NewsCard.tsx
autonomous: true
requirements: [BUG-01]
must_haves:
  truths:
    - "Article images show placeholder when original fails to load"
    - "onError handler exists on article image elements"
  artifacts:
    - path: "src/components/NewsCard.tsx"
      provides: "Thumbnail fallback implementation"
  key_links:
    - from: "src/components/NewsCard.tsx"
      to: "public/placeholder-news.svg"
      via: "onError handler"
      pattern: "onError.*placeholder"
---

<objective>
Fix B7: Article thumbnail fallback system shows placeholder when image fails to load.

Purpose: Ensure users see a placeholder instead of broken image icons when article thumbnails fail.
Output: NewsCard component with onError handler that swaps failed images to placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bug-fixes-code-quality/12-CONTEXT.md
</context>

<interfaces>
<!-- NewsCard image handling -->
The NewsCard component displays article thumbnails. When images fail to load (404, network error, invalid URL), the browser shows a broken image icon.

Solution: Add onError handler to swap to placeholder image.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add thumbnail fallback handler to NewsCard</name>
  <files>src/components/NewsCard.tsx</files>
  <read_first>
    - D:\NewsHub\src\components\NewsCard.tsx (current implementation)
    - D:\NewsHub\public (check for existing placeholder images)
  </read_first>
  <action>
1. Check if placeholder image exists in public/ directory. If not, note that a simple SVG placeholder may need to be created or use a data URI.

2. Find the img tag(s) displaying article thumbnails in NewsCard.tsx.

3. Add onError handler to replace failed images with placeholder:
```typescript
onError={(e) => {
  const target = e.currentTarget;
  target.onerror = null; // Prevent infinite loop
  target.src = '/placeholder-news.svg';
}}
```

4. If no placeholder exists, create one or use a base64 placeholder:
```typescript
const PLACEHOLDER_IMAGE = 'data:image/svg+xml,...'; // Simple news icon SVG
```

5. Ensure the fallback works for all article image locations in the component.
  </action>
  <verify>
    <automated>grep -n "onError" src/components/NewsCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/NewsCard.tsx contains `onError` handler on image element
    - grep -q "onError" src/components/NewsCard.tsx returns 0
    - Image fallback prevents broken image icons
  </acceptance_criteria>
  <done>NewsCard has onError handler for thumbnail fallback</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External images | Article thumbnails from external news sources |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | -- | -- | -- | UI-only change, no security impact |
</threat_model>

<verification>
1. Build passes: `npm run build`
2. Tests pass: `npm run test:run`
3. Manual: Load page with invalid image URL, verify placeholder shows
</verification>

<success_criteria>
- NewsCard.tsx has onError handler on thumbnail images
- Build and tests still pass
- No broken image icons when thumbnails fail to load
</success_criteria>

<output>
After completion, create `.planning/phases/12-bug-fixes-code-quality/12-01-SUMMARY.md`
</output>
