---
phase: 15-query-optimization
plan: 04
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03]
files_modified: []
autonomous: false
requirements: [PERF-05]
must_haves:
  truths:
    - "Server-Timing header appears in all API responses"
    - "p95 response time is measured and documented"
    - "Leaderboard endpoint makes only 1 database query"
    - "Article saves execute in parallel chunks"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 15 optimizations are working correctly.

Purpose: Manual verification that Server-Timing headers appear in Chrome DevTools, N+1 is eliminated, and chunked saves work as expected.
Output: Human confirmation of p95 response time baseline
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/15-query-optimization/15-CONTEXT.md
@.planning/phases/15-query-optimization/15-01-SUMMARY.md
@.planning/phases/15-query-optimization/15-02-SUMMARY.md
@.planning/phases/15-query-optimization/15-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite</name>
  <files></files>
  <read_first>
    - None
  </read_first>
  <action>
Run all tests to verify no regressions:

```bash
npm run test:run
npm run typecheck
```

Expected: All tests pass, TypeScript compiles with no errors.
  </action>
  <verify>
    <automated>npm run test:run && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All unit tests pass (expected 1050+ tests)
    - TypeScript compiles with 0 errors
  </acceptance_criteria>
  <done>Test suite passes with all Phase 15 changes</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Server-Timing middleware, N+1 query elimination, and chunked parallel saves.
  </what-built>
  <how-to-verify>
1. **Start the backend server:**
   ```bash
   npm run dev:backend
   ```

2. **Verify Server-Timing header (D-05, D-06):**
   - Open Chrome DevTools -> Network tab
   - Navigate to http://localhost:5173 (or use curl)
   - Click on any API request (e.g., /api/health)
   - Check "Timing" section or response headers
   - **Expected:** `Server-Timing: total;dur=X.XX` header present

   Or use curl:
   ```bash
   curl -I http://localhost:3001/api/health
   ```
   **Expected output includes:** `Server-Timing: total;dur=X.XX`

3. **Verify leaderboard response time:**
   ```bash
   curl -w "\nTotal time: %{time_total}s\n" http://localhost:3001/api/leaderboard
   ```
   - Should respond in < 200ms (p95 target)
   - Response should include `success: true` and `data` array

4. **Check leaderboard points calculation:**
   - If you have users with badges, verify points match:
   - bronze = threshold * 1
   - silver = threshold * 2
   - gold = threshold * 4
   - platinum = threshold * 10

5. **Verify Server-Timing in leaderboard:**
   ```bash
   curl -I http://localhost:3001/api/leaderboard
   ```
   **Expected:** `Server-Timing: total;dur=X.XX` with X < 200

  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | - | - | - | Verification-only plan |
</threat_model>

<verification>
Human verification via checkpoint.
</verification>

<success_criteria>
- [ ] All tests pass (1050+ tests)
- [ ] TypeScript compiles with no errors
- [ ] Server-Timing header appears in API responses
- [ ] Leaderboard responds in < 200ms
- [ ] Human approves all verification checks
</success_criteria>

<output>
After completion, create `.planning/phases/15-query-optimization/15-04-SUMMARY.md`
</output>
