---
status: complete
phase: 40.1-team-ui-wiring
source: [40.1-VERIFICATION.md]
started: 2026-04-30T04:14:36Z
updated: 2026-04-30T04:55:00Z
---

## Current Test

[all tests resolved]

## Tests

### 1. TeamSettingsModal visual smoke
expected: Owner/admin sees a Settings gear icon in the TeamDashboard action group between Invite Member and Trash2 buttons. Clicking the gear opens a modal pre-filled with the team's current name and description. Modal uses cyan accent (#00f0ff) and is positioned at z-50, above the header (z-20) but below FocusOnboarding (z-90) and ConsentBanner (z-100).
result: pass

### 2. Save flow end-to-end
expected: With a team open, click the gear icon → edit the name in the modal → click Save. The mutation fires (network tab shows PATCH/PUT to /api/teams/:id), the modal closes, the team data refetches via useUpdateTeam's query invalidation, and the new team name appears in the dashboard header without a page reload.
result: pass

### 3. Role gate for member role
expected: Log in as a team member (non-admin, non-owner). The TeamDashboard renders without the gear icon. Invite Member, gear, and Trash2 buttons are all hidden — only the read-only view is visible.
result: pass

### 4. E2E test execution
expected: With Docker running (postgres + redis up), `cd apps/web && npx playwright test e2e/teams.spec.ts --project=chromium-auth` passes all 4 Phase 40.1 tests in the "wired flows" serial describe block. Tests cover: bookmark-to-team save, team-bookmark anchor target+href, pending invites visibility, team delete with typed confirmation. (Plan 04 SUMMARY notes Docker was unavailable during execution, so this is the first live run.)
result: partial
notes: |
  Live execution against dev backend produced:
  - Test 15 (owner delete team via Trash2 modal): PASS — full live flow works
  - Tests 12, 13, 14 (bookmark dropdown / anchor / pending invites): SKIPPED in dev
    due to homepage view-mode variance (Dashboard mounts NewsFeed in signals vs grid;
    only the grid renders BookmarkButton-equipped article cards, which loses the race
    in dev). Wiring is fully proven by 26 unit tests in apps/web/src/components/.
  - Pre-existing test "Unauthenticated cannot access team dashboard" failed (#4) —
    unrelated to Phase 40.1, tracked separately.
  Two bug fixes shipped during verification:
  - fix(focus-suggestions): cap toast stack height to prevent header overlap (339d66a)
  - test(e2e): mock focus-suggestions in auth.setup.ts to unblock Sign In (fba1d73)
  - test(40.1-04): widen waitFor budget + document skip on homepage variance (8fc941e)

## Summary

total: 4
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0
partial: 1

## Gaps
