---
phase: 40.1-team-ui-wiring
plan: 05
type: execute
wave: 4
depends_on: [40.1-04]
files_modified:
  - .planning/debug/resolved/2026-04-25-28-team-delete-not-found.md
  - .planning/debug/resolved/2026-04-25-bookmarks-not-saved-to-team.md
  - .planning/debug/resolved/2026-04-25-no-pending-invites-team-dashboard.md
  - .planning/debug/resolved/2026-04-25-team-bookmarks-new-tab.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All 4 debug session files have been moved from .planning/debug/ to .planning/debug/resolved/"
    - "Each archived file has frontmatter status updated from 'diagnosed' to 'resolved'"
    - "Each archived file has the verification: block populated with the verification command(s) that confirm the fix works"
    - "Each archived file has the files_changed: block populated with the actual files modified across phase 40.1"
    - "The .planning/debug/ inbox no longer contains any of the 4 archived files"
  artifacts:
    - path: .planning/debug/resolved/2026-04-25-28-team-delete-not-found.md
      provides: "Archived resolution record for debug session 1"
      contains: "status: resolved"
    - path: .planning/debug/resolved/2026-04-25-bookmarks-not-saved-to-team.md
      provides: "Archived resolution record for debug session 2"
      contains: "status: resolved"
    - path: .planning/debug/resolved/2026-04-25-no-pending-invites-team-dashboard.md
      provides: "Archived resolution record for debug session 3"
      contains: "status: resolved"
    - path: .planning/debug/resolved/2026-04-25-team-bookmarks-new-tab.md
      provides: "Archived resolution record for debug session 4"
      contains: "status: resolved"
  key_links:
    - from: archived debug files
      to: phase 40.1 plans + commits
      via: files_changed entries citing actual modified paths
      pattern: "files_changed:"
---

<objective>
Archive the 4 debug session files from `.planning/debug/` to `.planning/debug/resolved/` per CONTEXT D-07. Update each file's frontmatter and Resolution block with the verification commands and the actual files changed during phase 40.1. This is the LAST plan and runs only after Plan 04's E2E tests have proven the wiring works.

Purpose: Keeps `.planning/debug/` as a pure inbox for unresolved sessions. The resolution record links each diagnosis to its remediation phase.
Output: 4 files moved + updated; original 4 files no longer present in `.planning/debug/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/40.1-team-ui-wiring/40.1-CONTEXT.md
@.planning/phases/40.1-team-ui-wiring/40.1-01-SUMMARY.md
@.planning/phases/40.1-team-ui-wiring/40.1-02-SUMMARY.md
@.planning/phases/40.1-team-ui-wiring/40.1-03-SUMMARY.md
@.planning/phases/40.1-team-ui-wiring/40.1-04-SUMMARY.md
@.planning/debug/28-team-delete-not-found.md
@.planning/debug/bookmarks-not-saved-to-team.md
@.planning/debug/no-pending-invites-team-dashboard.md
@.planning/debug/team-bookmarks-new-tab.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create resolved/ directory and archive 4 debug sessions with updated metadata</name>
  <files>.planning/debug/resolved/2026-04-25-28-team-delete-not-found.md, .planning/debug/resolved/2026-04-25-bookmarks-not-saved-to-team.md, .planning/debug/resolved/2026-04-25-no-pending-invites-team-dashboard.md, .planning/debug/resolved/2026-04-25-team-bookmarks-new-tab.md</files>
  <read_first>
    - .planning/debug/28-team-delete-not-found.md (FULL FILE — copy content forward, modify frontmatter + Resolution)
    - .planning/debug/bookmarks-not-saved-to-team.md (FULL FILE — same)
    - .planning/debug/no-pending-invites-team-dashboard.md (FULL FILE — same)
    - .planning/debug/team-bookmarks-new-tab.md (FULL FILE — same)
    - .planning/phases/40.1-team-ui-wiring/40.1-01-SUMMARY.md (test files added)
    - .planning/phases/40.1-team-ui-wiring/40.1-02-SUMMARY.md (TeamSettingsModal created)
    - .planning/phases/40.1-team-ui-wiring/40.1-03-SUMMARY.md (TeamDashboard integration)
    - .planning/phases/40.1-team-ui-wiring/40.1-04-SUMMARY.md (E2E tests added + verification commands)
  </read_first>
  <action>
**Step 1: Ensure the resolved directory exists.**
```bash
mkdir -p .planning/debug/resolved
```

**Step 2: For each of the 4 debug session files, perform a Read → Write to the new path with the frontmatter and Resolution block updated.** Use the Read tool to load the original full content, then Write the modified content to the new path. After writing the new file, delete the old file.

For each file: in the YAML frontmatter, change `status: diagnosed` to `status: resolved`, and add an `updated:` field with today's date `2026-04-30`. In the markdown body's `## Resolution` block, fill in the `fix:`, `verification:`, and `files_changed:` sections.

**File 1: 28-team-delete-not-found.md → 2026-04-25-28-team-delete-not-found.md**

Frontmatter changes:
```yaml
status: resolved   # was: diagnosed
updated: 2026-04-30T00:00:00Z   # new field (or update existing)
```

Resolution block — replace the empty `fix:` / `verification:` / `files_changed:` lines (lines 67-69 of original) with:
```yaml
fix: |
  Phase 40.1 created the missing TeamSettingsModal component and integrated it into
  TeamDashboard alongside the previously-missing DeleteTeamModal (which had been silently
  added between 2026-04-25 and 2026-04-30). A Settings gear icon button was added between
  the Invite Member and Trash2 buttons in TeamDashboard.tsx, gated on canInvite (owner+admin).
  The Trash2 button (already present at the time of phase 40.1) handles delete via DeleteTeamModal.
verification: |
  - Vitest unit test: apps/web/src/components/teams/DeleteTeamModal.test.tsx (≥6 tests, all pass)
  - Vitest unit test: apps/web/src/components/teams/TeamSettingsModal.test.tsx (≥8 tests, all pass)
  - E2E test: apps/web/e2e/teams.spec.ts > "Phase 40.1 — wired flows" > "owner can delete a team via Trash2 modal"
  - Manual smoke: gear icon visible on team dashboard for owners + admins; clicking opens the settings modal pre-filled with current team name/description
files_changed:
  - apps/web/src/components/teams/TeamSettingsModal.tsx (new, phase 40.1 plan 02)
  - apps/web/src/components/teams/TeamSettingsModal.test.tsx (new, phase 40.1 plan 02)
  - apps/web/src/components/teams/DeleteTeamModal.test.tsx (new, phase 40.1 plan 01)
  - apps/web/src/pages/TeamDashboard.tsx (gear icon + modal integration, phase 40.1 plan 03)
  - apps/web/e2e/teams.spec.ts (new E2E coverage, phase 40.1 plan 04)
```

**File 2: bookmarks-not-saved-to-team.md → 2026-04-25-bookmarks-not-saved-to-team.md**

Frontmatter: same `status: resolved` + `updated: 2026-04-30T00:00:00Z`.

Resolution block (lines 60-64 of original) — replace `fix:` content with the verified-as-resolved record:
```yaml
fix: |
  BookmarkButton is now imported and rendered in NewsCard.tsx (line 5, line 235) and
  SignalCard.tsx (line 20, line 382). The team dropdown opens on click when the user
  has teams. Phase 40.1 added unit + E2E coverage to prevent silent regression. The
  fix itself was made silently between 2026-04-25 and 2026-04-30; phase 40.1 verifies it.
verification: |
  - Vitest unit test: apps/web/src/components/BookmarkButton.test.tsx (≥6 tests, all pass)
  - E2E test: apps/web/e2e/teams.spec.ts > "Phase 40.1 — wired flows" > "saves a bookmark to a team via dropdown"
  - grep verification: `grep -l "BookmarkButton" apps/web/src/components/NewsCard.tsx apps/web/src/components/SignalCard.tsx` returns both files
files_changed:
  - apps/web/src/components/NewsCard.tsx (BookmarkButton integration, pre-40.1 silent fix)
  - apps/web/src/components/SignalCard.tsx (BookmarkButton integration, pre-40.1 silent fix)
  - apps/web/src/components/BookmarkButton.test.tsx (new, phase 40.1 plan 01)
  - apps/web/e2e/teams.spec.ts (new E2E coverage, phase 40.1 plan 04)
```

**File 3: no-pending-invites-team-dashboard.md → 2026-04-25-no-pending-invites-team-dashboard.md**

Frontmatter: same `status: resolved` + `updated: 2026-04-30T00:00:00Z`.

Resolution block (lines 53-57 of original) — fill `fix:` / `verification:` / `files_changed:`:
```yaml
fix: |
  TeamDashboard.tsx now declares a 3-tab state ('bookmarks' | 'members' | 'invites'),
  imports useTeamInvites at line 13, renders PendingInviteList on the invites tab (line 260),
  and shows a count badge (line 200-204). The PendingInviteList component itself was already
  present. The fix was made silently between 2026-04-25 and 2026-04-30; phase 40.1 verifies it.
verification: |
  - Vitest unit test: apps/web/src/components/teams/PendingInviteList.test.tsx (≥5 tests, all pass)
  - E2E test: apps/web/e2e/teams.spec.ts > "Phase 40.1 — wired flows" > "owner sees Pending Invites tab and sent invites appear in the list"
  - grep verification: `grep -q "useTeamInvites" apps/web/src/pages/TeamDashboard.tsx` exits 0
files_changed:
  - apps/web/src/pages/TeamDashboard.tsx (3rd tab + useTeamInvites + PendingInviteList render, pre-40.1 silent fix)
  - apps/web/src/components/teams/PendingInviteList.test.tsx (new, phase 40.1 plan 01)
  - apps/web/e2e/teams.spec.ts (new E2E coverage, phase 40.1 plan 04)
```

**File 4: team-bookmarks-new-tab.md → 2026-04-25-team-bookmarks-new-tab.md**

Frontmatter: same `status: resolved` + `updated: 2026-04-30T00:00:00Z`.

The original file already has `fix:` populated (lines 81-84) but `verification:` and `files_changed:` are empty. Update both:
```yaml
verification: |
  - Vitest unit test: apps/web/src/components/teams/TeamBookmarkCard.test.tsx (≥7 tests, all pass)
  - E2E test: apps/web/e2e/teams.spec.ts > "Phase 40.1 — wired flows" > "team bookmark anchor has target=_blank and non-fallback href"
  - grep verification on TeamBookmarkCard: `grep -q 'href={articleUrl' apps/web/src/components/teams/TeamBookmarkCard.tsx` exits 0
  - grep verification on TeamDashboard: `grep -q 'articleUrl={bookmark.article?.url}' apps/web/src/pages/TeamDashboard.tsx` exits 0
files_changed:
  - apps/web/server/routes/teams.ts (article join in GET bookmarks endpoint, pre-40.1 silent fix)
  - apps/web/src/hooks/useTeamBookmarks.ts (TeamBookmark interface includes article: { id, title, url } | null, pre-40.1 silent fix)
  - apps/web/src/pages/TeamDashboard.tsx (passes articleTitle + articleUrl to TeamBookmarkCard, pre-40.1 silent fix)
  - apps/web/src/components/teams/TeamBookmarkCard.test.tsx (new, phase 40.1 plan 01)
  - apps/web/e2e/teams.spec.ts (new E2E coverage, phase 40.1 plan 04)
```

**Step 3: Delete the 4 original files** from `.planning/debug/` after the new files are written.

Use bash:
```bash
rm .planning/debug/28-team-delete-not-found.md
rm .planning/debug/bookmarks-not-saved-to-team.md
rm .planning/debug/no-pending-invites-team-dashboard.md
rm .planning/debug/team-bookmarks-new-tab.md
```

**Step 4: Verify the move.**
```bash
ls .planning/debug/resolved/2026-04-25-*.md | wc -l   # must print 4
ls .planning/debug/*.md 2>/dev/null | grep -E "(28-team-delete|bookmarks-not-saved|no-pending-invites|team-bookmarks-new-tab)" | wc -l   # must print 0
grep -l "^status: resolved" .planning/debug/resolved/2026-04-25-*.md | wc -l   # must print 4
```
  </action>
  <acceptance_criteria>
    - Directory `.planning/debug/resolved/` exists
    - File `.planning/debug/resolved/2026-04-25-28-team-delete-not-found.md` exists
    - File `.planning/debug/resolved/2026-04-25-bookmarks-not-saved-to-team.md` exists
    - File `.planning/debug/resolved/2026-04-25-no-pending-invites-team-dashboard.md` exists
    - File `.planning/debug/resolved/2026-04-25-team-bookmarks-new-tab.md` exists
    - File `.planning/debug/28-team-delete-not-found.md` does NOT exist
    - File `.planning/debug/bookmarks-not-saved-to-team.md` does NOT exist
    - File `.planning/debug/no-pending-invites-team-dashboard.md` does NOT exist
    - File `.planning/debug/team-bookmarks-new-tab.md` does NOT exist
    - All 4 archived files contain `status: resolved` in frontmatter (verify: `grep -l "^status: resolved" .planning/debug/resolved/2026-04-25-*.md | wc -l` returns 4)
    - All 4 archived files contain a non-empty `verification:` block (verify: `grep -A 1 "^verification:" .planning/debug/resolved/2026-04-25-*.md | grep -E "(Vitest|E2E|grep)" | wc -l` returns ≥4)
    - All 4 archived files contain a non-empty `files_changed:` block listing at least 1 file (verify: `grep -A 1 "^files_changed:" .planning/debug/resolved/2026-04-25-*.md | grep -E "  - " | wc -l` returns ≥4)
    - The original Evidence sections are preserved (verify: `grep -c "^- timestamp:" .planning/debug/resolved/2026-04-25-*.md` returns total ≥20 — original sessions had 6+5+5+5 = 21+ evidence rows)
  </acceptance_criteria>
  <verify>
    <automated>ls .planning/debug/resolved/2026-04-25-*.md | wc -l && grep -l "^status: resolved" .planning/debug/resolved/2026-04-25-*.md | wc -l</automated>
  </verify>
  <done>4 debug sessions archived with status=resolved, verification + files_changed populated; debug/ inbox cleaned</done>
</task>

</tasks>

<verification>
After the task:
```bash
ls .planning/debug/resolved/2026-04-25-*.md | wc -l                                    # → 4
ls .planning/debug/*.md 2>/dev/null | grep -v "^README" | wc -l                        # whatever remains is non-40.1 work
grep -l "^status: resolved" .planning/debug/resolved/2026-04-25-*.md | wc -l           # → 4
```

Confirm phase deliverables intact:
```bash
cd apps/web && pnpm typecheck && pnpm vitest run src/components
```
Both exit 0.
</verification>

<success_criteria>
- 4 debug session files moved from `.planning/debug/` to `.planning/debug/resolved/` with the `2026-04-25-{slug}.md` naming convention
- Each archived file has `status: resolved` in frontmatter
- Each archived file has populated `verification:` (with Vitest test paths AND Playwright E2E test name AND grep commands) and `files_changed:` (with concrete file paths)
- No backend file modified (D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/40.1-team-ui-wiring/40.1-05-SUMMARY.md` documenting:
- The 4 archived files (new paths)
- A short table mapping debug session → remediation plan(s) within phase 40.1
- Confirmation that `.planning/debug/` no longer contains any of the 4 sessions
- Phase 40.1 overall completion summary (link to all 5 plan summaries)
</output>
