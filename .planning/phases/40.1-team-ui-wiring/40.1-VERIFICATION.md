---
phase: 40.1-team-ui-wiring
verified: 2026-04-30T00:00:00Z
status: human_needed
score: 15/15 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Navigate to a team dashboard as owner. Confirm a gear icon (Settings) appears between the 'Invite Member' button and the Trash2 (delete) icon. Click the gear icon."
    expected: "TeamSettingsModal opens, pre-filled with the team's current name in the name input and current description in the textarea."
    why_human: "Visual placement and pre-fill behavior requires a running dev server with real team data. Cannot be confirmed statically."
  - test: "In the TeamSettingsModal, change the team name and click 'Save Changes'."
    expected: "Toast 'Team updated successfully' appears. Modal closes. Team header on the dashboard shows the new name."
    why_human: "Form submit + mutation + UI refresh requires live backend (PATCH /api/teams/:teamId) and real TanStack Query invalidation."
  - test: "Navigate to a team dashboard as a regular 'member' (not owner or admin). Check the action button group."
    expected: "Neither the Settings gear icon nor the Invite Member button appears. Only the team bookmarks/members tabs are present."
    why_human: "Role-gated rendering (canInvite = false for member) requires a multi-user scenario or role manipulation."
  - test: "Run the 4 Phase 40.1 E2E tests: cd apps/web && npx playwright test e2e/teams.spec.ts --project=chromium-auth --grep='Phase 40.1'"
    expected: "All 4 tests pass or skip cleanly (no failures). Tests that self-skip due to missing seed data are acceptable."
    why_human: "E2E tests require Docker (postgres + redis), running dev server on 5173/3001, and playwright/.auth/user.json. These were NOT run during phase execution per plan (Docker unavailable in execution env)."
---

# Phase 40.1: team-ui-wiring — Verification Report

**Phase Goal:** Close the 4 team-UI debt sessions from Phase 28 by adding regression coverage (unit + E2E tests) and shipping the missing TeamSettingsModal component, then archiving the resolved debug sessions.
**Verified:** 2026-04-30
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 4 wired team components each have a Vitest unit test file | VERIFIED | BookmarkButton.test.tsx (6 tests, 169 lines), TeamBookmarkCard.test.tsx (8 tests, 183 lines), PendingInviteList.test.tsx (5 tests, 143 lines), DeleteTeamModal.test.tsx (7 tests, 157 lines) — all 4 files exist and meet min_lines thresholds |
| 2 | BookmarkButton tests prove team dropdown + personal fallback | VERIFIED | vi.mock('../hooks/useTeams') + vi.mock('../hooks/useTeamBookmarks') present; lucide-chevron-down assertion confirmed; mockAddBookmark wiring assertion present |
| 3 | TeamBookmarkCard tests prove articleUrl href, fallback, target=_blank always | VERIFIED | 8 it() blocks; explicit target="_blank" assertion at line 107; vi.stubGlobal('confirm') for remove flow; articleId fallback path tested |
| 4 | PendingInviteList tests prove role gating (member=null), cancel mutation, invite rows | VERIFIED | 5 it() blocks; container.firstChild toBeNull for member role confirmed at line 93; mockCancelInvite mutation assertion present |
| 5 | DeleteTeamModal tests prove confirmation gate, mutation call, navigate('/') on success | VERIFIED | 7 it() blocks; mockDeleteTeam mock present; toHaveBeenCalledWith('/') assertion confirmed |
| 6 | TeamSettingsModal exists as a new component file | VERIFIED | apps/web/src/components/teams/TeamSettingsModal.tsx — 197 lines (>130 required); named export confirmed |
| 7 | Modal renders editable name (≤50 chars) and description (≤500 chars) pre-filled with current team values | VERIFIED | maxLength={50} and maxLength={500} confirmed; inner/outer component split: TeamSettingsForm mounts with currentName/currentDescription props as initial useState values |
| 8 | Submitting calls useUpdateTeam.mutate with {name, description} and closes on success | VERIFIED | useUpdateTeam imported from '../../hooks/useTeams' (line 11); handleSubmit calls updateTeam({name:trimmedName, description:...}, {onSuccess: () => { toast.success(...); onClose(); }}); confirmed in component source |
| 9 | TeamSettingsModal has unit tests proving form validation, mutation, close-on-success | VERIFIED | TeamSettingsModal.test.tsx — 8 it() blocks (>8 required); vi.mock('../../hooks/useTeams') present; mockUpdateTeam + mutation assertion with {name, description: null}; onClose assertion present |
| 10 | TeamDashboard imports TeamSettingsModal and renders gear icon gated on canInvite | VERIFIED | import { TeamSettingsModal } from '../components/teams/TeamSettingsModal' at line 20; Settings added to lucide-react import; showSettingsModal state at line 29; gear button block at lines 122-131 gated on canInvite |
| 11 | Gear icon is positioned before Trash2 (owner-only delete), gear is canInvite (owner+admin) | VERIFIED | awk ordering check: setShowSettingsModal(true) at line 124 < team.role === 'owner' Trash2 block at line 132; canInvite gate confirmed |
| 12 | 4 new E2E tests exist in teams.spec.ts in a serial describe block | VERIFIED | All 4 test names present (grep count=1 each); test.describe.configure({ mode: 'serial' }) confirmed; Phase 40.1 describe block at line 263 |
| 13 | E2E tests follow all CLAUDE.md E2E conventions | VERIFIED | 127.0.0.1:3001 (not localhost); newshub-auth-token manual JWT attach via page.evaluate; waitForLoadState('domcontentloaded') present; networkidle appears only in comments (lines 235-236, 260) — zero actual calls |
| 14 | All 4 debug session files archived from .planning/debug/ to .planning/debug/resolved/ with status=resolved | VERIFIED | ls confirmed: 4 files in resolved/; originals confirmed GONE from inbox; grep -l "^status: resolved" returns 4 |
| 15 | Each archived file has populated verification: and files_changed: blocks | VERIFIED | verification: blocks cite Vitest paths + Playwright test names + grep commands; files_changed: blocks cite concrete paths with pre-40.1/plan labels |

**Score:** 15/15 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/src/components/BookmarkButton.test.tsx` | Unit tests for team dropdown wiring (≥80 lines, ≥6 tests) | VERIFIED | 169 lines, 6 tests |
| `apps/web/src/components/teams/TeamBookmarkCard.test.tsx` | Unit tests for anchor href/target wiring (≥60 lines, ≥7 tests) | VERIFIED | 183 lines, 8 tests |
| `apps/web/src/components/teams/PendingInviteList.test.tsx` | Unit tests for role gating + cancel (≥70 lines, ≥5 tests) | VERIFIED | 143 lines, 5 tests |
| `apps/web/src/components/teams/DeleteTeamModal.test.tsx` | Unit tests for confirmation gate (≥70 lines, ≥6 tests) | VERIFIED | 157 lines, 7 tests |
| `apps/web/src/components/teams/TeamSettingsModal.tsx` | New modal component (≥130 lines) | VERIFIED | 197 lines; inner/outer split (TeamSettingsForm + TeamSettingsModal) |
| `apps/web/src/components/teams/TeamSettingsModal.test.tsx` | Unit tests for modal wiring (≥80 lines, ≥8 tests) | VERIFIED | 156 lines, 8 tests |
| `apps/web/src/pages/TeamDashboard.tsx` | Integrated TeamSettingsModal trigger and instance | VERIFIED | Import + showSettingsModal state + gear button + modal instance all present |
| `apps/web/e2e/teams.spec.ts` | 4 new E2E tests in serial describe block | VERIFIED | 4 tests appended inside "Phase 40.1 — wired flows" describe |
| `.planning/debug/resolved/2026-04-25-28-team-delete-not-found.md` | Archived with status=resolved | VERIFIED | Exists; status: resolved confirmed |
| `.planning/debug/resolved/2026-04-25-bookmarks-not-saved-to-team.md` | Archived with status=resolved | VERIFIED | Exists; status: resolved confirmed |
| `.planning/debug/resolved/2026-04-25-no-pending-invites-team-dashboard.md` | Archived with status=resolved | VERIFIED | Exists; status: resolved confirmed |
| `.planning/debug/resolved/2026-04-25-team-bookmarks-new-tab.md` | Archived with status=resolved | VERIFIED | Exists; status: resolved confirmed |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| BookmarkButton.test.tsx | useTeams + useAddTeamBookmark hooks | vi.mock module factories | WIRED | vi.mock('../hooks/useTeams') at line 35, vi.mock('../hooks/useTeamBookmarks') at line 28 |
| TeamBookmarkCard.test.tsx | anchor element rendering | container.querySelector('a') + getAttribute assertions | WIRED | target="_blank" assertion at line 107; href URL assertion present |
| TeamSettingsModal.tsx | useUpdateTeam hook | import + mutate call | WIRED | import at line 11; updateTeam(…) call in handleSubmit |
| TeamSettingsModal.test.tsx | TeamSettingsModal.tsx | import + render | WIRED | vi.mock('../../hooks/useTeams') + mockUpdateTeam + mutation assertion |
| TeamDashboard.tsx | TeamSettingsModal.tsx | import + JSX render with isOpen state | WIRED | import line 20; showSettingsModal state line 29; isOpen={showSettingsModal} at line 286 |
| Gear icon button | setShowSettingsModal(true) | onClick handler | WIRED | onClick={() => setShowSettingsModal(true)} at line 124 |
| teams.spec.ts Phase 40.1 describe | fixtures.ts auth setup | import { test, expect } from './fixtures' | WIRED | Line 6: import confirmed |

---

### Data-Flow Trace (Level 4)

Level 4 not applicable to test files (they mock data sources by design). For TeamSettingsModal.tsx:

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| TeamSettingsModal.tsx | name, description state | currentName + currentDescription props (passed from TeamDashboard's useTeam query) | Yes — props flow from live team state; inner component mounts fresh on each open | FLOWING |
| TeamSettingsModal.tsx | updateTeam mutation | useUpdateTeam(teamId) → PATCH /api/teams/:teamId | Yes — calls existing backend endpoint, not hardcoded | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED for test files (not runnable entry points). For TeamSettingsModal:

| Behavior | Check | Result |
|----------|-------|--------|
| Component renders null when isOpen=false | grep -q "if (!isOpen) return null" TeamSettingsModal.tsx | PASS — outer wrapper gates at line 195 |
| Named export present | grep "export function TeamSettingsModal" | PASS |
| No hardcoded placeholder data | grep -n "TODO\|PLACEHOLDER" (all new files) | PASS — zero hits |

Note: Unit test suite (1446/1446 passing) and typecheck (exit 0) were confirmed passing per SUMMARY.md self-checks. These are trusted as recent (same-day) CI evidence. E2E run was NOT executed per plan context (Docker unavailable in execution environment) — this is the primary driver of `human_needed` status.

---

### Requirements Coverage

No REQUIREMENTS.md IDs to map — this is a debt-payback decimal phase. The binding contract is 40.1-CONTEXT.md. All 4 debug session truths from the context are addressed:

| Debug Session | Debt Type | Coverage Delivered | Status |
|---|---|---|---|
| 28-team-delete-not-found | Missing TeamSettingsModal | Plan 02 (component) + Plan 03 (integration) + DeleteTeamModal.test.tsx + TeamSettingsModal.test.tsx + E2E delete test | SATISFIED |
| bookmarks-not-saved-to-team | BookmarkButton not wired (silent fix pre-40.1) | BookmarkButton.test.tsx (6 tests) + E2E bookmark-to-team test | SATISFIED |
| no-pending-invites-team-dashboard | Invites tab missing (silent fix pre-40.1) | PendingInviteList.test.tsx (5 tests) + E2E invites tab test | SATISFIED |
| team-bookmarks-new-tab | Article join + props not wired (silent fix pre-40.1) | TeamBookmarkCard.test.tsx (8 tests) + E2E anchor test | SATISFIED |

---

### Anti-Patterns Found

None. Scan of all 6 new source files (TeamSettingsModal.tsx + 5 test files) returned zero hits for:
- TODO/FIXME/PLACEHOLDER/HACK
- return null stubs (the one `return null` in TeamSettingsModal is correct gate behavior, not a stub)
- Hardcoded empty data flowing to rendering
- console.log only implementations

One architectural deviation from the plan's provided code was made and is correct: the `useEffect`-based state reset was replaced with an inner/outer component split (TeamSettingsForm mounts only when open) to avoid React Compiler's `setState-in-effect` lint rule. This is a quality improvement, not a gap.

---

### Human Verification Required

#### 1. TeamSettingsModal visual + functional smoke

**Test:** Start the dev server (`pnpm dev`). Log in as a team owner. Navigate to a team dashboard (`/team/:teamId`). Look at the header action group.
**Expected:** A gear icon (Settings) appears between the "Invite Member" button and the Trash2 (delete) icon. Clicking it opens a modal with "Team Settings" header, cyan accent palette, name input pre-filled with the current team name, and a description textarea.
**Why human:** Visual placement, pre-fill correctness, and modal open/close behavior require a running dev server with real PostgreSQL data.

#### 2. TeamSettingsModal save flow

**Test:** With the modal open, modify the team name to something new. Click "Save Changes".
**Expected:** A sonner toast appears ("Team updated successfully"). Modal closes automatically. The team name in the dashboard header (`h1`) reflects the new name without a page refresh.
**Why human:** Requires live backend mutation (PATCH /api/teams/:teamId) and TanStack Query cache invalidation working end-to-end.

#### 3. Role gate — member sees no gear icon

**Test:** Log in as a team member (not owner/admin). Navigate to the team dashboard.
**Expected:** The Settings gear icon and Invite Member button are NOT rendered. Only the Trash2 icon is absent too. The page shows only the Back button and the three tabs.
**Why human:** Multi-user role scenario requires a second test account with member role.

#### 4. E2E test suite execution

**Test:** With Docker running (`docker compose up -d postgres redis`) and dev servers running, execute:
```bash
cd apps/web && npx playwright test e2e/teams.spec.ts --project=chromium-auth --reporter=line
```
**Expected:** All 4 Phase 40.1 tests in the "Phase 40.1 — wired flows" describe block either pass or self-skip. Zero failures. The 8 pre-existing tests in teams.spec.ts continue to pass.
**Why human:** Playwright tests require Docker + live servers. Docker was unavailable in the execution environment per Plan 04 SUMMARY. This is the primary automated verification gap.

---

### Notable Findings

**Worktree recovery:** The Wave 1 worktree-isolation quirk mentioned in the verification context was resolved by merging branches into `docs/maintenance-log`. The work landed correctly — all 4 Plan 01 test files exist at the expected paths with correct content. No evidence of partial or corrupted state.

**Inner/outer component split (TeamSettingsModal):** The plan's reference implementation used `useEffect` to reset state on `isOpen` change. The executor correctly identified a React Compiler lint error with this pattern and adopted the cleaner inner/outer split. This is a strict improvement: same observable behavior, no eslint-disable comments, no pre-existing lint debt introduced. The test file was written to test the outer `TeamSettingsModal` export and passes cleanly with this architecture.

**Pre-existing lint errors (out of scope):** SUMMARY.md documents pre-existing lint errors in VirtualizedList.tsx, VirtualizedGrid.tsx, aiService.ts, and bootLifecycle.ts. None of these files were touched in Phase 40.1. Full `pnpm lint` exits 1 due to these pre-existing errors, but `pnpm eslint src/pages/TeamDashboard.tsx` and `pnpm eslint src/components/teams/TeamSettingsModal.tsx` both exit 0. This is not a Phase 40.1 gap.

**E2E not run:** Plan 04 explicitly documents that Docker was unavailable during execution, so Playwright tests were written and typecheck-verified but not run live. The E2E tests are correctly shaped (4 test names, serial mode, CLAUDE.md conventions) and are statically verifiable. Live execution is the only gap, which is correctly routed to human verification item 4.

---

### Gaps Summary

No BLOCKER gaps. All 15 must-haves are VERIFIED in the codebase. The `human_needed` status is driven exclusively by 4 items requiring a running dev environment:
- E2E test suite execution (primary — the plan contract explicitly deferred this to UAT)
- TeamSettingsModal visual and functional smoke (gear icon placement, pre-fill, save flow)
- Role gate verification for member role (requires two-user setup)

These are standard post-phase UAT items, not implementation gaps. The code is complete.

---

_Verified: 2026-04-30_
_Verifier: Claude (gsd-verifier)_
