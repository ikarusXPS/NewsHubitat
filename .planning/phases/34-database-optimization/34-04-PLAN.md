---
phase: 34-database-optimization
plan: 04
type: execute
wave: 2
depends_on: [34-02]
files_modified:
  - grafana/provisioning/dashboards/newshub-operations.json
autonomous: false
requirements: [DB-04]
must_haves:
  truths:
    - "Grafana dashboard shows database pool metrics panel"
    - "Pool utilization visible: total connections, idle connections, waiting requests"
    - "Phase 34 verification confirms all success criteria met"
  artifacts:
    - path: "grafana/provisioning/dashboards/newshub-operations.json"
      provides: "Database Pool Metrics panel in Grafana"
      contains: "db_pool_total_connections"
  key_links:
    - from: "grafana/provisioning/dashboards/newshub-operations.json"
      to: "/metrics endpoint"
      via: "Prometheus scrape"
      pattern: "db_pool_"
---

<objective>
Add database pool metrics panel to Grafana dashboard and validate complete phase implementation.

Purpose: Per D-14, pool metrics must be visualized in Grafana for workload-based tuning decisions. This plan adds the panel and runs final verification to confirm all DB-01 through DB-04 requirements are met.

Output: Updated Grafana dashboard with pool metrics panel, verification checkpoint confirming phase success.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-database-optimization/34-CONTEXT.md
@.planning/phases/34-database-optimization/34-02-SUMMARY.md (pool metrics implementation)

<interfaces>
<!-- Key types and contracts from existing dashboard -->

From grafana/provisioning/dashboards/newshub-operations.json (panel structure):
```json
{
  "datasource": { "type": "prometheus", "uid": "prometheus" },
  "fieldConfig": {
    "defaults": {
      "color": { "mode": "palette-classic" },
      "custom": { ... },
      "mappings": [],
      "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] },
      "unit": "short"
    }
  },
  "gridPos": { "h": 8, "w": 8, "x": 0, "y": 17 },
  "id": 10,
  "targets": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "expr": "websocket_connections_active",
      "legendFormat": "WebSocket",
      "refId": "A"
    }
  ],
  "title": "WebSocket Connections",
  "type": "timeseries"
}
```

Prometheus metric names (from Plan 02):
- `db_pool_total_connections` - Total connections in pool
- `db_pool_idle_connections` - Idle connections in pool
- `db_pool_waiting_requests` - Requests waiting for connection
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add database pool metrics panel to Grafana dashboard</name>
  <files>grafana/provisioning/dashboards/newshub-operations.json</files>
  <read_first>
    - grafana/provisioning/dashboards/newshub-operations.json (current panels and structure)
  </read_first>
  <action>
Modify `grafana/provisioning/dashboards/newshub-operations.json` to add pool metrics panel:

1. Find the "System Metrics" row (id: 9, around line 269)
2. Insert new panel after the WebSocket Connections panel (id: 10, ends around line 323)

**New panel to add (insert as new element in the panels array):**
```json
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": {
        "defaults": {
          "color": { "mode": "palette-classic" },
          "custom": {
            "axisCenteredZero": false,
            "axisColorMode": "text",
            "axisLabel": "",
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "line",
            "fillOpacity": 10,
            "gradientMode": "none",
            "hideFrom": { "legend": false, "tooltip": false, "viz": false },
            "lineInterpolation": "linear",
            "lineWidth": 1,
            "pointSize": 5,
            "scaleDistribution": { "type": "linear" },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": { "group": "A", "mode": "none" },
            "thresholdsStyle": { "mode": "off" }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              { "color": "green", "value": null },
              { "color": "yellow", "value": 8 },
              { "color": "red", "value": 10 }
            ]
          },
          "unit": "short",
          "max": 12
        },
        "overrides": [
          {
            "matcher": { "id": "byName", "options": "Waiting" },
            "properties": [
              { "id": "color", "value": { "fixedColor": "red", "mode": "fixed" } }
            ]
          }
        ]
      },
      "gridPos": { "h": 8, "w": 8, "x": 8, "y": 25 },
      "id": 13,
      "options": {
        "legend": { "calcs": ["lastNotNull"], "displayMode": "table", "placement": "bottom", "showLegend": true },
        "tooltip": { "mode": "multi", "sort": "desc" }
      },
      "pluginVersion": "10.0.0",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "db_pool_total_connections",
          "legendFormat": "Total",
          "refId": "A"
        },
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "db_pool_idle_connections",
          "legendFormat": "Idle",
          "refId": "B"
        },
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "db_pool_waiting_requests",
          "legendFormat": "Waiting",
          "refId": "C"
        }
      ],
      "title": "Database Pool (D-14)",
      "type": "timeseries"
    }
```

**Panel configuration rationale:**
- `gridPos`: Place after existing System Metrics panels (y=25 to go after Memory Usage at y=17)
- `max: 12`: Pool max is 10, give some headroom for visualization
- `thresholds`: Yellow at 8 (80% utilization), Red at 10 (pool full)
- `overrides`: Waiting requests always shown in red (indicates pool exhaustion)
- `legend.calcs: ["lastNotNull"]`: Show current values in legend table
- Title includes D-14 reference for traceability

**Position adjustment:**
You may need to adjust y positions of existing panels or the new panel to fit properly. The WebSocket panel is at y=17, Memory at y=17, Event Loop at y=17. The new panel should be at y=25 (next row of 8-height panels).
  </action>
  <verify>
    <automated>grep -c "db_pool_total_connections\|db_pool_idle_connections\|db_pool_waiting_requests" D:/NewsHub/grafana/provisioning/dashboards/newshub-operations.json</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard JSON contains `db_pool_total_connections` expression
    - Dashboard JSON contains `db_pool_idle_connections` expression
    - Dashboard JSON contains `db_pool_waiting_requests` expression
    - Panel has title "Database Pool (D-14)"
    - Panel has thresholds for yellow at 8 and red at 10
    - JSON is valid (no syntax errors)
  </acceptance_criteria>
  <done>Grafana dashboard has Database Pool panel showing all three pool metrics</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 34 Database Optimization implementation:
1. N+1 detection middleware (34-01)
2. EXPLAIN ANALYZE audit report (34-01)
3. Pool metrics exposed to Prometheus (34-02)
4. Composite index on User(showOnLeaderboard, emailVerified) (34-03)
5. Partial index on Team WHERE deletedAt IS NULL (34-03)
6. Grafana dashboard panel for pool metrics (34-04)
  </what-built>
  <how-to-verify>
**Automated Verification:**
```bash
# 1. TypeScript compiles
npm run typecheck

# 2. Tests pass
npm run test:run

# 3. Build succeeds
npm run build
```

**Manual Verification:**

1. **Start services:**
```bash
docker compose up -d
npm run dev:backend
```

2. **Check pool metrics in Prometheus:**
```bash
curl http://localhost:3001/metrics | grep db_pool
```
Expected: Three gauges with numeric values.

3. **Trigger N+1 warning (dev mode):**
Make multiple requests to leaderboard endpoint, check console for any `[N+1 WARNING]` messages.

4. **Check Grafana dashboard:**
- Open http://localhost:3000 (admin/admin)
- Navigate to NewsHub Operations dashboard
- Verify "Database Pool (D-14)" panel shows Total, Idle, Waiting metrics

5. **Verify indexes in PostgreSQL:**
```bash
docker compose exec postgres psql -U newshub -d newshub -c "
  SELECT indexname, indexdef
  FROM pg_indexes
  WHERE tablename IN ('User', 'Team')
  ORDER BY tablename, indexname;
"
```
Expected: See `User_showOnLeaderboard_emailVerified_idx` and `idx_team_active` (partial).

**Success Criteria Checklist:**
- [ ] EXPLAIN ANALYZE audit documented in 34-AUDIT.md
- [ ] Composite indexes added for slow queries (DB-02)
- [ ] N+1 patterns identified via query counter (DB-03)
- [ ] Connection pool metrics in Prometheus (DB-04)
- [ ] Pool metrics visible in Grafana
- [ ] No TypeScript errors
- [ ] All tests pass
  </how-to-verify>
  <resume-signal>Type "approved" after verifying all criteria, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grafana dashboard | Internal monitoring UI, not user-facing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-07 | Information Disclosure | Pool metrics in Grafana | accept | Grafana is internal infrastructure; not exposed publicly |
</threat_model>

<verification>
1. Dashboard JSON is valid: Load in Grafana without errors
2. Pool metrics panel appears in dashboard
3. All three metrics (Total, Idle, Waiting) display values
4. Full phase verification checklist completed
</verification>

<success_criteria>
1. `newshub-operations.json` contains Database Pool panel with three metrics
2. Grafana loads dashboard without errors
3. All verification checklist items pass
4. Phase 34 requirements DB-01 through DB-04 verified complete
</success_criteria>

<output>
After completion, create `.planning/phases/34-database-optimization/34-04-SUMMARY.md`
</output>
