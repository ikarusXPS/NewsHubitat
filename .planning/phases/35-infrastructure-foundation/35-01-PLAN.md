---
phase: 35-infrastructure-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - .npmrc
  - package.json
  - apps/web/package.json
  - packages/types/package.json
  - packages/types/index.ts
  - packages/types/tsconfig.json
  - tsconfig.base.json
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "Developer can install dependencies with pnpm and see workspace protocol resolution"
    - "Apps and packages are isolated in separate directories"
    - "Shared types package exports TypeScript source files without build step"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace configuration for apps and packages"
      min_lines: 3
    - path: "packages/types/index.ts"
      provides: "Shared type exports from existing src/types/"
      exports: ["PerspectiveRegion", "Sentiment", "NewsArticle", "ApiResponse"]
    - path: "apps/web/package.json"
      provides: "Web app package with workspace dependency"
      contains: "@newshub/types"
  key_links:
    - from: "apps/web"
      to: "packages/types"
      via: "workspace protocol dependency"
      pattern: "\"@newshub/types\": \"workspace:\\*\""
    - from: "packages/types/index.ts"
      to: "src/types/index.ts"
      via: "re-export existing types"
      pattern: "export.*from.*types"
---

<objective>
Establish monorepo structure with pnpm workspaces and extract shared types package for code sharing between web app and future mobile app.

Purpose: Enable code reuse across web and mobile platforms by creating workspace-based monorepo with shared TypeScript types package
Output: Functioning pnpm workspace with apps/web consuming packages/types via workspace protocol
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-infrastructure-foundation/35-CONTEXT.md
@.planning/phases/35-infrastructure-foundation/35-RESEARCH.md
@.planning/phases/35-infrastructure-foundation/35-PATTERNS.md
@src/types/index.ts
@package.json
</context>

<interfaces>
<!-- Key types to extract from existing codebase -->
From src/types/index.ts:
```typescript
export type PerspectiveRegion = 'usa' | 'europa' | 'deutschland' | 'nahost' | 'tuerkei' | 'russland' | 'china' | 'asien' | 'afrika' | 'lateinamerika' | 'ozeanien' | 'kanada' | 'alternative';
export type Sentiment = 'positive' | 'negative' | 'neutral';
export type EventSeverity = 'critical' | 'high' | 'medium' | 'low';
export type EventCategory = 'conflict' | 'humanitarian' | 'political' | 'economic' | 'military' | 'protest';

export interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: string;
  meta?: { total: number; page: number; limit: number };
}

export interface NewsArticle {
  id: string;
  title: string;
  content: string;
  // ... full interface available in src/types/index.ts
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo workspace configuration</name>

  <read_first>
    - package.json (root - current configuration to understand scripts and dependencies)
    - .planning/phases/35-infrastructure-foundation/35-RESEARCH.md (Pattern 1: pnpm Workspace Configuration, lines 297-354)
  </read_first>

  <files>
    pnpm-workspace.yaml
    .npmrc
    tsconfig.base.json
  </files>

  <action>
Create pnpm workspace configuration per D-01 (pnpm workspaces with apps/ + packages/ layout).

**pnpm-workspace.yaml** (new file):
```yaml
packages:
  - 'apps/*'
  - 'packages/*'
```

**.npmrc** (new file):
```
# Prevent phantom dependencies (per RESEARCH.md Pitfall 1)
auto-install-peers=true
strict-peer-dependencies=false

# Use hard links for speed
node-linker=isolated
```

**tsconfig.base.json** (new file):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "allowJs": true,
    "checkJs": false,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "forceConsistentCasingInFileNames": true
  }
}
```

These files establish the workspace root configuration. All apps and packages will extend tsconfig.base.json for consistent TypeScript settings.
  </action>

  <acceptance_criteria>
    - pnpm-workspace.yaml contains `packages: ['apps/*', 'packages/*']`
    - .npmrc contains `auto-install-peers=true` and `node-linker=isolated`
    - tsconfig.base.json contains `"module": "ESNext"` and `"moduleResolution": "bundler"`
  </acceptance_criteria>

  <verify>
    <automated>
cat pnpm-workspace.yaml | grep -q "apps/\*" && cat pnpm-workspace.yaml | grep -q "packages/\*" && cat .npmrc | grep -q "auto-install-peers" && cat tsconfig.base.json | grep -q "moduleResolution" && echo "PASS: Workspace config files created" || echo "FAIL: Config files incomplete"
    </automated>
  </verify>

  <done>
Workspace configuration files exist with correct content. pnpm-workspace.yaml defines apps and packages globs. .npmrc configures peer dependency handling. tsconfig.base.json provides shared TypeScript config.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move existing app to apps/web</name>

  <read_first>
    - package.json (to understand current structure and move to apps/web)
    - vite.config.ts (will move to apps/web)
    - tsconfig.json (will move to apps/web and extend base)
  </read_first>

  <files>
    apps/web/package.json
    apps/web/vite.config.ts
    apps/web/tsconfig.json
    package.json
  </files>

  <action>
Move existing NewsHub application to apps/web subdirectory per D-02.

**Steps:**
1. Create apps/web directory
2. Move all existing app files (src/, server/, public/, e2e/, tests/, vite.config.ts, tsconfig.json, etc.) to apps/web/
3. Create apps/web/package.json with workspace dependency on @newshub/types
4. Update root package.json to delegate commands to workspace

**apps/web/package.json** (move existing package.json here and modify):
```json
{
  "name": "@newshub/web",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:frontend": "vite",
    "dev:backend": "tsx watch server/index.ts",
    "build": "tsc && vite build",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui",
    "lint": "eslint .",
    "preview": "vite preview",
    "seed": "tsx server/scripts/seed.ts",
    "seed:badges": "tsx server/scripts/seedBadges.ts",
    "seed:personas": "tsx server/scripts/seedPersonas.ts",
    "seed:load-test": "tsx server/scripts/seedLoadTestUsers.ts",
    "load:smoke": "k6 run tests/load/smoke.js",
    "load:full": "k6 run tests/load/full.js"
  },
  "dependencies": {
    "@newshub/types": "workspace:*",
    // ... copy all existing dependencies from root package.json
  },
  "devDependencies": {
    // ... copy all existing devDependencies from root package.json
  }
}
```

**apps/web/tsconfig.json** (modify existing):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@newshub/types": ["../../packages/types"]
    }
  },
  "include": ["src", "server", "e2e", "tests"],
  "exclude": ["node_modules", "dist", "build"]
}
```

**package.json** (root - minimal delegation):
```json
{
  "name": "newshub-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @newshub/web dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm --filter @newshub/web test",
    "test:run": "pnpm --filter @newshub/web test:run",
    "test:coverage": "pnpm --filter @newshub/web test:coverage",
    "test:e2e": "pnpm --filter @newshub/web test:e2e",
    "test:e2e:headed": "pnpm --filter @newshub/web test:e2e:headed",
    "test:e2e:ui": "pnpm --filter @newshub/web test:e2e:ui",
    "lint": "pnpm -r lint",
    "seed": "pnpm --filter @newshub/web seed",
    "seed:badges": "pnpm --filter @newshub/web seed:badges",
    "seed:personas": "pnpm --filter @newshub/web seed:personas",
    "seed:load-test": "pnpm --filter @newshub/web seed:load-test",
    "load:smoke": "pnpm --filter @newshub/web load:smoke",
    "load:full": "pnpm --filter @newshub/web load:full",
    "validate:ci": "yamllint .github/workflows/*.yml"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
```

**File move commands** (use git mv to preserve history):
```bash
mkdir -p apps/web
git mv src apps/web/
git mv server apps/web/
git mv public apps/web/
git mv e2e apps/web/
git mv tests apps/web/
git mv vite.config.ts apps/web/
git mv tsconfig.json apps/web/
git mv playwright.config.ts apps/web/
git mv vitest.config.ts apps/web/
git mv coverage.json apps/web/
# Move other app-specific files (keep .planning, .github, docker-compose.yml at root)
```
  </action>

  <acceptance_criteria>
    - apps/web/package.json exists with `"name": "@newshub/web"`
    - apps/web/package.json contains `"@newshub/types": "workspace:*"` in dependencies
    - apps/web/tsconfig.json extends `../../tsconfig.base.json`
    - Root package.json contains `"name": "newshub-monorepo"` and `"private": true`
    - apps/web/src directory exists (moved from root)
    - apps/web/server directory exists (moved from root)
  </acceptance_criteria>

  <verify>
    <automated>
test -f apps/web/package.json && grep -q "@newshub/web" apps/web/package.json && grep -q "@newshub/types" apps/web/package.json && grep -q "extends.*tsconfig.base.json" apps/web/tsconfig.json && test -d apps/web/src && test -d apps/web/server && echo "PASS: App moved to apps/web" || echo "FAIL: Move incomplete"
    </automated>
  </verify>

  <done>
Existing application moved to apps/web/ with workspace protocol dependency on @newshub/types. Root package.json delegates commands to workspace packages. TypeScript config extends shared base.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract shared types package</name>

  <read_first>
    - apps/web/src/types/index.ts (source types to extract)
    - apps/web/src/types/focus.ts (additional types to extract)
    - apps/web/src/types/feeds.ts (additional types to extract)
    - .planning/phases/35-infrastructure-foundation/35-PATTERNS.md (packages/types/index.ts pattern, lines 26-62)
  </read_first>

  <files>
    packages/types/package.json
    packages/types/index.ts
    packages/types/tsconfig.json
  </files>

  <action>
Create packages/types with shared TypeScript interfaces per D-03 and D-04 (source-only exports, no build step).

**packages/types/package.json**:
```json
{
  "name": "@newshub/types",
  "version": "0.0.0",
  "type": "module",
  "main": "index.ts",
  "types": "index.ts",
  "exports": {
    ".": "./index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3"
  }
}
```

**packages/types/tsconfig.json**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "composite": true,
    "declaration": true,
    "declarationMap": true,
    "rootDir": ".",
    "outDir": "dist"
  },
  "include": ["./*.ts"],
  "exclude": ["node_modules", "dist"]
}
```

**packages/types/index.ts** (extract from apps/web/src/types/index.ts):
```typescript
// Re-export all shared types from existing src/types/
// This package provides type-only exports with no runtime code

// Core domain types
export type PerspectiveRegion = 'usa' | 'europa' | 'deutschland' | 'nahost' | 'tuerkei' | 'russland' | 'china' | 'asien' | 'afrika' | 'lateinamerika' | 'ozeanien' | 'kanada' | 'alternative';
export type Sentiment = 'positive' | 'negative' | 'neutral';
export type OwnershipType = 'state' | 'private' | 'public' | 'mixed';
export type EventSeverity = 'critical' | 'high' | 'medium' | 'low';
export type EventCategory = 'conflict' | 'humanitarian' | 'political' | 'economic' | 'military' | 'protest';

// API response wrapper
export interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: string;
  meta?: {
    total: number;
    page: number;
    limit: number;
    hasMore?: boolean;
  };
}

// News domain
export interface NewsSource {
  id: string;
  name: string;
  country: string;
  region: PerspectiveRegion;
  language: string;
  bias: {
    political: number;
    reliability: number;
    ownership: OwnershipType;
  };
  apiEndpoint: string;
  rateLimit: number;
}

export interface NewsArticle {
  id: string;
  title: string;
  content: string;
  url: string;
  imageUrl?: string;
  sourceId: string;
  source?: NewsSource;
  region: PerspectiveRegion;
  topics: string[];
  sentiment: Sentiment;
  publishedAt: string;
  createdAt?: string;
  titleTranslated?: Record<string, string>;
  contentTranslated?: Record<string, string>;
}

// Events domain
export interface GeoEvent {
  id: string;
  title: string;
  description: string;
  category: EventCategory;
  severity: EventSeverity;
  location: {
    lat: number;
    lng: number;
    country: string;
    city?: string;
  };
  timestamp: string;
  sourceArticleIds: string[];
}

export interface TimelineEvent {
  id: string;
  title: string;
  description: string;
  date: string;
  category: EventCategory;
  articleCount: number;
  regions: PerspectiveRegion[];
}

// Filter state (used across web and future mobile)
export interface FilterState {
  regions: PerspectiveRegion[];
  topics: string[];
  sentiment?: Sentiment;
  search?: string;
}
```

**Important:** Leave apps/web/src/types/index.ts unchanged for now. In next phase, apps/web will import from @newshub/types instead of local src/types. This task only creates the package; migration happens separately.
  </action>

  <acceptance_criteria>
    - packages/types/package.json exists with `"main": "index.ts"` and `"types": "index.ts"`
    - packages/types/index.ts contains `export type PerspectiveRegion =`
    - packages/types/index.ts contains `export interface ApiResponse<T>`
    - packages/types/index.ts contains `export interface NewsArticle`
    - packages/types/tsconfig.json extends `../../tsconfig.base.json`
  </acceptance_criteria>

  <verify>
    <automated>
test -f packages/types/package.json && grep -q "\"main\": \"index.ts\"" packages/types/package.json && grep -q "export type PerspectiveRegion" packages/types/index.ts && grep -q "export interface ApiResponse" packages/types/index.ts && grep -q "export interface NewsArticle" packages/types/index.ts && echo "PASS: Types package created" || echo "FAIL: Types package incomplete"
    </automated>
  </verify>

  <done>
Shared types package created at packages/types/ with source-only TypeScript exports. Package exports common types for news, events, and API responses. Apps can consume via workspace protocol without build step.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Monorepo packages | Internal boundary between workspace packages (no security boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-01 | Tampering | Phantom dependencies | mitigate | .npmrc with node-linker=isolated prevents hoisting. CI runs pnpm install --frozen-lockfile. |
| T-35-02 | Information Disclosure | Source-only package exports | accept | TypeScript source files exposed by design for code sharing. No secrets in types. |
| T-35-03 | Denial of Service | Build complexity | accept | Source-only exports avoid build step. No performance impact. |

</threat_model>

<verification>
Run after all tasks complete:

```bash
# Verify workspace structure
ls -la apps/web/package.json packages/types/package.json pnpm-workspace.yaml

# Install dependencies with pnpm
pnpm install

# Verify workspace protocol resolution
grep "@newshub/types" apps/web/package.json
pnpm list @newshub/types --depth=0

# Type check entire workspace
pnpm -r typecheck

# Verify no phantom dependencies
pnpm-check || echo "pnpm-check not installed, manual review needed"
```

Expected output:
- pnpm install completes without errors
- pnpm list shows @newshub/types linked from workspace
- pnpm -r typecheck passes for all packages
</verification>

<success_criteria>
- [ ] pnpm-workspace.yaml defines apps and packages globs
- [ ] .npmrc configures auto-install-peers and node-linker=isolated
- [ ] apps/web exists with all application code moved from root
- [ ] apps/web/package.json declares workspace dependency on @newshub/types
- [ ] packages/types exports shared TypeScript types without build step
- [ ] pnpm install resolves workspace protocol dependencies
- [ ] pnpm -r typecheck passes for entire workspace
</success_criteria>

<output>
After completion, create `.planning/phases/35-infrastructure-foundation/35-01-SUMMARY.md`
</output>
