---
phase: 40-content-expansion
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/LanguageSwitcher.tsx
autonomous: true
gap_closure: true
requirements: []
tags: [gap-closure, react, i18n, phase-40-uat]

must_haves:
  truths:
    - "Header LanguageSwitcher dropdown exposes DE, EN, AND FR — all three are selectable"
    - "Clicking FR re-renders UI in French via the existing i18next + Zustand pipeline (no additional plumbing change)"
    - "Selecting FR while already on FR is a no-op (existing toggle logic unaffected)"
  artifacts:
    - path: "apps/web/src/components/LanguageSwitcher.tsx"
      provides: "languages array with DE + EN + FR entries"
      contains: "code: 'fr'"
  key_links:
    - from: "LanguageSwitcher dropdown render (`languages.map(...)`)"
      to: "i18next + Zustand language pipeline"
      via: "i18n.changeLanguage('fr') → useAppStore.subscribe propagates → re-renders i18n consumers"
      pattern: "code: 'fr'"
---

<objective>
Close UAT Test 10 gap — Header LanguageSwitcher exposes only DE + EN; FR is missing despite (a) `apps/web/src/i18n/i18n.ts` declaring `supportedLngs: ['de', 'en', 'fr']`, (b) Zustand language validators accepting 'fr', (c) `apps/web/public/locales/fr/` containing real French translations across 6 namespaces (common, credibility, factcheck, podcasts, pricing, videos), and (d) every Phase 40 SUMMARY documenting "i18n triple-write across DE/EN/FR". The UI just never had FR added to its hardcoded array.

Root cause is one missing array entry at `apps/web/src/components/LanguageSwitcher.tsx:9`. Git history confirms the strings 'fr' / 'Français' have never existed in this file. This is a UI omission, not a regression.

Purpose: Close the obvious DE/EN/FR contract that the rest of the i18n pipeline already supports. Restore alignment with CLAUDE.md's "languages **DE / EN / FR**" claim.
Output: One array entry inserted in LanguageSwitcher.tsx. No other changes. No new dependencies. No backend changes.

Debug session evidence: .planning/debug/language-switcher-missing-fr.md (full root-cause + git-blame + supportedLngs/Zustand/locales evidence).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-content-expansion/40-UAT.md
@.planning/debug/language-switcher-missing-fr.md

@apps/web/src/components/LanguageSwitcher.tsx
@apps/web/src/i18n/i18n.ts

<interfaces>
<!-- Current array (LanguageSwitcher.tsx:7-10) — pure literal, immutable, deliberately `as const` -->

```typescript
const languages = [
  { code: 'de', label: 'Deutsch', flag: 'DE' },
  { code: 'en', label: 'English', flag: 'EN' },
] as const;
```

<!-- i18next config already accepts 'fr' (apps/web/src/i18n/i18n.ts:15) -->
supportedLngs: ['de', 'en', 'fr']

<!-- Zustand store accepts 'fr' (i18n/i18n.ts:39-46, 50-60, 64-67) — validators check 'de' || 'en' || 'fr' -->

<!-- FR locale directory exists with real translations: -->
<!-- apps/web/public/locales/fr/{common,credibility,factcheck,podcasts,pricing,videos}.json -->

<!-- Two namespaces declared in i18n config but missing FR translations: share.json, teams.json -->
<!-- Graceful fallback already wired (fallbackLng: 'en') so these yield harmless 404s + per-key fallback. -->
<!-- This plan does NOT backfill those — see Task 2 (todo file). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FR entry to the languages array</name>
  <files>apps/web/src/components/LanguageSwitcher.tsx</files>
  <action>
Insert a single object entry for French into the `languages` array at line 9 (between English and the closing `]`).

Before:
```typescript
const languages = [
  { code: 'de', label: 'Deutsch', flag: 'DE' },
  { code: 'en', label: 'English', flag: 'EN' },
] as const;
```

After:
```typescript
const languages = [
  { code: 'de', label: 'Deutsch', flag: 'DE' },
  { code: 'en', label: 'English', flag: 'EN' },
  { code: 'fr', label: 'Français', flag: 'FR' },
] as const;
```

That's the entire change. The component (`languages.map(...)`) renders whatever's in the array, so a third dropdown option appears automatically. Click handler delegates to `i18n.changeLanguage(lang.code)` which already accepts 'fr' (per supportedLngs).

Constraints:
- DO NOT change the dropdown render block, the click handler, the icon, the framer-motion animation, the Check-mark logic, the click-outside hook, or anything else in this file.
- DO NOT add a fourth language (Spanish, Italian, etc.) — out of scope; project i18n contract is DE/EN/FR per CLAUDE.md.
- DO NOT change the `as const` assertion — TypeScript needs the literal-narrowed types so the click handler can pass `lang.code` to a typed `changeLanguage('de'|'en'|'fr')`.
- DO NOT modify `apps/web/src/i18n/i18n.ts` — its `supportedLngs` already includes 'fr'.
- DO NOT backfill missing FR namespaces (`share.json`, `teams.json`) in this plan — see Task 2 (filed as a separate todo). Graceful English fallback already covers those at runtime; missing translations don't break the UI.
- DO NOT use a different label for French (e.g. "French" or "FR" or the country name "France") — `Français` is the locale's endonym and matches the pattern of `Deutsch` / `English`.
- DO NOT use a different `flag` value — keep `'FR'` consistent with `'DE'` / `'EN'` (these are likely flag-character keys consumed by the dropdown's icon renderer; deviating breaks the visual parity).
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck</automated>
    Manual probe (the live UAT replay):
    1. `pnpm dev`, navigate to any page (e.g. `/`).
    2. Click the language switcher in the top-right header.
    3. Dropdown shows three options: Deutsch (DE), English (EN), Français (FR).
    4. Click `Français` — UI re-renders into French (e.g. sidebar nav labels, common buttons). The Zustand store + i18n state both flip to 'fr'.
    5. Reload the page — language persists at FR (Zustand subscribe re-hydrates).
    6. Toggle back to DE — UI re-renders to German.
    7. DevTools Network tab on the FR switch: confirm the 6 namespaces with FR files load (`/locales/fr/common.json`, etc.) with 200, and the 2 missing namespaces (`share.json`, `teams.json`) return 404 — that's expected and benign per `fallbackLng: 'en'`.
  </verify>
  <done>
    Three-option dropdown rendered. FR selectable. UI flips to French on click. typecheck passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: File a follow-up todo to backfill missing FR namespaces</name>
  <files>.planning/todos/pending/40-10-fr-namespace-backfill.md</files>
  <action>
The debug session noted that 2 namespaces (`share.json`, `teams.json`) are declared in `i18n.ts ns: ['common', 'share', 'teams', 'pricing', 'factcheck', 'credibility', 'videos', 'podcasts']` but absent from `apps/web/public/locales/fr/`. Selecting FR yields harmless 404s on initial load + per-key English fallback per `fallbackLng: 'en'`. Not blocking. Track for cleanup.

Create:

```markdown
---
status: pending
phase: 40-content-expansion
created: 2026-05-05
priority: low
labels: [i18n, polish, frontend]
related_plans: [40-10]
related_uat: 10
related_debug: .planning/debug/language-switcher-missing-fr.md
---

# Backfill FR translations for missing namespaces

## Why

Plan 40-10 added FR to the LanguageSwitcher dropdown. Selecting FR triggers i18next-http-backend to lazy-load all 8 declared namespaces (`common`, `share`, `teams`, `pricing`, `factcheck`, `credibility`, `videos`, `podcasts`). 6 of those exist in `apps/web/public/locales/fr/` with real translations. 2 are missing:

- `apps/web/public/locales/fr/share.json` — corresponds to share/social-sharing UI strings (used by SharedContent components)
- `apps/web/public/locales/fr/teams.json` — corresponds to team-collaboration UI strings (used by TeamDashboard, TeamMember, etc.)

Effect: harmless 404s in DevTools Network tab on first FR load. Per-key fallback to `fallbackLng: 'en'` keeps the UI functional. So the bug is cosmetic + observability noise.

## What

Copy `apps/web/public/locales/de/share.json` and `apps/web/public/locales/de/teams.json` (or `en/...`) into `apps/web/public/locales/fr/`. Translate every value into French. Keep the JSON keys identical.

Example minimal content (illustrative — do not commit until translated):
- `share.json`: keys like `shareButton`, `copyLink`, `shareViaEmail`, `tweetThis`, …
- `teams.json`: keys like `teamDashboard`, `inviteMember`, `roleOwner`, `roleAdmin`, `roleMember`, …

Use a translator agent or DeepL CLI for bulk translation, then human-review for tone consistency with the existing FR namespaces in the repo (which use formal French — "Enregistrer" not "Sauve", "Annuler" not "Annule").

## Acceptance

- `apps/web/public/locales/fr/share.json` exists with all keys present in `de/share.json` and `en/share.json`.
- `apps/web/public/locales/fr/teams.json` exists with all keys present in `de/teams.json` and `en/teams.json`.
- DevTools Network tab on first FR selection shows 200 (not 404) for both files.
- Visual sanity: open /team and /article/{id}, switch to FR, observe team/share strings rendering in French (not English fallback).

## Out of scope

- New namespaces (auth, bookmarks, dashboard, settings — these are already not in the i18n.ts ns array, so they're either inlined or use `common`).
- Mobile-only namespaces.
- Changing `fallbackLng` behavior.
- Translation QA process documentation (separate concern).
```
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/40-10-fr-namespace-backfill.md</automated>
  </verify>
  <done>
    Pending-todo file exists; the lazy-load 404s are tracked for follow-up.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm typecheck` exits 0.
2. `cd apps/web && pnpm test:run` exits 0 — no LanguageSwitcher unit tests should regress (any tests of the component should pick up the third option automatically without test changes; if a test asserts `languages.length === 2` it must be updated to 3 — but per debug session inspection, no such test exists).
3. Manual UAT replay (above): three-option dropdown, FR selection flips UI to French.
4. UAT Test 10 status flips from `issue` to `pass` once /gsd-verify-work 40 is rerun.
</verification>

<success_criteria>
- `apps/web/src/components/LanguageSwitcher.tsx` `languages` array contains 3 entries: `{de, Deutsch, DE}`, `{en, English, EN}`, `{fr, Français, FR}`.
- Header dropdown renders 3 options.
- Selecting FR flips i18n state to 'fr' and propagates through the existing Zustand pipeline (no plumbing changes needed).
- `pnpm typecheck && pnpm test:run` green.
- Follow-up todo files (`40-10-fr-namespace-backfill.md`) documents the cosmetic 404 cleanup work.

Out of scope (explicit deferrals):
- Backfilling missing FR namespaces share.json + teams.json (separate todo).
- Adding a new language beyond DE/EN/FR.
- Auto-detecting browser language and pre-selecting FR for francophone visitors (separate UX concern, already partially handled by i18next-browser-languagedetector if configured).
- Updating any other place that references the language list (sitemap, OG tags, etc.) — none currently exist; this is a future concern.
</success_criteria>

<output>
After completion, create `.planning/phases/40-content-expansion/40-10-SUMMARY.md` capturing:
- Commit SHA for the one-line array addition.
- Manual probe evidence: screenshot of the 3-option dropdown + screenshot of UI in French.
- Cross-link to .planning/debug/language-switcher-missing-fr.md (set its status to `resolved` with files_changed populated).
- Confirmation that .planning/todos/pending/40-10-fr-namespace-backfill.md was filed.
</output>
