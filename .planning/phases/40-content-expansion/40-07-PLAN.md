---
phase: 40-content-expansion
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/ClusterSummary.tsx
  - apps/web/src/components/FramingComparison.tsx
  - apps/web/src/components/PerspectiveCoverageStats.tsx
  - apps/web/src/App.tsx
autonomous: true
gap_closure: true
requirements: []
tags: [gap-closure, auth, react, analysis, phase-40-uat]

must_haves:
  truths:
    - "Authenticated user opening /analysis sees cluster groupings render (no 'Fehler beim Laden der Cluster' German error string)"
    - "Authenticated user can use FramingComparison topic search and receive framing data without a 401"
    - "Authenticated user sees PerspectiveCoverageStats coverage cards populated (no silent 401 → empty fallback)"
    - "Anonymous user navigating to /analysis is redirected to /login (not exposed to a 401-derived error UI)"
  artifacts:
    - path: "apps/web/src/components/ClusterSummary.tsx"
      provides: "Authenticated GET /api/analysis/clusters fetcher"
      contains: "Authorization: `Bearer ${"
    - path: "apps/web/src/components/FramingComparison.tsx"
      provides: "Authenticated GET /api/analysis/framing fetcher"
      contains: "Authorization: `Bearer ${"
    - path: "apps/web/src/components/PerspectiveCoverageStats.tsx"
      provides: "Authenticated GET /api/analysis/coverage-gaps fetcher"
      contains: "Authorization: `Bearer ${"
    - path: "apps/web/src/App.tsx"
      provides: "/analysis route gated by RequireAuth"
      contains: "<RequireAuth>"
  key_links:
    - from: "apps/web/src/components/ClusterSummary.tsx"
      to: "apps/web/server/routes/analysis.ts (mounted with authMiddleware in server/index.ts:181)"
      via: "fetch with Bearer token from localStorage('newshub-auth-token')"
      pattern: "Authorization: `Bearer \\$\\{"
    - from: "apps/web/src/App.tsx"
      to: "apps/web/src/pages/Analysis.tsx"
      via: "RequireAuth wrapper component (must already exist; reuse — do not create new)"
      pattern: "<Route path=\"/analysis\""
---

<objective>
Close UAT Test 3 gap — Analysis page renders the German error string "Fehler beim Laden der Cluster" instead of cluster content because three Analysis-page components fetch /api/analysis/* without an Authorization header. Backend has gated /api/analysis behind authMiddleware since commit c5553f9 (2026-04-28). The fix attaches Bearer JWT to the three fetchers (mirror useFactCheck.ts pattern) and gates the route with RequireAuth so anonymous visitors are redirected to login instead of seeing a 401-derived error.

Purpose: Unblock UAT Test 3 (bias-diversity-note check) and restore Analysis page functionality for all logged-in tiers (FREE, PREMIUM, ENTERPRISE).
Output: Three patched fetchers + one route guard. No backend changes. No new dependencies.

Debug session evidence: .planning/debug/analysis-cluster-load-error.md (full root-cause + git commit attribution).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/40-content-expansion/40-UAT.md
@.planning/debug/analysis-cluster-load-error.md

@apps/web/src/components/ClusterSummary.tsx
@apps/web/src/components/FramingComparison.tsx
@apps/web/src/components/PerspectiveCoverageStats.tsx
@apps/web/src/hooks/useFactCheck.ts
@apps/web/src/App.tsx

<interfaces>
<!-- Working pattern to mirror — already-shipped, already-tested. Use this verbatim. -->
<!-- Source: apps/web/src/hooks/useFactCheck.ts:31-58 -->

```typescript
function getToken(): string {
  if (typeof window === 'undefined') return '';
  return localStorage.getItem('newshub-auth-token') ?? '';
}

const r = await fetch('/api/ai/fact-check', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: `Bearer ${getToken()}`,
  },
  body: JSON.stringify(args),
});
```

<!-- Backend mount order (do not modify) — apps/web/server/index.ts:181 -->
app.use('/api/analysis', authMiddleware, aiTierLimiter, analysisRoutes);
<!-- authMiddleware returns 401 when Authorization header missing -->

<!-- RequireAuth: confirm presence before use. If missing, do NOT invent — see Task 4 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attach Bearer JWT to Analysis-page fetchers</name>
  <files>
    apps/web/src/components/ClusterSummary.tsx
    apps/web/src/components/FramingComparison.tsx
    apps/web/src/components/PerspectiveCoverageStats.tsx
  </files>
  <action>
For each of the three components, attach an `Authorization: Bearer <token>` header on every `/api/analysis/*` fetch call. Mirror the pattern from `apps/web/src/hooks/useFactCheck.ts:31-58` exactly. Do NOT introduce a shared apiFetch() wrapper now — that is filed as a separate tech-debt todo (see Task 3).

(a) `apps/web/src/components/ClusterSummary.tsx`:
- Add a top-level `function getToken()` near the existing imports (or co-locate with `fetchClusters` at line 41) returning `localStorage.getItem('newshub-auth-token') ?? ''` with the SSR guard `typeof window === 'undefined' ? '' : ...`.
- Update `fetchClusters` (currently line 41-48) to pass `headers: { Authorization: `Bearer ${getToken()}` }` to the existing `fetch(url)` call. The function still returns `response.json()` on 2xx and throws `'Failed to fetch clusters'` on non-2xx — error UX unchanged.

(b) `apps/web/src/components/FramingComparison.tsx`:
- Same `getToken()` helper.
- Update the inline `queryFn` at lines 84-91 (`const r = await fetch(`/api/analysis/framing?topic=...&locale=...`)`) to add `headers: { Authorization: `Bearer ${getToken()}` }`.

(c) `apps/web/src/components/PerspectiveCoverageStats.tsx`:
- Same `getToken()` helper.
- Update `fetchCoverageGaps` (currently line 77-81) to pass `headers: { Authorization: `Bearer ${getToken()}` }` to the existing `fetch('/api/analysis/coverage-gaps')` call.

Constraints:
- DO NOT change the error message strings (`'Failed to fetch clusters'`, `'Framing fetch failed'`, `'Failed to fetch coverage gaps'`) — they are surface-internal and not user-visible.
- DO NOT change React Query options (queryKey, staleTime, enabled).
- DO NOT change function signatures or return types.
- DO NOT introduce a shared module — three local copies of `getToken()` are deliberate (Task 3 follow-up will consolidate). Add a one-line `// TODO(api-fetch-wrapper): see todos/pending/40-07-shared-api-fetch.md` above each helper.
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck</automated>
    Manual probe (the live UAT replay):
    1. `pnpm dev` (frontend + backend up).
    2. Open `http://localhost:5173/analysis` while logged in (any tier — use the existing UAT test account).
    3. Network tab: confirm `GET /api/analysis/clusters` request has `Authorization: Bearer ...` header and returns 200 JSON.
    4. Page renders cluster cards (no German error string `Fehler beim Laden der Cluster`).
    5. Type a topic into FramingComparison search — `GET /api/analysis/framing?topic=...` returns 200 with framing data.
    6. PerspectiveCoverageStats card renders coverage entries (not the empty-state branch).
  </verify>
  <done>
    typecheck passes. All three components attach Bearer JWT. The three /api/analysis/* requests return 200 JSON for an authenticated UAT account. No "Fehler beim Laden der Cluster" rendered for the logged-in path.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gate /analysis route with RequireAuth</name>
  <files>apps/web/src/App.tsx</files>
  <action>
Wrap the `/analysis` route element with the existing `<RequireAuth>` guard so anonymous users are redirected to login (which is the correct UX for a route the backend now refuses to serve without a token), instead of seeing a 401-derived error UI.

Steps:
1. Verify `RequireAuth` already exists in the codebase. Search: `grep -nR "RequireAuth\|function RequireAuth\|export.*RequireAuth" apps/web/src/`. If a `<RequireAuth>` wrapper or equivalent (e.g. `<ProtectedRoute>`) is already used in App.tsx for other routes (e.g. `/profile`, `/bookmarks`, `/team`), reuse the SAME wrapper exactly — do not invent a new one.
2. If found, modify `apps/web/src/App.tsx` line 122 from:
   ```
   <Route path="/analysis" element={<Analysis />} />
   ```
   to (replace `RequireAuth` with whatever the codebase actually uses):
   ```
   <Route path="/analysis" element={<RequireAuth><Analysis /></RequireAuth>} />
   ```
3. If NO existing auth-guard wrapper component is found in the codebase, do NOT invent one — instead, leave Task 2 as a no-op and add a one-line comment above line 122: `{/* TODO(40-07): gate /analysis with auth guard once a RequireAuth/ProtectedRoute wrapper exists; backend rejects unauthenticated requests with 401 since c5553f9 */}`. Then write a NEW pending todo at `.planning/todos/pending/40-07-add-requireauth-wrapper.md` describing the missing component, the routes that need it (/analysis is one), and a sketch of the implementation (uses AuthContext, redirects to /login on null user, preserves intended path via Navigate state).

Pick path 2 OR path 3 based on what the grep returns. Do not pick both.
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck</automated>
    Manual probe:
    - If path 2 was taken: Open browser in incognito (no localStorage token). Navigate to `http://localhost:5173/analysis`. Browser redirects to `/login` (or whatever the existing wrapper does for anonymous users). Then log in — Analysis page renders normally.
    - If path 3 was taken: Confirm `.planning/todos/pending/40-07-add-requireauth-wrapper.md` exists and the App.tsx comment is present. typecheck still passes.
  </verify>
  <done>
    Either /analysis is gated with the existing project auth-guard wrapper and anonymous nav redirects to login, OR a TODO + pending-todo file documents the deferral. typecheck passes either way.
  </done>
</task>

<task type="auto">
  <name>Task 3: File tech-debt todo for shared apiFetch() wrapper</name>
  <files>.planning/todos/pending/40-07-shared-api-fetch.md</files>
  <action>
Create a new pending-todo file capturing the systemic follow-up the debug session recommended (option 2 — "shared apiFetch() wrapper"). Out of scope for this plan but must not be lost.

File contents:

```markdown
---
status: pending
phase: 40-content-expansion
created: 2026-05-05
priority: medium
labels: [tech-debt, frontend, auth, refactor]
related_plans: [40-07]
related_debug: .planning/debug/analysis-cluster-load-error.md
---

# Shared apiFetch() wrapper for JWT auto-attach

## Why

Plan 40-07 patched three Analysis-page fetchers (ClusterSummary / FramingComparison / PerspectiveCoverageStats) by inlining a `getToken()` helper + Authorization header — same pattern that already lives in 19+ places under `apps/web/src/`. Every time the backend gates a new route with `authMiddleware`, the frontend silently breaks until somebody tracks down the missing header.

The next backend auth-gate change WILL break another surface unless we centralize JWT-attachment.

## What

Create `apps/web/src/lib/api.ts` exporting:
- `apiFetch(input, init)` — Wraps `fetch` with `Authorization: Bearer <token>` auto-attached when token present in `localStorage('newshub-auth-token')`. SSR-safe (typeof window guard). Returns the same Response Promise as fetch.
- Optional: standardized 401-handling — emit a `auth:expired` CustomEvent on `window` so AuthContext can react (logout + redirect).

Refactor scope (sweep, not surgical):
- Replace every `fetch('/api/...')` direct call across `apps/web/src/` with `apiFetch(...)`.
- Audit grep target: `grep -rn "localStorage.getItem('newshub-auth-token')" apps/web/src/` — every hit consolidates into the wrapper.
- Update tests that vi.mock `fetch` to mock the wrapper (or keep fetch mocking and verify the wrapper passes through correctly).

## Acceptance

- `apps/web/src/lib/api.ts` exists with `apiFetch` exported and unit tests.
- Zero direct `localStorage.getItem('newshub-auth-token')` calls outside `lib/api.ts` and the `useAuth` hook (verified via grep).
- `pnpm typecheck && pnpm test:run` green; no behavioral regressions.

## Out of scope

- New auth-context APIs (kept stable).
- Backend changes.
- Refresh-token / silent renewal — separate concern.
```
  </action>
  <verify>
    <automated>test -f .planning/todos/pending/40-07-shared-api-fetch.md && head -3 .planning/todos/pending/40-07-shared-api-fetch.md | grep -q "status: pending"</automated>
  </verify>
  <done>
    Pending-todo file exists with frontmatter, motivation, scope, and acceptance criteria. Linked from Task 1's inline TODO comments.
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm typecheck` exits 0.
2. Replay UAT Test 3 manually:
   - Login as the UAT test account.
   - Navigate to `/analysis`.
   - DevTools Network tab: `GET /api/analysis/clusters` carries `Authorization: Bearer ...` and returns 200 with cluster JSON.
   - Page DOES NOT render the German string `"Fehler beim Laden der Cluster"`.
   - FramingComparison + PerspectiveCoverageStats cards render populated data, not 401-derived empty/error states.
3. Replay anonymous-user path:
   - Open incognito tab, navigate to `/analysis`.
   - If RequireAuth wired in Task 2: browser redirects to login. After login, Analysis renders.
   - If RequireAuth deferred: TODO + pending-todo file exist; the 401-derived error path is documented and tracked.
4. Tech-debt follow-up tracked: `.planning/todos/pending/40-07-shared-api-fetch.md` exists.

NOTE on existing tests: There are no existing unit tests for ClusterSummary/FramingComparison/PerspectiveCoverageStats fetchers (grep `apps/web/src/components/*.test.tsx` for these files returns empty). This plan does NOT add new component tests — that scope is captured implicitly in the apiFetch refactor todo where centralization makes mocking tractable. Do not pad this plan with brittle component tests against three fetchers that will be rewritten by the apiFetch follow-up.
</verification>

<success_criteria>
- All three Analysis-page components attach `Authorization: Bearer ${localStorage('newshub-auth-token')}` to every `/api/analysis/*` fetch.
- /analysis route is gated by the existing project auth-guard wrapper, OR a documented TODO + pending-todo file captures the deferral with reasoning.
- A pending-todo file at `.planning/todos/pending/40-07-shared-api-fetch.md` captures the apiFetch wrapper refactor as a tracked follow-up.
- `pnpm typecheck` passes.
- UAT Test 3 root cause closed: authenticated /analysis renders cluster groupings instead of the German error.

Out of scope (explicit deferrals):
- The shared apiFetch() wrapper itself (separate todo, separate plan in a future phase).
- Component-level unit tests for the three fetchers (deferred — will be added when apiFetch refactor lands and mocking is centralized).
- Updating other DIY-fetchers across `apps/web/src/` that read `newshub-auth-token` directly (19+ files; in scope for the apiFetch refactor).
- Any backend auth-middleware change.
- Re-validating UAT Test 3's bias-diversity-note assertion (the diagnose verifier will re-run UAT after this plan lands).
</success_criteria>

<output>
After completion, create `.planning/phases/40-content-expansion/40-07-SUMMARY.md` capturing:
- Commit SHAs for the fetcher patches and the route-guard change (or TODO).
- Manual probe evidence: screenshot or DevTools Network screenshot showing 200 + Bearer header on /api/analysis/clusters.
- Confirmation that .planning/todos/pending/40-07-shared-api-fetch.md was filed.
- Cross-link to .planning/debug/analysis-cluster-load-error.md (set its status to `resolved` with files_changed populated).
</output>
