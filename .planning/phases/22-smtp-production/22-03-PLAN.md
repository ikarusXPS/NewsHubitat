---
phase: 22-smtp-production
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
  - 22-02
files_modified:
  - prometheus/alert.rules.yml
  - docs/SENDGRID_SETUP.md
autonomous: false
requirements:
  - SMTP-01
  - SMTP-02
  - SMTP-03

must_haves:
  truths:
    - "Alert rule HighEmailBounceRate fires when bounce rate > 5% for 5 minutes"
    - "Alert rule LowEmailDeliveryRate fires when delivery success < 95% for 1 hour"
    - "Setup guide documents SendGrid API key creation steps"
    - "Setup guide documents SPF, DKIM, DMARC DNS record requirements"
    - "Setup guide documents webhook configuration in SendGrid dashboard"
    - "User verifies production email flows work correctly"
  artifacts:
    - path: "prometheus/alert.rules.yml"
      provides: "Email alert rules for bounce rate and delivery rate"
      contains: "HighEmailBounceRate"
    - path: "docs/SENDGRID_SETUP.md"
      provides: "Production SendGrid setup guide with DNS and webhook instructions"
      contains: "SPF"
  key_links:
    - from: "prometheus/alert.rules.yml"
      to: "server/services/metricsService.ts"
      via: "metric queries"
      pattern: "email_bounced_total\|email_sent_total"
---

<objective>
Add Prometheus alert rules for email metrics and create SendGrid production setup documentation.

Purpose: Complete SMTP production readiness with monitoring alerts and operator documentation per SMTP-01, SMTP-02, and SMTP-03. Human verification checkpoint ensures production email flows work correctly.

Output:
- Prometheus alert rules for email bounce rate and delivery rate
- SendGrid production setup guide with DNS authentication instructions
- Verified production email delivery
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-smtp-production/22-CONTEXT.md
@.planning/phases/22-smtp-production/22-RESEARCH.md
@.planning/phases/22-smtp-production/22-PATTERNS.md
@.planning/phases/22-smtp-production/22-01-SUMMARY.md
@.planning/phases/22-smtp-production/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add email alert rules to Prometheus</name>
  <files>prometheus/alert.rules.yml</files>
  <read_first>
    - prometheus/alert.rules.yml (full file - current alert rules)
    - .planning/phases/22-smtp-production/22-PATTERNS.md (alert rule patterns)
  </read_first>
  <action>
Add email alert rules to prometheus/alert.rules.yml. Append to the existing `newshub` group after the ServiceDown rule (after line 42):

```yaml

      # High email bounce rate alert (D-13)
      - alert: HighEmailBounceRate
        expr: |
          (
            sum(rate(email_bounced_total[5m]))
            /
            sum(rate(email_sent_total[5m]))
          ) > 0.05
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "Email bounce rate above 5%"
          description: "Bounce rate is {{ $value | humanizePercentage }} over the last 5 minutes"

      # Low email delivery rate alert (D-14)
      - alert: LowEmailDeliveryRate
        expr: email_delivery_success_rate < 0.95
        for: 1h
        labels:
          severity: warning
        annotations:
          summary: "Email delivery success below 95%"
          description: "Delivery rate is {{ $value | humanizePercentage }} over the last hour"
```

The alert rules:
- HighEmailBounceRate: Fires when bounced/sent ratio > 5% for 5 minutes (critical severity)
- LowEmailDeliveryRate: Fires when email_delivery_success_rate gauge < 0.95 for 1 hour (warning severity)
  </action>
  <verify>
    <automated>grep -n "HighEmailBounceRate\|LowEmailDeliveryRate\|email_bounced_total\|email_delivery_success_rate" prometheus/alert.rules.yml | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - prometheus/alert.rules.yml contains `- alert: HighEmailBounceRate`
    - prometheus/alert.rules.yml contains `expr: |` followed by rate calculation with `email_bounced_total` and `email_sent_total`
    - prometheus/alert.rules.yml contains `) > 0.05`
    - prometheus/alert.rules.yml contains `for: 5m` under HighEmailBounceRate
    - prometheus/alert.rules.yml contains `severity: critical` under HighEmailBounceRate
    - prometheus/alert.rules.yml contains `- alert: LowEmailDeliveryRate`
    - prometheus/alert.rules.yml contains `expr: email_delivery_success_rate < 0.95`
    - prometheus/alert.rules.yml contains `for: 1h` under LowEmailDeliveryRate
    - prometheus/alert.rules.yml contains `severity: warning` under LowEmailDeliveryRate
    - grep command returns at least 4 matches
  </acceptance_criteria>
  <done>Prometheus alert rules for email bounce rate and delivery rate added</done>
</task>

<task type="auto">
  <name>Task 2: Create SendGrid production setup guide</name>
  <files>docs/SENDGRID_SETUP.md</files>
  <read_first>
    - .planning/phases/22-smtp-production/22-RESEARCH.md (setup instructions, DNS patterns)
    - .planning/phases/22-smtp-production/22-CONTEXT.md (D-21 to D-26 decisions)
  </read_first>
  <action>
Create docs/SENDGRID_SETUP.md with production setup instructions:

```markdown
# SendGrid SMTP Production Setup

This guide covers SendGrid configuration for NewsHub production email delivery.

## Overview

NewsHub uses SendGrid SMTP relay for transactional emails:
- Email verification
- Password reset
- Password change confirmation

**Important:** SendGrid's free tier is a 60-day trial (100 emails/day). After trial expiration, a paid plan ($19.95/month minimum) is required.

## Prerequisites

- SendGrid account at [sendgrid.com](https://sendgrid.com)
- Domain with DNS access (for SPF/DKIM/DMARC)
- NewsHub backend deployed and accessible

## Step 1: Create SendGrid API Key

1. Log in to [SendGrid](https://app.sendgrid.com/)
2. Navigate to **Settings** > **API Keys**
3. Click **Create API Key**
4. Name: `NewsHub Production SMTP`
5. Permissions: **Full Access** (or "Mail Send" minimum)
6. Click **Create & View**
7. **Copy the API key** (shown only once!)

## Step 2: Configure Environment Variables

Add to your production `.env` file:

```bash
# SendGrid SMTP
SENDGRID_API_KEY=SG.xxxxxxxxxxxxxxxxxxxxx

# Webhook signature verification (from Step 5)
SENDGRID_WEBHOOK_PUBLIC_KEY=xxxxxxxxxxxxxxxxxxxxx

# Sender identity (D-24, D-26)
SMTP_FROM=noreply@newshub.app
```

## Step 3: Authenticate Domain (SPF/DKIM/DMARC)

Domain authentication improves deliverability and prevents emails from landing in spam.

### 3.1 Start Domain Authentication

1. Go to **Settings** > **Sender Authentication** > **Authenticate Your Domain**
2. Select your DNS host (e.g., Cloudflare, AWS Route53, GoDaddy)
3. Enter domain: `newshub.app` (or your domain)
4. Click **Next**

### 3.2 Add DNS Records

SendGrid will generate DNS records. Add them to your DNS provider:

#### SPF Record (TXT)

```
Type: TXT
Host: @ (or leave blank for root)
Value: v=spf1 include:sendgrid.net ~all
```

**Note:** If you already have an SPF record, add `include:sendgrid.net` to the existing record.

#### DKIM Records (2 CNAMEs)

```
Type: CNAME
Host: s1._domainkey
Value: s1.domainkey.u12345.wl234.sendgrid.net

Type: CNAME
Host: s2._domainkey
Value: s2.domainkey.u12345.wl234.sendgrid.net
```

**Important:** The actual CNAME values are unique to your SendGrid account. Use the values shown in your SendGrid dashboard.

#### DMARC Record (TXT)

```
Type: TXT
Host: _dmarc
Value: v=DMARC1; p=quarantine; rua=mailto:admin@newshub.app
```

### 3.3 Verify DNS Records

1. Wait 24-48 hours for DNS propagation
2. Return to SendGrid dashboard
3. Click **Verify** to confirm records are detected
4. Status should change to **Verified**

### 3.4 Verify with dig (optional)

```bash
# Check SPF
dig TXT newshub.app | grep sendgrid

# Check DKIM
dig CNAME s1._domainkey.newshub.app

# Check DMARC
dig TXT _dmarc.newshub.app
```

## Step 4: Verify Sender Identity

If using a single sender (not full domain auth):

1. Go to **Settings** > **Sender Authentication** > **Single Sender Verification**
2. Click **Create New Sender**
3. From Email: `noreply@newshub.app`
4. From Name: `NewsHub`
5. Check your inbox for verification email
6. Click verification link

## Step 5: Configure Event Webhook

The webhook receives delivery notifications for bounce handling and metrics.

### 5.1 Enable Event Webhook

1. Go to **Settings** > **Mail Settings** > **Event Webhook**
2. Enable **Event Webhook**
3. HTTP Post URL: `https://newshub.app/api/email/webhook`
4. Select events:
   - [x] Delivered
   - [x] Bounce
   - [x] Dropped
   - [x] Spam Reports
   - [x] Unsubscribes
5. Leave unchecked: Processed, Deferred, Open, Click

### 5.2 Enable Signed Webhooks

1. Scroll to **Signed Event Webhook Requests**
2. Enable signing
3. Copy the **Verification Key** (ECDSA public key)
4. Add to `.env` as `SENDGRID_WEBHOOK_PUBLIC_KEY`

### 5.3 Test Webhook

1. Click **Test Your Integration**
2. Verify your server returns 200 OK
3. Check server logs for webhook receipt

## Step 6: Test Production Email Flows

### 6.1 Test Registration Email

```bash
curl -X POST https://newshub.app/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"your-test-email@example.com","password":"Test1234!","name":"Test User"}'
```

Verify:
- Email arrives in inbox (not spam)
- Subject: "Verify your NewsHub account / Bestaetige dein NewsHub-Konto"
- Verification link works

### 6.2 Test Password Reset Email

```bash
curl -X POST https://newshub.app/api/auth/request-password-reset \
  -H "Content-Type: application/json" \
  -d '{"email":"your-test-email@example.com"}'
```

Verify:
- Email arrives in inbox (not spam)
- Subject: "Reset your NewsHub password / NewsHub Passwort zuruecksetzen"
- Reset link works

### 6.3 Test Password Change Confirmation

Complete a password reset, then verify:
- Confirmation email received
- "This wasn't me" recovery link works

## Monitoring

### Prometheus Metrics

Access at `/api/metrics`:

| Metric | Description |
|--------|-------------|
| `email_sent_total` | Total emails sent by type |
| `email_delivered_total` | Successfully delivered |
| `email_bounced_total` | Hard/soft bounces |
| `email_complained_total` | Spam complaints |
| `email_delivery_success_rate` | Delivery rate gauge |

### Alert Rules

| Alert | Condition | Severity |
|-------|-----------|----------|
| HighEmailBounceRate | Bounce rate > 5% for 5m | Critical |
| LowEmailDeliveryRate | Delivery < 95% for 1h | Warning |

### SendGrid Dashboard

Check **Activity** > **Feed** for:
- Delivery status per email
- Bounce reasons
- Spam report details

## Troubleshooting

### Emails Not Arriving

1. Check SendGrid Activity Feed for delivery status
2. Verify DNS records: `dig TXT _dmarc.newshub.app`
3. Check spam/junk folder
4. Verify `SENDGRID_API_KEY` is correct

### Webhook Signature Verification Fails

1. Verify `SENDGRID_WEBHOOK_PUBLIC_KEY` matches SendGrid dashboard
2. Check server logs for raw body availability
3. Ensure no proxy is modifying request body

### High Bounce Rate Alert

1. Review bounce events in SendGrid Activity Feed
2. Check for test/invalid email addresses
3. Verify domain authentication is complete (SPF/DKIM/DMARC pass)
4. Review sender reputation in SendGrid dashboard

### Emails Landing in Spam

1. Verify all DNS records (SPF, DKIM, DMARC)
2. Check email headers for `SPF: PASS`, `DKIM: PASS`
3. Review content for spam triggers
4. Check sender reputation score

## Trial Expiration

SendGrid's 60-day trial ends on day 60 from account creation.

**Day 50 reminder:** Set calendar reminder to upgrade plan.

**After trial:**
- Emails will fail to send
- Upgrade to Essentials ($19.95/month) or Pro ($89.95/month)
- No code changes required after upgrade

## Support

- SendGrid Documentation: [docs.sendgrid.com](https://docs.sendgrid.com)
- SendGrid Status: [status.sendgrid.com](https://status.sendgrid.com)
- NewsHub Issues: Repository issue tracker
```
  </action>
  <verify>
    <automated>grep -n "SPF\|DKIM\|DMARC\|SENDGRID_API_KEY\|Event Webhook" docs/SENDGRID_SETUP.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - docs/SENDGRID_SETUP.md exists
    - File contains `# SendGrid SMTP Production Setup`
    - File contains `## Step 1: Create SendGrid API Key`
    - File contains `## Step 3: Authenticate Domain (SPF/DKIM/DMARC)`
    - File contains `v=spf1 include:sendgrid.net ~all`
    - File contains `s1._domainkey` and `s2._domainkey`
    - File contains `v=DMARC1; p=quarantine`
    - File contains `## Step 5: Configure Event Webhook`
    - File contains `https://newshub.app/api/email/webhook`
    - File contains `## Step 6: Test Production Email Flows`
    - File contains `## Monitoring` section with Prometheus metrics
    - File contains `## Troubleshooting` section
    - grep command returns at least 10 matches
  </acceptance_criteria>
  <done>SendGrid production setup guide created with DNS authentication and webhook configuration instructions</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
SendGrid SMTP integration with:
1. EmailService configured for SendGrid SMTP (smtp.sendgrid.net:587)
2. Bounce/optout checks before sending emails
3. Webhook endpoint with ECDSA signature verification
4. Prometheus email metrics and alert rules
5. Production setup documentation
  </what-built>
  <how-to-verify>
**Prerequisites:**
1. Ensure SendGrid account is created and API key is available
2. Set environment variables:
   - `SENDGRID_API_KEY=SG.your_api_key`
   - `SENDGRID_WEBHOOK_PUBLIC_KEY=your_public_key` (if webhook configured)
   - `SMTP_FROM=noreply@your-domain.com`

**Verification Steps:**

1. **Start the server:**
   ```bash
   npm run dev:backend
   ```

2. **Verify metrics endpoint has email counters:**
   ```bash
   curl http://localhost:3001/metrics | grep email_
   ```
   Expected: Should show `email_sent_total`, `email_delivered_total`, `email_bounced_total`, `email_delivery_success_rate`

3. **Test email sending (with valid SendGrid API key):**
   ```bash
   curl -X POST http://localhost:3001/api/email/test \
     -H "Content-Type: application/json" \
     -d '{"email":"your-real-email@example.com"}'
   ```
   Expected: Test email arrives in inbox

4. **Verify webhook endpoint exists:**
   ```bash
   curl -X POST http://localhost:3001/api/email/webhook
   ```
   Expected: 400 error with "Missing signature headers" (endpoint exists but requires auth)

5. **If SendGrid configured, test full flow:**
   - Register a new user with real email
   - Verify email arrives with "Verify your NewsHub account" subject
   - Click verification link
   - Request password reset
   - Verify reset email arrives

6. **Review documentation:**
   - Open `docs/SENDGRID_SETUP.md`
   - Verify all sections are present and instructions are clear

**Expected Results:**
- Metrics endpoint shows email counters (even if all 0)
- Test email is received (if SendGrid configured)
- Webhook endpoint responds with proper error when missing headers
- Full email flows work end-to-end (if SendGrid configured)
  </how-to-verify>
  <resume-signal>
Type one of:
- "approved" - Email flows verified, production ready
- "approved-partial" - Metrics/endpoints work, full email testing deferred (no SendGrid key yet)
- Describe issues found for fixes
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> DNS | DNS records must be configured by operator |
| Operator -> SendGrid | API key and webhook key are secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Information Disclosure | Setup guide | mitigate | No actual API keys in documentation, placeholders only |
| T-22-08 | Spoofing | DNS records | mitigate | Documentation includes DNS verification steps |
</threat_model>

<verification>
1. Alert rules syntax: `docker compose exec prometheus promtool check rules /etc/prometheus/alert.rules.yml`
2. Documentation exists: `ls docs/SENDGRID_SETUP.md`
3. TypeScript compiles: `npm run typecheck`
4. Tests pass: `npm run test:run`
</verification>

<success_criteria>
- Prometheus alert rules include HighEmailBounceRate (>5% for 5m, critical)
- Prometheus alert rules include LowEmailDeliveryRate (<95% for 1h, warning)
- docs/SENDGRID_SETUP.md exists with complete setup instructions
- Setup guide documents API key creation, DNS authentication, webhook configuration
- Human verification confirms production email flows work (or partial approval for deferred testing)
</success_criteria>

<output>
After completion, create `.planning/phases/22-smtp-production/22-03-SUMMARY.md`
</output>
