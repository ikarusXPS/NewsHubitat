# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# This is a pnpm workspace — no npm/yarn lockfiles
package-lock.json
yarn.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea/
.idea
.vs/
.history/
.DS_Store
Thumbs.db
desktop.ini
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript / build incremental
*.tsbuildinfo
.tsbuildinfo

# Claude Code (v6 Post-Dev Workflow)
.claude/handoff.md
.claude/checkpoints/
.claude/web-checklist-state.json
.claude/worktrees/
.claude/settings.local.json

# Prisma generated client (regenerated via `prisma generate` — never edit, never commit)
/src/generated/prisma
**/generated/prisma/
**/src/generated/prisma/

# Database
dev.db
*.db

# Playwright auth state (CRITICAL: contains JWT session tokens)
# Old root-relative path kept for compatibility; the apps/web monorepo path is the one that actually matters.
playwright/.auth/user.json
**/playwright/.auth/*
!**/playwright/.auth/.gitkeep

# Load test data with plain passwords
k6/data/users.json
**/k6/data/users.json

# Secrets — never commit
.env
.env.*
!.env.example
apps/web/.env
apps/web/.env.*
!apps/web/.env.example

# Test output and coverage
coverage/
test-results/
playwright-report/
playwright-report-*/
apps/web/playwright-report/
apps/web/test-results/

# k6 load-test reports
summary.html
summary.json

# GSD planning workspace — kept locally for the GSD workflow, never published to a public repo.
# Purged from public history 2026-05-12 (contained Stripe CLI test-mode webhook secrets,
# bug investigations, and internal architecture decisions). Stays on disk; never re-add.
.planning/

# Diagnostic dumps at repo root (claude-code-diagnose.md and similar)
/claude-code-diagnose.md

# Windows / shell-quoting accidents
nul
NewsHub/
